---
状态: active（第五周插画待出图；结构与措辞照第三周模板，出图执行人与素材路径按本周实况改写）
source_of_truth: self（画风真相源仍是 assets/ 下第一到四周定稿图，冲突时以图为准）
日期: 2026-09-10
---

# 第五周插画生成提示词 v1.0

**用途**：拿去生成第五周课件所需插画。提示词按第一到四周定稿的实际画风反推，照抄即可，不要自行"优化"风格描述。结构与措辞抄自 `docs/插画生成提示词_第三周_20260902_v1.0.md`，本文件只改与本周相关的部分（出图执行人、素材路径、词表、新音素）。

**画风真相源**：`assets/word-cards/*.png`、`assets/phoneme-mnemonics/*.png`、`assets/wall-nat/*.png`（第一周定稿）与 `assets/week02/**/*.png`、`assets/week03/**/*.png`、`assets/week04/word-cards/*.png`（第二到四周定稿）。生成结果与它们并排看不出是几批，才算过。

**本周与第三周的三处不同**（详见对应小节）：

1. **执行人改为主机**：2026-09-10 起配图由 Claude 经远端生图链路完成，不再经副机（`docs/三方分工与交接规范_20260908_v1.2.md` §1「配图执行人」）。素材目录改为 `resources/assets/week05/{phoneme|word-cards|book}/` + `celebrate.png`，注入 target 是 `week05.html`。
2. **`ai` 是双字母字位**，本周第一次教"两个字母一块积木"，助记词命名沿用第一周"字位-助记词"约定，但没有孩子认识的 `ai` 开头词，需要用户在两个替代方案里定夺。
3. **只出稳定词表**：本周教学词减去已有词卡的、减去周检词与备选的，剩 15 个，按具象/动作状态/抽象三档分类，抽象词建议跳过。

**本周词表来源**：`docs/第五周学习设计_20260910_v1.1.md` §词表边界"已核可解码且不撞保留词的教学示例词"：`rain tail nail pain sail mail main paid hail rail gain jam jab jug jig pan man ran`（18 词）。其中 `pan.png` 已在 `resources/assets/word-cards/`（第一周）、`man.png`/`ran.png` 已在 `resources/assets/week02/word-cards/`（第二周），本周不重画，剩 **15 个新词**。周检五词 `raid maid pail jet jog` 与备选 `fail jot` 按规范**绝不配图**，本文档不出现它们的插画提示词。

---

## 0. 出图管线

```
主机经远端生图链路生成 (1024×1024 透明底 PNG，远端 codex 内置 image_gen)
  → 按 art 键命名，存入 assets/week05/{phoneme|word-cards|book}/ 与 assets/week05/celebrate.png
  → python tools/embed_assets.py --target week05.html --assets assets/week05
     （这一步内含规格化：192px / 描边 #232B28 3px / 留白 8px / 去外光）
  → node tools/week-checks/check_data.js week05.html
     python tools/week-checks/smoke_w3_browser.py week05.html
```

**`week05.html` 尚未装配**（数据层与模板还在推进），本节的注入命令留到模板就位后再执行；出图与规格化两步现在就可以做。

**文件名必须等于 art 键**（表格里"文件"那一列就是），工具按名字自动配对；
下划线开头的文件（角色定妆图，本周没有新角色，不涉及）会被跳过，不进课件。

图是**可以分批补**的：跑一次注入一次，工具幂等，补几张跑一次都行。
先用 `--dry-run` 看一眼覆盖情况和有没有孤儿图：

```bash
python tools/embed_assets.py --target week05.html --assets assets/week05 --dry-run
```

**规格由脚本兜底，你不用纠结尺寸和留白**：`embed_assets.py` 内部就会调用 `unify_card_outlines.py` 做 192px 缩放、补 3px 深色描边、留白与去外光，而且**幂等**（重跑不会把描边越叠越粗）。**不需要单独跑 `unify_card_outlines.py`**。生图那边只要保证三件事：画风对、透明底、主体完整不出血。

远端生图链路的具体命令形态、四个坑与本机/远端差异对照见 `codex-cli-bridge` SKILL「远端生图」节（唯一真相源，本文不复述）；本周不走副机白名单流程。

### 0.6 关系场景覆盖条款（第三周新增，本周沿用）

§1 的通用前缀写着 `Single subject, centered, facing the viewer`。**下面这些图天然是"关系图"**——两个主体、或主体与物件的位置关系才是词义——对它们，前缀里这三句**按本条款覆盖**，其余约束（透明底、无背景景物、无地面线、无文字、平涂无渐变、缩到 112px 可辨认）一律不变：

- 允许一个**紧凑场景**里出现最多两个主体（角色 + 角色，或角色 + 物件），两者贴近、整体仍居中；
- 允许**侧视或四分之三视角**的动作；
- 仍然不要背景、不要地面线、不要环境道具。

适用清单（本周）：词卡 `jab`（手臂 + 针管）；庆祝图（雨云 + 果酱罐两个主体）。生成这些图时，把这段话连同前缀一起给模型：

```
Exception for this relational scene: up to two closely placed subjects are allowed,
side or three-quarter view is allowed, the pair stays centered as one compact group.
Everything else in the style prefix still applies: transparent background, no scenery,
no ground line, no text, flat fills, no gradients.
```

---

## 0.5 每次生成都要附的参考图

**光靠文字描述对不准画风。** 每次开新对话生成时，先把这三张风格参考图附上（与前四周相同，逐个已核实存在）：

| 文件 | 它锚定什么 |
|---|---|
| `assets/word-cards/pin.png` | 词卡的构图、描边粗细、留白比例 |
| `assets/phoneme-mnemonics/s-snake.png` | 角色化助记图的配色与平涂程度 |
| `assets/wall-nat/natSit.png` | 动物角色的造型语言（圆润、大眼、无渐变） |

本周词卡 `tail` 复用第三周定稿的大狗角色（不重画角色本体，只截取局部特写），需要额外附这张定妆图：

| 文件 | 角色 |
|---|---|
| `assets/week03/book/_char_dog.png` | The Big Dog（第三周定稿，本周只用于 `tail` 词卡的局部特写参照） |

小书两个角色 Dan 与 The Cat 沿用第二周，画到场景里要带这两张：

| 文件 | 角色 |
|---|---|
| `assets/week02/book/_char_dan.png` | Dan（男孩主角，第二周定稿） |
| `assets/week02/book/_char_cat.png` | The Cat（橘猫，第二周定稿） |

本周**没有新角色**，不需要额外先出定妆图再等确认这一步。

附了参考图之后，下面的文字前缀仍然要照抄——**图管"像不像"，文字管"能不能用"**。

## 1. 通用风格前缀（每次生成都要带上）

> 复制这一段放在每个提示词前面。这是整套图的一致性来源，**一个字都不要改**。第四周补图文档核实过没有增量，仍照第三周原文。

```
Flat vector children's sticker illustration, storybook style for a 7-year-old.
Bold uniform dark outline in #232B28, consistent stroke weight, fully closed contours.
Flat fills with at most two-tone shading. No gradients, no glossy highlights,
no drop shadows, no outer glow, no cream halo.
Warm muted palette: cream #F0DFB8, tan #D9A441, coral red #D8492F, teal #1C6675,
sage green #48874E, soft brown #8B6239.
Rounded friendly shapes, simple confident forms, slightly chunky proportions.
Single subject, centered, facing the viewer, fully inside the frame with even margin.
Transparent background. Square 1:1.
Must stay clearly readable when scaled down to 112 pixels.
No text, no letters, no numbers, no watermark, no border, no frame,
no background scenery, no ground line.
```

### 如果模型不支持透明底

改成 `Pure flat white #FFFFFF background, absolutely no shadow`，其余不变；`tools/embed_assets.py` 会去背。
边缘带浅灰渐变会去出脏边，所以"absolutely no shadow"不能省。

---

## 2. 角色设定

**本周没有新角色**。小书主题围绕 `rain` 与 `jam`（学习设计 v1.1 §小书：下雨天 Dan 和猫在家做 jam），沿用第二周定妆的 Dan 与 The Cat，不重画：

- Dan：直接用 `assets/week02/book/_char_dan.png` 当参考图。
- The Cat：直接用 `assets/week02/book/_char_cat.png` 当参考图。

词卡 `tail` 需要借用第三周定妆的大狗角色（`assets/week03/book/_char_dog.png`）做局部特写，不算"新角色出场"，见 §4 该词条说明。

---

## 3. 音素助记图（2 张，`ai` 待用户定方案，`j` 待用户定候选）

尺寸 512×512 即可（页面显示 112px）。**文件名 = 数据层里 `SOUNDS[].art` 的键**，沿用第一周"字位-助记词"命名约定（如 `s-snake.png`），不是字母名+动作描述（那是第三周 `phoneme/` 目录另一套口令图的命名，本周助记图不走那套）。**助记词不要求可解码**（参照 `s-snake` 本身不可解码），但要六岁孩子一眼认得、且以该音**开头**。

### 3.1 `j`：给 3 个候选，待用户从中选 1（或另指定）

| 候选文件名 | 助记词 | 提示词主体（接在通用前缀后） | 备注 |
|---|---|---|---|
| `j-jam.png` | jam（果酱） | `Subject: a round glass jar filled with coral-red jam, a cream lid on top, a wooden spoon resting beside it with a small dollop of jam on the spoon tip.` | 与本周词卡 `jam.png`（§4）主体相同，若选用会与词卡插画构图接近，建议词卡换个角度拍或直接选另一候选 |
| `j-jug.png` | jug（水壶） | `Subject: a chunky ceramic jug with a curved handle and spout, teal body, tipped slightly as if about to pour.` | 与词卡 `jug.png`（§4）同主体，存在同样的重复问题 |
| `j-jelly.png` | jelly（果冻） | `Subject: a wobbly round dome of coral-red jelly on a small cream plate, two thin wobble motion lines beside it.` | 不与本周任何词卡重复，六岁孩子熟悉度高，**推荐** |

### 3.2 `ai`：两个方案，待用户定

`ai` 开头的词六岁孩子基本不认识（`aim` `aid` `ail` 都不行），只能换形态处理。两个方案都不在画面里出现字母（通用前缀 `no text, no letters` 硬约束，"把 ai 两个字母高亮"这件事请放到墙面 UI 层做，不要指望插画本身画出字母）：

**方案 A：`ai-rain.png`，画 rain（云+雨滴），走可解码路线**

```
Subject: a rounded flat raincloud in cream #F0DFB8 with a simple two-tone teal
underside, five short diagonal raindrop shapes falling evenly beneath it.
No lightning, no umbrella, no sun.
```

> 注意：本周词卡 `rain.png`（§4 第一档）主体也是"云+雨滴"，两张图构图会很接近。若选方案 A，建议词卡 `rain.png` 换成不同视角（如带地面小水洼的雨滴特写）避免两图几乎一样。

**方案 B：`ai-train.png`，画 train（火车），不可解码但形象突出、无重名问题**

```
Subject: a chunky friendly cartoon train engine, teal body with a tan round
front lamp, one big round black wheel visible, a short coral-red smokestack
with one small puff shape above it. Side view, full body.
```

存入 `assets/week05/phoneme/`，最终只落盘用户选定的那一个文件（`ai-rain.png` 或 `ai-train.png`，`j-jam.png`/`j-jug.png`/`j-jelly.png` 三选一）。

---

## 4. 词卡插画（12 张要画 + 3 张建议跳过，按优先级分三档）

存入 `assets/week05/word-cards/`，文件名就是单词，如 `rain.png`。**词表来源与去重见文档开头"本周词表来源"**：教学词 18 个减去已有卡的 `pan`/`man`/`ran`（3 个），剩 15 个；再按下表分档，其中第三档 3 个建议跳过，实际要画 12 张。

### 第一档 · 具象名词（9 张，优先出）

| 文件 | 词 | 中文 | 提示词主体 |
|---|---|---|---|
| `rain.png` | rain | 雨 | `Subject: a rounded flat raincloud in cream #F0DFB8 with a simple two-tone teal underside, several short diagonal raindrop shapes falling evenly beneath it, one small round puddle with a single ripple ring at the bottom edge. No lightning, no umbrella.`（若 §3.2 选了方案 A，把此图换成更明显不同的构图，比如加上落地水洼特写，避免与 `ai-rain.png` 雷同） |
| `tail.png` | tail | 尾巴 | `Subject: a close-up of the big fluffy dog's tail only, soft-brown #8B6239 fur matching the dog character sheet, mid-wag with two small curved motion marks beside it. No other body parts visible, no head, no legs.` 附参考图 `assets/week03/book/_char_dog.png`（沿用第三周定稿角色的毛色与描边，不重新设计角色） |
| `nail.png` | nail | 钉子 | `Subject: a single metal nail standing upright point-down, teal-grey #1C6675 shaft with a flat tan #D9A441 head, drawn big and simple, no hammer, no wood.` |
| `sail.png` | sail | 帆 | `Subject: a single triangular sailboat sail on a tan wooden mast, cream #F0DFB8 fabric with one soft-brown horizontal seam line, no hull, no water, no boat body.` |
| `mail.png` | mail | 邮件 | `Subject: one closed cream #F0DFB8 envelope with a coral-red #D8492F triangular flap and a small round teal seal in the center, front view, no stamps, no address lines, no letters.` |
| `hail.png` | hail | 冰雹 | `Subject: four round white-cream hailstones of slightly varying size falling together in a loose compact cluster, each with a simple two-tone highlight, no cloud, no ground line.` |
| `rail.png` | rail | 栏杆 | `Subject: a short complete section of tan wooden fence rail — two horizontal bars evenly spaced between two vertical posts, no gaps, no missing planks, front view.`（与第三周 `gap.png` 的"缺口栏杆"刻意区分：本周这张栏杆是完整的，没有关系场景条款） |
| `jam.png` | jam | 果酱 | `Subject: a round glass jar of coral-red jam, cream lid on top, a teal-tinted glass body showing the jam through it, a small round paper label circle with no text.` |
| `jug.png` | jug | 水壶 | `Subject: a single chunky ceramic jug with a curved handle and a spout, teal #1C6675 body, front three-quarter view, empty look, no liquid shown.` |

### 第二档 · 动作与状态（3 张，次优先）

| 文件 | 词 | 中文 | 提示词主体 |
|---|---|---|---|
| `pain.png` | pain | 疼 | `Subject: a child's face and one raised hand, wincing with eyes scrunched shut and mouth in a small "ow" shape, one finger with a small coral-red bump highlighted. Head and shoulders only.` |
| `jab.png` | jab | 打针/戳 | `Subject: a small cream-colored medical syringe with a teal plunger gently touching a child's upper arm, the child calm and looking away, no needle tip detail, no blood.`（关系图，适用 §0.6：物件 + 角色手臂两个主体） |
| `jig.png` | jig | 跳吉格舞 | `Subject: a child mid dance step, one knee raised, both arms out to the sides, big joyful open-mouth smile, slight three-quarter angle capturing a lively hop. Full body.`（视角轻微侧偏，参照第三周 `run.png` 处理，单主体，不套用 §0.6 两主体条款） |

### 第三档 · 抽象词（3 个，建议跳过）

| 文件 | 词 | 中文 | 处置理由 |
|---|---|---|---|
| `main.png` | main | 主要的 | **建议跳过**：纯修饰词/表语，与第三周 `let` 同类，无法具象化。数据层 `art:null`，页面按 hasIll 守卫降级为"单词 + 中文"。 |
| `paid.png` | paid | 已付款 | **建议跳过**：过去分词状态动词，"钱已付出"这个完成态很难与 `jab`/`jig` 这类可摆姿势的动作区分出独立画面，勉强画（如硬币+打勾符号）容易被读成"给钱"而非"已付完"，歧义风险高于收益。数据层 `art:null`。 |
| `gain.png` | gain | 获得 | **建议跳过**：抽象及物动词，强调渐进增加或利益获得，不像第三周 `get`（伸手拿到）那样有一个瞬间动作可画。数据层 `art:null`。 |

---

## 5. 小书插画（6 页，待学习设计 v1.2 定稿故事后补）

主题围绕 `rain` 与 `jam`，沿用 Dan 与 The Cat（学习设计 v1.1 §小书：例如"下雨天 Dan 和猫在家做 jam"）。故事文本尚未定稿，本节先留骨架，定稿后照第三周 §5 的格式补齐每页的英文句、中文与提示词。

必须带上 §2 的两张角色定妆图作为参考图（Dan、猫），六页一次连续生成，不要隔天补，避免角色漂移。`ai` 词要在小书里至少出现 4 个不同词（学习设计 v1.1 §小书硬约束），选词时优先用 §4 第一档已出图的词（`rain` `tail` `nail` `sail` `mail` `hail` `rail` 任选四个以上）以便插画与词卡呼应。

| 文件 | 页 | 英文句 | 中文 | 提示词主体 |
|---|---|---|---|---|
| `rainStart.png`（占位文件名，定稿后可能改） | 1 | 待定 | 待定 | 待学习设计 v1.2 故事定稿后补 |
| — | 2 | 待定 | 待定 | 待补 |
| — | 3 | 待定 | 待定 | 待补 |
| — | 4 | 待定 | 待定 | 待补 |
| — | 5 | 待定 | 待定 | 待补 |
| — | 6 | 待定 | 待定 | 待补 |

存入 `assets/week05/book/`

---

## 6. 庆祝图（1 张，必出）

第六天读完整本书时的全屏庆祝动画主角，尺寸大一些（1024×1024），页面显示 190px。本周主题元素：雨、果酱、`ai` 积木（不出现字母，见 §3.2 的说明）。

```
[通用风格前缀]
Exception for this relational scene: up to two closely placed subjects are allowed,
side or three-quarter view is allowed, the pair stays centered as one compact group.
Everything else in the style prefix still applies: transparent background, no scenery,
no ground line, no text, flat fills, no gradients.

Subject: a joyful rounded raincloud with a big happy face on the left, bouncing
mid-air, and a round glass jam jar with a big happy face on the right, also
bouncing, coral-red jam visible through its teal-tinted glass; the two are close
together as one compact festive pair, both mid-hop with small motion curves
beneath them.
```

存为 `assets/week05/celebrate.png`，出 **1024×1024**（注入时归一到 384px @2x，不是词卡的 192px）。

---

## 7. 常见翻车与对策

沿用第三周 §7 全部条目（画面出现文字、投影外发光、描边粗细不一、主体太小出血、白底带脏边），本周补充两条：

| 翻车 | 表现 | 对策 |
|---|---|---|
| **`ai` 助记图不小心画出字母** | 模型自作主张在云或火车上加了 "ai" 字样 | 通用前缀已禁 `no text, no letters`；出现就加一句 `Absolutely no typography of any kind` 重生成 |
| **`rain.png` 与 `ai-rain.png`（若选方案 A）撞脸** | 词卡与音素图构图几乎一样，缩到 112px 分不清 | 词卡换视角（如加地面水洼特写）或干脆改选 §3.2 方案 B（train） |
| **`jam.png`/`jug.png` 与音素候选 `j-jam.png`/`j-jug.png` 撞脸** | 若 §3.1 选了这两个候选之一 | 优先选 `j-jelly.png`；若坚持选 jam/jug，词卡与音素图至少换一个视角角度 |

---

## 8. 交付检查

出完图后，逐条核对：

1. [ ] 和 `assets/word-cards/pin.png`、`assets/phoneme-mnemonics/s-snake.png`、`assets/week02/book/_char_dan.png` 并排看，像同一批
2. [ ] 全部透明底（或纯白底待去背），无投影无外光
3. [ ] 画面内零文字零字母（尤其 `mail.png` 的信封、`ai` 助记图）
4. [ ] 缩到 112px 仍认得出是什么
5. [ ] `tail.png` 的毛色与描边跟 `assets/week03/book/_char_dog.png` 一致，看得出是同一只狗的一部分
6. [ ] `main.png`/`paid.png`/`gain.png` 三个抽象词没有画
7. [ ] 关系图（`jab.png`、庆祝图）里两个主体贴近、整体居中，没有背景和地面线
8. [ ] `ai`、`j` 助记图各只落盘用户选定的那一个文件，其余候选不进 `assets/week05/phoneme/`
9. [ ] `python tools/embed_assets.py --target week05.html --assets assets/week05 --dry-run` 待模板装配后再跑，孤儿图为 0，四个常量的"已就位"数 = 2 / 12 / 6 / 1（小书暂缺，跑不满属预期）
10. [ ] 真跑 `embed_assets.py`（去掉 `--dry-run`）留到模板就位后执行；不需要也不要单独跑 `unify_card_outlines.py`
