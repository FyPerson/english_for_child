# 38 · 首审 — 工程健康化第 0 期第 2 步"生成物不入库，产物改到 build/"（code-review）

## 元信息

- 审查日期：2026-09-07 上午
- 审查对象：commit a4b388a（代码与测试）、67215dc（文档联动）、53b2891（CI 两 job、verify_manifest、package 自检）；对照方案 v1.3 §3.4、§3.7、第 0 期第 2 步、§6
- codex 模式：invoke_codex.ps1 / code-review / advice-only / **API 中转，显式 `-Model gpt-5.6-sol`**；材料 8.7 万字符（代码 diff、文档 diff、方案节选、五个核心文件全文）；**通道事件**：Reconnecting 1/5 到 3/5 后仍成功返回（约 12 分钟）
- codex 版本：codex-cli 0.153.4，confidence: **high**
- 结果：**0 critical / 4 high / 4 medium / 1 low**
- 分流：high 必修 → 用户拍板 9 条全采纳、与第 3 到 5 步的 10 条并成一个 fix commit（9aeda69）→ 复审（归档 40）
- 原话档案：[_originals/codex_code-review_health_step2_round1_20260907_104619.final.txt](_originals/codex_code-review_health_step2_round1_20260907_104619.final.txt)

## 第一层：非技术总结

三个 commit 的主流程对了，但有三处违反了方案 §3.4 写死的契约，都是我实作时"做了一半"：构建只做了逐文件原子写入，没有按方案"整目录原子替换"，旧文件会残留；`--week` 单周构建后 `build/` 里新旧混杂，直接打包会出混合版本；`release` 先自己构建一次再让 check 构建两次，方案要求 check 通过后直接打包。第四条 high 是材料没给全（根产物删除在 a4b388a 里），代码本身没问题，但它建议的"布局测试断言根目录没有产物"值得加。四条 medium 里一条是文档过期用法，一条是材料漏项（六年方案 §9.1 已改），两条是白名单与路径规范化的收紧。

## Claude 核实记录

| # | codex 结论 | 本地核实 | 结论 |
|---|---|---|---|
| H-1 | 根产物删除证据缺失 | `git show --stat a4b388a`：五个根产物均为删除；工作树无 | 不属实（材料按路径过滤漏掉）；采纳其布局断言建议 |
| H-2 | 构建未整目录原子替换，旧文件残留 | `build()` 逐文件 `.tmp` 后 `replace`，未按 §3.4"原子替换与残留"条 | 属实（方案已写，实作降级） |
| H-3 | `--week` 后 `build/` 新旧混杂可被打包 | `build(week=)` 只写指定周与壳；`package` 只查必需文件存在 | 属实 |
| H-4 | release 构建三次 | `main()` release 分支先 `run('build_lessons.py')` 再 `run_checks`（其 ①② 各建一次） | 属实 |
| M-5 | package 静默忽略 build/ 多余文件，测试把 secret.txt 当可接受 | 属实 | 属实 |
| M-6 | 交付规范仍写 `build_lessons.py --check` | 第 17 行原文如此 | 属实 |
| M-7 | 六年方案 §9.1 与项目记忆生产线未见修改 | §9.1 "→ build" 在 67215dc；生产线 L9 本已是 build → check | 不属实（材料漏项） |
| M-8 | verify_manifest 路径未规范化、大小写、符号链接 | 属实 | 属实 |
| L-9 | package 先写最终目录再 verify，失败留半成品 | 属实 | 属实 |

## Claude 拍板记录（用户 2026-09-07 拍板：9 条全采纳，一个 fix commit 后复审）

| # | severity | 摘要 | 落地（9aeda69） | 用户决策 |
|---|---|---|---|---|
| H-1 | high | 根产物删除证据 | `test_layout` 断言根目录无产物、无根目录 HTML 被跟踪、移出目录不存在；复审材料不再按路径过滤 | 采纳 |
| H-2 | high | 整目录原子替换 | `render()` 与 `build()` 分离；同级 `.build.tmp-*` 写完后 `replace_directory()` 换名，失败恢复；四例测试 | 采纳 |
| H-3 | high | `--week` 残留 | `--week` 必须显式 `--output-dir`；测试 | 采纳（未删 `--week`，第五周装配副机可用） |
| H-4 | high | release 多构建 | release = run_checks 后直接 package；serve = build + package | 采纳 |
| M-5 | medium | 严格白名单 | `build_contents()` 递归枚举与白名单精确相等；测试改为必须失败 | 采纳 |
| M-6 | medium | 文档过期 `--check` | 交付规范改写 | 采纳 |
| M-7 | medium | 材料漏项 | 只补材料 | 不需改 |
| M-8 | medium | 路径规范化 | 盘符/绝对/反斜杠/空段/./..、casefold 查重、hex64、符号链接；测试 | 采纳 |
| L-9 | low | dist 半成品 | `.staging-*` 写完 verify 后换名；测试 | 采纳 |

## 第二层：4 件套

- **采纳建议**：9 条全采纳，与第 3 到 5 步 10 条并一个 fix commit 后复审。
- **推荐理由**：三条契约级 high 都是方案已定、codex 上一轮就盯着的"临时目录与原子替换"，这次不做完第 1b 期还会回来；其余六条在同一批文件里。
- **反向理由**：H-3 的另一选项是删 `--week`，它现在没有任何文档在用，删掉最干净；但第五周装配时副机的单周快速构建有用，所以选"必须显式输出目录"。整目录换名在 Windows 上目录被占用会失败，代码处理为"恢复并报错"，不静默重试。
- **第十人视角**：H-1 与 M-7 都是 codex 没看到材料才报的，说明我送审的 diff 按路径筛过头了；以后 diff 一律不按路径过滤，只截大而机械的文件正文。三条契约级 high 本质是同一个教训：方案里写了的原子替换我在实作时降级成了逐文件原子，是《能力声明 ≠ 实作》的反向变体，实作前要逐句对照方案而不是凭记忆。
