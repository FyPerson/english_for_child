# 39 · 首审 — 工程健康化第 0 期第 3、4、5 步（过渡护栏、doctor Node 门槛、移出仓库）（code-review）

## 元信息

- 审查日期：2026-09-07 上午
- 审查对象：commit a0e9cca（过渡体量护栏）、0fa7c64（doctor Node 版本门槛）、0937b1a（截图与候选音移出仓库）；对照方案 v1.3 §3.5、第 0 期第 3 到 5 步
- codex 模式：invoke_codex.ps1 / code-review / advice-only / **API 中转，显式 `-Model gpt-5.6-sol`**；材料 2.3 万字符（代码与文档 diff、归档清单前 30 行、方案节选）；与第 2 步审查并行送出，约 8 分钟返回
- codex 版本：codex-cli 0.153.4，confidence: **high**
- 结果：**0 critical / 1 high / 7 medium / 2 low**
- 分流：high 必修 → 用户拍板 10 条全采纳、与第 2 步 9 条并成一个 fix commit（9aeda69）→ 复审（归档 40）
- 原话档案：[_originals/codex_code-review_health_step345_round1_20260907_105415.final.txt](_originals/codex_code-review_health_step345_round1_20260907_105415.final.txt)

## 第一层：非技术总结

三步的功能都对，唯一的 high 是第 5 步的迁移只留了一份 markdown 清单，没有能重新核对的脚本，换机时只能人肉比对。七条 medium 是边角收紧：体量检查应对临时目录跑、预算拒绝 NaN 与无穷、预算限定整数 MB 避免报错时截断、doctor 要把超时与解码异常也转成可读失败、stdout 与 stderr 合并输出、测试用例里手写配置要补 `sizeBudgetMB`、项目记忆 README 一处残留。两条 low：版本正则改 fullmatch、真跑本机 node 的测试在无 node 环境应跳过并打印原因。

## Claude 核实记录

| # | codex 结论 | 本地核实 | 结论 |
|---|---|---|---|
| [8] high | 迁移只有 markdown 清单，无可复核脚本与归档提交号 | worklog 目录不是 git 仓库（`git rev-parse` 报 not a git repository），无提交号可记；确实没有校验脚本 | 属实 |
| [1] | 体量检查固定读 build/ 而非可重现临时目录 | `reproducible()` 逐字节比对，等价；但意图不明确 | 属实（收紧） |
| [2] | NaN / Infinity 通过 `<= 0` | 属实 | 属实 |
| [3] | 浮点预算时 `int(limit)` 截断 | 属实；方案数字本就是 8 与 20 | 属实 → 限定整数 |
| [4] | doctor 只捕获 OSError | 属实 | 属实 |
| [5] | 非零退出时丢 stderr | 属实 | 属实 |
| [6] | `^v(\d+)\.` 无结束约束 | 属实 | 属实 |
| [7] | `load_config` 强制 `sizeBudgetMB` 可能让手写配置的测试在被测逻辑前失败 | 现有测试仍绿（weeks 校验先于预算校验），但 `test_manifest_rejects…` 只断言 ValueError 类型，证明力下降 | 属实（收紧） |
| [9] | 第四周设计文档与全仓残留未证明 | 第四周设计无路径引用；全仓再扫发现 `docs/项目记忆/README.md` L33 一处残留（上轮 grep 未覆盖该文件） | 部分属实 |
| [10] | 真跑本机 node 的测试无条件 | 属实 | 属实 |

## Claude 拍板记录（用户 2026-09-07 拍板：10 条全采纳，与第 2 步并一个 fix commit，一次复审）

| # | severity | 摘要 | 落地（9aeda69） | 用户决策 |
|---|---|---|---|---|
| [8] | high | 迁移可复核 | `tools/verify_archive.py` + JSON 侧车（由归档本体重算并与表逐条核对生成）；主机复核 109/109 写入清单；`test_verify_archive.py` 挂进 check | 采纳 |
| [1] | medium | 体量检查目录 | run_checks 对临时目录跑 `size_budget --dir` | 采纳 |
| [2][3] | medium | 预算数值 | 限定正整数 MB，浮点 / NaN / inf 拒绝；测试 | 采纳 |
| [4][5][6] | medium/low | doctor 异常、双流、正则 | 捕获 TimeoutExpired/UnicodeError；stdout 与 stderr 一并展示；`fullmatch`；测试 | 采纳 |
| [7] | medium | 测试配置 | 手写配置补 `sizeBudgetMB`，并加"只缺预算"反例按消息断言 | 采纳 |
| [9] | medium | 残留引用 | 项目记忆 README L33 改写 | 采纳 |
| [10] | low | 真跑 node 测试 | 无 node 时 skipTest 并打印原因 | 采纳 |

## 第二层：4 件套

- **采纳建议**：10 条全采纳，与第 2 步并一个 fix commit 后一次复审。
- **推荐理由**：两批共 19 条里 12 条落在同一组文件，分两次修会互相覆盖。
- **反向理由**：[3] 预算限定整数 MB 比"按浮点格式化"简单，方案数字本来就是整数；[10] 跳过真跑 node 的测试与"跳过不算通过"原则有张力，但 doctor 本身就是真环境检查，单测跳过并打印原因可接受。
- **第十人视角**：[8] 的根源是我把"核对过"当成了证据，证据必须能被别人重跑；与第 1 步 codex 要求"真实 git 集成测试"是同一种要求：过程要留可复验的产物。
