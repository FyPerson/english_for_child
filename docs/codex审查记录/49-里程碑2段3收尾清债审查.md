# 49 · 里程碑 2 段 3（收尾清债 + W5 阻塞解除）编码审查

## 元信息

- 审查日期：2026-09-10 11:25 → 18:37（`/long-task` 段 3·在场连续；18:37 后到 22:07 为等待用户裁定的空档，22:07 用户拍板「停手收尾」）
- 审查对象：段 3 全部 **16 个 commit**，`b1b846a` → `ec732e4`。外审随「改一批、审一批」的复审链条滚动推进，送审对象逐轮变化：

  | 批次 | commit | 内容 | 状态 | 被哪轮外审 |
  |---|---|---|---|---|
  | T1 归档 48 | `b1b846a` | 段 2 五轮外审 + 六轮预筛归档入库 | ✅ | — |
  | 规范更正 | `90e3c89` | DATA-PATTERN-01 撤回误标「已实现」改回「部分实现」+ §3 补 W3 实测值 | ✅ | 落实轮 B 的 M2(文档面)/L2 |
  | T7/T8 | `59e7963`→`6e68435` | 学习设计 v1.0→v1.1；合并入口体量归因 | ✅ 草稿·六条教学口径待拍板 | 送审对象＝**T7** |
  | 轮 A 修复批 | `d6dbdaa` | 落实**轮 A**（S6+S7 首审）3H6M2L | 已提交待复审 | 落实轮 A；后由**轮 C**复审 |
  | 提示词文档 | `87b72e9` | W5 插画提示词 v1.0 | ✅ | — |
  | 第二批 | `130250f` | 落实**轮 B**（S8+S9 首审）2M + T5 + T8①② | 已提交待复审 | 落实轮 B；后由**轮 C**复审 |
  | 素材批 1 | `dc68ca9` | 词卡 12 张 | ✅ | — |
  | 第三批 | `a9725d0` | 落实**T3**（四模板一致性）4M3L + 契约脚本 CVC 按字位判 | 已提交待复审 | 送审对象＝**轮 D** |
  | 第四批 | `10622eb` | 落实**轮 C**（`d6dbdaa`+`130250f` 复审）2H4M2L + 月测 CVC 同改按字位 | 已提交待复审 | 送审对象＝**轮 E** |
  | 第五批 | `8a68aa6` | P16（周检词入 W）+ 落实**轮 D**（第三批复审）2H4M2L | 已提交待复审 | 送审对象＝**轮 F** |
  | 素材批 2 | `2ef0160` | 音素图 2 + 小书 6 + 庆祝 1 | ✅ | — |
  | 第六批 | `e03fbd3` | 落实**轮 E**（第四批复审）3H4M2L | 已提交待复审 | 送审对象＝**轮 G** |
  | 第七批 | `4dfd197` | 落实**轮 F**（第五批复审）2H4M1L + 补挂七份从未进流水线的测试 | 已提交待复审 | 送审对象＝**轮 H** |
  | 第八批 | `d45fc25` | 落实**轮 G**（第六批复审）2H3M2L：豁免规则抽成唯一判据 `exemptions.js` | 已提交待复审 | 送审对象＝**轮 I** |
  | 第九批 | `ec732e4` | 落实**轮 H + 轮 I**（第七/八批复审）共 2H7M3L | **已提交·待审查阻塞完成** | 送审对象＝**轮 J**（不收敛，本段最后一轮） |

- codex 模式：`invoke_codex.ps1 -Remote`，advice-only；codex-cli 0.153.4
- **逐轮 issue 计数与 confidence（权威口径）**：下表的数字由主会话 2026-09-11 对 `_originals/` 十二份原话 JSON
  逐份 `grep -o '"severity":"..."' | wc -l` 数出，**这是本段计数的唯一真相源**。锚点 §7／§11c 的文字记载曾有两处
  与此不符（轮 A 写成 5M、轮 J 写成 5M），已于 2026-09-11 在锚点原处标注更正。

  | 轮次 | task_type | confidence | critical | high | medium | low |
  |---|---|---|---|---|---|---|
  | 轮 A（S6+S7 首审） | code-review | medium | 0 | 3 | 6 | 2 |
  | 轮 B（S8+S9 首审） | code-review | medium | 0 | 1 | 4 | 2 |
  | T7（学习设计首审） | spec-critique | high | 0 | 2 | 9 | 4 |
  | T3（四模板一致性） | code-review | medium | 0 | 0 | 4 | 3 |
  | 轮 C（前两批复审） | code-review | medium | 0 | 2 | 4 | 2 |
  | 轮 D（第三批复审） | code-review | medium | 0 | 2 | 4 | 2 |
  | 轮 E（第四批复审） | code-review | high | 0 | 3 | 4 | 2 |
  | 轮 F（第五批复审） | code-review | high | 0 | 2 | 4 | 1 |
  | 轮 G（第六批复审） | code-review | high | 0 | 2 | 3 | 2 |
  | 轮 H（第七批复审） | code-review | high | 0 | 1 | 4 | 1 |
  | 轮 I（第八批复审） | code-review | high | 0 | 1 | 3 | 2 |
  | 轮 J（第九批复审） | code-review | high | 0 | 3 | 6 | 0 |
  | **合计** | — | — | **0** | **22** | **55** | **23** |

  ⭐ **本段零 critical**。HIGH 曲线 3-1-2-0-2-2-3-2-2-1-1-3（按送审时序 A/B/T7/T3/C/D/E/F/G/H/I/J），
  **十二轮未收敛到 0**——轮 J 的三条 HIGH 全部落在「为修上一轮而新加的机制」上，这正是段 3 停手的判据。

## 通道账本

段 3 的事实是：**远端订阅 11 轮（A/B/C/D/E/F/G/H/I/J 共 10 个字母轮次 + T7/T3 两次专项送审，合计 12 次送审）**，每轮 3 分钟左右回卷，**12 次全部一次成功**——无 API/本机订阅回退、无额度墙事件。

- **事件措辞固定为**：「通道回退→API/订阅」「turn.failed」「额度墙」。本段这三种事件**均为 0**。
- ⚠️ 锚点 §11c「本段真正的产出」末尾原写「远端订阅 11 轮（A/B/T7/T3/C/D/E/F/G/H/I/J）…11 次全部一次成功」——括号内实际列了 12 个标签，「11」与标签数自相矛盾。2026-09-11 段 4 已按标签数在锚点原处更正为 **12 次送审**并保留更正痕迹；本归档与更正后的锚点一致。
- **口径纪律（延续段 1/段 2 的教训，不升级为规律）**：12 次送审全部一次成功、3 分钟回卷，看起来像「远端通道已经很稳」，但项目记忆里已有两次「据连续成功/失败推规律又被自己后续数据证伪」的实证（[codex-channel-notes.md]）。本归档只记录「12/12 一次成功」这一可观测事实，不据此断言远端通道恒稳；下一段若出现失败，不构成对本段结论的推翻，只是两个独立观测。
- **成功判据**：仍是「`final.txt` 存在且非空」，不是 `exitCode 0`——本段 12 次送审均满足该判据，`skipped-context-budget` 除个别轮次因 diff 原文里字面出现该字符串而命中之外（属性形态命中 0 次），未见真实预算裁切。

## 逐轮审查表

### 轮 A · S6+S7 首审（code-review · confidence medium · 3 high + 6 medium + 2 low）

送审对象：段 2 遗留未过外审的 S6+S7 diff（`03a6839..cfd10cc`，30 文件，测试/校验/数据/模板）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | 六类墙数据失败 fixture 里只有「乱序」一类真正走了真实 `check_data.js` CLI，其余五类只证明了纯函数能诊断，不能证明 CLI 已接线 | `d6dbdaa`（补五类真实 CLI 变异测试） |
| high | 真实周语义扫描器只收集「已经写了 `segments`」的词，W5 若漏写 `segments` 的新多解词会被静默跳过，不算真正枚举 | `d6dbdaa`（改用共享严格消费词抽取器逐词分词） |
| high | `gen_segments.js` 退出码优先级与「磁盘有未复核标记必为 4」的文档承诺矛盾（标记与 tie/未写候选并存时实际返回 1 或 3） | `d6dbdaa`（写死单一优先级：标记判断置于 1/3 之前） |
| medium | 三份周模板 `renderHome` 点亮态条件对「本周字位缺 `FIRST_TEACH_DAY`」这类坏数据失败开放（静默显示已点亮） | `d6dbdaa`（仅收窄为失败开放提示，不改点亮口径） |
| medium | 方案 §2.4 原文写历史积木「未点亮态」，与实现「历史字位恒亮」文字冲突 | **未落实**——不自决，转 **P15 待裁定**（教学口径，见下方未决项相关备注） |
| medium | `smoke_w3_browser.py` 按 `META` 索引定位到 DOM 位置后只比较 `tagName`，未核对该元素身份属性是否确为目标字位 | `d6dbdaa`（改按 `data-sayph`/字形精确匹配） |
| medium | `check_data.js` 新增数组守卫只验证外层是数组，数组内含 `null`/非法 ID 等畸形元素仍可能中止后续检查 | `d6dbdaa`（补入口级畸形元素用例） |
| medium | `wall_order.js` 的 `gatherWeekRecordsUpTo` 对 `newPatterns` 缺失静默当空数组、历史文件读取失败未做结构化区分 | `d6dbdaa` |
| medium | `computeTeachingOrder` 的「连续」判定只从 `minWeek` 开始验证，历史记录取自源码树而非产物自身 | `d6dbdaa` |
| low | `test_migration_audit.js` 注释仍写旧状态描述，与紧邻代码已改的实际状态矛盾 | `d6dbdaa` |
| low | `test_gen_segments.py` 按标记「恰好出现 4 次」硬编码断言，文案改动即可能无语义回归 | `d6dbdaa` |

### 轮 B · S8+S9 首审（code-review · confidence medium · 1 high + 4 medium + 2 low）

送审对象：段 2 遗留未过外审的 S8+S9+晨间三条待裁定落地 commit diff（135 KB）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | 基线重取声称「差异恰好 5 条」，但 `course.sha256` 也发生变化且材料未解释 | **驳回**——方案 §2.5 三审已裁定 `course.sha256` 是审计元数据、由四周骨架派生，codex 未拿到该文档 |
| medium | `load_data.js` 固定错误码可能被更早的 VM 异常截断，新测试未证明真实旧产物能到达该分支 | `130250f`（先探测 `META` 存在性再执行，补真实旧产物 fixture） |
| medium | `DATA-PATTERN-01` 标「已实现」证据不足，共享抽取器实际只被差分测试引用，`word_consumers.js`/`BOOK.pages`/`wordforge` 未接生产调用 | `90e3c89`（规范改回「部分实现」）+ `130250f`（`check_data.js` ③ 实际接入全 15 类来源） |
| medium | 基线固件同时把生成环境的 Python/Node 运行时版本降级，削弱可复现性 | 未改代码，**降为备注**（09-07 基线已在现行版本以 `--check` 验证过可复现） |
| medium | week04 体量仅余 0.33 MB，只有事后硬上限、没有预警 | `130250f`（T8②，体量护栏改分级预警） |
| low | 锚点同一收尾文档内「26 个 commit」与「22 个 commit」两种数量矛盾 | 未见独立 commit hash——记入待办，由**收尾 T9** 按 `git rev-list` 统一口径 |
| low | 规范「四周实测现值」一句话遗漏 W3，容易让副机误以为 W3 未确认 | `90e3c89`（同句补 W3=19 项） |

### T7 · 第五周学习设计首审（spec-critique · confidence high · 2 high + 9 medium + 4 low）

送审对象：`docs/第五周学习设计_20260910_v1.0.md` 草稿（61 行）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | 周检候选 `bait`/`jab`/`laid` 分别已在 D2/D3/D4 教学中出现，与「周检词本周任何练习不出现」的自定规则矛盾 | `6e68435`（改「先冻结再反向排除」，推荐 `raid maid pail jet jog`） |
| high | 周检候选只规定「至少 2 个含 `ai`」，未规定必须测 `j`，可能选出全 `ai` 组合使第二个新字位完全不进周检 | `6e68435`（明确五词构成，如 3 `ai` + 2 `j`） |
| medium | D1「`tan/tail` 只换 a→ai」表述有误，`tan`→`tail` 末尾辅音也从 n 变成 l，不是单变量对照 | `6e68435`（改用 `ran/rain` 干净对照） |
| medium | D1 直接从字形与积木数切入，缺少先稳定听觉目标 `/eɪ/` 的明确动作 | `6e68435`（改为「先听音→看整块→一个干净对照→迁移 2 词」） |
| medium | 国庆维持档「19+2 字位快闪」与「不教新音」表述含糊，`ai`/`j` 能否复习、遗忘能否纠正未定义 | `6e68435`（写入推荐口径，六条教学口径之一，待用户最终拍板） |
| medium | D4 断档恢复动作只写「试课时观察是否要补」，无可执行分支或阈值 | `6e68435`（补固定回顾流程，六条待拍板之一） |
| low | `j` 放在 D3 混排本身顺序合理，但 D3 缺少对 `ai` 的主动提取环节 | `6e68435` |
| medium | 「`j` 不读成 `/j/`（椅）」的 IPA 标注不精确，可能污染音频验收标准 | `6e68435`（改精确三条：不念字母名/不读 y 音/不加尾音） |
| medium | 周检候选 `jot`/`jib` 等词的语音环境或熟悉度可能引发孩子「这是不是词」的犹豫 | `6e68435` |
| low | 示例词表边界经核对均可由 21 字位解码，未撞附录 B 保留词——本方向未发现阻断问题 | 已确认无需改动 |
| medium | D5 陌生词练习未规定应覆盖 `ai`/`j` 还是旧 CVC，也未定义「练」的反馈流程 | `6e68435`（分层：2 个 `ai` + 1 个词首 `j`，六条待拍板之一） |
| low | 认读词/小书新增 0 个认读词是否会为迁就词表写出不自然句子，未经验证 | `6e68435`（六条待拍板之一，用户已当日拍板「小书六句通过」） |
| medium | `week05.data.js` 作为「唯一权威来源」与本稿是段 4 唯一教学输入之间的权威边界不清 | `6e68435`（定义阶段性权威：审定前本稿、过审后 data.js） |
| medium | 每日时长、D7 周检时长/可拆次、RESERVED 不生成音频等固定口径未在本稿重申 | `6e68435` |
| low | 家长试课重点未覆盖 `j` 的目标音是否稳定、D7 时长与疲劳观察点 | `6e68435` |

⚠️ 上表 15 条问题在 v1.1（`6e68435`）中均已按建议改写方案正文，但其中**六条教学口径**（维持档快闪范围、D4 恢复动作、D5 陌生词分层、周检 3+2 构成、认读词是否新增、`j` 是否留 D3）当时只是把「推荐值」写入正文，本段结束时仍有部分待用户最终拍板（见 §11c「待用户裁定」）；用户已于当日 16:03 拍板其中三项（小书六句、`j`=jelly、`ai` 方案 A）。

### T3 · 四模板一致性（code-review · confidence medium · 0 high + 4 medium + 3 low）

送审对象：`frontend/src/weeks/week01~04.template.html` 四份周模板全量（历史遗留，非本段新写）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| medium | week04 的 hero 墙硬编码 `const lit = true`，绕过共享的 `wallTileLitState` 函数 | `a9725d0`（改调共享函数） |
| medium | week01 的 `bookArt`/`celebrateNat`/`printBook` 缺存在性守卫（后三周有），素材缺失或键错误时会渲染 `undefined`/破图 | `a9725d0`（补三处存在性守卫） |
| medium | week01 的 `Phone.say` 未走 `phAudioKey` 别名查询，同音多形录音会出现可点击按钮但点击找不到录音 | `a9725d0`（改与后三周一致查询/缓存） |
| low | week01 的 `renderDay` 今日音未去重（后三周用 `Set`），当前无差别但存在两套逻辑 | `a9725d0` |
| low | week03 文件头仍写「真人发音与助记插画尚未就位」，与正文「六段新音已注入」矛盾 | `a9725d0`（更新文件头） |
| low | week04 多处注释仍是第三周内容（第三本书/g 排序/111 词白名单），与实际数据不符 | `a9725d0`（按第四周实际数据重写注释） |
| medium | 四模板初始化调用集合不完全一致：week01 缺 `initWordForge`、week02 缺 `initInitialPick` | `a9725d0`（四模板统一调用初始化全集） |

附件可见范围内**未发现按字符切词残留与硬编码墙字面量**；codex 建议的「构建期禁 `.split('')`/`[...word]`/`word.length` 静态检查」记入候选、不入本段。

### 轮 C · 前两批复审（code-review · confidence medium · 2 high + 4 medium + 2 low）

送审对象：轮 A 修复批 + 第二批合并 diff（`d6dbdaa` + `130250f`，158 KB）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | 语义扫描把所有 `segment-unknown` 无条件归为 `zero` 计入 `skippedUnknown`，未按来源区分是否允许零解——严格拼读来源出现零解本应是数据错误却保持绿色（第八次「测试声称验接线实际验辅助函数」候选） | `10622eb`（按记录来源与语义分流，仅特定来源允许豁免） |
| high | `check_data.js` ③ 保留了规范之外的 `nat` 全局豁免与 W1 G1 pos 桶整体豁免，与规范「例外只有认读词、G1 干扰词」矛盾 | `10622eb`（去 `nat` 特例） |
| medium | `wall_order.js` 把历史记录的 `week` 强制等于文件名对应的 `w`，未校验 `box.META.week`，周序比较接近恒真 | `10622eb`（补历史文件 `META.week` 校验） |
| medium | `load_data.js` 的 `META_DECLARATION_RE` 只认行首严格形态 `const META = `，多种合法写法会被误报为缺 `META` | `10622eb`（放宽正则） |
| medium | `build_course.py` 把 JSON 中 `</` 转义为 `<\/` 大小写敏感，`</SCRIPT>` 等混合大小写标签仍会截断 payload | `10622eb`（改全量转义 `<` 为 `<`，补混合大小写测试） |
| medium | `DATA-PATTERN-01` 实现仍按 `[...word]` 拆 Unicode 字符比对 `TAUGHT`，未真正升级为字位级判断 | `10622eb`（改按字位 ID 逐一比对） |
| low | `size_budget.py` 文档写「等于预算也算 OVER」，代码仍是 `size > limit`，文档与实现不一致 | `10622eb`（改注释与文档口径一致） |
| low | `smoke_w3_browser.py` 用字符串拼接 CSS 选择器定位，未转义引号/反斜杠等特殊字符 | `10622eb`（改用 `data-grapheme-id` 定位） |

### 轮 D · 第三批复审（code-review · confidence medium · 2 high + 4 medium + 2 low）

送审对象：第三批 diff（`a9725d0`，49 KB，T3 修复 + 契约脚本 CVC 按字位判）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | 契约脚本 `explicitSegments` 取自 `d.W[word].segments`，但周检保留词按规范只进 `RESERVED` 不进 `W`，拿不到显式分词 | `8a68aa6`（坐实为 W5 数据契约缺口：去掉 `check_data.js:81` 的 `>=4` 豁免、W4 数据补十词入 W，定契约「周检五词必须写进 W 且带 segments」） |
| high | `taught = Object.keys(SOUNDS)` 把「出现在 SOUNDS」等同「已教」，若 SOUNDS 预置未教字位会误判 | **驳回**——规范 §3 已写死 `wallLetters` 等于 `SOUNDS` 全部键，不存在「预置未教」的合法数据形态，记备注不改代码 |
| medium | `soundType` 对 `type` 缺失/非法的回退行为未被完整实现验证 | `8a68aa6`（补测试，代码已是契约失败而非字符猜测） |
| medium | 四模板 `key => bookArtHTML(key)` 的跨 `<script>` 时序问题未被拼接式 vm 冒烟测试真实覆盖 | `8a68aa6`（改按原 script 顺序分段执行） |
| medium | `test_render_smoke.js` 的 T3-1 接线断言只是全文源码正则匹配，属新的恒真式变体（第十次候选） | `8a68aa6`（改运行时调用观测） |
| medium | `test_assessment_contract.js` 新增用例未覆盖 CVC 类型边界、`type` 非法、显式 `segments` 含未知 ID 等 | `8a68aa6`（补边界反例） |
| low | `initWordForge` 从其他周模板复制到 week01，而非共享实现 | `8a68aa6`（收敛到 `shared/games.js`） |
| low | `test_migration_audit.js` 用硬编码行号跟随新增行数手工调整，脆弱 | `8a68aa6`（改现场定位） |

### 轮 E · 第四批复审（code-review · confidence high · 3 high + 4 medium + 2 low）

送审对象：第四批 diff（`10622eb`，落实轮 C 的实现）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | `check_data.js` ③ 先按词聚合全部来源再整词 `continue`，某拼写只要出现过 G1，它在 `book-page`/`wordforge-family` 的零解也被一并放行（与注释「按记录来源精确匹配」不符） | `e03fbd3`（豁免下沉到记录级 `isExemptRecord`） |
| high | G1 pos 豁免被扩成所有周次全部 `g1-rounds` 记录豁免，规范只豁免「全部 neg + 第一周 pos」 | `e03fbd3`（收回为「全部 neg + W1 pos」） |
| high | 认读词豁免按拼写无视消费来源（`wordforge`/`blend` 等字位操作来源也被放行），且只收集本周 `sight`，历史周认读词出现在后续书页反而不豁免 | `e03fbd3`（改累计认读词集合 + 按阅读类来源限定） |
| medium | `declaration()` 仍扫描整份原始 HTML，与探测阶段已限定的 script 文本口径不一致 | `e03fbd3`（探测与抽取同用一份 script 文本） |
| medium | `extractScriptContents` 脚本提取正则大小写敏感，`<SCRIPT>` 等标签会被当作无脚本 | `e03fbd3`（正则加 `i` 标志） |
| medium | `wall_order.js` 的 `META.week` 断言只在 `META` 为真时才比较，缺失时不抛错 | `e03fbd3`（缺失也抛 `teaching-order-week-mismatch`） |
| medium | `test_build_course.py` 依赖仓库中预先存在的 `build/course.html`，非干净检出环境会失败或验证到旧产物 | `e03fbd3`（改临时目录现场生成产物） |
| low | `check_data.js` ③ 与 `assessment_contract.js` 的「含未教字位」分支在当前结构下属恒真分支 | `e03fbd3`（标注说明，不强行制造判别力） |
| low | `smoke_w3_browser.py` 的 `data-grapheme-id` 选择器仍未转义引号/反斜杠 | `e03fbd3`（改用 `page.evaluate` 传参比较） |

### 轮 F · 第五批复审（code-review · confidence high · 2 high + 4 medium + 1 low）

送审对象：第五批 diff（`8a68aa6`，落实轮 D 的实现 + P16）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | 入口校验只查 `RESERVED` 是否在 `W`，未同样约束一起补入的 `RESERVED_RETEST` | `4dfd197`（统一校验两个词池） |
| high | `wallCallTrackingForWeek` 猴子补丁原样透传返回值，只证明「被调用」不证明「返回值驱动了 class」（第十二次恒真式候选） | `4dfd197`（改三组扰动返回值 + 每块恰一次 + wallon/walloff 互斥） |
| medium | `observed` 用 `Map` 只保留每个 ID 最后一次调用结果，发现不了重复调用 | `4dfd197`（并入上条一并修复） |
| medium | `excludeDictionaryKeys` 整条排除 owner 键，与注释「另一词条引用仍应查出」矛盾，存在交叉引用盲区 | `4dfd197`（改只排除自匹配） |
| medium | week04 新增十个 `W` 条目与 `ASSESSMENT_WORDS` 重复写入同一中文释义，非单一真相源 | `4dfd197`（改从 `W` 派生） |
| medium | `W` 新增十词是否会被枚举型消费者（`Object.keys(W)`）展示给儿童，未经审计 | `4dfd197`（审计五处枚举点 + 真实 W4 产物负向测试） |
| low | `test_migration_audit.js` 的 `expectMigrated` 用生产同源 pattern 重算行号，独立检错能力弱 | `4dfd197`（补诱饵 fixture） |

### 轮 G · 第六批复审（code-review · confidence high · 2 high + 3 medium + 2 low）

送审对象：第六批 diff（`e03fbd3`，落实轮 E 的实现）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | `check_data.js` ③ 循环开头仍 `if (SIGHT.has(w)) continue` 整词跳过，认读词在 `words`/`wordforge`/`blend` 等字位操作来源也被免检，与轮 E 刚立的「只豁免 sight/sentences/book-page」规则冲突 | `d45fc25`（去 `SIGHT` 整词跳过） |
| high | G1 pos 豁免两套实现互相矛盾：`check_data.js` 只豁免 W1 pos，语义套件仍无条件豁免全周 `g1-rounds` | `d45fc25`（抽成 `tools/validation/exemptions.js` 唯一判据，两处 `require`） |
| medium | `g1-rounds` 记录携带 `bucket` 字段的存在性未被测试直接证明，字段缺失也可能「碰巧失败」而通过 | `d45fc25`（直接测 `collectWordConsumption` 输出结构） |
| medium | `verifyCumulativeSightWordsAndReadingKindScope` 在测试内部重新实现了一遍生产的跨周处理循环（第十三次候选） | `d45fc25`（抽真实入口，传入合成周数据） |
| medium | `wall_order.js` 的 `META.week` 校验只检查 `Number.isInteger`，漏掉 0、负数、字符串 `'1'` 等非法输入 | `d45fc25`（改判 `>0` 且类型正确） |
| low | 多 `<script>` 页面若存在前置伪声明，仍可能抢在真实数据声明之前被 `declaration()` 选中 | **未落实**——本轮列为观察项，未见对应代码改动，转入 W5 数据契约的长期风险观察 |
| low | `check_data.js` 失败消息把非豁免记录折叠成 `kind` 集合，同词多来源时无法定位具体是哪条记录触发 | `d45fc25`（改列出非豁免记录的完整字段） |

### 轮 H · 第七批复审（code-review · confidence high · 1 high + 4 medium + 1 low）

送审对象：第七批 diff（`4dfd197`，落实轮 F 的实现）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | `test_w4_ten_words_not_exposed.js` 只检查结构化记录未执行真实渲染，`Object.keys(W)` 直渲路径抓不到；构建产物缺失时以退出码 0 静默跳过 | `ec732e4`（改渲染级负向测试 + 产物缺失必须失败；⚠️ 轮 J 复审发现该测试仍存在恒真/漏检路径，见未决项） |
| medium | `migration_audit.js` 的注释过滤识别不了行尾 `//` 及字符串/模板字符串中的同形文本 | `ec732e4`（引入 `js_lex.js` 词法扫描共用） |
| medium | `\bpit\b` 不会误删 `pits`（属实、无需改代码），但契约未写明是只挡精确词元还是也挡屈折形式 | 代码确认无需改；是否已同步写入契约文档，本轮材料未覆盖，**未核实** |
| medium | week04 新增 `Object.fromEntries` 需 Safari 12.1+，项目最低支持版本未核实，且原写法是多行 | `ec732e4`（改单行 `reduce`；agent 实测多行写法会破坏 `declaration()` 的文本提取） |
| medium | `W[w].zh` 派生表达式在 W4 数据漏某条目时会先抛 `TypeError`，而不是走 `check_data.js` 新增的精确诊断 | `ec732e4`（加 `(W[w]||{}).zh` 守卫） |
| low | `test_migration_audit.js` 诱饵测试复用生产同一 `pattern`/辅助函数，独立性弱 | `ec732e4`（改用固定合成源码） |

### 轮 I · 第八批复审（code-review · confidence high · 1 high + 3 medium + 2 low）

送审对象：第八批 diff（`d45fc25`，落实轮 G 的实现）。

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | 共享判据 `isExemptConsumptionRecord` 接收的 `cumulativeSightWords` 在 `check_data.js` 里是本周 `SIGHT`，语义套件里是跨周累计——「共享了布尔函数没共享输入语义」，历史周认读词在后续周零解时两边结果相反 | `ec732e4`（`cumulativeSightWordsUpTo` 改复用 `wall_order` 读历史周的同一机制） |
| medium | `rec.week` 与 `options.week` 两个周次来源未校验一致性，`options.week` 缺失/非法时静默返回 `false` | `ec732e4`（`resolveG1Week` 双来源校验） |
| medium | `duplicate-declaration` 普查把函数内局部同名 `const` 也计入，比「顶层唯一」的契约更宽，可能误拒合法文件 | `ec732e4`（改限定顶层，后续被轮 J 指出仍有正则字面量导致的深度漂移风险，见未决项） |
| medium | `check_data.js` 调用共享判据抛错时位于既有分词错误处理之外，若无外层统一捕获会输出未处理异常栈 | 轮 I 建议在 CLI 顶层统一捕获；agent 在第九批交付时自报「无法构造 CLI 触发实例」，实际落地情况**未核实**，且轮 J 后续指出「累计集合构造仍在 try/catch 之外」（见未决项），推测本条未被完全闭合 |
| low | `wall_order.js` 非法 `META.week` 错误消息里 `JSON.stringify(NaN)` 显示成 `null`，且表驱动测试删掉了对 `actual` 字段的断言 | `ec732e4`（改用能区分 `NaN` 的格式化，逐例断言 `actual`） |
| low | `check_data.js` 的 `nonExemptDescr` 按原始记录直接拼接，未去重，同词大量重复时消息过长 | 本轮材料未见明确对应改动，**未核实** |

### 轮 J · 第九批复审（code-review · confidence high · 3 high + 6 medium，不收敛）

送审对象：第九批工作树 diff（`ec732e4`，落实轮 H+轮 I 的实现），本段最后一轮，未通过。⚠️ 本轮 medium 计数曾在锚点内部矛盾（§7 执行日志写「5 medium」、§11c「未完成项」写 3+3=6）。**2026-09-11 段 4 已裁定：以原话 JSON 逐条数出的 6 条为准**（3 条标 `[阻塞合并]` + 3 条标 `[可合并后补]`），锚点 §7 那处已标注更正。

详见「未决项（转段 4）」一节的完整列表，此处不再重复列表格（本轮全部未提交处置，本段停在「已提交待审查阻塞完成」）。

## 未决项（转段 4）

轮 J 发现的问题全部未在段 3 内处理，用户 22:07 拍板「停手收尾」后原样转入段 4。逐条列出（编号按原话 JSON 顺序）：

**3 条 HIGH**

1. `tools/validation/load_data.js`：`countDeclarationOccurrences()` 只统计括号深度为 0 的顶层声明，但 `declaration()` 仍按文本顺序抽取第一个匹配——若函数内局部同名声明排在真实顶层声明之前，计数判定"无重复"从而放行，但抽取阶段却可能拿到局部值，导致 `loadData` **静默返回错误数据**。新增测试只断言"不抛错"，未核对返回值。
2. `tools/validation/word_consumers.js`：`cumulativeSightWordsUpTo()` 当前周使用调用方传入的 `currentBox`，历史周固定从 `frontend/src/weeks/weekNN.data.js` 源码树读取——校验构建产物（`build/weekNN.html`）时会混用「当前构建产物」与「历史源码树」两种 provenance，源码与产物不同步时可能错误豁免或错误拒绝。
3. `tests/unit/test_w4_ten_words_not_exposed.js`：④⑤⑥渲染级验证仍可能恒真或漏检——⑤只调用 `blockHTML()` 未真正执行 `g1`/`g4`/`g5` 初始化器；⑥只调用 `colorPlainText()`/`bookArt()` 未走 `initBook()`/`draw()` 完整 DOM 路径；`findLeakedWords` 直接扫描 HTML 字符串，若目标词被按字位拆成多个 `span`，肉眼可见的泄漏词在 HTML 里会不连续而无法命中。

**3 条阻塞合并的 medium**

4. `tools/validation/js_lex.js`：`maskStringsAndComments()`/`scanState()` 不识别正则字面量，合法正则中的未配对括号字符（如 `/\{/`）会永久改变 `bracketDepthAt` 的括号深度净值，可能令后续真实顶层声明计数出错——安全相关的重复声明判定建立在一个已知有缺陷的自制词法器上。
5. `tools/validation/check_data.js`：`cumulativeSightWords` 累计集合的构造位于逐词 `try/catch` 之外，历史文件缺失、解析失败或 `META.week` 不一致时会直接抛出未捕获异常和 Node 栈，与要消除的 CLI 崩溃形态是同一类问题。
6. `tests/unit/test_check_data_g1_exemption_granularity.py`、`tests/unit/test_check_data_reserved_in_w_week4.py`：两组测试会临时改写真实受版本控制的 `week01.data.js`/`week04.data.js`，并行执行、进程强杀或另一进程同时编辑时可能污染工作树。

**3 条可合并后补的 medium**

7. `tools/validation/exemptions.js`：`resolveG1Week()` 注释承诺"任一方非法都显式抛错"，但 `null` 被当成"未提供"处理，`rec.week` 为 `null`、`options.week` 为合法值时会静默采用 `options.week`，与描述的契约不一致。
8. `tools/validation/load_data.js`：`declaration()` 要求顶层声明单行以分号结束、或多行以顶格 `};`/`];` 结束，这条格式契约目前只写在某个具体数据声明旁，未集中写入公共接口注释或数据格式规范。
9. `tools/validation/check_data.js`：③ 节的 catch 分支缺少可触发的 CLI 级回归测试，未来调用位置变化或错误信息退化时缺少保护；对 CommonJS 解构导入的猴子补丁也不可靠。

**段 4 处置方向（已拍板）**：段 4 启动门已于 2026-09-11 12:23 拍板**换 AST**（用 acorn 等成熟 JS 解析器替换 `js_lex.js` + `countDeclarationOccurrences` 这套自制词法机制）。锚点 §7 段 3 收尾原话（1289 行）明确写「换：**H1 与正则字面量那条 medium** 一起消失」——即上表 **问题 1（HIGH）** 与**问题 4（正则字面量 medium）**会随 AST 替换直接消失。**两条 medium 的确切所指（2026-09-11 主会话核实）**：段 3 收尾原话那句「H1 与正则字面量那条 medium 一起消失」写于段 3 末尾，当时只点了一条；段 4 启动门拍板块（锚点 §3「段 4 启动门拍板」）写得更全——「真正指向 AST 的是两条 medium：正则字面量导致括号深度永久漂移、『顶层声明必须单行分号或顶格 `};` 结尾』的格式契约依赖换行缩进形态」。**即上表的问题 4 与问题 8**，本归档原先的推断正确。

**其余六条的去向**：问题 2、3（HIGH）与问题 5、6（阻塞 medium）、7、9（可合并后补）不因换 AST 而消失，段 4 按 U2/U3 两批定点修（见锚点 §6 段 4 清单）。

## 原话档案

[_originals/](_originals/) 下（十二份，字节数取自 `ls -la` 实测）：

- `49-轮A_S6S7首审.final.txt`（12,635 字节）
- `49-轮B_S8S9首审.final.txt`（7,586 字节）
- `49-T7学习设计首审.final.txt`（12,153 字节）
- `49-T3四模板一致性.final.txt`（7,896 字节）
- `49-轮C_前两批复审.final.txt`（10,317 字节）
- `49-轮D_第三批复审.final.txt`（7,812 字节）
- `49-轮E_第四批复审.final.txt`（8,200 字节）
- `49-轮F_第五批复审.final.txt`（7,511 字节）
- `49-轮G_第六批复审.final.txt`（7,087 字节）
- `49-轮H_第七批复审.final.txt`（7,790 字节）
- `49-轮I_第八批复审.final.txt`（6,727 字节）
- `49-轮J_第九批复审.final.txt`（9,238 字节）
