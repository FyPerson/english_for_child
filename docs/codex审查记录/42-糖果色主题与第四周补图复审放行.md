# 42 · 复审放行 — 四周糖果色主题与第四周词卡墙补图（code-review）

## 元信息

- 审查日期：2026-09-07 下午
- 审查对象：首审（归档 41）9 条加冒烟的修复三 commit：b854302（`tools/theme_palette.py` 入库、按 CIELAB L* 校准深色底、四周模板重套方案 A、第二周冒烟改读 `--ground` 令牌）、57b19eb（五张词卡按"只抠边缘连通白底"重做，原图 + 提示词 + 来源文档入库）、0ffde13（基线重取）；材料含三 commit diff 全文、工具与测试全文、一次性注入脚本、首审原话
- 送审要求：只做两件事——逐条判定 9 条加冒烟"已修 / 部分修 / 未修"，以及修复自身引入的新问题（点名：二分求解在极端色相与 chroma 下是否越出 sRGB 被裁剪、软色底列表是否与模板一致、`text.index('}')` 找块尾是否稳、冒烟改读令牌是否失去事实断言）；硬上限：本轮无 high 即放行
- codex 模式：invoke_codex.ps1 / code-review / advice-only；**通道事件**：API 中转第一次（135240）Reconnecting 5/5 后 turn.failed，第二次 API（140535）成功；材料 5.7 万字符
- codex 版本：codex-cli 0.153.4，confidence: **high**
- 结果：**0 critical / 0 high / 5 medium / 5 low** → 按硬上限**放行**
- 判定：[2][3][7] 已修；[1][4][5][6] 与冒烟部分修；[8][9] 未修（首审已拍板只记录）
- 原话档案：[_originals/codex_code-review_candy_round2_20260907_140535.final.txt](_originals/codex_code-review_candy_round2_20260907_140535.final.txt)

## 第一层：非技术总结

没有阻断项，六个 commit 可以推送。本轮真正有分量的一条是我送审时自己点名的：调色板求解器算出来的浅色底，对香芋、天空、蜜桃三周已经超出屏幕能显示的颜色范围，被裁剪后才落到页面上。裁剪后的颜色本身合规（亮度、对比度都是按裁剪后实测的），用户在预览里看到并拍板的也是裁剪后的颜色，所以现状不用改；但这个工具以后要给四五十周选色，色相一换裁剪幅度不可预测，应该让工具自己把彩度收进色域并报出来。其余 medium 多数是对一个已经退役的一次性脚本提更严的门槛，实测五张卡都已满足，不再投入。

## Claude 核实记录

| # | codex 结论 | 本地核实 | 结论 |
|---|---|---|---|
| [1] medium | 注入脚本在全部键校验前就写 `_192.png`；两文件连续 replace 不是跨文件原子 | 属实：`card()` 第 128 行逐键写 `_192.png`；`main()` 两次 `replace` | 属实，但脚本已退役（首审拍板不入库，第 1 期管线替代），成品已入仓库并重取基线 |
| [2] medium | 极端 chroma 下 sRGB 裁剪后 L* 不可预测；CLI 不拒绝非有限值 | 扫 24 色相 × 9 档 chroma：chroma ≤ 0.08 全部通过 validate，0.1 起有色相被 validate 拒绝（目标不可达已显式失败）；**方案 A 本身在 chroma 0.045 就裁剪**：香芋 ground 蓝通道线性超 12.3%、天空 10.1%、蜜桃红通道 16.2%（surface-2 / sunk 也有 2.6% 到 9.7%）；二分是对裁剪后的实际 sRGB 算 L*，所以 L* 断言仍成立，代价是色相与彩度偏离设计值；`--hue nan` 报 ValueError、`inf` 报 math domain error、负数与 >360 按周期接受 | 属实（比 codex 说的更实：现行四周就在裁剪） |
| [3] medium | SEMANTIC_SOFT 手工硬编码，未与模板建契约 | 四周模板浅色四值 `#FBE7E2 #E0EEF1 #FBF0DA #E4EFE4`、深色两块 `#3A211B #12313A #372A13 #1B2D1D`，与常量逐一相同；模板里 pnote / pairbtn / checkitem / g1__target 等 14 处只设软色底不设文字色，文字继承 ink，所以这组检查确有对象 | 属实（现状一致，缺的是防漂移测试） |
| [4] low | `text.index('}', start)` 遇块内花括号会截断 | 四周三块实测无 `/*`、无 `}`，只有 `rgba(` 括号 | 属实但当前无触发条件 |
| [5] medium | 无主体像素损失比例检查；`fill_interior_holes` 不区分应保留透明 | 五张卡实测：连通域均为 1、无触边；主体内本无设计透明 | 属实（门槛缺失），事实上已满足 |
| [6] low | 未约束碎片化与质心偏移 | 五张卡最大连通域 100%，质心偏移最大 (+8.2, −7.1) 与 (−1.5, +9.7) px | 属实（门槛缺失），事实上已满足 |
| [7] medium | 补图管线仍靠 scratchpad 脚本与硬编码路径 | 属实；首审已拍板不入库 | 属实，维持首审决定 |
| [8] low | 阴影基色仍旧 | `--shadow-*` 基色 rgba(35,43,40)，色相 157 的低饱和灰绿，四周共用 | 记录 |
| [9] low | 壳层与周主题无组合验收 | `course.template.html` 固定色 #f7f4ed / #245c4a 等，未随周变 | 记录 |
| [10] low | 冒烟改读令牌后丢了"第二周是紫色"事实 | **不属实**：`tests/unit/test_theme_palette.py::test_real_templates_carry_set_a_and_dry_run_is_read_only` 已对四周模板断言与方案 A 生成值零差异（第二周 = 香芋 hue 300），codex 建议的"在主题契约测试里断言方案 A 生成值"就是这条测试 | 已覆盖 |

## Claude 拍板记录（用户 2026-09-07 拍板：放行，用户自行推送；[2][3][4] 采纳，收彩度进色域 + 重套模板 + 重取基线，不单独复审，并入下一轮送审）

| # | severity | 摘要 | 落地 | 用户决策 |
|---|---|---|---|---|
| [2] | medium | 色域裁剪 | `week()` 逐令牌把 chroma 收进 sRGB 色域（二分求最大可显示彩度）并在 checks 里报出；非有限 hue 直接拒绝；测试扫多档 chroma。四周模板与基线随之重取 | 采纳 |
| [3] | medium | 软色底契约 | 加一条测试读四周模板 `--*-soft` 令牌与 SEMANTIC_SOFT 逐一比对 | 采纳 |
| [4] | low | 块尾解析 | 改按花括号深度找块尾，块内含注释即拒绝 | 采纳 |
| [10] | low | 冒烟事实断言 | 已由主题契约测试覆盖 | 不采纳 |
| [1][5][6][7] | medium / low | 注入脚本门槛与入库 | 脚本已退役、成品已入库并实测满足门槛；第 1 期媒体清单化替代 | 不采纳 |
| [8][9] | low | 阴影 / 壳层 | 首审已拍板 | 记录 |

## 第二层：4 件套

- **采纳建议**：放行推送六个 commit；[2][3][4] 作一个小 fix commit（工具 + 测试 + 四周模板重套 + 基线重取），不单独复审，并入下一轮送审；其余不采纳或记录。
- **推荐理由**：[2] 是我点名请 codex 查的，核实结果比它说的更实——现行三周的底色已经在裁剪，工具以后要为几十周选色，色相一换裁剪幅度不可预测，让工具自己收彩度并报数是正确性问题，不是锦上添花；[3][4] 各十来行，是防以后漂移的便宜保险。
- **反向理由**：[2] 的裁剪结果是用户预览并拍板的颜色，收彩度后三周底色会略变淡，需要用户再看一眼；如果不想再动颜色，也可以只把裁剪量报出来（warn 不 fail），现状照旧。
- **第十人视角**：codex 对 [10] 判"部分修"是没看到那条契约测试就下结论，与它首审 [4] 一样是凭材料推断；我这次把测试全文附了却没在简报里点明"紫色事实在哪条测试里"，说明简报的"落地对照"该写到测试名一级。另一条：我送审时问"极端 chroma 会不会裁剪"，其实一行代码就能先算出现行四周有没有裁剪，问 codex 之前自己没算，是 quality_first A 类根因（没读真相源）的又一次。
