# 40 · 复审（放行）— 工程健康化第 0 期第 2 到 5 步修复 commit 9aeda69（code-review）

## 元信息

- 审查日期：2026-09-07 上午
- 审查对象：commit 9aeda69（归档 38 的 9 条与归档 39 的 10 条合并落地）；材料为完整 diff（不按路径过滤，只略去 JSON 侧车正文）、四个核心文件全文、两轮首审原话
- 复审焦点：① 19 条已修 / 部分修 / 未修；② 修复自身引入的新问题（Windows 换名回滚、run_checks 临时目录、1b 期 media 白名单、casefold 与 resolve、归档 UNLISTED 过严）；③ 硬上限：无 high 即放行
- codex 模式：invoke_codex.ps1 / code-review / advice-only / **API 中转，显式 `-Model gpt-5.6-sol`**；材料 9.7 万字符；**通道事件**：Reconnecting 1/5、2/5 后成功返回（约 14 分钟）
- codex 版本：codex-cli 0.153.4，confidence: **high**
- 结果：19 条 **18 已修 / 1 部分修（[18] 全仓引用扫描证据未附材料）/ 0 未修**；新问题 **0 critical / 0 high / 5 medium / 3 low**（另 17 条 low 为逐条"已修"状态记录，不计）
- codex 收敛判断（原话）："按硬上限本轮无 high，可放行，但建议在后续提交补齐归档清单 schema 与 Windows 回滚测试。"
- 分流：放行；用户拍板六条小改现在做一个收口 commit（5d1e8c6）、不单独复审；[22] media 白名单版本化记进第 1b 期待办；[18] 本文附扫描结果
- 原话档案：[_originals/codex_code-review_health_step2345_round2_20260907_111425.final.txt](_originals/codex_code-review_health_step2345_round2_20260907_111425.final.txt)

## 第一层：非技术总结

第 2 到 5 步放行。19 条里 18 条判已修，1 条"部分修"只是我没把全仓引用扫描的结果放进材料（结果见下）。新提的 5 条 medium 都是边角：整目录换名失败时"回滚也失败"的路径没捕获、旧目录删不掉会静默留下；归档复核对 Windows 自动生成的 Thumbs.db 会误报；归档清单本身没做 schema 校验；第 1b 期加 `media/` 时白名单要一起版本化。3 条 low 是文档语义与 Unicode 规范化。六条能现在做的已在收口 commit 落地，随后不再为第 0 期开新的审。

## [18] 引用扫描结果（补材料）

`grep -rn "resources/reference\|docs/archive/screenshots"`，范围 `*.md *.py *.js *.json *.yml`，排除 `docs/_archive/`、`docs/codex审查记录/`、`外部归档清单`、`tmp/`、`build/`：修复前仅 `docs/项目记忆/README.md` 第 33 行一处（9aeda69 已改写），修复后为 0。`docs/第四周学习设计_20260905_v1.0.md` 无路径引用（第 49 行"不使用新的 OpenLearn 候选"是结论句，不是路径），不需改。

## Claude 核实记录

| # | codex 结论 | 本地核实 | 结论 |
|---|---|---|---|
| [18] 部分修 | 扫描证据未附 | 见上节 | 材料漏项，本文补 |
| [20] medium | 回滚失败未捕获、.old 删不掉静默 | `replace_directory()` 确实只捕获第一次换名 | 属实 |
| [21] low | 非空 `--output-dir` 会被整目录替换而无提示 | 属实，这是设计（目录归构建所有） | 采纳为帮助文本 |
| [22] medium | 1b 期 media/ 白名单未版本化 | 属实，但属第 1b 期设计 | 记待办 |
| [23] low | casefold 未做 Unicode 规范化 | 属实 | 采纳 NFC |
| [24] medium | 归档 UNLISTED 对 Thumbs.db 误报 | 属实 | 采纳：只忽略三个明确的系统元数据文件并打印 |
| [25] medium | 归档清单未校验 schema，`../` 可逃逸 | 属实 | 采纳 |
| [26] low | 既有版本目录只查顶层 | 属实 | 采纳 |

## Claude 拍板记录（用户 2026-09-07 拍板：六条现在做一个收口 commit，不单独复审）

| # | severity | 摘要 | 落地（5d1e8c6） | 用户决策 |
|---|---|---|---|---|
| [20] | medium | 回滚失败与 .old 清理 | 回滚失败报出旧目录路径；删不掉打印告警；测试两例 | 采纳 |
| [21] | low | output-dir 语义 | 帮助文本写明整目录替换 | 采纳 |
| [23] | low | NFC | 清单键 NFC 后 casefold；测试 | 采纳 |
| [24] | medium | 系统元数据 | 忽略并打印 Thumbs.db / desktop.ini / .DS_Store；测试 | 采纳 |
| [25] | medium | 清单 schema | `load_inventory()` 完整校验，畸形转 SystemExit；测试十二种变体 | 采纳 |
| [26] | low | 既有版本目录 | 递归相对路径集合套同一白名单；测试 | 采纳 |
| [22] | medium | media 白名单版本化 | 记进第 1b 期待办 | 待办 |

## 第二层：4 件套

- **采纳建议**：六条现在做一个收口 commit，不单独复审；[22] 记待办；[18] 补材料。
- **推荐理由**：六条改动都在四个工具文件，十几行加测试；第 6 步清历史前把工具收干净，之后没有代码 commit 可以搭车。
- **反向理由**：[24] 也可以要求归档目录绝对纯净；但 Windows 资源管理器一打开目录就会生成 Thumbs.db，复核时报错只会让人手动删文件再跑，所以选"只忽略明确列出的三个文件并打印忽略了什么"。
- **第十人视角**：六轮下来 codex 每轮都能从新代码里找出边角，"收口"没有终点；停的依据应该是"剩余项都不影响第 6 步与第 1 期"，这六条确实不影响，所以这一轮之后不再为第 0 期开新的审。
