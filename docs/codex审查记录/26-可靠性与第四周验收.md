# 可靠性、第四周与移动端验收

日期：2026-09-05。范围：W1—W4 课件、共享源码、构建与回归工具、交接文档。未发布网站。

## 已完成

- 保留原周存储键，清洗异常打卡／游戏记录；保存失败提示、重试、JSON 导出和预览后恢复。
- 对齐 W4 巩固周、两组五词基线、全年保留词和月测隔离契约；无首教新字位。
- 抽出共享状态、游戏、渲染、测评、长按与移动端样式；周数据独立，仍交付全内嵌 HTML。
- 第四周装配完成：六天巩固、三本小书复读、19 字位、十词基线与 62 词阅读测评。
- 主要触屏按钮至少 44 像素，窄屏面板换行、输入字号与焦点提示；切页、失焦、隐藏和取消手势回收长按。

## 验证结果

完整浏览器套件通过。最终新增静态检查和测评词修订后，另行重跑构建、契约及 W4 测评测试通过。

| 检查 | 结果 |
|---|---|
| 四周数据契约、生成一致性、脚本语法 | 248 / 373 / 524 / 359 项通过，零失败 |
| 测评契约反例 | 合法样例及 16 个非法变体通过；跨前三周文字扫描通过 |
| 进度与备份 | 四周异常读取、写入失败、重试、刷新、跨周／版本拒绝、实际文件导入通过 |
| G1—G6 | 四周完成、计分、撤回、取消、重试、落盘及刷新恢复通过 |
| W4 测评 | 隐藏题目、禁播、7/10 与 8/10、阅读分级、提示、刷新和备份通过 |
| 移动端 | HTTP 下 320 / 390 / 768 / 1280 宽度、逐日展开、触摸摆放与撤回、断网后页内导航通过 |
| 家长面板 | 四周共 108 项通过 |
| 既有 W2 / W3 冒烟 | 156 / 145 项通过 |
| 媒体覆盖 | 各周 42 / 65 / 119 / 54 条实际引用词句录音与音素别名完整；跨周音素字节一致 |
| 素材回收工具 | 未修改素材的回收前后模板哈希一致 |

跨周复核发现第三周拆读说明已经出现 `lag`，因此第四周第二组改为 `gab / gal / hub / rib / sod`。回归扫描包含说明文字和嵌套字段，防止只检查词卡词典而漏掉已接触的材料。

## 验证边界与上线准备

游戏测试使用确定性音频桩，证明交互与状态，不证明实际发音。沿用已采用录音，未替换 OpenLearn 候选。

触屏使用桌面 Chromium 模拟，截图在 `screenshots/reliability-mobile/`。真实 Android、iPhone/iPad Safari 与微信内置浏览器的音频、后台恢复、下载导入仍需真机试用。

课件可作为 HTTPS 静态文件托管。尚无账号、跨设备同步或 Service Worker；网页关闭后的离线重开不在当前承诺范围内。迁移到新域名之前先导出逐周进度备份。

入口：根目录 `README.md`；课程细节见 `docs/第四周学习设计_20260905_v1.0.md`，工程细节见 `docs/工程与交付规范_20260905_v1.0.md`。
