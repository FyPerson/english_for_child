# 48 · 里程碑 2 段 2（S 前 + 第 4a—9 步）编码审查

## 元信息

- 审查日期：2026-09-09 15:16 → 2026-09-10 03:57（`/long-task` 段 2，**无人值守**·心跳 28 分钟兜底）
- 审查对象：22 个 commit，`8d7938d`（S 前首个）→ `52a61e6`（第 9 步基线重取）
  - **S 前**（五条裁定落地）：`8d7938d` `6ebdffb` `786a9b0` `a640850`
  - **第 4a 步**（`L`→`grapheme` 收敛）：`96305ff`
  - **文档批/代码批修复中间提交**：`1d527de` `fbe836b` `71ac518` `e9748d0` `9bbc754`
  - **第 4b 步**（消费者兼容层·要害段）：`b064cb8` `effa5f9` `0be7c43`
  - **第 5 步**（`assessmentMode` 路由 + P8 换词）：`de2938a` `343fed5` `03a6839`
  - **第 6 步**（synthetic-ai fixture，引擎第一次真跑双字母）：`4b2636f` `ee2879f`
  - **第 7 步**（数据迁移，四字段改数组 + 墙语义统一）：`cde0a14` `cfd10cc`
  - **第 8 步**（收紧兼容层）：`06479cd`
  - **第 9 步**（基线重取）：`52a61e6`
- codex 模式：`invoke_codex.ps1` / advice-only；codex-cli 0.153.4
- confidence：五轮全部 **high**

## 通道账本

用户 09-09 定的规则（本段延续）：**每一轮送审都先从 API 中转起算、重试 3 次，3 次全败才该轮换订阅；下一轮仍从 API 重新起算**（单轮级回退，不是段级）。

段 2 汇总（据 `docs/任务_里程碑2双字母积木_20260909.md` §11b）：**API 中转 14 次调用 3 成功；订阅 4 次调用 4 成功**。逐轮明细：

| 送审对象 | API 中转 | 结果 | 换订阅后 |
|---|---|---|---|
| S 前文档批（`8d7938d`+`6ebdffb`+`786a9b0`） | 96 KB，3 次全败（`stream disconnected before completion`） | turn.failed ×3 | 订阅 1 次成功（16:16，`final.txt` 8102 字节）→ `codex_out_sub1.final.txt` |
| S 前代码面（`a640850`+`96305ff`+`71ac518`+`e9748d0`） | 167 KB，3 次全败（同形态） | turn.failed ×3 | 订阅 1 次成功（17:57，7615 字节）→ `codex_code_sub.final.txt` |
| S4b 前置·DOM 解析岔路裁定 | 7 KB，**API 中转第 1 次即成功**（18:41） | — | 未回退 → `codex_dom1.final.txt` |
| S4b 外审（`b064cb8`+`effa5f9`） | 188 KB，3 次全败（本段最大一次材料） | turn.failed ×3 | 本机订阅 1 次成功（20:52）→ `codex_4b_sub.final.txt` |
| S5 外审（`de2938a`+`0be7c43`+`343fed5`） | 170 KB，**第 3 次成功**（23:11，未回退订阅） | 第 1、2 次失败，第 3 次通 | → `codex_s5_3.final.txt` |

**事件措辞固定为**：「通道回退→API/订阅」「turn.failed」「额度墙」。本段无额度墙事件（额度 5h 全程未过 32%，只跨了一次 5h 重置窗口）。

**口径纪律**：连续三轮出现「大材料 API 三连败 → 订阅一次即通」的相同模式，仍**不升级为「API 通道已坏」的规律**——段 1 已两次「据连续失败推出规律又被自己后续数据证伪」；本段 DOM 裁定（7 KB）与 S5 外审（170 KB 第 3 次成功）都是与「材料越大越易败」相悖的反例，按纪律只记可观测事实、不写推论。

⚠️ **`exitCode 0` 与 `timedOut false` 具欺骗性**这条延续段 1 结论：本段多次 API 失败包装器均正常退出，唯独 `final.txt` 未生成；成功判据仍是「`final.txt` 存在且非空」。

⚠️ **原话文件不在 `tools/invoke_codex.ps1` 默认输出目录 `tmp/codex-runs/`**——该目录内本段窗口（09-09 15:00 后）无任何落盘文件，五份 `.final.txt` 实际留在上一会话的 scratchpad 临时目录，已按用户指示原样复制到 `_originals/`（见文末）。未找到对应 `.json` wrapper，`apiMode`/`remoteMode`/`contextFilesCount` 无法从结构化字段核实，本表的通道信息全部来自 `docs/任务_里程碑2双字母积木_20260909.md` §7 执行日志的文字记录。

## 逐轮审查表

### 轮 1：S 前文档批（spec-critique · API 三败转订阅 · 1 critical + 3 high + 3 medium + 2 low）

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| **critical** | 我写的「四步机械判断法」判定哪些词要写 `segments` 存在可证明漏判：字位集合 `a`/`ab`/`bc`/`c` 时 `abc` 有两个完整解，但四步法算出的「风险字形集合」为空 | 已重写：唯一权威判据改为逐词枚举完整解析、按解析基数分档，字形扫描只作预筛优化；`abc` 反例写入规范正文（`71ac518`） |
| high | 「含风险字形」只是子串条件，零解/多解优先级未定义 | 已写死处理顺序：先求解析集合，零解优先（`71ac518`） |
| high | 规范开头「门槛 A 前不构成交付门槛」与 §4.1「已实现规则任何时候都生效」自相矛盾 | 拆成「校验器会不会跑」与「能不能据它打回交付」两栏表（`71ac518`） |
| high | 门槛 A6 不能仅凭表头总注满足 | A6 状态从「已满足」改回「尚未满足」，写死满足判据（`71ac518`） |
| medium | 差分测试要按语料来源断言总数/执行数/跳过数守恒 | 派编码 agent 落实（`e9748d0`） |
| medium | 门槛 A 缺裁定人与版本化激活记录 | 写死「唯一裁定人=用户」+ 激活记录六项内容（`71ac518`） |
| medium | 生成器把启发式当权威 | 降格为「候选、不具契约优先级、必须人工复核」（`e9748d0`） |
| low ×2 | A1/A2 范围拆不清；建议全文核实 | 已落实（`71ac518`） |

### 轮 2：S 前代码面（code-review · API 三败转订阅 · 3 high + 5 medium + 1 low）

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high | 我补的自适应断言 `assert.equal(totalSkippedMultiLetter, 0)` 写在分支判断之外、无条件执行，与「第 7 步后自动放松」的设计矛盾 | 移入 `if (!anyMultiLetterInData)` 分支内（`9bbc754`） |
| high | 五项差分的 `catch` 不看错误码，会把 `segment-ambiguous`（需消歧）与 `segment-unknown`（真未教）混为一谈，歧义词被伪装成未知词假绿 | `resolveOrSkip` 按错误码严格三分，其他错误码重新抛出不许吞（`9bbc754`） |
| high | 兼容差分排除全部多字母词、语义测试只用合成数据，两者合起来仍未验证真实周数据的新语义 | 第 7 步新增真实数据语义套件，写入方案（文档面） |
| medium ×5 | `git ls-files` 对未跟踪新文件不成立却声称自动纳入；白名单粒度是整文件+整模式；适配层已成默认严格校验器但名称仍是 legacy fallback；三段式签名把迁移状态传播到每个消费者；生成器材料不足以估计错误率 | 前两条落实，`--write` 默认改为只写 `unique` 状态词（`9bbc754` + 续agent补做） |
| low | `sys.executable and 'node' or 'node'` 恒等于 `'node'`，误导 | 已修 |

### 轮 3（dom1）：S4b 前置·Node 侧 DOM 解析岔路裁定（spec-critique · API 一次即通 · 2 high + 结构性建议）

不是常规「代码审查」，是方案 §5 安全边界的岔路裁定（按停机条款不自决，送外审）。

| 级别 | 问题/裁定 | 处置 |
|---|---|---|
| high | ②-a（浏览器原生 `DOMParser` + Node 手写解析器）严格说是两套实现，不满足「一份实现同时被浏览器与 Node 使用」的字面要求；且 `DOMParser` 会自动修复畸形嵌套、手写解析器通常拒绝，理论差异会变实际差异 | **裁定走 ②-b**：两端共享一份字符串扫描 + 白名单，浏览器侧也不用 `DOMParser` |
| high | 「没有终端用户输入」不是免检理由——AI 仍可能误写事件属性、危险标签，产物是本地双击打开的单个 HTML，`innerHTML` 会让内容直接执行 | 白名单校验维持必需，判 high；纯文本转义 + 富文本构建期白名单列为安全最低线（不可协商），是否着色列为功能选择（可协商） |
| — | 最易写错处点名：冻结严格语法、对畸形输入整体拒绝不兼容浏览器自动修复；测试须覆盖实体/嵌套/缺闭合/裸 `<` | 写入方案 §5（`fbe836b` 前后文档面） |

### 轮 4：S4b 外审（code-review · API 三败转本机订阅 · 3 high + 5 medium + 2 low）

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| **high（H2，本轮最重要）** | 「当前可唯一解码」不是稳定数据契约——第 7 步加入 `ai` 等新 ID 后，原本唯一解析的旧词（如 `sat`）会变多解，严格着色按设计抛错，整页渲染死 | 「开工前必做字位表扩充分析」四步 + 改写验收判据写入方案第 7 步（文档面） |
| high | `gen_segments` 默认写回时若只有 resolved 候选、无 tie/unknown/error，退出码为 0，会被误认为全部处理完成 | 代码面等 S5 交付后一并做（`0be7c43`） |
| high | `check_data` 对表级 schema 问题漏报——`validateSoundsSchema` 表级问题用 `id:null`，但只遍历自有键读取，永远传不到 `ok()` | 同上，`0be7c43` 落实并加端到端 fixture 用污染原型驱动真实 CLI |
| medium ×5 | `surfaceOfSafe` 捕获所有异常与白名单原则不一致；多处数据文本仍裸插 `innerHTML`；`test_render_smoke` 只测初始化未测交互态；白名单 `count` 统计命中行数可绕过；按 `graphemeLabel.length` 切原串只对 ASCII 可靠 | 部分落实于 `0be7c43`，`surfaceOfSafe` 改为只显式检查 `null` 槽位 |
| low | 四模板一致性仅凭 diffstat 无法核实，需完整内容 | **未处置**——记入未完成项，需单独送审 |

### 轮 5：S5 外审（code-review · API 第 3 次成功 · 2 high + 4 medium + 1 low）

| 级别 | 问题 | 处置（commit） |
|---|---|---|
| high（H1，第五次假覆盖） | `test_render_smoke.js` 的 G5 场景注释声称测「摆词填满 + 撤回」，实际只调 `initG5` 并断言初始 HTML 非空 | 记入机制观察，S6 起补真实交互态测试 |
| high | `--write-heuristic` 写入未复核标记后强制返回 0，而 0 的定义是「全部处理完毕」，`check_data.js` 明确会拦截这些标记，语义矛盾 | 落实（`03a6839`）——第 6 步预筛发现该退出码修复重跑仍恒返 0，见「内部预筛」轮 6 |
| medium ×4 | 泄漏检测排除范围边界未定义（`pits`/`pitfall` 查不出，子串匹配又误报 `spit`/`span`）；`tools/legacy/port.py` 硬编码旧周检词（换词影响面第五层）；不支持路由仍调 `ctx()`/`checkConsolidation()`；`META` 只查 `typeof object`，数组也当合法 | `port.py` 部分落实（`03a6839`），泄漏判据边界挂起（P13，见待裁定索引） |
| low | 媒体例外靠注释表达，没有按原因分类的机器断言 | 未处置 |

## 内部预筛（Opus，每 commit 固定环节）抓到的、codex 之外的问题

本段共走 **六轮**独立预筛 + 一次主会话抽查（S4a），累计 **2 critical + 16 high**——按六个「落实预筛」commit 的标题相加：`786a9b0`（S 前文档批 3H）/ `e9748d0`（S 前代码面 3H）/ `effa5f9`（S4b 2C+2H）/ `343fed5`（S5 2H）/ `ee2879f`（S6+S5 修复批 2H）/ `cfd10cc`（S7 4H）。⚠️ 锚点 §11b「产出质量」层的汇总句写「11 high」，与 commit 级明细不符，**以本表为准**（段 3 收尾时更正 §11b）。**codex 之外**的关键条目：

1. **S 前文档批预筛**：3 条 high 全命中——「`segments` 走查对象窄化为『本周新教』」（实际累计已教字位皆需覆盖）、「W5 示例词 `air`/`hair`/`chair` 教学顺序不成立」、「锚点 §0 快照未同步」。三条均确认属实，处置见方案文档面。
2. **S4a 抽查**（主会话亲验，非独立预筛轮但发现门槛②漏扫）：源码扫描漏 `.py` 内嵌 JS 字符串（`tools/legacy/w2data.py`），已自决扩大扫描范围（P10）。
3. **S4b 预筛**（**2 critical**）：**C1** demo 词（`sun`/`apple` 等展示性举例词）走 strict 分类，本周字位根本拼不出，W1/W2 页面会白屏；**C2** `colorizeToken` 用 `graphemeLabel(id)` 取显示文字（恒小写）导致全部小书标题与正文丢大小写。两条 codex 均未提及，全部实测确认。
4. **S6+S5 修复批预筛**（**第六次假覆盖，本段自查发现**）：`test_synthetic_ai_integration.js`「墙/首教日」6 条断言里 4 条不接触任何生产代码（纯恒真、数组分支永远走不到、断言的是测试自身的桩）。
5. **S7 预筛**（**4 high**，均为 codex 之外）：HIGH-1 浏览器套件 `smoke_w2_browser.py` 写死墙上 8 块、扩容后实际 14，而验收一直用 `--quick` 跳过浏览器套件；HIGH-2 `smoke_w3_browser.py` 取墙上前 6 块比对，扩容后量的是别的元素但断言照跑；HIGH-3（**第七次假覆盖**）`test_grapheme_semantics.js` 的「分辨力验证」四条断言一条没执行；HIGH-4 `week02.template.html` 文案「七块新积木」与实际渲染 13 块不一致（孩子/家长可见落差）。

## 「测试看起来在验证 X、实际验证 Y」——本段七次逐一列出

1. 守恒断言恒真式（三个计数器同循环递增，PASS 文案声称按 `ENTRY_KINDS` 统计但从未 require 它）——预筛发现，`e9748d0` 修。
2. 门槛把数量锁死在 6（`assert.equal(length, 6)` 保证「W5 忘了加」永远不红）——预筛发现，同批修。
3. 污染原型只测校验器、不测生产入口（`check_data` 未消费表级 `id:null` 问题）——S4b 外审 H3 发现。
4. 禁止项 `ANNUAL_DECODING` 绕过数据加载器（`load_data.js` 白名单未收录，参数化测试直接往裸对象赋值恒绿）——S5 预筛 H1 发现。
5. G5 场景只验初始化却声称验交互态（`test_render_smoke.js`）——S5 外审 H1 发现（第五次）。
6. 墙那节把测试字面量与自身定义值比对（`test_synthetic_ai_integration.js` 6 条断言 4 条纯恒真）——S6 预筛发现（第六次）。
7. 分辨力验证的四条断言一条没执行（`test_grapheme_semantics.js`，删光四条 assert 测试照样通过）——S7 预筛发现（第七次，任务专门点名去找的那次）。

## 未完成项

1. **四模板一致性未审成**——S4b 外审时为压材料只附了 week01 全量 + 四份 diffstat，codex 明确说「仅凭 diffstat 无法核实」，需单独送一轮只装四份模板的材料。
2. **S6/S7/S8/S9 未过外审**——段末连续推进，只走了内部预筛未送 codex；S7 是破坏性数据迁移、S8 移除了兼容能力，风险最高，建议下段（段 3）补审。
3. **S7 预筛剩余条目未处理**：MED-7（跨周读取四处耦合）、MED-9（交给副机的契约文档没跟着改——规范仍写「实测 week02 的 `wallLetters` 就是 `"ckehrmd"`」，**直接影响 W5 交接，优先级高**）、LOW-10/11/12。

## 待裁定索引

| # | 一句话 | 状态 |
|---|---|---|
| P2 | 数据层规范 §8 生效条件第 3 条与新状态第 17 条对撞，规范生效点被钉在 2026-10-31 之后而 W6 审定截止 10-04 | 已裁定·待执行（拆两个生效门槛） |
| P3 | `rain` 类多解口径：本周新教多字母字位若组成字母都已单独教过，该周所有含该字形的词必然多解 | 已裁定·待执行 |
| P4 | `normalizeIdList` 签名的 legacy 上下文范围 | 已裁定·待执行（S前，定签名 `normalizeIdList(value,{legacy})`） |
| P5 | 分裂字位（Magic e）远期坑是否记入方案 §7 | 已裁定·待执行（记入） |
| P7 | 五项差分强制断言字符级与字位级相等，第 7 步后自相矛盾 | 已裁定·待执行（拆兼容差分/语义测试两套） |
| P8 | W1 周检词 `spit` 换 `pit`（教学口径：送气 p 音变） | 已裁定·待执行（第 5 步已落地） |
| P11 | ⚠️ 换词的媒体侧收尾（禁区）——`audio_manifest.json` 保留词黑名单、音频源文件 | **待裁定**，不处理有重跑构建覆盖修复的风险 |
| P13 | ⚠️ 泄漏判据边界（教学口径）——`pit` 的复数/时态/复合词算不算泄漏 | **待裁定**，三选项已列 |
| 另 | 三方分工是否因远端生图链路打通而改（配图归 GPT/副机出图条款） | **待裁定** |

> **后记（2026-09-10 08:20—08:40，段 2 收尾之后）**：上表三条「待裁定」已由用户全部拍板并落地——P11 主机改（`f82aff9`）；P13 维持精确词元匹配、不动；三方分工只改配图执行人为主机、不动边界（`d83c88b`）。未完成项第 3 条的 **MED-9 亦已于 `c95d2ae` 同步规范**（§3 三处改实测现值 + §4.1 四条转已实现，并新记 `DATA-RESERVED-02` 缺口）。S6—S9 补外审与四模板一致性由段 3 承接（归档 49 起）。

详见任务锚点 `docs/任务_里程碑2双字母积木_20260909.md` §9 待追认清单与 §11b 收尾报告。

## 原话档案

[_originals/](_originals/) 下：

- `48-S前外审.final.txt`
- `48-S前代码面外审.final.txt`
- `48-dom1外审.final.txt`
- `48-4b外审.final.txt`
- `48-S5外审.final.txt`
