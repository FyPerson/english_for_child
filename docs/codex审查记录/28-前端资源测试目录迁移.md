# 前端、资源与测试目录迁移

日期：2026-09-05。当前目录入口见 `docs/architecture.md`。

前端源码移动至 `frontend/src/`；素材及候选音源分别进入 `resources/assets/` 与 `resources/reference/`，四份处理清单进入 `resources/manifests/`。测试拆为 `tests/unit/`、`tests/browser/`、`tests/fixtures/`；构建契约实现进入 `tools/validation/`，历史移植工具进入 `tools/legacy/`。

`backend/README.md` 明确当前没有服务端实现。自动化截图进入被忽略的 `test-results/`，历史截图和设计页面归档到文档目录。根目录课件路径、存储键与公开交付边界保留。

验证结果：577 个移动资源及原 HTML 的 SHA-256 均保持不变；源码生成一致性、四周数据契约、媒体、测评反例、五项构建边界与三项目录测试通过。重新运行四周进度、合并课件导航／周锁及四周移动端浏览器检查通过。没有重跑内容未变的全部旧周长按／游戏套件，不把此前验收当作本次新执行结果。

构建、素材工具默认路径、测试入口、CI 截图路径与当前维护文档已更新。历史原始来源记录保留旧路径，通过迁移表定位。本轮没有重新生成素材、启动后端、提交或发布网站。
