# 35 · 定向复核（硬上限）— 工程健康化实施方案 v1.2 → v1.3 定稿（spec-critique）

## 元信息

- 审查日期：2026-09-07 晚
- 审查对象：`docs/工程健康化实施方案_20260907_v1.2.md`，只审 v1.1 → v1.2 的 diff（归档 34 的 13 条落地）
- 复审焦点：① 13 条修对了没有；② 修订自造的新缝；③ 硬上限：无 critical 且 high ≤ 2 且可局部修即"修完即可定稿"
- codex 模式：invoke_codex.ps1 / spec-critique / advice-only / **API 中转（aiapipay），显式 `-Model gpt-5.6-sol`**；1 个 ContextFiles 约 7.4 万字符（diff、v1.2 全文、8 个实作文件），`skipped-context-budget` 0；后台 1200 秒模式，约 5 分钟返回
- codex 版本：codex-cli 0.153.4，confidence: **high**
- 结果：上轮 13 条判定 **6 已修 / 6 部分修 / 1 未修**（M-8 去桥接哈希算法与第 1b 期步骤矛盾）；新问题 **0 critical / 2 high / 5 medium / 1 low**
- codex 收敛判断（原话）："当前没有发现必须推倒重来的 critical；两条 high（哈希算法、Release 占位符）都可局部修复，修完即可定稿。"
- 六轮走势：14 → 12 → 11 → 7 → 13 → 8 条；critical 2 → 2 → 0 → 0 → 0 → 0；high 7 → 6 → 6 → 2 → 3 → 2
- 分流：硬上限达成。用户在归档 34 拍板"再送一轮，硬上限，无论结果如何那轮后定稿"，据此两条 high 必修、五条 medium 与一条 low 修法明确一并落地，落成 **v1.3 定稿**，落地后不再送审
- 原话档案：[_originals/codex_spec-critique_health_plan_round6_20260907_090359.final.txt](_originals/codex_spec-critique_health_plan_round6_20260907_090359.final.txt)

## 第一层：非技术总结

方案定稿了。这一轮没有推翻任何决定，两条 high 都是我上一轮修订时写得不够具体：一条是"去掉桥接再算骨架哈希"的算法只剥了脚本正文、没剥外面的 script 标签，按那个写法第 1b 期的验收在理论上过不去；另一条是 Release 发布命令里还留着尖括号占位符，照抄跑不起来。两条都改成了逐字可执行的写法。剩下六条是把协议、检查流程、验收措辞写得更不留解释空间：消息字段表、离开时的状态机、check 的四步顺序、"测试红"改成"反例断言通过"这类语义修正。

接下来不再送审，按你之前的拍板进入第 0 期：先把未提交的 docs 用 stash 暂存，再逐 commit 做基线、不入库、护栏、doctor、移出、清历史六步，每 commit 送审。

## Claude 核实记录

| # | codex 结论 | 本地核实 | 结论 |
|---|---|---|---|
| H-1 | 去桥接哈希只替换 SOURCE 标记之间的脚本，外层 `<script>` 仍在，等不上旧基线；第 1b 期第 1 步无桥接却要求两个标记各出现一次 | v1.2 §3.3 末段确实只写"两段各替换为固定占位符"；桥接经 `@include` 展开后带 `/* SOURCE */` 注释，但包住它的 `<script>` 标签是模板新增文本；第 1b 期第 1 步"本步尚未加桥接"与算法前提冲突 | 属实 |
| H-2 | Release 命令仍含 `soundblocks-<tag>.zip`、`<值>` 占位符 | v1.2 §3.7 release 行原文如此 | 属实 |
| M-3 | `check` 契约没写单文件形态构建属于哪一步、比什么、临时目录何时清理 | §3.3 写了"check 自己额外跑一次 `build --single-file`"，§3.4 的 ①②③ 没有它 | 属实 |
| M-4 | 消息字段类型、壳侧周号校验、http 下 origin 校验未定义 | §3.6 只写"字段缺失或类型不符即忽略"，无字段表；接收条件没有 `week === selectedWeek`，也没写非 file 协议下校验 origin | 属实 |
| M-5 | leave 超时后的重试、取消、迟到回包未定义状态机 | §3.6 只有一句"重试与留在本周两个按钮" | 属实 |
| M-6 | "非累计"表述仍过宽：壳 payload 随周数线性增长 | 每周约 0.3 KB，50 周约 15 KB，远低于 0.5 MB，但措辞确实把它说成不增长 | 属实 |
| M-7 | "测试红"与"check 全绿"读起来矛盾 | §4 第 1b 期第 1 步与 §5 三处用了"测试红" | 属实 |
| L-8 | `progressDigest` 的读取异常、长度定义未规定 | §3.6 ready 行只写 `{exists, length, hash}` | 属实 |

## Claude 拍板记录（用户 2026-09-07 晚授权：再送一轮硬上限，那轮后定稿）

| # | severity | 摘要 | 落地 | 用户决策 |
|---|---|---|---|---|
| H-1 | high | 去桥接骨架哈希 | §3.3：桥接改为 `<!-- @course-bridge start/end -->` 起止标记包住的整块 `<script>` 注入块，算法整块删除（含标记、标签与换行）后等于普通骨架；第 1b 期第 1 步比普通骨架、第 2 步比去桥接骨架；两例可证伪测试 | 硬上限授权落地 |
| H-2 | high | Release 占位符 | §3.7：`TAG`、`DIR`、`ZIP`、`ZIP_SHA` 变量化，新增 `tools/verify_manifest.py` 供本机与 CI 共用，解压后二次核对，`--notes` 写实际哈希 | 硬上限授权落地 |
| M-3 | medium | check 编号化 | §3.4：①默认构建 ②可重现比对 ③基线比对（1b 起单文件形态另建，之前直接读 ② 的产物）④其余；临时目录必删 | 硬上限授权落地 |
| M-4 | medium | 消息字段表 | §3.6 新增字段与接收条件表，含 `requestId`；壳侧校验 `week === selectedWeek`；非 file 协议加 origin 校验 | 硬上限授权落地 |
| M-5 | medium | 离开状态机 | §3.6 新增 `idle / waiting / timedOut` 状态机，`requestId` 单调递增，迟到回包丢弃；测试三例 | 硬上限授权落地 |
| M-6 | medium | 护栏措辞 | §0、§2、§3.5：改为"受护栏约束的对象都是单文件或单周新增指标"，壳是唯一随周数增长的产物并给出 50 周约 15 KB | 硬上限授权落地 |
| M-7 | medium | 测试红语义 | §3.5 加一句"反例都是测试内部断言"，§4、§5 全部改为"反例断言通过（被测命令按预期返回非零）" | 硬上限授权落地 |
| L-8 | low | progressDigest 异常 | §3.6：对原始字符串算，`length` 为 UTF-16 码元数，异常固定为 `{exists:false, length:0, hash:0, error:true}`，两侧同一函数；测试一例 | 硬上限授权落地 |

## 第二层：4 件套

- **采纳建议**：8 条全部采纳，落成 v1.3 定稿，不再送审。
- **推荐理由**：两条 high 都是"写法不可执行"而不是"方向错"，修法唯一；六条 medium/low 都是把上一轮新增段落写到实现者无需猜测的程度，与第 1b 期的编码直接对应，现在写比编码时临时定便宜。
- **反向理由**：M-4 的字段表与 M-5 的状态机让 §3.6 从 20 行涨到 40 行，方案开始有"规格书"味道；但第 1b 期编码时每 commit 都要送审，没有这份规格审的人和写的人都只能猜，所以接受这份长度。新增 `verify_manifest.py` 是一个额外工具，但它替代了 CI 里手写的核对脚本，净复杂度不增。
- **第十人视角**：六轮下来条数 14 → 8 没有收敛到 0，每轮修订都在自造缝，第六轮 8 条里 6 条仍是上一轮改动引出的。真正的止损点不是"再审一轮"，而是本方案已经细到"编码时逐 commit 审"能兜住的粒度：第 0 期六步、第 1 期九步、第 1b 期六步都各有验收条目，实施审会把剩余的缝在代码层暴露。所以硬上限在这一轮停是对的，再审一轮的边际收益低于开工。另一条：codex 对 M-8 判"未修"是对的，但它上一轮给的修法（替换两段占位符）正是我照抄后出错的来源，说明"按 codex 修法原样落地"也会自造缝，落地前要自己走一遍算法。
