# 项目工程结构整理

日期：2026-09-05。范围：构建、源码媒体布局、配置、开发命令、发布边界与维护说明。

## 结果

- `project.json` 集中管理四个正式周与前三周合并范围，校验重复、缺号、范围和模板清单。
- 七类媒体声明从每周模板移入 `src/media/weekNN/`，共 28 份。模板缩减到约 166–178 KB；采用素材内容完全保留。生成文件统一 LF，使跨平台内容哈希稳定。
- 合并版直接从渲染后的源码打包，构建单周也同步合并入口；全部目标校验完成后再逐个原子写出。
- `tools/project.py` 提供 doctor、build、check、serve、release。旧工具路径兼容，历史专用脚本明确标记用途。
- 发布目录仅含 HTML 与校验清单，内容哈希决定版本目录，保留历史目录便于回退。预览仅服务发布文件、仅绑定本机。
- 新增 GitHub 回归配置、EditorConfig 与生成文件标记；已添加文件，未提交、推送或运行远程 CI。
- 维护入口更新为根目录 README 与 `docs/development.md`，同步项目记忆与工具索引。

## 验证

完整本地 Chromium 回归通过：四周进度、G1—G6、W4 测评、移动端、合并导航与锁、家长面板 108 项、W2 冒烟 156 项、W3 冒烟 145 项。

最终静态检查通过：四周数据 248 / 373 / 524 / 359 项、生成一致性、脚本语法、测评 16 个反例和跨周暴露检查、媒体覆盖、五个构建边界测试。后续仅换行与构建测试补充，已重跑最终构建与快速检查。

五个构建测试覆盖配置异常、include 越界／循环、合并失败不覆盖旧周、发布内容与哈希、生成物 LF。28 份采用媒体的值哈希与整理前一致。

发布预览实测：首页进入合并课件，周锁正常，manifest 文件哈希与 HTTP 内容一致，源码／配置请求返回 404。最终 LF 发布目录：`dist/8eb24859056e84d8/`。此目录仅在本机，未发布到公网。

## 保留边界

播放器与部分周主题仍在周模板中；没有宣称完全消除重复代码。真人发音听检、真实移动设备与远程 CI 未在本轮代替执行。进度键、课程内容、既有独立 HTML 路径和用户候选音源目录保留。
