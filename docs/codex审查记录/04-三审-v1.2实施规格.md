# 04 · 三审 — v3 实施规格 v1.2（spec-critique，复审后确认轮）

## 元信息

- 审查日期：2026-08-29
- 审查对象：《声音积木v3游戏版实施规格_20260829_v1.2.md》
- codex 模式：invoke_codex.ps1 / spec-critique / advice-only / 3 个 ContextFiles 完整读入（0 截断）
- codex 版本：codex-cli 0.150.1，confidence: high
- 原话档案：[_originals/codex_spec-critique_20260829_112152.final.txt](_originals/codex_spec-critique_20260829_112152.final.txt)

## 门禁结论

**不通过（1 high）**。codex 确认复审 R-1、R-3～R-8 已闭环、R-2/R-9 方向正确，但二者组合产生新矛盾（stopAll 自我取消）。codex 建议：修正后**只针对音频令牌/兜底状态机做一次窄范围确认审查**。处置：按用户预授权修订出 v1.3，送四审（窄）。

## Claude 拍板记录（依据用户预授权采纳；最终拍板权在放行汇报时归用户）

逐条核实（S-4 的 v2 循环翻卡 `i=(i+1)%items.length` 于 1844 行实锤）：**5 条全真、0 伪**。其中 S-1 是 v1.2 修订自身引入的矛盾（把"外部请求互斥"与"请求内部音源切换"混写），承认为修订质量问题。

| # | severity | codex 意见 | 真伪 | 处置（→ v1.3 落点） |
|---|---|---|---|---|
| S-1 | high | speakFallback 开播前也调 stopAll，会取消等待兜底的父请求；play 先分配令牌再 stopAll 可能取消自身——破坏"一个 Promise 等到底"与"兜底成功=ended" | 真 | ✅ §2.1 重写为 ownerToken 模型（一个逻辑请求=一个 Promise=一个 token，内嵌→TTS 两个物理阶段沿用同 token）+ 生命周期五步表；§2.2 stopAll 合法调用方收窄为"新请求第①步 + 切页清理"，speakFallback/提示条禁止调用 |
| S-2 | medium | 失败条由谁创建、continuation 注册接口未定；G2 第二遍/G3 纠正/G4 出题/词组接续的失败转移没逐项定义；重试可能清掉自己的 continuation | 真 | ✅ §2.3：play 纯结果不渲染 UI，调用方走 showAudioFailure({retry})；补 8 行播放阶段失败转移表；重试=先原子消费 continuation 再执行 |
| S-3 | medium | G5 只有 3/4 槽，白名单却含 at/it/in/an 四个 2 字母词——声明为真词却不可构造，断言也发现不了 | 真 | ✅ §4.5 加 2 槽（2/3/4 槽切换）。选加槽不改白名单：拍板意图是"A 组练习词都是真词"，砍白名单会让孩子拼出 at 却收到假词反馈，违背意图更重 |
| S-4 | medium | v2 翻卡循环（1844 行），30 秒挑战可超 18 张，成绩超清洗上限刷新即被删 | 真 | ✅ §4.6 计时中不循环、翻完 18 张立即停表（上限 18 与清洗一致）；§7.2 门禁 |
| S-5 | medium | 仲裁器命名 Audio.stopAll 遮蔽原生 Audio 构造器，而 v3 内嵌播放依赖 new Audio() | 真 | ✅ 全文改名 AudioBus.stopAll |

## Claude 自检

- 第十人法则统计：5 条中"第十人=找不到"0 次（S-3 走了实质权衡：加槽 vs 改白名单，选择依据拍板意图完整性）。
- quality-first：S-1 属于修订引入的新矛盾（D 类根因·跨章节自我矛盾的变体——两条修订各自正确、组合矛盾），说明大改动后应做"组合语义"通读，已在本轮补上。

## codex 原话（摘要，全文见 _originals）

summary：R-1、R-3～R-8 落点基本闭环，R-2 题目停留与分母口径已修正；但 R-2 与 R-9 组合产生同一播放请求被仲裁器自我取消的 high 级矛盾。不满足 0 high，建议修正音频令牌所有权后再进入 P0。

recommendations：① 修 ownerToken 与内部音源切换规则（唯一 high 门禁项）；② 冻结"一个逻辑请求、一个 Promise、一个 ownerToken、embedded→TTS 两阶段"；③ 补多阶段失败转移表并用验收覆盖四条路径；④ 补 G5 两字母词可达性与 G6 第 18 张行为。

risks：兜底被误返 cancelled 致 G1 卡题；continuation 无唯一所有者致重试语义分裂；G5 四个 2 字母白名单词成死数据；G6 合法成绩刷新被清洗；仲裁器遮蔽原生 Audio 致全部内嵌音频失效。
