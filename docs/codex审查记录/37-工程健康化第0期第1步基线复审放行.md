# 37 · 复审（放行）— 工程健康化第 0 期第 1 步"基线"修复 commit（code-review）

## 元信息

- 审查日期：2026-09-07 上午
- 审查对象：commit 11b234e（归档 36 十条落地），材料含 diff、`tools/baseline.py` 与 `tools/validation/media_declarations.js` 全文、首审原话
- 复审焦点：① 首审 10 条已修 / 部分修 / 未修；② 修复自身引入的新问题；③ 硬上限：无 high 即放行
- codex 模式：invoke_codex.ps1 / code-review / advice-only / **API 中转，显式 `-Model gpt-5.6-sol`**；材料 6.4 万字符
- **通道事件**：第一次（094103）Reconnecting 1/5 到 5/5 后 `turn.failed`，无 final.txt；按用户规则 API 重试，第二次（095356）中途吐出一条空壳 JSON（issues 空、summary 空，是流断开的残片，未采信），最终 `turn.completed` 成功返回。两次都是 API，未切订阅
- codex 版本：codex-cli 0.153.4，confidence: **high**
- 结果：首审 10 条 **8 已修 / 2 部分修（H-2 过渡实现、M-4 未上 AST）/ 0 未修**；新问题 **0 critical / 0 high / 4 medium / 10 low**（10 条 low 里 8 条是逐条"已修"的状态记录）
- codex 收敛判断（原话）："当前未见 high 严重度阻断项，可有条件放行。"
- 分流：0 high 放行；用户拍板五条小改一个 commit（c3dfce2）搭进第 2 步的审，不单独复审；[11] 不采纳；[2] 按方案排期
- 原话档案：[_originals/codex_code-review_health_step1_round2_20260907_095356.final.txt](_originals/codex_code-review_health_step1_round2_20260907_095356.final.txt)

## 第一层：非技术总结

第 1 步放行。首审的十条里八条判已修，两条"部分修"都是方案本来就排在后面的事：临时目录在第 2 步、`--single-file` 在第 1b 期、不上 AST 解析器是决定 H（不加 package.json）的结果。新冒出的四条 medium 里不采纳一条：codex 要求 `check` 也比对合并入口的哈希，但方案 §3.3 明文"course 只记录整文件哈希，不参与任何比对"，合并入口内嵌全部周文件，任何周变了它必变，比它只会重复报错。其余三条 medium 与四条 low 是十行以内的局部收紧，已在收口 commit 落地。

## Claude 核实记录

| # | codex 结论 | 本地核实 | 结论 |
|---|---|---|---|
| [2] H-2 部分修 | 仍读写根目录，未在临时目录显式单文件构建 | v1.3 §3.4 ③：第 0 期与第 1 期直接读默认构建；临时目录第 2 步、`--single-file` 第 1b 期 | 属实，按方案排期，不另改 |
| [4] M-4 部分修 | 仍用 VM；直接调 CLI 无进程级超时 | 决定 H 不加 package.json，Node 无内置解析器；CLI 确实无超时 | 部分采纳：CLI 改 worker 线程 + 120 秒终止 |
| [11] course.sha256 未比对 | 属实的事实，但与 §3.3 相悖 | §3.3："course.html 只记录整文件哈希，不参与任何比对" | 不采纳 |
| [12] declarationOrder 未查类型 | `sorted()` 直接作用于畸形值会抛 TypeError | 属实 | 采纳 |
| [13] base64 不严格 | `Buffer.from` 宽松解码 | 属实 | 采纳：长度、填充位置、往返重编码相等 |
| [14] mock 不证明真实 build 改树 | 属实 | codex 建议跑真实 `build_lessons.py`，成本高；改为真实 git 仓库 + 会改写受控文件的假构建函数 | 采纳（换形态） |
| [8] 多余 week_paths 键静默忽略 | 属实 | 属实 | 采纳 |
| [5] .gitattributes 未核实 | codex 材料里没有 | `* text=auto eol=lf` 已核实（归档 36） | 已核实 |

## Claude 拍板记录（用户 2026-09-07 拍板：按建议，五条小改一个小 commit，[11] 不采纳，[2] 按方案排期）

| # | severity | 摘要 | 落地（c3dfce2） | 用户决策 |
|---|---|---|---|---|
| [2] | medium | H-2 过渡实现 | 不改；第 2 步临时目录、第 1b 期 `--single-file` | 按方案排期 |
| [4] | medium | CLI 进程级超时 | worker 线程提取，主线程 120 秒 `terminate()`，退出码 2 | 采纳 |
| [11] | medium | check 比 course.sha256 | 不采纳（§3.3） | 不采纳 |
| [14] | medium | 真实构建集成测试 | 临时 git 仓库：干净树 create 成功且 sourceCommit 等于 HEAD；构建改写受控文件即拒绝且不写 fixture；未跟踪文件算不干净 | 采纳（换形态） |
| [8] | low | 多余路径键 | 拒绝并点名 | 采纳 |
| [12] | low | declarationOrder 类型 | 先查字符串列表 | 采纳 |
| [13] | low | 严格 base64 | 四例反例测试 | 采纳 |
| 其余 | low | 状态记录 | 无改动 | 无需动作 |

## 第二层：4 件套

- **采纳建议**：五条小改一个 commit，搭进第 2 步的审；[11] 不采纳；[2] 按方案排期。
- **推荐理由**：0 high 已放行；五条都是十行以内的局部收紧，顺手做比留 todo 便宜。
- **反向理由**：[14] 若按 codex 说的跑真实 `build_lessons.py` 做集成测试，要在临时目录复制模板、媒体与校验器，成本高收益低；临时 git 仓库加假构建函数验证的是"构建改动受控文件会被真实 git 检出并拒绝"这条路径，够用。
- **第十人视角**：codex 两轮都在推"临时目录显式单文件构建"和"AST 解析"，说明它把过渡实现当成了风险敞口。它担心的点有一条成立：如果第 2 步拖延，过渡实现会在默认形态切换前一直存在。方案已把它绑在第 2 步，只要第 2 步不跳过就没事；第 2 步的 brief 会明确回应。另一条：通道两次抖动都发生在同一小时内，说明中转在这个时段不稳，后续送审若再失败应直接切订阅而不是机械等第二次。
