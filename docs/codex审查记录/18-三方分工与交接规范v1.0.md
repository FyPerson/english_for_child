# 18 · 一审 — 三方分工与交接规范 v1.0（spec-critique）

## 元信息

- 审查日期：2026-09-02
- 审查对象：《三方分工与交接规范_20260902_v1.0.md》（用户拍板：审一轮）
- codex 模式：invoke_codex.ps1 / spec-critique / advice-only / 4 个 ContextFiles 全部完整读入（0 截断、0 缺失）
- codex 版本：codex-cli 0.150.1，confidence: **medium**（它没拿到校验器、音频脚本与规范全文，两条意见因此判断失准，见下表"真伪"）
- 上下文：总则全文 + 周课件规范节选 + 第二周插画提示词前 60 行 + 音素 README
- 协作模式：双层汇报 + 每条 4 件套 + 拍板汇总表
- 原话档案：[_originals/codex_spec-critique_20260902_120012.final.txt](_originals/codex_spec-critique_20260902_120012.final.txt)

## 门禁结论

**不通过**。codex 原话："角色框架基本成立，但尚不足以让副机使用者'无需询问即可完成并回传'。主要阻塞是副机是否能操作本地文件与终端未定义、工具注入 HTML 与'GPT 不改 HTML'冲突、音频参数权限不一致，以及并行工作会同时修改 README、共享音频和 main 分支。"

10 high、5 medium、2 low，共 17 条。Claude 逐条核实（校验器、音频脚本、插画注入脚本均实读）：15 条为真，1 条为假（H-9，校验器本就接受独立数据层文件），1 条部分为真（M-4，优先级问题在措辞不在方向）。处置：v1.1 另存。

## 拍板记录

| # | severity | codex 意见（要点） | 真伪 | Claude 建议 | 用户决策 |
|---|---|---|---|---|---|
| H-1 | high | 禁止 GPT 改 HTML，步 7 却要求副机注入并提交 HTML；冲突时"HTML 主机赢"会丢掉刚注入的音频 | 真 | 采纳。区分"手改 HTML"（禁止）与"工具生成的 HTML"（派生物，允许提交）。冲突不手工合并：主机从源文件（manifest、raw mp3、PNG、data.js）重生成 | 全按（2026-09-02） |
| H-2 | high | 副机只说有"GPT、git、Python"，没写谁保存文件、谁跑命令、谁提交 | 真 | 采纳。§0 写明执行模型（见连带决策 1）；§3 每步写操作者；步 6、步 7 各给一份从 pull 到 push 的可复制命令清单 | 全按（2026-09-02） |
| H-3 | high | 声线"固定"与 §5"voice 可调"矛盾 | 真 | 采纳。voice 由资源类型决定、不可调：单词句子 Jenny（manifest 顶层），小书 Emma（逐条覆盖）；只允许纠正分类错误 | 全按（2026-09-02） |
| H-4 | high | 小书 -30% 与 -60 dB 既写"固定"又写"可调"，没分固定值与默认值 | 真 | 采纳。§5 改成参数表：固定 / 默认 / 可覆盖三栏。实读脚本：全局默认阈值 -40，小书逐条 -60；rate 按类型默认；manual_trim 仅人工核听后 | 全按（2026-09-02） |
| H-5 | high | 开场提示词要求 1024，庆祝图又要求 384，验收不知看哪个 | 真 | 采纳。GPT 一律交 1024×1024；384 是 `embed_assets.py` 处理后的尺寸（脚本常量 `CELEBRATE_PX=384`）；验收看处理后文件 | 全按（2026-09-02） |
| H-6 | high | 并行时两台机都改 README、可能碰共享音频、都推 main | 真 | 采纳。副机可改路径白名单：`assets/weekNN/**`、`tools/audio_manifest_wN.json`、`assets/audio_raw/`、`assets/audio_processed/`、`weekNN.html`（仅工具注入）；README 只由主机改，副机把状态写进提交说明；push 被拒先 pull 再 push，出现冲突停下交主机；`assets/phonemes/` 与跨周复用词不并行改 | 全按（2026-09-02） |
| H-7 | high | 离开流程先 push 再改 README，工作区立刻又脏 | 真 | 采纳。顺序改为：更新 README → status → commit → push → 确认与 origin 同点；无状态变化不改 README；副机不改 README | 全按（2026-09-02） |
| H-8 | high | 按扩展名裁决冲突太粗，"手工合并 manifest"没指定人 | 真 | 采纳。按真相源裁决：源 = manifest、raw mp3、PNG、data.js；派生 = processed mp3、HTML。冲突由 Claude 按周次和键合并后统一重生成、注入、校验；用户不手工合并 JSON | 全按（2026-09-02） |
| H-9 | high | 步 1 要求 data.js 过校验器，但校验器只见过吃 HTML 的命令 | **假**（`check_data.js` 头注释与代码明确支持独立数据层文件，第二周实跑过） | 部分采纳。步骤不改；把完整命令 `node tools/week-checks/check_data.js tools/week-checks/weekNN-data.js` 与成功标志写进步 1 | 全按（2026-09-02） |
| H-10 | high | `git diff tools/build_audio.py` 只能看未提交改动，查不出已提交的脚本篡改 | 真 | 采纳。主机验收改为：以最近一次带 `Claude-Session` 标记的提交为基准（`git log -1 --grep=Claude-Session`，自算不写死），`git log --stat <基准>..HEAD` 核对副机提交触碰的文件是否全在白名单内 | 全按（2026-09-02） |
| M-1 | medium | 听检结果"记入总表"没有路径、格式、写入者 | 真 | 采纳。用户逐键结论写在发音包回传说明与提交说明；Claude 在步 8 录入总表（前 10 周纸表期间记在 README §4 一行） | 全按（2026-09-02） |
| M-2 | medium | 验收表混合了自动项与人工项 | 真 | 采纳。表加"类型"列：自动（给命令与成功输出）/ 人工签字（用户） | 全按（2026-09-02） |
| M-3 | medium | 配图包要求提供定妆图又说先出定妆图；文件名规则没写扩展名、大小写、序号 | 真 | 采纳。配图拆两阶段（先定妆、确认后场景）；文件名 `<art键>.png`、小写严格一致、禁空格、中文、版本后缀、"(1)" | 全按（2026-09-02） |
| M-4 | medium | 头部"以专题文档为准"与 §7"实作 > 规范"矛盾；上周 bug 会覆盖规范 | 部分真 | 部分采纳。保留项目既定顺序（周课件规范自己的头部就是"实作 > 规范"，理由是规范抽自实作、可能滞后），但加限定：只在规范写漏或写错时以实作为准并回改规范，实作里的已知 bug 不因此成为规范。头部措辞同步改 | 全按（2026-09-02） |
| M-5 | medium | 出处引用不可定位；"第三周数据层交接 §4"是一次性文档 | 真 | 采纳。出处全部改为仓库相对路径 + 章节；文案口吻规则回收进周课件规范 §7.3（小改不升版），本文改引规范 | 全按（2026-09-02） |
| L-1 | low | "0-high"措辞不清 | 真 | 采纳。改为"critical 与 high 为 0；medium 逐条处置或由用户书面接受" | 全按（2026-09-02） |
| L-2 | low | §5 与规范重复；§8 每周复盘是文档噪声 | 真 | 采纳。§5 收成"谁能改什么 + 参数表"（与 H-4 合并），处理细节指向规范 §6、§11；§8 改为"职责、路径、验收规则变化时才修订" | 全按（2026-09-02） |

recommendations 6 条与 issues 同源；risks 5 条为不修后果陈述，无独立拍板项。

## 连带决策（同轮用户拍板，2026-09-02）

1. 17 条处置：全按 Claude 建议（H-9 只补命令；M-4 保留"实作 > 规范"并加限定）
2. 副机执行模型：GPT 客户端自己能读写本地文件和跑终端；命令清单写给 GPT，用户逐步确认
3. 副机可以提交工具注入后的 HTML（派生物）；主机冲突时按源文件重生成，不手工合并
4. 修订为 v1.1 另存，不再送审，随 README 入口一起提交
