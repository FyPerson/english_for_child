# 34 · 轻复审（只审 diff）— 工程健康化实施方案 v1.1（spec-critique）

## 元信息

- 审查日期：2026-09-07 晚
- 审查对象：`docs/工程健康化实施方案_20260907_v1.1.md`，只审两段 diff：v0.4 → v1.0（四审后落地的 7 条，归档 33 时未送审）与 v1.0 → v1.1（用户按 40 到 50 周规模复议，拍板路线 C 目录交付加共享媒体目录、保留 `--single-file`、PNG 不转 webp、v1.0 §8 五项落地）
- 复审焦点：① 两段 diff 有没有引入新缝；② 新旧段落矛盾；③ 8 条基础假设逐条证伪（A 本地文件下 Chromium 系父子共享 localStorage；B 前端只把声明值当 src；C 四个非累计指标 50 周不触线；D 壳 payload 只含清单足以门控；E `event.source` 校验足够；F 两种形态共用基线 media 表可比；G 09-14 到 09-24 能赶上第五周；H `build/` 联动列全）；④ 硬上限：无 critical 且 high ≤ 2 即"修完即可定稿"
- codex 模式：invoke_codex.ps1 / spec-critique / advice-only / **API 中转（aiapipay），显式 `-Model gpt-5.6-sol`**；预检小请求通过后正式送审；1 个 ContextFiles 约 10 万字符（两段 diff、v1.1 全文、8 个实作文件），`skipped-context-budget` 0；后台 1200 秒模式，实际约 6 分钟返回
- codex 版本：codex-cli 0.153.4，confidence: **high**
- 结果：**0 critical / 3 high / 10 medium / 0 low**；八条假设：A 条件成立、B 条件成立、**C 不成立**（"从构造上不会触线"说过头：指标不累计，但单周新增、单个文件、代码本身仍可超阈值）、D 条件成立、E 条件成立但要补会话号与 targetOrigin、F 条件成立但验收偏弱、G 条件成立但延期预案与核心约束冲突、H 条件成立但要写成可执行断言
- codex 收敛判断（原话）："由于 high 超过两条，当前不建议直接定稿，完成上述修订后再做一次定向复核。"
- 五轮走势：14 → 12 → 11 → 7 → 13 条；critical 2 → 2 → 0 → 0 → 0；high 7 → 6 → 6 → 2 → 3。本轮条数回升是因为 v1.1 新增了 §3.6 协议、§3.7 zip、第 1b 期三段新内容，13 条里 9 条落在这三段
- 分流：用户拍板 13 条全部采纳落成 v1.2（H-3 选"硬截止不写预案"）；并拍板再送一轮只审 v1.1 → v1.2 diff 的定向复核，硬上限（归档 35）
- 原话档案：[_originals/codex_spec-critique_health_plan_round5_20260907_084918.final.txt](_originals/codex_spec-critique_health_plan_round5_20260907_084918.final.txt)

## 第一层：非技术总结

路线 C 的方向没有被推翻：八条基础假设里七条成立或条件成立，唯一不成立的是我写在方案里的一句话说过头了。护栏改成非累计指标以后，能保证的是"不会因为复习媒体越积越多而触线"，不能保证"某一周素材放大了也不触线"。后者本来就是护栏该抓的事，措辞要改回诚实的说法。

三条 high 都是我在 v1.1 新写的段落里留下的缝，不动八项决定，也不动路线 C：Release 步骤里发布命令引用的文件路径没写清，真跑会找不到文件；基线命令在第 1b 期默认形态切换后会取错形态；延期预案允许临时放宽护栏，和"无论如何不触线"自相矛盾。十条 medium 集中在壳与周文件之间的消息协议（会话号、超时、发送目标）、构建残留、硬编码数字、zip 可重现、单文件暗门，修法都明确。

需要用户拍板两件事：延期时怎么处理（用课程方案已有的"复习包顶一周"机制，还是保留一次有记录的临时放宽）；修完是否按 codex 建议再送一轮只审 v1.1 → v1.2 diff 的定向复核。

## Claude 核实记录

| # | codex 结论 | 本地核实 | 结论 |
|---|---|---|---|
| H-1 | Release job 里 `gh release create … manifest.json` 未写工作目录与文件路径；GH_TOKEN、权限未写成可执行契约 | v1.1 §3.7 表格：下载到 `dist-artifact/`，断言一个 `soundblocks-*` 目录，然后直接 `gh release create ${{ github.ref_name }} soundblocks-<tag>.zip manifest.json`；未写 cd 或绝对路径，未写 `GH_TOKEN` 环境变量；`contents: write` 写在表格"权限"列，不是 YAML 契约 | 属实 |
| H-2 | `baseline --create` 内部执行默认 build，第 1b 期默认形态已是引用形态，重取的基线形态会错 | §3.3"命令内部执行 build 并记录 HEAD"；§3.4"第 1b 期起默认输出引用形态"；第 1b 期第 6 步"`baseline --create --force`（`form: single-file`）"三处合起来确实矛盾 | 属实 |
| H-3 | 延期预案"临时把 week 改 10、course 改 24"是对核心约束的显式例外，且无审批、到期、防扩散规则 | v1.1 §7 第五条与 §3.5 过渡指标段都写了这个预案；六年课程方案 §9.1 已有现成机制："某周课件没赶上，就用它（W4 复习包）做复习周，周次不推进" | 属实；修法有课程方案依据 |
| M-4 | "从构造上不会触线"不成立，只能保证不因累计触线 | §0、§2 推算表、§3.5 都用了这句话；实测前三周单周新增原始字节最大 2.47 MB，预算 6 MB 是它的 2.4 倍；每周新词数受每日 30 分钟与新旧 2:8 约束，但方案没写这层依据 | 属实 |
| M-5 | 协议无会话号与周号，旧回包可能被误套用到新周 | §3.6 五条消息只带 `week`（部分）与 `version`，壳只校验 `event.source`；切换周时 iframe 换 src，旧文档卸载，但壳侧未到期的计时器仍可能收旧回包 | 属实 |
| M-6 | ready 2 秒、leave 1 秒写死；leave 超时按不可离开会锁死用户 | §3.6"1 秒无回执按不可离开处理并提示"，无重试与强制返回入口 | 属实 |
| M-7 | 未规定发送端 `targetOrigin` | §3.6 只写了接收端 `event.source` 校验；本地文件下 origin 为 "null" 只能用 `*`，http 下应用精确 origin | 属实 |
| M-8 | 第 1b 期不比骨架就重取基线，桥接改造期间的任意代码改动会漏过 | §3.3 末段确实写"第 1b 期只比 declarationOrder 与 media"；桥接是固定 include 片段，可定义"去掉桥接段后的骨架哈希等于基线" | 属实 |
| M-9 | `build/media/` 文件数硬编码 348；16 位前缀碰撞未写 | 第 1b 期第 1 步与 §5 都写了 348 与 6.2 MB；§3.8 末条只写命名未写碰撞 | 属实 |
| M-10 | 未写每次构建是否清空 `build/`，陈旧媒体会被 package 纳入 | §3.4 只写"产物写 build/"；`--single-file` 写进 build/ 后 package 白名单也拦不住（它就是 weekNN.html） | 属实 |
| M-11 | zip 字节不可重现；"可重现"只覆盖目录文件 | §3.7 验收写"解压 zip 后 manifest 逐文件核对通过"，未声明 zip 自身不作可重现验收 | 属实 |
| M-12 | `--single-file` 只报告不判定，成为绕过护栏的正式交付暗门 | §1 第二表 I 与 §3.5 报告项确实如此；但 v1.1 已写"不产壳、不产 media/"，未写"不进 Release、不在 CI 用" | 部分属实 |
| M-13 | `hasProgress` 布尔值只能发现一侧有键另一侧无键 | §3.6 协议表如此 | 属实 |

## Claude 拍板记录

| # | severity | 摘要 | Claude 建议 | 用户决策 |
|---|---|---|---|---|
| H-1 | high | Release 步骤路径、GH_TOKEN、权限 | 采纳：§3.7 写成可执行契约：`working-directory` 固定、发布前 `test -f` 两个附件、`GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`、job 级 `permissions: contents: write`；zip 与 manifest 从同一版本目录生成，上传前解压核对 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| H-2 | high | 基线命令形态 | 采纳：`baseline --create` 固定调用 `build --single-file --output-dir <临时目录>`，基线 `form` 恒为 `single-file`；比对命令与验收由同一形态驱动；两种形态各加一个"错误形态拒绝"测试 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| H-3 | high | 延期预案与"永不触线"冲突 | 二选一交用户：① 删除放宽预案，第 1b 期没赶上时第五周不装配，按六年方案 §9.1 启用复习包顶一周、周次不推进（推荐，护栏不碰、机制现成，代价是孩子少一周新内容）；② 保留一次有记录的临时放宽，但写成带到期日与审批记录的独立配置，CI 标记为非发布构建 | **用户选第三条：不写预案，第 1b 期与里程碑 2 都是硬截止，赶不上届时另议，护栏不放宽**（2026-09-07 晚，先问清"延期"含义后拍板） |
| M-4 | medium | "从构造上不会触线"措辞过强 | 采纳：改为"不会因累计复习媒体触线；单周新增由预算约束，触线是信号不是故障"，并写明依据：单周新增受每日 30 分钟与新旧 2:8 限定，实测最大 2.47 MB，预算 6 MB 为其 2.4 倍 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-5 | medium | 协议加会话号 | 采纳：壳每次打开周生成 `sessionId`，五条消息都带 `sessionId` 与 `week`，壳只接受当前会话回包，切换周先作废旧会话 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-6 | medium | 超时可配置与 leave 不锁死 | 采纳：两个超时值放 `course-gate.js` 常量并被慢端测试覆盖；leave 超时显示"正在等待课件响应"加"重试"与"留在本周"两个按钮，不永久阻塞 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-7 | medium | 发送端 targetOrigin | 采纳：本地文件用 `*` 并依赖会话号与 source 校验；http/https 用 `location.origin`；iframe 换 src 即作废会话 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-8 | medium | 第 1b 期骨架验收 | 采纳：定义"去桥接段骨架哈希"：把 `/* SOURCE: shared/course-bridge.js */ … /* END SOURCE */` 段替换为占位符后取哈希，必须等于基线 `skeletonSha256`；重取基线前保存差异报告 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-9 | medium | 348 硬编码与前缀碰撞 | 采纳：改为"等于四周清单引用文件按 sha256 去重后的数量"，6.2 MB 只报告；构建前按完整 sha256 分组，同 16 位前缀对应不同完整哈希即失败 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-10 | medium | 构建残留 | 采纳：构建先写临时目录再原子替换 `build/`；`--single-file` 必须显式 `--output-dir` 且不得是 `build/`；package 遇未声明文件失败（已有白名单） | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-11 | medium | zip 可重现 | 采纳第二分支：声明 zip 仅为封装，不作可重现验收；解压后逐文件哈希是唯一验收；zip 自身 sha256 写入 Release 说明 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-12 | medium | 单文件暗门 | 采纳：`--single-file` 仅限人工导出，不进 Release，CI 不使用；命令打印每个文件字节并在超过过渡值 8 MB 时给出非零退出的显式确认参数 `--allow-large` | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |
| M-13 | medium | hasProgress 布尔 | 采纳：改为 `progressDigest`（键是否存在、存储串长度、简单哈希），壳比较摘要，不一致时给导出与恢复提示 | 采纳，落 v1.2（2026-09-07 晚拍板"13 条全部采纳"） |

## 第二层：4 件套

- **采纳建议**：13 条全部采纳，落成 v1.2；H-3 的修法由用户在两个分支里选。
- **推荐理由**：13 条里 9 条落在 v1.1 新增的 §3.6 协议、§3.7 zip、第 1b 期三段，另 3 条在 §3.3 与 §3.5 的改动处，只有 M-4 涉及 §0 的措辞。全部是修订自造的缝（《修订稿机械核对》第三次实证：机械核对抓得住引用与数字，抓不住"新段落之间的语义矛盾"），修法局部，不碰八项决定与路线 C。
- **反向理由**：M-11 若按"zip 可重现"分支修会引入固定时间戳与排序等额外工程，收益只是附件字节一致，所以选"不作验收"分支；M-6 把超时做成配置会多一个配置面，但慢机实测比数值本身更重要，所以采纳"常量加慢端测试"而不是"用户可配"。
- **第十人视角**：H-3 codex 把"无论如何不触线"读成绝对约束。用户原话的语境是设计层面（不要因为累计而必然触线），排期延误是另一类风险。"复习包顶一周"的代价是孩子少学一周新内容，这个代价比一次有记录的临时放宽更重，工程纯度不该默认压过教学节奏。我仍推荐复习包分支，因为它是课程方案已有机制、不需要新造"临时配置加审批"这套东西，但这是教学代价换工程一致性的取舍，必须用户拍板。另一条：codex 建议再送一轮定向复核；四轮实证里我每轮修订都自造缝，本轮 13 条又有 9 条集中在同一协议段，再审一轮的 7 分钟成本远低于第 1b 期实施时才发现协议矛盾的返工成本，所以我倾向再送一轮，但用户此前只授权"补一轮"，由用户决定。
