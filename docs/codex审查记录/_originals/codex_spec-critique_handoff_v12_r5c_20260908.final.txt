{"task_type":"spec-critique","mode":"advice-only","summary":"第五稿解决了工作流所有权、临时工作树清理、Chromium 替代门槛等多数问题，但音频键级校验和 `LOCAL_BEFORE` 基线仍存在实质性逻辑错误，当前命令不能作为可靠门禁。`META.status` 仍只是待实现设计，三道人工作法只能降低风险，不能形成发布保证。","issues":[{"severity":"high","category":"contradiction","location":"§6.3 键级校验第②至③步","problem":"`allowed.txt` 每行只有路径，而 `actual.txt` 每行写成“状态加路径”，例如 `M resources/assets/audio_raw/milk.mp3`。随后执行 `comm -13 /tmp/allowed.txt /tmp/actual.txt` 时，两边记录格式不同，合法改动也会被判为越界。此外，`--find-renames` 的重命名记录由多个路径字段组成，`awk` 只取 `$NF`，无法核验被移走的源路径。","suggestion":"把实际变更规范化为纯路径后再比较，并对重命名同时输出源路径和目标路径；状态另行检查，发现 `D` 或 `R` 可直接拒绝。不要让状态字符进入与 `allowed.txt` 比较的集合。"},{"severity":"high","category":"edge-case","location":"§6.3 键级校验第④步 JSON 脚本","problem":"`changed = {k.split('.')[-1] ...}` 取得的是发生变化的叶字段名，典型结果会是 `rate`、`manual_trim`，而不是音频键 `milk`。`flat()` 也只递归字典；若 manifest 条目存于数组，整个数组会被视为一个叶值。脚本因此可能持续误报，也可能把多个条目错误折叠成同一个字段名。","suggestion":"按 manifest 的真实 schema 定位“音频条目键”，不要从扁平路径最后一段猜测。分别解析基准版和当前版为 `音频键 -> 条目对象`，对键集合的新增、删除及每个条目对象的变化求差；若条目是数组，则按明确的 `key` 或 `id` 字段建索引。应使用一份真实 manifest 和新增、修改、删除三类夹具验证脚本。"},{"severity":"high","category":"missing-constraint","location":"§6.3 键级校验第④步","problem":"音频文件差异同时覆盖已提交、暂存区和工作树，但 manifest 脚本只比较 `<BASE>` 与 `HEAD`，提交前尚未提交的 manifest 修改不会被检查。此时文件检查与 manifest 检查观察的状态范围不一致。","suggestion":"提交前校验应读取工作树中的 manifest，并与 `<BASE>` 版本比较；主机回收已推送提交时才比较 `<BASE>` 与 `HEAD`。最好拆成“副机提交前”和“主机回收”两条明确命令。"},{"severity":"high","category":"contradiction","location":"§6.3 `LOCAL_BEFORE` 定义及 pull 流程","problem":"文档要求“每次 pull 之前”记录 `LOCAL_BEFORE`，但 pull 成功后若引入主机提交，`LOCAL_BEFORE..HEAD` 必然包含这些主机提交，并非文中声称的“只看自己的改动”。连续 pull 会重复产生同类误归属。提交或 stash 只能解决未提交改动遗漏，不能解决 pull 引入提交被纳入范围的问题。","suggestion":"每次 pull 完成且冲突处理结束后，将 `LOCAL_BEFORE` 更新为新的 `HEAD`，然后才开始本轮副机修改。若 pull 前已有副机提交，则先记录其提交列表或单独核验后再 pull，不能通过覆盖一个 SHA 同时保留前后两段归属信息。"},{"severity":"high","category":"missing-constraint","location":"§3 `META.status` 与人工闸门","problem":"规范已正确承认工具尚未实现，但表格仍用“加入 `courseWeeks` 必须失败、release 必须拒绝”的强制口吻，容易被读者误认为当前已有机器保证。标题前缀虽然会随独立 HTML 被复制，但仍可被人工提前删除，且不能阻止文件被使用。","suggestion":"把当前状态与目标状态分成两张表：当前仅有人工警示，目标工具门禁尚未生效。在 `build_lessons.py`、发布或打包入口以及 CI 落地并有反例测试前，不得把该条件列为已具备的门禁。"},{"severity":"medium","category":"contradiction","location":"§3 步骤1验收列","problem":"步骤1的产物已改为“不含代码”的教学设计工作包，但验收仍是对 `weekNN.data.js` 执行 `check_data.js`。这只能验证已有起点或旧数据，不能验收教学设计工作包，也与步骤1b的数据层职责重叠。","suggestion":"步骤1验收改为明确的人工教学设计检查表；把 `check_data.js` 完整移至步骤1b。若希望步骤1提前验证词表，应增加专门的结构化工作包或预审工具，而不是校验尚未产出的最终数据文件。"},{"severity":"medium","category":"ambiguity","location":"§3 步骤顺序及阶段A节奏","problem":"表格顺序为1、1b、2，但步骤1b又要求“在步2那份起点上改”；阶段A节奏写成1、2、1b。读者按表格顺序执行会遇到不存在的起点。","suggestion":"将表格物理顺序统一为1、2、1b，或把骨架改为1a、数据层改为1b，并在阶段A、阶段B均使用同一编号顺序。"},{"severity":"medium","category":"edge-case","location":"§6.3 manifest 键集合双向一致规则","problem":"规则要求 `KEYS` 中每个键都必须对应 manifest 值变化，但合法场景可能只是因工具或源音频问题重新生成同参数的 MP3，此时 manifest 无需变化，却会被判为缺失。相反，manifest 改动与文件重新生成应分别验证。","suggestion":"明确本流程是否禁止“参数不变的重新生成”。若允许，应把 `KEYS` 拆为 `MANIFEST_KEYS` 与 `AUDIO_KEYS`，或规定 manifest 改动键必须是音频键集合的子集，而非强制双向相等。"},{"severity":"medium","category":"missing-constraint","location":"§6.3 键名约束","problem":"仅禁止空白和路径分隔符不足以保证 shell、文件系统和 Git 路径安全，例如前导短横线、点路径、控制字符、通配符或 Windows 保留名称仍可能破坏命令或文件映射。把完整字符集约束放进 manifest 契约是合理边界，但本文目前没有指向具体章节，也没有说明门禁是否已经存在。","suggestion":"在 manifest schema 或周课件规范中定义并自动校验键名，例如限定为 `^[a-z0-9][a-z0-9_-]*$`，并排除 Windows 保留名称；本文只保留明确引用和“校验失败交主机”的操作规则。"},{"severity":"medium","category":"ambiguity","location":"§6.6 Node 硬门槛及§0说明","problem":"一处规定副机没有 Node 时“不要继续任何任务”，随后又说由主机构建并回传截图或字节数，容易被理解为副机收到回传后仍可继续配图或调音。是否允许这种降级协作没有说清。","suggestion":"明确二选一：要么无 Node 时副机任务整体终止；要么定义只允许生成尚未入库的候选素材、禁止提交和宣称验收通过的降级流程。"},{"severity":"low","category":"readability","location":"§6.6 体量护栏脚本","problem":"脚本缺少非零退出码；即使显示“触线”或“不存在，未判定”，后续命令仍可能继续，和“每次必跑、触线即停”的门禁措辞不一致。","suggestion":"累计触线或缺失状态并以非零码退出，或者明确该脚本只是人工观察项，真正门禁由 `check` 承担。"}],"recommendations":["四审24条复核结论：1已修；2部分修；3已修；4未修；5已修；6部分修；7已修；8已修；9已修；10已修；11部分修；12无法从提供的四审摘要独立判定；13部分修；14已修；15已修；16已修；17部分修；18部分修；19未修；20部分修；21已修；22已修；23已修；24部分修。这里的“部分修”主要表示文字已补充但门禁逻辑仍不完整。","优先重写§6.3键级校验：统一比较记录格式、正确处理重命名、按真实 manifest schema 提取音频键，并分别覆盖工作树与已提交状态。","把 `LOCAL_BEFORE` 的记录时点改为同步完成后，并为“pull前已有本机提交”的情况定义独立提交集合，避免用单个移动基线表达两段历史。","在 `META.status` 工具支持落地前，将三个措施统一称为人工风险缓解措施；发布保证只能来自构建、打包和CI共同拒绝 scaffold 的自动测试。","将键名完整字符集放入 manifest 契约是合理的，但本文必须指向具体文件、章节和校验命令，不能只写“出现就找主机改”。","为流程类条目标注“待首次真实交接演练验证”是合理做法；验证记录应至少包含基准SHA、pull前后HEAD、副机提交列表及最终差异范围。"],"risks":["当前键级脚本可能把全部合法音频改动误判为越界，导致流程无法执行；修正格式后，错误的 manifest 键提取又可能产生误报或漏报。","错误的 `LOCAL_BEFORE` 时点会把主机并行提交归到副机名下，可能诱发错误回滚或错误的越权结论。","在 `META.status` 未实现前，独立的 scaffold 周文件仍可能被误拷贝并用于上课；标题警示不是强制控制。","步骤编号与验收对象不一致，可能使执行者在尚未完成数据层时误认为教学设计已通过自动验收。"],"confidence":"high","notes_for_claude_code":"本次仅基于所给文档和 `run_checks.py` 文本进行静态审查，没有执行命令。四审原始24条全文未提供，因此编号12无法可靠复原，其他逐条结论是按第五稿的落实对照和现行规范反推。进一步确认需要读取真实 `audio_manifest_wN.json` schema、`tools/build_lessons.py`、release/package入口、CI发布工作流及四审原报告；尤其不要在未看真实 manifest 结构前直接采用第④步脚本。"}