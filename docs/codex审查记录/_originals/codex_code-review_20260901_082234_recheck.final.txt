{"task_type":"code-review","mode":"advice-only","summary":"复审未完全通过，0 high。从提供的片段看，核心运行时代码方向基本正确，未发现明确的新业务功能 bug；但 M-5、M-6、M-8 的验证闭环仍不完整，并新增了规范表述不一致和冒烟断言不足。","issues":[{"severity":"medium","category":"contradiction","location":"04-spec-changed-sections.md §5.3、§5.4","problem":"这是修复过程引入或遗留的规范不一致。§5.3 仍写“PHONEME_AUDIO[b.s]”并把 G4/G5 无录音状态称为“静态积木”，但 §5.4 明确要求所有判断经过 hasPhoneme/phAudioKey；实际 G4/G5 仍是承担摆词行为的按钮，并不静态。后续换周照 §5.3 实现可能重新引入 c/k 直查问题，或错误地把 G4/G5 改成不可操作元素。","suggestion":"把音素按钮守卫改写为 hasPhoneme(b.s)。将积木行拆成两类：tileHTML/点亮墙无录音时改为非交互元素；G4/G5 保留摆词按钮，仅移除 data-sayph，并把 aria-label 改为“字母 x”。"},{"severity":"medium","category":"missing-constraint","location":"tools/build_audio.py：check_week_pairing、apply_manifest_overrides","problem":"M-8 修复不完整。check_week_pairing 在 manifest.week 缺失时直接返回，而顶层字段也不拒绝拼写错误；第二周清单若漏写 week 或误写为 weeks，错配保护会被静默关闭。当前第二周清单能受保护，但机制本身尚未闭环。","suggestion":"对显式传入的 --manifest/--target，或包含 source_specs 的新版清单，强制要求 week 存在且为大于零、非 bool 的整数；仅为旧版默认第一周清单保留缺省兼容。最好同时校验 g1_round_keys、g5_whitelist_group、expected_items 的类型。"},{"severity":"medium","category":"edge-case","location":"tools/week-checks/smoke_w2_browser.py §⑦","problem":"这是新增冒烟段带来的可靠性问题。G3/G4 多处依赖 900、1500、2000 毫秒固定等待，实际流程又受音频结束和动画驱动，较慢机器上可能竞态。刷新后的 confirms 只断言 bool(cf)，若 G4、G5 两项中有一项恢复失败而另一项仍在，测试仍会通过。localStorage.clear 后 reload 的做法本身足以消除前序打卡 toggle 的状态依赖，1400 毫秒长按也有合理余量。","suggestion":"用积木架出现、按钮可用、轮次结束或 localStorage 字段出现等状态条件代替固定等待；刷新前保存 G4 订单词和 cat 的值，刷新后分别断言 confirms[word] 与 confirms.cat，而不是只断言对象非空。"},{"severity":"medium","category":"missing-constraint","location":"tools/week-checks/check_w2_data.js：visibleText","problem":"M-6 修复仍有漏网面。收集器只递归字符串和数组，并只读取若干 DAYS/BOOK/G1_THEME 字段；SOUNDS 中会显示的 mem、cue、challenge、try、pass、how、warn、demo 未纳入，对象形 items 也会被跳过。因此“所有会显示给孩子看的英文文本”这一注释强于实际覆盖能力。当前材料不能证明这些遗漏字段里已经存在保留词，但回归保护并不完整。","suggestion":"至少显式收集 SOUNDS 的全部可见字段及 demo；让递归器处理普通对象，或改为在各天实际渲染后克隆 DOM、移除 exam 节点，再扫描 textContent。exam 是按块排除的，不会连带排除同一步骤里的 sibling checks；按 [a-z]+ 分词也不会误伤包含保留词字母的较长单词，若可见说明中完整出现保留词，应当视为真实泄漏。"},{"severity":"medium","category":"other","location":"tools/week-checks/smoke_w2_browser.py §①及容器相关检查","problem":"H-1 的实现片段正确，但现有回归断言无法锁住关键语义：它只检查新音积木没有 data-sayph，原先那种“button 仍在但没有 data-sayph”的错误也会通过。新增的 G1 图标容器省略和 book__art 收起同样没有对应的几何或可见性断言。","suggestion":"明确断言首页七个新音均为 div.tile[role=\"img\"]、只有 cat 示例是按钮，并检查不存在 button.tile:not([data-sayph]) 的新音积木；对缺图 G1 图标断言容器不存在，对小书图位断言 hidden 且计算样式或尺寸确实为不可见。"},{"severity":"low","category":"missing-constraint","location":"tools/audio_manifest_w2.json：unreachable_keys、frozen、unreachable_note","problem":"M-3 属于明确接受的技术债，而不是彻底修复：8 条不可达音频及体积仍保留。新增字段纯登记且不参与校验，未来数据变化后可能失真；同时 frozen 所称“全部运行时可播放键”与 unreachable_keys 的存在语义不够一致。","suggestion":"保留音频的产品取舍可以接受，但应至少校验 unreachable_keys 为无重复字符串数组、全部属于 items、与 unreferenced_keys 不相交，并按当前提取结果核对。把“全部运行时可播放键”改成“十个静态提取源的引用键并集”，明确它包含暂时无入口的键。"}],"recommendations":["先修正规范 §5.3，使其与 hasPhoneme 及 G4/G5 的双重能力模型一致。","补齐 manifest.week 的必填条件，否则 M-8 的错配保护存在静默绕过路径。","将 M-5 的固定延时改为状态等待，并逐项验证 G4/G5 刷新恢复。","扩展 M-6 到 SOUNDS 可见字段或直接扫描渲染后的非周检 DOM。","为 H-1 和两个缺图容器增加针对元素类型及实际可见性的回归断言。"],"risks":["目前没有材料显示上述缺口已导致当前第二周页面的实际运行错误，但它们可能让未来回归在自检全绿时漏过。","M-5 的固定延时可能造成跨机器偶发失败，削弱冒烟结果的可信度。","缺少完整 week02.html，无法穷举确认是否仍存在绕过 hasPhoneme/phAudioKey 的直接 PHONEME_AUDIO 下标访问。"],"confidence":"medium","notes_for_claude_code":"就特别核查点而言：H-1 的换元素实现成立，G4/G5 保留 button 也成立，因为点击仍有摆词行为；Phone.say 用录音键缓存合理，stopAll 若只是遍历 cache 的 Audio 值，则键从字母改为录音键不会影响停止逻辑；M-4 路径修改、M-2 数据化和 L-1 配色片段未见问题；source_specs 要求字段恰好三个并不过严，旧第一周缺少该字段时走兼容分支也不会受这项覆盖影响。最终签字前仍需读取完整 week02.html，搜索 PHONEME_AUDIO[、检查 Phone.stopAll 全文、缺图容器 CSS，以及 README 中对 M-5 覆盖缺口的实际说明。"}