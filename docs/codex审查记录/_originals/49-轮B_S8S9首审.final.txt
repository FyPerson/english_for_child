{"task_type":"code-review","mode":"advice-only","summary":"静态审查发现一项高风险基线口径遗漏：除声称的五条语义差异外，`course.sha256` 也发生变化且未解释。第 8 步可见调用点的加载顺序基本合理，但固定错误码、生产残留清零及部分规则“已实现”的证据仍不充分。","issues":[{"severity":"high","category":"contradiction","location":"tests/fixtures/baseline_20260907.json:3950；第 9 步基线重取说明","problem":"基线说明称重取前差异“恰 5 条”，即四周 `skeletonSha256` 加 week01 移除 `pit` 媒体；但差异中 `course.sha256` 也从 `dd6eee...` 变为 `0555e6...`。它不是日期、提交号或工具版本一类审计元数据，至少构成一项未解释的内容哈希变化。四个完整 `sha256` 的变化可由骨架或媒体变化派生，但 `course.sha256` 不能据当前材料归入这五条。","suggestion":"在批准基线前单独追溯 `course.sha256` 的输入和差异来源；若它是里程碑数据迁移的预期结果，应将其明确列为第 6 条并加入基线差异白名单；若比较器没有检查该字段，应补上检查，避免静默重取未知课程级变化。"},{"severity":"medium","category":"edge-case","location":"tools/validation/load_data.js:18-22","problem":"固定错误码只在 `vm.runInNewContext(...)` 成功且随后发现 `box.META` 缺失时才会产生。真实旧 HTML 会先经过声明提取和 VM 执行；若旧声明存在重复、语法不兼容、引用缺失或执行异常，调用方会先收到 `SyntaxError`、`ReferenceError` 或超时，而不是声称的 `legacy-html-fallback-rejected`。新增测试的旧 HTML 样例未证明真实历史产物能够越过这一阶段。","suggestion":"在执行提取出的声明前先可靠检测 HTML 是否含可提取的 `META`；缺失时立即抛固定错误码。至少再用一份真实旧产物 fixture 覆盖该路径，并断言错误码，而不是仅使用最小合成字符串。"},{"severity":"medium","category":"other","location":"docs/周课件数据层交接规范_20260908_v2.0.md:319-336，规则 DATA-PATTERN-01","problem":"`DATA-PATTERN-01` 被改为“已实现”，理由是共享抽取器覆盖 15 类来源并有逐类差分测试，但本轮差异没有包含 `word_consumers.js` 或对应测试实现，无法从所给材料核实 `wordforge`、`BOOK.pages` 以及全部 15 类来源确实接入常规校验器。相比之下，DATA-RACK-01、DATA-PATTERN-02 和 DATA-WALL-01 至少能在本差异中看到部分生产调用或测试证据。","suggestion":"Claude Code 应回读引入共享抽取器的早期提交及 `word_consumers.js`、`check_data.js` 的调用链和逐来源测试，再决定是否保留“已实现”；若本轮无法补齐证据，文档应写明对应实现提交和测试文件，而不是只给叙述性结论。"},{"severity":"medium","category":"other","location":"tests/fixtures/baseline_20260907.json:4-10","problem":"基线重取同时把 Python 3.12.14 降为 3.12.0、Node v24.19.0 降为 v24.11.1。即使这些字段当前不作为门槛，使用不同且更旧的运行时生成固件会削弱基线可复现性，并可能把版本相关序列化或构建差异混入已接受哈希。","suggestion":"固定生成基线的受支持工具版本，或至少先在项目标准版本上重复生成并比较哈希；若必须接受不同机器版本，应记录其非门槛范围，并增加跨版本产物一致性检查。"},{"severity":"medium","category":"performance","location":"体量记录；第 8 步收尾说明","problem":"week04 仅余 0.3302 MB，而第五周会新增页面内容和内联媒体；当前材料只记录事后硬上限，没有显示接近上限时的预警或按周增长预算。`course.html` 当前 13.78/20 MB 虽尚有空间，但逐周合并后风险会持续累积。","suggestion":"保留现有硬失败阈值，并增加分级预警，例如单周剩余不足 0.5 MB 或预计新增媒体超过剩余额度时提前失败；对合并入口增加按新增周估算的增长预算和趋势输出。不要仅提高硬上限，否则会延后暴露问题。"},{"severity":"low","category":"contradiction","location":"docs/任务_里程碑2双字母积木_20260909.md：顶部状态、§11b、质量三层","problem":"同一收尾文档中出现“26 个 commit”与“22 个 commit”两种数量；“已自决 5 条”列出 P6/P9/P10/P12/P14，但质量三层写成“自决待追认 4 条”。这些数字会使收尾审计和后续交接失真。","suggestion":"从明确的起止提交统一机械统计 commit 数，并把自决数量统一为 5；同步检查顶部快照、§11b、质量指标和返工账本。"},{"severity":"low","category":"readability","location":"docs/周课件数据层交接规范_20260908_v2.0.md:123-128","problem":"“四周实测现值”只列 W1、W2 和 W4，遗漏 W3；下文才说明 W3 为十九项。这会让副机照规范编写 W5 时误以为 W3 状态未确认。","suggestion":"在同一句中完整列出 W1=6、W2=13、W3=19、W4=19，或改为链接到唯一的逐周实测表。"}],"recommendations":["基线提交暂缓批准，先解释并审计 `course.sha256` 这一额外变化。","用真实旧 HTML fixture 检查缺少内联 META 时是否在 VM 执行前稳定返回固定错误码。","回读 `word_consumers.js`、墙顺序实现及对应早期测试，逐条为七项状态提供实现提交和测试证据。","把体量护栏从单一硬上限扩展为“预警阈值、预计增量、最终硬上限”三层。","修正文档内 commit 数、自决数和 W3 实测值的不一致。"],"risks":["未知的课程级内容变化可能被基线重取永久接受，后续回归将无法发现。","旧产物可能在到达兼容层拒绝逻辑前以非结构化 VM 异常失败，错误提示与既定契约不一致。","副机可能依据证据不足的“已实现”状态开始 W5，直到真实多字母数据进入常规校验才暴露漏检。","第五周媒体注入可能触发单周体量上限，若没有提前预警会在交付末端才返工。"],"confidence":"medium","notes_for_claude_code":"六个攻击方向结论：①生产代码零残留：所给差异中的可见运行时调用已改为 `assertIdList`，但没有全仓文件清单或扫描输出，无法确认未改文件中绝对零残留；`load_data.js` 的固定错误码还可能被更早的 VM 异常截断，属于有代码依据的风险。②逐项存在性：可见的 games、四模板和 check_data 调用均在可访问 SOUNDS 的作用域内，未看到先校验后加载的问题；但需回读模板脚本拼接顺序确认浏览器执行时 `SOUNDS` 已初始化。③基线：四个 skeleton 和一项媒体删除确实可见，但另有未解释的 `course.sha256`，因此不能接受“恰好五条”。④`expected_items:55`：写死本身适合作为有意增删时必须同步更新的漂移门槛，不建议自动从 items 长度推导；应确保校验器同时比较声明值、items 数量和实际文件集合。⑤七条状态：DATA-RESERVED-02 的描述按所给上下文是准确的，字符长度会把 `rain` 判为 4 而非三个字位；DATA-PATTERN-01 的实现证据最不足，其余仍应回读早期提交。⑥体量：材料不足以评价现有护栏实现代码，但余量已值得加入预警。更深审查需要读取完整的 `load_data.js` 声明提取逻辑、`word_consumers.js`、`wall_order.js`、基线比较器、体量校验器及四周数据文件。"}