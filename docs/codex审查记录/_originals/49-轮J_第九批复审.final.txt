{"task_type":"code-review","mode":"advice-only","summary":"本轮发现 3 条 HIGH：顶层声明计数与实际抽取逻辑不一致会静默加载错误数据，跨周认读词混用了源码与构建产物，新增渲染级防泄漏测试仍存在显著恒真/漏检路径。另有若干阻塞合并的词法扫描、异常处理和测试隔离问题。","issues":[{"severity":"high","category":"contradiction","location":"tools/validation/load_data.js：countDeclarationOccurrences() 与 declaration()","problem":"新逻辑只把括号深度为 0 的声明计入重复检查，但 declaration() 仍按文本顺序抽取第一个匹配。新增测试恰好把函数内局部 RESERVED 放在真实顶层 RESERVED 前面：计数会得到 1 并放行，随后 declaration() 却可能抽取局部数组，导致 loadData 静默返回错误 RESERVED。测试只断言“不抛错”，没有核对返回数据，因此会掩盖该问题。","suggestion":"让计数和抽取共用同一个“查找顶层声明”函数，并由该函数返回匹配位置或声明文本；declaration() 必须跳过非顶层匹配。修改回归测试，明确断言 loadData(...).RESERVED 等于顶层的 ['ram','hem','rid','dam','kid']，且不等于局部 shadow 值。"},{"severity":"high","category":"contradiction","location":"tools/validation/word_consumers.js：cumulativeSightWordsUpTo()；tools/validation/check_data.js：③","problem":"当前周使用传入的 currentBox，但历史周固定从 frontend/src/weeks/weekNN.data.js 读取。若 check_data.js 正在校验 build/weekNN.html，集合就混合了“当前构建产物”和“历史源码树”两个版本；源码比构建产物新或旧时均可能产生错误豁免或错误拒绝。wall_order 采用源码树口径不能自动证明认读词验证也应采用这种混合口径。","suggestion":"明确 check_data 的输入契约并保持同一 provenance：校验构建产物时从对应历史 build/weekNN.html 累计，校验源码候选时从源码累计；或者由调用方一次性传入同批次的历史 box。至少增加“历史源码与历史产物故意不同”的正反测试，固定期望采用哪一侧。"},{"severity":"high","category":"edge-case","location":"tests/unit/test_w4_ten_words_not_exposed.js：④⑤⑥渲染级验证","problem":"这些断言仍可能恒真或严重漏检：⑤只调用 blockHTML()，没有实际调用 g1/g4/g5 初始化器，动态插入的游戏词不会被观察；⑥只分别调用 colorPlainText()/bookArt()，没有执行 initBook()/draw() 的完整 DOM 路径；findLeakedWords 又直接扫描 HTML 字符串，若单词被按字位渲染成多个 span，肉眼可见的“dab”可能在 HTML 中不连续而无法命中。与此同时 TEN_WORDS 来自 build 产物，而④～⑥执行的是源码模板，构建过期时还会比较两个版本。","suggestion":"基于同一个新鲜构建产物执行全部检查，使用能解析 innerHTML 的 DOM 环境并扫描最终 textContent，同时单独扫描属性值；实际调用并断言 g1/g4/g5 初始化入口和 initBook/draw 入口已经执行。加入破坏性验证：让非测评渲染路径直接输出一个保留词，并覆盖“逐字 span 拆分”的输出，确认测试必然失败。"},{"severity":"medium","category":"edge-case","location":"tools/validation/js_lex.js：maskStringsAndComments()、scanState()；tools/validation/load_data.js：countDeclarationOccurrences()","problem":"[阻塞合并] 词法器明确不识别正则字面量，但它已被用于安全相关的顶层声明唯一性判定。合法正则中的未配对括号字符（例如 /\\{/）会永久改变 bracketDepthAt 的净深度，可能令后续真实顶层声明计数为 0；正则中的引号或反引号也可能让 scanState 错入字符串/模板状态，导致 migration_audit 漏判真代码。后果既可能是合法文件被拒绝，也可能是重复声明未被发现。","suggestion":"优先使用成熟 JS 解析器按 AST 查找 Program.body 的 VariableDeclaration。若必须保留轻量扫描器，至少实现正则字面量及字符类、转义和除法/正则上下文判定，并增加 /\\{/, /`/, /[\"']/, 模板插值内正则以及正则位于声明前的回归测试。"},{"severity":"medium","category":"edge-case","location":"tools/validation/check_data.js：const cumulativeSightWords = cumulativeSightWordsUpTo(...)","problem":"[阻塞合并] 累计集合的构造位于逐词 try/catch 之外；历史文件缺失、解析失败或 META.week 不一致时会直接抛出未捕获异常和 Node 栈，恰好重复了 I-M3 声称要消除的 CLI 崩溃形态。","suggestion":"在 check_data 的报告框架内包裹累计集合构造，将 TeachingOrderError 的 code、week、file 和 message 转成一条明确失败并按约定退出；若无法继续③，可跳过该节但仍完成其余独立检查。补历史文件读取失败、解析失败和周次不一致的 CLI 测试。"},{"severity":"medium","category":"other","location":"tests/unit/test_check_data_g1_exemption_granularity.py；tests/unit/test_check_data_reserved_in_w_week4.py","problem":"[阻塞合并] 两组测试会临时改写真实受版本控制的 week01.data.js/week04.data.js。并行测试、进程强杀或另一个进程同时编辑时，可能污染工作树或用 cleanup 覆盖他人的新修改；newline='' 只能解决换行转换，不能解决并发和异常终止。","suggestion":"重构校验入口以接受历史数据根目录或加载器，把完整最小周数据复制到 TemporaryDirectory 后运行子进程；也可通过显式测试环境变量注入只读 fixture 根目录。不要让单元测试改写真实源码文件。"},{"severity":"medium","category":"contradiction","location":"tools/validation/exemptions.js：resolveG1Week()","problem":"[可合并后补] 注释承诺“任一方非法都显式抛错”，但 null 被 hasRec/hasOpt 当成未提供。如果 rec.week 为 null、options.week 为 1，逻辑会静默采用 options.week，而不是抛出 g1-rounds-invalid-rec-week，与描述的契约不一致。","suggestion":"区分“字段不存在”和“字段存在但值非法”：使用 Object.prototype.hasOwnProperty.call(rec, 'week') 或仅把 undefined 视为缺失，将 null 交给 isValidWeek 判非法。为 rec.week=null、options.week=null 及两者组合补测试。"},{"severity":"medium","category":"missing-constraint","location":"tools/validation/load_data.js：declaration()；frontend/src/weeks/week04.data.js：ASSESSMENT_WORDS 注释","problem":"[可合并后补] “顶层声明只能单行以分号结束，或多行以顶格 };／]; 结束”的格式契约目前主要写在某个具体数据声明旁，没有在 declaration() 的公共契约或数据格式规范中集中说明。后续 W5 或自动格式化很容易再次生成合法 JS、却被加载器拒绝。","suggestion":"把该限制写到 declaration() 的接口注释和课程数据格式规范中，并增加格式契约测试；更稳妥的方案仍是用 AST 提取声明，从根本上取消对换行和缩进形态的依赖。"},{"severity":"medium","category":"other","location":"tools/validation/check_data.js：I-M3 catch；tests/unit","problem":"[可合并后补] I-M3 的 catch 分支没有可触发的 CLI 级回归，当前正常抽取器又会硬编码合法 bucket，因此未来调用位置变化、catch 被移走或错误信息退化时缺少保护。直接猴子补丁 CommonJS 的解构导入也不可靠，因为依赖在模块加载时已固定。","suggestion":"把③的“按 records 校验一个词”提取为可导出的纯函数，并允许注入 exemption predicate，或让 check_data 主入口接受 collectWordConsumption 依赖；测试注入 bucket 非法记录，断言生成 ok(false) 报告、包含结构化 code、继续处理后续词且最终退出码为 1。"}],"recommendations":["合并前优先统一 load_data 的顶层声明发现与抽取实现，并加入返回值语义断言。","为 check_data 明确定义源码模式与构建产物模式，禁止跨版本混合累计历史数据。","将 H-H1 渲染测试改为真实初始化入口加最终 DOM 文本检查，并加入能够证明测试会失败的破坏性用例。","避免自制不完整词法器承担声明唯一性判定；若暂不能引入解析器，至少先覆盖正则字面量导致的深度漂移。"],"risks":["重复声明检查可能放行后抽取错误的局部声明，造成校验对象与真实顶层数据不一致。","源码和构建产物不同步时，跨周认读词可能被错误豁免或错误拒绝。","当前渲染级测试可能全部通过，但动态游戏或逐字拆分后的可见 DOM 仍泄漏测评词。","测试改写真实源码可能在并行执行或异常终止后污染工作树。"],"confidence":"high","notes_for_claude_code":"本审查仅基于提供的 roundJ.diff 文本，未执行命令、测试或行为验证。建议进一步阅读 load_data.js 的完整 declaration()/loadData()、check_data.js 的整体异常与退出码流程、g1/g4/g5 和 initBook 的真实初始化入口，以及 CI 是否保证先构建 build/week04.html。本轮 HIGH 数：3。"}