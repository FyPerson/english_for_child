{"task_type":"spec-critique","mode":"advice-only","summary":"门禁结论：暂不通过“可直接按规格完整编码”，但可以先开展音色样品、音频生成试验等不依赖未决接口的工作。一审 20 条均在正文中有对应处置，但黑名单隔离、音频清单、播放状态机、确认状态和分层验收仍有若干仅停留在原则层、尚未形成可执行契约的问题。","issues":[{"severity":"critical","category":"contradiction","location":"§0 D4、§3 exam、§4 G5、§7 验收项 6、附件 4 周检纪律","problem":"保留词隔离尚未真正闭环。G5 允许自由拼出 sap、tan、pip、nit、spit，并提示“读读看”；页面只是“建议先做周检”，而 DAYS 顺序仍把造词工坊放在周检前。孩子若先玩 G5，就已经练过周检保留词，直接破坏“一个都没练过”的迁移测试前提。","suggestion":"把“建议”改成程序约束：周检未由家长完成并记录前，不开放 G5，或将周检做成进入 Day7 游戏区前的独立隔离入口。运行层还应在真词判定、音频播放、目标生成、示例渲染、战利品写入五个入口统一调用黑名单守卫。"},{"severity":"high","category":"contradiction","location":"§1 音频清单","problem":"表格宣称冻结 56 条，但 A 组和 C 组都包含 sit。若 WORD_AUDIO 以 key 为对象键，实际只有 55 个唯一音频键；若要生成两份 sit，又没有给出两个不同键及用途。当前“56 条”“每个键”“预算估算”三者不一致。","suggestion":"冻结一份去重后的权威清单，明确每项的稳定 ID、类型、朗读文本和用途。通常应让两个场景复用同一个 word:sit，并把总唯一音频数、生成数和预算同步改为 55；如果确需不同读法，则使用不同 ID 并说明差异。"},{"severity":"high","category":"ambiguity","location":"§1 构建自检、§7 验收项 2","problem":"项目约束是单文件、无构建工具，但规格多次要求“脚本强制”和“构建自检”，没有说明该脚本是临时生成工具、浏览器内开发断言还是独立交付文件，也没有定义失败时如何阻断交付。现有三项检查还只覆盖音频表和 data-say 引用，无法证明黑名单没有进入题库、示例或战利品路径。","suggestion":"在 P0 开工前选定一种可执行方式并写入规格：例如单独的开发期校验脚本，或 HTML 内只在开发模式调用的 validateSpec()。列明输入数据、退出条件和输出报告；至少校验唯一键、重复朗读文本、全部音频引用、所有游戏目标池、真词白名单、示例文本以及奖杯写入前的黑名单守卫。"},{"severity":"high","category":"missing-constraint","location":"§2 播放层改造","problem":"“全局单播放器”和“播放期间锁答”不足以直接实现可靠状态机。现有 Phone.say() 吞掉 play() 拒绝且只返回布尔值，Voice 的结束事件也可能在 cancel 后迟到；规格没有规定 WordAudio.play() 的返回值、结束/失败/取消语义、超时以及旧回调失效规则，容易出现按钮永远锁住、旧音频结束后错误解锁新题或失败提示重试错题。","suggestion":"冻结异步契约：play() 返回可区分 ended、failed、cancelled 的 Promise；每次播放带递增令牌；停止或切题后旧令牌的回调不得改变界面；解锁放在当前令牌的最终处理；Phone 和系统 TTS 也必须接入同一仲裁器并暴露可观察的失败与结束状态。"},{"severity":"high","category":"missing-constraint","location":"§4 G2/G4/G5、§6 状态模式、D5","problem":"状态结构无法稳定支持“所有读出确认可撤销”。confirmed 只记录 G2，G4 的“他指读了吗”没有记录字段，G5 只写 trophies；而 trophies 是无来源的扁平数组。同一词若在 G2 和 G5 都确认，撤销其中一次时无法判断是否仍应保留战利品。","suggestion":"明确确认的来源与身份，例如按游戏、天、词记录确认布尔值，再由这些确认派生 trophies；或者为奖杯保存来源集合。同步规定 G4 确认是否进入战利品墙、撤销后如何更新以及重复确认是否幂等。"},{"severity":"high","category":"ambiguity","location":"§4 G6、§6 state.games.flash","problem":"flash.best 同时承载“完成六音所用秒数”和“30 秒内读出个数”，但组键、值结构和破纪录比较方向均未定义。实现者必须临场决定低值优先还是高值优先，也无法安全处理以后组内容改变。","suggestion":"为每个计时组冻结稳定组键和结构，例如包含 metric、value、itemSetVersion；elapsedSeconds 以更小为优，correctCount 以更大为优。还需定义首次成绩、相同成绩和未完成一轮时是否算纪录。"},{"severity":"high","category":"contradiction","location":"§4 G1 降级、§7 验收项 9、§8 P0 与底线","problem":"G1 契约允许工期不足时只做轮 A，但 P0 内容写 G1、验收 #9 明确要求 /s/ 和 /æ/ 两轮走通，底线又要求 P0 全量及验收 8-10 通过。当前无法判断只交轮 A 是合格降级还是 P0 失败。","suggestion":"冻结唯一门禁：若轮 B 是周一首课必需，就删除降级条款；若允许降级，则把 P0 最低交付和验收 #9 改为轮 A，并明确轮 B 的补齐日期及使用原 v2 块时的验收方式。"},{"severity":"medium","category":"edge-case","location":"§2 首次播放、§4 G1","problem":"G1 写“自动播词”，§2 又规定首次播放必须由用户点击且页面不自动出声；此外没有定义三秒窗口从音频开始还是结束计时、重听是否暂停或重置窗口、失败重试是否仍消耗重听次数，以及“不点”如何被判定。","suggestion":"增加 G1 回合状态表：先由“开始本轮”用户手势启动；音频成功结束后才进入三秒作答窗；重听的允许阶段和是否重置计时固定下来；播放失败不计题、不消耗重听；窗口结束时根据正负例判定未点击结果。"},{"severity":"medium","category":"contradiction","location":"§3、§5、§7 验收项 7","problem":"周检隔离边界仍不够精确。exam 后面的 checks 会触发 dayDone()，而全局规则又允许 step 完成、日卡完成时播放 Nat 或彩带。仅在 exam 组件内部禁用动画，未必能阻止周检所在 step 或整个 Day7 页面触发全局庆祝和战利品刷新。","suggestion":"定义隔离作用域为“周检会话或周检 step”，而非单个 exam renderer；该作用域内统一屏蔽音频、即时反馈、提示、计时、纪录、奖杯写入以及所有全局庆祝事件。周检完成只做家长记录和普通静态完成标记。"},{"severity":"medium","category":"missing-constraint","location":"§6 状态模式","problem":"“深合并默认值”和“games 解析失败”缺少结构校验定义。数组若被当对象合并、best 出现字符串或负数、games 为 null 时都可能产生异常。KEY 与 v2 相同但又要求 v3 零进度，也没有说明如何处理同一 file URL 上已有的开发或彩排数据。file:// 下 localStorage 的归属行为还依赖浏览器实现，不能仅凭“天然保留”推导。","suggestion":"定义字段级清洗规则、数组替换规则、数值范围和 schemaVersion；只保留通过校验的 days，games 无效时重建。首次正式上课前明确清理测试数据的步骤，并在目标 Chrome、Edge及最终绝对文件路径上分别执行覆盖保留验收。"},{"severity":"medium","category":"missing-constraint","location":"§7 验收矩阵、§8 交付分层","problem":"验收矩阵主要覆盖 P0 音频和 Day1，几乎没有为 G2、G3、G4、G5、G6 定义可重复的功能验收；P2 仅靠截止日期，无法证明长按撤销、提示次数、纪录比较、未学字母过滤及奖杯幂等符合契约。","suggestion":"按 P0、P1、P2 拆分门禁。为每个游戏至少增加一条正常路径、一条失败或撤销路径和一条刷新后状态路径；黑名单与周检隔离应采用数据层断言加界面路径双重验收。"},{"severity":"medium","category":"ambiguity","location":"§1 音频清单、§2 Voice 移除、附件 3 v2 节选","problem":"“单词/句子全部内嵌”与实际可点击朗读范围尚未对齐。v2 节选中存在 English time!、I can read “sat”!、字母序列等 say 内容，也存在 Voice.sayParts()；清单没有覆盖这些文本，规格也未说明它们将取消点击、拆成已有词音频，还是继续走系统 TTS。data-say/data-phrase 检查也可能漏掉旧 class=say 绑定。","suggestion":"先从完整 v2 DOM 和渲染器导出所有可触发朗读的入口，再逐项标记为内嵌整段、复用单词序列、保留音素播放或取消可点击。删除 Voice 前必须明确 sayParts 的每个现有调用点如何迁移。"},{"severity":"medium","category":"contradiction","location":"§3 分钟守恒、附件 3 Day7 DAYS","problem":"规格称所有 step 的 min 不变且“30 分钟守恒”，但 Day7 原数据为四个可选游戏加周检，总标称 52 分钟。虽然课程目标写“游戏任选”，实现若直接汇总分钟数会与 30 分钟陈述冲突。","suggestion":"明确 30 分钟守恒只适用于 Day1-Day6；Day7 标记为可选活动池，并规定推荐组合或页面不显示总时长，避免渲染器把 52 分钟呈现为当天硬性任务。"},{"severity":"low","category":"ambiguity","location":"§3 pair 映射、§4 G3","problem":"Day3 的同一个 pair block 同时包含可进入 G3 的 sat/sit 和必须保留家长玩法的两组未学词。规格说明了结果，却未说明渲染器如何在一个 block 内拆分，也未规定后两组是否仍可由家长点播音频。","suggestion":"在数据或映射契约中明确分流：eligible pairs 进入 G3，ineligible pairs 渲染静态家长块；静态块若允许点播，必须说明音频入口、是否锁答及其不写游戏成绩。"}],"recommendations":["P0 开工前先冻结四项最小接口：去重后的音频清单、可执行自检方式、统一播放 Promise 状态机、G1 用户手势与三秒回合状态。","建立单一权威数据源，让音频生成、页面渲染、游戏题库、真词判断和校验都从该数据派生，避免多份手写清单漂移。","把保留词守卫设计为公共运行层函数，并将“周检完成后才开放造词工坊”写成硬性流程，而不是提示文案。","将家长确认改为有来源、可幂等、可撤销的数据，再由确认状态派生战利品墙。","为 P1、P2 各补独立验收门禁；当前 10 条矩阵可保留为首课 P0 门禁，但不足以验收整份 v1.0。"],"risks":["保留词在周检前被自由造词练习，会使第一周迁移测试结果失去解释价值。","播放取消、失败和迟到回调未统一时，听辨游戏可能卡死、提前解锁或把上一题事件作用到下一题。","音频重复键及旧朗读入口未盘清，可能出现自检表面通过但页面仍静音或偷偷回退系统 TTS。","扁平 trophies 与多来源撤销组合后，可能误删仍有效的战利品或留下已经撤销的战利品。","未清理彩排状态或 file:// 存储行为与预期不同，可能造成首课非零进度或同名覆盖后进度丢失。"],"confidence":"high","notes_for_claude_code":"本审查仅依据提供的规格、一审归档、设计原则和 v2 关键节选。若要进一步确认迁移遗漏，需要只读检查完整 week01-v2.html，重点包括 PHONEME_AUDIO/Phone 全实现、renderBlock 或各区块渲染器、class=say 与 data-say 事件绑定、clearPageTimers、save/dayDone、flash、exam、词卡墙和 localStorage 读取写入路径；若已有音频生成或校验辅助脚本，也应一并核对。Claude Code 仍负责最终取舍与执行。"}