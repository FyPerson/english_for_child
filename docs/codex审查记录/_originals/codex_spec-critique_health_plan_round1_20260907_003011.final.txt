{"confidence":"high","issues":[{"category":"missing-constraint","location":"§4 第0期第1步、§4 第1期第1和第4步","problem":"基线只复制 Release 的 manifest.json，里面只有哈希，不能作为“读取基线构建产物并逐键导出媒体”的输入。第0期第5步还会故意改变 course.html，当前方案没有定义基线中 course 哈希的弃用范围，也没有要求导出前确认四个周文件仍与基线一致。这样可能在源文件或生成物已变更后导出媒体，却仍声称字节不变。","severity":"critical","suggestion":"明确基线结构：至少记录四个 week HTML 的哈希并标注 course 为“第0期前快照、不参与第1期比对”。导出工具必须先对当前输入文件做哈希校验，只有匹配基线才允许逐声明导出；同时记录第0期第5步后的 course 新哈希。不要只依赖本机 dist 目录，因为它被忽略且不会随 clone 保留。"},{"category":"security","location":"§4 第0期第7步“历史清理”","problem":"这是不可逆操作，但前置条件不完整：没有要求工作区干净、先 fetch 并核对所有远端引用、检查 tags/stash/未推送提交，也没有说明 filter-repo 后如何推送重写后的 tags 和其他需要保留的引用。仅删除列出的几个分支并强推 main，可能遗失远端独有提交、标签或归档仍引用的对象。预期 .git 体量还会受 reflog 和未回收对象影响。","severity":"critical","suggestion":"在步骤开头加入“工作区和 index 必须干净、fetch --prune 后核对 heads/remotes/tags/stash”的硬门槛；bundle 创建后做可恢复性验证。明确 filter-repo 的引用范围、重写后强推 main 与必要 tags，更新所有 clone/CI/归档引用；完成后执行并记录 gc/prune 后的体量，而不是直接承诺约40 MB。"},{"category":"ambiguity","location":"§3 清单示例、§4 第1期第2步","problem":"文字称清单保存“键→文件路径 + mime”，示例却只有字符串路径；同时没有定义七个声明的顶层结构、空声明、别名、键顺序、重复路径和 MIME 的精确格式。不同实现可能生成不同 data URL，导致哈希和字节不变验收不可证伪。","severity":"high","suggestion":"给出完整 schema，例如每个声明是对象，值包含 path 与 mime，规定相对仓库根解析、键排序按旧声明顺序或稳定排序、别名可共享同一路径、空对象如何表示，并对缺失/重复/未知 MIME 直接失败。用 JSON Schema 或等价校验器覆盖这些规则。"},{"category":"contradiction","location":"§4 第0期第2步、§4 第1期第4步、§5 第0期验收","problem":"现有 run_checks.py 的 generated lessons 作业是 `build_lessons.py --check`，依赖根目录已存在且未过期的 HTML。生成物改为不入库后，fresh clone 没有可比较文件；如果改成先 build 再 --check，比较又变成对刚写出的文件自检，失去发现过期生成物的意义。方案没有决定 check 是构建到临时目录比较，还是仅验证构建成功。","severity":"high","suggestion":"明确唯一契约：例如 check 在临时目录构建并校验，不写根目录；或规定 check 先构建再只做结构/哈希/浏览器检查，并移除“generated lessons 一致性比较”的表述。同步修改 project.py、run_checks.py、README、development.md 及 CI 命令，保证 fresh clone 的行为一致。"},{"category":"missing-constraint","location":"§4 第0期第2步、§4 第1期第2和第3步","problem":"媒体迁移只描述了工具行为，没有列出必须改动的所有输入和测试。现有每周模板仍通过七个 `@include media/...` 引入 JS，build_lessons.py 只有 include 展开；test_media.js 逐字比较七个 JS，test_layout.py 强制七个 JS 存在，frontend/src/media/README.md 及 .gitattributes 也仍描述旧模型。若只改构建脚本，构建会找不到新清单或单元测试必然失败。","severity":"high","suggestion":"把所有 `frontend/src/weeks/weekNN.template.html` 的媒体标记迁移、build_lessons.py 的 manifest 展开和路径安全校验、test_media.js/test_layout.py 的新断言、媒体 README、.gitattributes 及相关 fixture 列成明确子步骤，并为每周七类声明、空声明、跨周复用增加测试。"},{"category":"other","location":"§4 第1期第5步、§3 数据流","problem":"build_audio.py、build_phonemes.py、embed_assets.py 目前以 `--target weekNN.html` 注入 HTML；方案只写“改为 --week NN”，没有定义它们如何选择清单、如何写入 resources/adopted、如何处理已有源与无源媒体、失败时是否原子更新多个文件，也没有列出实现和测试文件。capture_lesson_media.js 删除后，README、development.md、frontend/src/media/README.md 和三方交接规范仍会调用它。","severity":"high","suggestion":"为三个工具分别规定新参数、输入/输出、幂等性、失败回滚和 dry-run 行为，并明确删除 capture 工具后的全仓文档/脚本引用清单。至少增加一个端到端测试：素材工具更新 adopted 与 manifest 后，build 再由 test_media 校验内嵌字节。"},{"category":"edge-case","location":"§4 第2期、§3 目标结构","problem":"薄壳方案依赖多个未验证假设：srcdoc 在 file:// 和 http(s) 下是否继承父页源、子页与父页的 localStorage 是否共享、HTTPS 页面 fetch 周文件失败或返回非2xx时如何处理。网络断开、静态主机路径前缀、CORS/重定向或浏览器对 file localStorage 的差异都可能使课件空白或进度不可见。","severity":"high","suggestion":"在 test_course.py 中分别覆盖 file、HTTP 成功、404/网络失败和带路径前缀部署；在 Chrome、Windows 本地文件和目标 Safari 上验证父子页读写同一进度。为 fetch 非2xx、超时和 srcdoc 初始化失败定义可见错误及回退策略，并把这些列入第2期验收。"},{"category":"edge-case","location":"§2 媒体溯源、§3 adopted、§4 第1期第1步","problem":"把 HTML 中的插画字节导出为 `.png` 并作为新真相源，未规定从 data URL 保留原 MIME/扩展名、是否禁止重新编码，也未保留原始来源、处理参数和“由产物恢复”的 provenance。若原字节实际是 JPEG/WebP 或存在多个同哈希文件，导出可能改变字节或产生不可追溯的正式素材。","severity":"high","suggestion":"导出时解析并保留 data URL 的真实 MIME 和原始字节，扩展名与 MIME 一致，禁止解码后重编码；同哈希多候选必须有确定的选择规则。为每个 adopted 文件写来源/恢复日期/哈希记录，并明确它是“采用的派生媒体”而非原始素材，不能覆盖 assets 真相源。"},{"category":"other","location":"§4 第0期第6步、§3 目标结构、§5 验收","problem":"方案一处规定 `resources/reference/` 只保留正在引用的候选音，另一处又把唯一的 OpenLearn 目录整体移到仓库外；目标树和 architecture/development 中的 reference 职责因此不一致。截图和候选音移到固定 `E:/worklog/...` 后，README 中若留下绝对路径，其他机器、CI 和新 clone 都无法按文档定位。","severity":"medium","suggestion":"明确迁移后仓库内 reference 是否保留为空、仅保留哪些文件，并同步目录树、测试和文档。仓库内只写相对的外部归档说明、迁移日期和校验哈希，不写依赖某台机器盘符的路径；迁移前增加目标目录存在性、复制完成和源文件哈希校验。"},{"category":"missing-constraint","location":"§4 第0期第3步、§5 验收","problem":"sizeBudgetMB 的单位、计算对象和边界未定义：是十进制 MB 还是 MiB，是否包含 course、index、换行和临时文件，阈值是大于还是大于等于才失败。第0期先用 week 8/course 20，第1期再下调，但没有把“下调已执行”变成可检查条件。","severity":"medium","suggestion":"在 project.json 或测试契约中固定单位、取整和包含文件集合，明确 `>`/`>=`，并让 test_size_budget.py 从配置读取而非写死。第1期验收应检查配置已切换为 week 6/course 12，并给出超限样例测试。"},{"category":"contradiction","location":"§4 第0期第5步、§5 第0期验收","problem":"直接把 HTML 字符串放进 JSON 后，course.template.html 必须改为直接读取字符串，移除 atob/TextDecoder；但方案没有列出 build_course.py 中 base64 逻辑、course payload 转义、测试和文档的同步。只替换模板会导致 payload 仍是 base64 或内嵌 HTML 的 `</script>` 截断 JSON。","severity":"medium","suggestion":"明确 build_course.py、course.template.html、tests/browser/test_course.py 及相关 fixture 的联合修改；在 JSON 脚本块写入前对所有 `</script` 做安全转义，并增加包含脚本标签、中文和非ASCII内容的回归测试。第0期基线应分别记录旧 course 哈希和新 course 哈希。"},{"category":"readability","location":"§4 第0期第4步、§5 第0期验收","problem":"doctor 版本检查只写“输出并校验 Node 主版本≥18”，但没有规定解析 `node --version` 的格式、预发布版本、缺失 node 时的错误，以及与 CI Node 22 的一致性测试。当前 project.py 仅检查 node 是否存在，验收无法证明 H 已落地。","severity":"low","suggestion":"规定使用 `node --version` 解析主版本并对非零退出、异常格式和低版本失败；在单元测试中覆盖 18、22、低版本和缺失命令，验收记录实际版本而非仅路径。"},{"category":"missing-constraint","location":"§4 第0期第2步、§5 CI 验收","problem":"“CI 构建并挂 Release”没有对应工作流闭环。当前 .github/workflows/check.yml 只有 check，artifact 仅在失败时上传，权限是 `contents: read`，没有 tag 触发、成功上传四周与 course、或用同一批文件创建 Release 的步骤。第0期“CI绿且产物可下载”因此无法由现有配置实现。","severity":"high","suggestion":"明确工作流触发矩阵和 job：成功 build 后上传固定文件；tag job 使用构建出的同一批字节创建 Release，并授予最小 `contents: write` 权限或采用受控发布凭据。验收增加 push、pull_request、tag 三种场景及下载后 SHA 与本次构建一致的检查。"},{"category":"missing-constraint","location":"§4 第0期第6步、§4 第1期第5步、文档联动","problem":"方案只泛称同步 README、development.md、architecture.md 和三方规范，没有覆盖工程与交付规范、课程方案 §9.2 生产流程、资源 README、GitHub workflow、.gitignore/.gitattributes 中仍出现的旧入口和“生成物随源码版本管理”表述。文档漂移会继续让副机执行已删除的路径或把 HTML 当真相源。","severity":"medium","suggestion":"建立逐文件迁移清单，至少包含上述文档和配置；在第0期结束时用全仓搜索验证 `tools/week-checks`、`capture_lesson_media.js`、`assets/weekNN`、`frontend/src/media/*.js` 旧命令/路径只出现在历史记录或迁移说明中。"}],"mode":"advice-only","notes_for_claude_code":"本审查仅基于题面提供的方案、脚本和文档，未读取未附内容（尤其是各周模板、project_config.py、load_data.js、test_course.py 以及三个素材工具的完整实现）。实施前应按上述迁移清单补读这些文件，并把基线校验、历史重写前置条件和 CI 发布权限作为阻断项处理。","recommendations":["先补齐第0期与第1期之间的基线协议，再执行媒体导出；导出前必须验证四个周文件哈希匹配。","先完成工具、模板、测试和文档的全量契约迁移，再删除旧媒体 JS 和 capture 工具。","将历史重写与 CI 发布分别做成可验证的操作清单，保留可恢复 bundle，并在重写后核对所有引用和远端状态。","把 file/HTTP、srcdoc/localStorage、fetch 失败和发布下载纳入自动化验收，而不是只写成说明。"],"risks":["基线不匹配时错误导出媒体，导致无法证明第1期字节不变。","历史重写遗漏远端引用、标签或未推送提交会造成不可恢复的数据丢失。","清单 schema 不明确会造成 MIME、键顺序或别名差异，进而改变课件字节。","CI 权限和发布工作流未闭环时，主分支可能通过检查但没有可下载的 Release 产物。","srcdoc 或 file:// 存储行为不一致会使合并入口在家长实际使用环境中显示空白或丢失进度。"],"summary":"方案方向与八项决定一致，但当前仍有几个阻断性缺口：第1期没有可验证的媒体导出基线，历史重写缺少足够的不可逆操作前置条件，且清单迁移与 CI 发布契约尚未闭环。建议先补齐这些硬约束和逐文件迁移清单，再进入执行。","task_type":"spec-critique"}