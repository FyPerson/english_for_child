{"task_type":"code-review","mode":"advice-only","summary":"静态审查发现 1 个高严重度竞态：词→词组的 300ms 延迟续接没有所有权校验，旧词组可能抢占更新的播放请求，因此当前不满足“0 high”放行门禁。预筛 H1、H2、M4、M5 的修复本身在所给 diff 中逻辑闭环，未发现新的请求复活或双重完成路径。","issues":[{"severity":"high","category":"edge-case","location":"week01-v3.html:2078–2083，中央 data-say 的词→词组接续","problem":"首词播放结束后，通过 laterOnce 延迟 300ms 播放 phrase，但延迟回调没有检查期间是否出现了新的逻辑请求。旧版用 saySeq 在回调执行前校验；新版移除了该校验。复现路径是点击带 data-phrase 的词，首词结束后 300ms 内点击另一个词或音素：新请求先开始，随后旧 phrase 调用 WordAudio.play()，反过来取消新请求并播放过期内容。clearPageTimers 只能处理切页，无法处理同页快速连点。","suggestion":"在 AudioBus 增加逻辑请求代次或可取消的“待续接阶段”槽，并让 stopAll()、stopForPhone() 在每个新逻辑请求开始时同步递增代次或清除待续接。首词 ended 后记录当前代次，300ms 回调仅在代次仍一致时播放 phrase。不要只恢复 data-say 局部 saySeq，否则 Phone.say、blend、pair 等其他新请求仍无法使旧 phrase 失效。修复后再放行 S2。"},{"severity":"low","category":"edge-case","location":"week01-v3.html:844–853，speakFallback","problem":"旧版 utter() 在 speak() 前显式调用 speechSynthesis.resume()，新版兜底删除了这一步。如果浏览器语音合成对象处于 paused 状态，utterance 可能一直不启动，最终被 8 秒超时判为 failed；重试时仍可能重复失败。当前页面未主动调用 pause，因此这是低概率环境状态回归。","suggestion":"在 60ms 错拍后的实际 speak() 前恢复一次 try { speechSynthesis.resume(); } catch(e) {}，并保留现有 settled、ownerToken、phase 三重校验。也可先在目标 Chrome/Edge 真机确认 cancel() 后 paused 状态是否必然清除，再决定是否补回。"}],"recommendations":["高严重度续接竞态修复后，覆盖以下顺序场景：带词组词播放结束→300ms 内点击另一单词、点击音素、切页；均不得再播旧词组或取消新请求。","保留当前 H1 的 settled 守卫与 finish() 中 seq 作废；H2 的 60ms 回调复校能阻止取消后的 TTS 复活，未跟踪该短定时器不会造成出声竞态。","M4 的 stopForPhone() 已实现“取消单词/TTS但保留音素间叠音”，WordAudio.play() 反向通过 stopAll() 停止音素，符合已拍板的互断语义。","M5 同时使用 const t 捕获 target 和 WordAudio.play() 空键守卫，已覆盖可变外层变量与非法键两层风险。"],"risks":["若直接叠加 S2，真实内嵌音频结束后的 300ms 窗口会稳定暴露旧 phrase 抢占问题，孩子可能听到与当前卡片不一致的提示。","当前材料是累计 diff 而非完整 week01-v3.html；未展示区域中的动态调用点、CSS 交互叠加及完整脚本初始化顺序仍需 Claude Code 结合完整文件复核。","speechSynthesis 的暂停状态行为具有浏览器实现差异，应以首课目标 Chrome/Edge 真机结果决定是否将 resume() 回补提升为必修项。"],"confidence":"high","notes_for_claude_code":"建议将词组延迟续接问题作为 S1 阻断项处理；修复后重新做一次窄审即可。以上结论仅基于附件静态代码与规格对照，未执行代码或测试；最终取舍与实现仍由 Claude Code 决定。"}