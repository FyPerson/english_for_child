{"task_type":"code-review","mode":"advice-only","summary":"可按“0 个高严重度问题”通过 P0 静态合并审门禁，进入验收自测与真机彩排。当前仍有 2 个跨模块中严重度语义问题，建议收官前修正或明确接受。","issues":[{"severity":"medium","category":"contradiction","location":"week01-v3.html：speakFallback、WordAudio.play、initBlenders、initPairs","problem":"`opts.rate` 在内嵌音频阶段表示相对播放倍率，但进入 TTS 兜底后被直接赋给 `SpeechSynthesisUtterance.rate`。因此常速兜底使用 0.75，而标称慢速的 `{rate:0.8}` 反而使用 0.8，导致 G2 的“慢速→常速”和 pair 答错慢放在兜底路径中变成“较快→较慢”，与调用方语义相反。","suggestion":"把 TTS 的教学基准语速与相对倍率分开，例如令 `u.rate = 0.75 * (rateFactor ?? 1)`，并让 WordAudio 把 `opts.rate` 作为倍率传入；至少静态确认普通播放、0.8 慢放及两阶段接续在内嵌音频和 TTS 两条路径上的快慢关系一致。"},{"severity":"medium","category":"edge-case","location":"week01-v3.html：initG1、AudioBus、全局 data-say/data-sayph 点击委托","problem":"G1 进入 `window` 后出题音的 ownerToken 已自然结束，AudioBus 不再持有该题；此时若用户点击同页其他单词或音素按钮，新的音频不会通知 G1，4 秒作答窗仍继续计时并按原题判分。`playing` 阶段已有 cancelled→interrupted 处理，另一轮 G1 也有显式互斥，但 `window` 阶段缺少对应的跨入口仲裁，孩子可能在听另一段声音时被判原题漏拍。","suggestion":"增加页面级活动 G1 控制器：进入 `window` 时登记当前回合，任何非该回合发起的 data-say/data-sayph 播放先清除作答计时器并把卡片转为 interrupted；开始另一轮、折叠和切页继续沿用现有 reset/stopAll 路径。不要把该逻辑塞进 WordAudio 内部，以免 G1 自身的重播误伤自己。"},{"severity":"low","category":"edge-case","location":"week01-v3.html：audio-fail-bar 样式与打印小书流程","problem":"失败提示条使用 fixed 定位和高层级，但没有打印媒体隐藏规则。如果音频失败后直接打印小书，提示条可能进入打印预览并覆盖页首，形成对既有打印功能的条件性回归。","suggestion":"补充 `@media print { .audio-fail-bar { display:none !important; } }`，并将“失败条可见时打印”加入一次简短回归检查。"}],"recommendations":["修正 TTS 倍率映射后，分别覆盖内嵌音频与强制 TTS 兜底下的慢速→常速顺序。","集中验证 G1 的 playing、window、audioFailed 三种状态与顶部重试、卡内重试、其他音频按钮、折叠、切页和另一轮开局的组合矩阵。","完成 Chrome 与 Edge 的 file:// 真机验收，重点覆盖快速交替播放、缺键调试旗、刷新持久化、1366×768 布局和打印预览。"],"risks":["附件中的 base64 数据块被省略，无法静态核对 56 个键、数据 URI 完整性及实际音频内容。","审查材料是累计 diff 而非 week01-v2.html 与 week01-v3.html 两份完整文件，未显示的既有路由、打印和初始化代码仍需 Claude Code 在完整文件中交叉核实。","浏览器媒体解锁、speechSynthesis 语音加载和 file:// 行为存在环境差异，静态审查不能替代目标电脑真机彩排。"],"confidence":"medium","notes_for_claude_code":"本结论仅基于所给累计 diff、build_audio.py 与 v1.3 规格进行静态复核，没有执行命令或测试。未发现阻断 P0 的高严重度缺陷；建议优先处理两个中严重度问题，再由 Claude Code 结合完整 week01-v3.html 决定最终提交。"}