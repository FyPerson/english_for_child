{"task_type":"spec-critique","mode":"advice-only","summary":"v0.4 已实质修复三审 11 条中的全部核心问题，历史重写、基线入口和 Release 条件均已达到可实施程度。但新引入的第 1 期 6 MB 周课件预算与“媒体字节不变”直接冲突，音频工具是否参与 adopted/PROVENANCE 的契约也前后不一；这两项应修正后再定稿。","issues":[{"severity":"high","category":"contradiction","location":"§3.3“字节不变”、§3.5 第1期预算、§4 第1期第4—5步、§5 第1期验收","problem":"第1期要求四个周文件的骨架、声明顺序以及全部媒体字节与基线完全相等，清单化也仍会把媒体重新内嵌进每个独立周文件，因此不会显著缩小周HTML。与此同时，预算却从8 MB降为6 MB；现状表又记载第四周媒体本身为7.05 MB，若这是解码后二进制体量，重新base64后只会更大。这样第1期即使完全正确地保持字节等价，也很可能必然被6 MB护栏判失败。","suggestion":"不要预先写死第1期week=6。改为在第1期等价验收后按四个实际输出的最大值加明确余量确定预算，或继续维持8 MB；若7.05 MB的统计口径不是解码媒体字节，应在§2明确口径并用当前week04.html的磁盘字节证明6 MB可行。course=12可单独保留。"},{"severity":"high","category":"contradiction","location":"§3.1 数据流与工具映射、§3.8“整批发布”、§4 第1期第7步","problem":"§4第1期第7步开头称“三个工具都改为写adopted、PROVENANCE与manifest”，§3.8也称三个素材工具统一在resources/adopted/.staging-*发布；但随后又规定build_phonemes.py和build_audio.py写resources/assets下既有目录，而且PROVENANCE明确只记录adopted媒体。实现者无法据此判断两个音频工具是否应更新PROVENANCE、应在哪个父目录 staging，以及失败时如何原子发布assets文件。","suggestion":"把工具分成两类明确写契约：embed_assets写adopted、PROVENANCE和manifest；两个音频工具写resources/assets及manifest，不写PROVENANCE。各自在目标父目录创建同文件系统的staging，完成全部处理和校验后发布文件，最后原子写manifest。同步改掉“导出工具与三个素材工具都按同一流程”和“三个工具都写PROVENANCE”的表述。"},{"severity":"medium","category":"edge-case","location":"§3.8 内容寻址命名与整批发布第3步","problem":"“rename只会新增文件”没有覆盖幂等重跑：相同slug和相同内容会得到同一目标名，而目标文件已经存在。不同平台的rename对已存在目标行为不同；直接覆盖会削弱“已引用字节不变”保证，直接失败又会让工具无法幂等。10位哈希前缀也存在极低概率碰撞或已有文件损坏的情况。","suggestion":"规定目标存在时先计算完整SHA-256并核对mime：完全相同则跳过rename并复用；不同则视为前缀碰撞或仓库损坏并整体失败，绝不覆盖。为相同内容重跑和同名但字节不一致各加一个测试；成功或失败后都清理staging目录。"},{"severity":"medium","category":"ambiguity","location":"§3.8“同内容多键”与PROVENANCE.json字段","problem":"同一媒体允许被多个键、声明和周引用，且同内容只落一个文件，但每条PROVENANCE仅定义单数的“周文件、声明名、键”。虽然文件slug取首次出现键，文档没有说明provenance也只记录首次来源，还是应保存所有来源；素材工具后续再次引用同一文件时是否改写来源也不明确。","suggestion":"二选一固定schema：要么明确每个文件只记录首次采用来源且后续引用不改写；要么把来源定义为有稳定排序的数组。若采用首次来源，排序必须沿用“周序、声明序、键序”，并测试后续新增别名不会改变既有provenance。"},{"severity":"medium","category":"missing-constraint","location":"§4 第1期第7步 build_phonemes.py","problem":"新参数只说明src文件名等于音素键、输出PHONEME_AUDIO清单条目，但没有确定它是全量重建还是合并现有周manifest。现工具是向HTML累计合并新音素；若新实现只按本次src重建，本周仍需复用的前周音素会丢失；若默认合并，则删源文件不会删旧条目，与--remove语义也需协调。","suggestion":"明确PHONEME_AUDIO更新算法。建议读取当前周SOUNDS所需的全部audioKey，以resources/assets/phonemes为真相源全量重建该声明；别名只引用实际音频键。若保留增量合并，则明确已有条目保留、--remove的合法性检查以及构建覆盖检查如何阻止删除仍在使用的键。"},{"severity":"medium","category":"missing-constraint","location":"§3.7 release job、§5 第0期验收","problem":"Release命令按manifest中的文件键逐个传给gh release create，而manifest.json自身不在files映射中，因此GitHub Release不会包含manifest.json。与此同时方案把manifest作为下载后校验依据，且本地“project.py build”只生成五个HTML，不能产生index.html和manifest.json，§5所述artifact与本机构建结果逐文件一致无法覆盖完整发布包。","suggestion":"创建Release时把manifest.json显式加入上传列表，再追加manifest.files中的文件；验收时把五个HTML与同提交build结果比较，把完整七文件发布包与同提交project.py release/package结果比较，并明确index.html按字节或哈希参与比较。"},{"severity":"low","category":"readability","location":"§3.3 基线命令测试要求","problem":"要求单元测试断言build与check的代码路径“不引用写入基线的函数”过度绑定实现结构；即使安全地共享只读辅助函数也可能误报，而真正风险是命令执行后fixture发生变化。","suggestion":"改为行为契约测试：预置baseline并记录字节，分别运行build和check后断言baseline字节及mtime不变；另测baseline --create在文件已存在且无--force时拒绝。"}],"recommendations":["三审11条状态：H-1强推顺序已修；H-2文件名规则已修；H-3 PROVENANCE与prune已修；H-4整批原子部分修（仍缺目标已存在时的幂等规则）；H-5基线入口已修；H-6 Release条件与下载目录已修，但遗漏上传manifest自身；M-7引用集合断言已修；M-8路径约束已修；M-9声明唯一性已修；M-10重复构建已修；L-11数据流措辞已修。","开工前必须修正第1期6 MB预算与字节等价目标的冲突，并统一两个音频工具、adopted和PROVENANCE之间的职责表述；否则实施会在验收或工具改造阶段遇到互斥要求。","目标文件已存在时的内容寻址发布规则、PROVENANCE多来源语义、音素清单更新算法和Release上传manifest，可在对应第1期或CI编码前补入，但应作为实现前检查项，不宜留给编码者自行猜测。","完成两项high修订后，本方案可以定稿进入第0期；历史重写子步骤7—10和baseline命令本身不再构成开工阻塞。"],"risks":["第1期严格保持媒体字节后，week04.html可能天然超过6 MB，使所有正确实现都无法通过验收。","音频工具若误按§3.8更新PROVENANCE，会破坏“PROVENANCE条目集合等于adopted媒体集合”；若在错误目录 staging，也可能失去预期的原子性。","幂等重跑时覆盖已存在的内容寻址文件，可能破坏当前manifest引用的字节稳定性。","GitHub Release若不附带manifest.json，下载者无法按方案执行独立完整性核验。","音素工具若从本次src全量替换声明，可能静默丢失跨周复用的旧音素。"],"confidence":"high","notes_for_claude_code":"本审查仅依据提供的v0.4方案、三审结果和相关文件文本进行，未执行命令或验证实现。八项用户决定未重新讨论。收敛建议是只再修改两项high及紧邻的契约措辞，不做结构性重写；修完即可按硬上限定稿。"}