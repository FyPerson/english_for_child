{"task_type":"spec-critique","mode":"advice-only","summary":"两个原 critical 的核心矛盾基本消除：W5 已保留测评最小路由，ai fixture 也改为墙全集模型。但 v1.1 新增了一个可执行性阻断：基线作业失败会让 quick 检查提前停止，因此第 3—7 步无法据该命令证明“除基线外全绿”；此外着色接口命名、墙顺序真相源和基线验收仍有契约缝隙。","issues":[{"severity":"critical","category":"contradiction","location":"实施方案 §4“全绿”定义、第3—7步；tools/run_checks.py 主循环","problem":"方案要求第3—7步执行“check --quick 除基线作业外全绿”，但 run_checks.py 遇到 single-file baseline 失败后会立即 break，后续 size、unit、assessment、media 等作业根本不会执行。记录基线差异并不能证明其余作业全绿，基础假设5不成立。","suggestion":"在进入第3步前先给检查入口增加明确的 --skip-baseline 或 --allow-known-baseline-mismatch 模式，且只跳过该作业、不吞掉其他失败；或者在方案中列出等价的逐项命令并要求全部通过。不要仅靠人工忽略 run_checks.py 的失败结果。"},{"severity":"high","category":"contradiction","location":"实施方案 §3.2“配套导出”与 §3.3","problem":"§3.2 导出的是 colorTokenizedText，§3.3 又把正式边界定义成 colorPlainText 和 colorRichText，并提到“单词场景仍走严格路径”却没有给这个严格接口命名。实现者无法判断最终公共 API 是两个、三个还是四个接口，基础假设4目前证据不足。","suggestion":"统一公共 API，例如明确导出 colorStrictWord、colorPlainText、colorRichText，并删除 colorTokenizedText；随后把42个调用点按三类逐项登记。若 colorPlainText 同时承担严格单词与可降级句子，应增加显式 strict 参数，但独立函数更不易误用。"},{"severity":"high","category":"missing-constraint","location":"实施方案 §5“墙”；交接规范 §3“点亮墙与首教日”","problem":"验收要求 wallLetters 与过滤后的 SOUNDS 键“顺序相等”，同时又声明墙顺序不得依赖对象键枚举顺序，但材料中没有独立、机器可读的累计教学顺序来源。直接用 Object.keys(SOUNDS) 会违反后一句；用 wallLetters 自身作期望值则形成循环校验。","suggestion":"指定唯一顺序真相源，例如按 project.json 周序累计各周 newPatterns，并在同周按 newPatterns 顺序追加；别名追加规则也要明确。DATA-WALL-01 应拿 wallLetters 与该独立序列比较，而不是仅与 Object.keys(SOUNDS) 比较。"},{"severity":"high","category":"contradiction","location":"实施方案 §2.5、§4第8步、§5“基线”；tools/baseline.py differences()","problem":"方案称 sha256 与 skeletonSha256 都会变化，并要求核对两者；但 baseline.py 的 differences() 不比较每周 sha256，实际基线检查只比较 skeletonSha256、declarationOrder 和 media。第8步无法通过现有工具证明“sha256 差异仅为预期”。此外课程总产物 course.sha256 也被写入 fixture，却同样没有在 check() 中比较。","suggestion":"要么把验收口径改成现有工具真正检查的三项；要么先修 baseline.py，让 differences() 比较周 sha256 及 course.sha256，并增加正反测试。若完整字节哈希只是审计元数据，也应明确它不属于自动门槛。"},{"severity":"high","category":"ambiguity","location":"实施方案 §0.3、§4第4步；交接规范 §4.3 assessmentMode矩阵","problem":"“weekly周只要求 RESERVED”没有明确是只做存在性/禁止项路由，还是仍执行 RESERVED 的长度、W释义、已教学字位、泄漏检查。第4步排在分词消费者接线后，暗示会依赖 segmentWord，但步骤依赖没有写明。W1—W3此前被早退完全跳过，补 assessmentMode 后首次进入 weekly 路径，可能暴露历史数据不满足新检查，而方案只写“四周补声明”没有逐周兼容验收。","suggestion":"列出最小路由的精确检查集合：必需常量、禁止常量、禁止块，以及 RESERVED 共用校验是否启用。明确第4步依赖第3步的分词接线；为W1、W2、W3、W5各加weekly正例，并为缺RESERVED、携带禁止常量、携带retest/probe分别加失败例。W4继续走monthly旧行为，并加回归 fixture。"},{"severity":"medium","category":"missing-constraint","location":"实施方案 §4第5步 ai集成fixture","problem":"fixture 已修正墙为 r/ai/n 三块，但没有规定 rackG4、rackG5 与目标词、槽数、重复项的关系。只声明三个墙字位并不能保证测试真正覆盖字位多重集和表面串查询，基础假设2中的fixture部分仍不完整。","suggestion":"至少固定 target=rain、segments=['r','ai','n']、rackG4=['r','ai','n']、rackG5=['r','ai','n']、G4_WORDS与G5_WHITELIST含rain，并断言数组元素ai只占一块。再增加一个需要重复字位的合成词与重复rack项，验证按索引消耗而非Set去重。"},{"severity":"medium","category":"edge-case","location":"实施方案 §3.2 segmentWord契约","problem":"分词契约尚未定义 word、sounds、grapheme 本身的输入结构：非字符串word、空或非对象sounds、继承键、缺失/空grapheme、非字符串grapheme、大小写归一后为空，以及零长度grapheme导致DP不前进等情况。offset究竟指原串还是规范化串也未说明。基础假设3只在输入合法且每条边严格前进时成立。","suggestion":"在建索引阶段先验证 sounds 为普通映射、只读自有键、每项grapheme为非空字符串且规范化后非空；word必须为字符串。规定offset以规范化后的UTF-16索引或码点索引计，并保持浏览器与Node一致。空grapheme必须在DP前失败，确保状态图无零长度环。"},{"severity":"medium","category":"security","location":"实施方案 §3.3；render-blocks.js 中多个 innerHTML 调用","problem":"colorRichText 提到“按既定白名单解析”，但材料中没有既定解析器、属性规则或URL规则。仅允许标签名不足以防止 span 上的事件属性、style、危险实体或畸形标签进入 innerHTML。","suggestion":"明确允许的完整结构：b无属性、br无属性、span仅允许class且值严格等于en；拒绝而非保留其他标签和属性。优先用DOMParser构建节点并重新序列化，Node侧采用同一实现或同一测试向量。"},{"severity":"medium","category":"edge-case","location":"实施方案 §3.5、第7步；load_data.js HTML兜底","problem":"第7步虽要求两条入口拒绝旧字符串，但现有 load_data.js 在缺META时会自行合成字符串 rack/wall。若该HTML兜底仍存在，调用方可能继续依赖旧格式，基础假设7证据不足；而若直接收紧，旧式HTML解析会从兼容变为失败，需明确是否有调用者。","suggestion":"把 load_data.js 的兜底分支列为第3步和第7步的显式迁移对象。第7步删除字符串合成或改为生成ID数组，并为“缺内联META的旧HTML”定义明确结果：受支持并返回数组，或以结构化错误拒绝。检索所有 loadData(raw,true) 调用方后再宣告无字符串依赖。"},{"severity":"low","category":"readability","location":"实施方案 §3.7","problem":"标题写“三个字段的数组形态”，示例实际列出 wallLetters、rackG4、rackG5、newPatterns 四个字段，与全文“四个字段”口径不一致。","suggestion":"标题改为“四个字段的数组形态”，避免再次造成迁移范围歧义。"}],"recommendations":["先解决 quick 检查的提前终止问题，否则九步提交策略缺少可执行的逐步验收闭环。","为42个 colorWord 调用点增加调用点清单，逐项标注严格单词、纯文本、受限富文本，不能只凭两个抽象接口声称覆盖。","将第4步的 assessment 最小路由写成精确矩阵，并对W1—W4历史数据逐周回归；W5正例不足以证明迁移安全。","为墙顺序建立独立于 SOUNDS 对象枚举和 wallLetters 自身的权威序列。","补读四个 weekNN.template.html 和四周 data.js 后，再对迁移矩阵15行及第7步字符串依赖作最终覆盖判定。"],"risks":["第3—7步可能在基线处提前退出，隐藏后续单测或测评契约失败。","W1—W3从早退切入weekly校验后可能出现未预估的历史兼容失败。","富文本白名单若只限制标签名，仍可能把危险属性送入innerHTML。","墙顺序若继续依赖对象键顺序，重构SOUNDS声明可能造成无教学意图的展示重排。","load_data.js旧HTML兜底可能让字符串schema在第7步后继续潜伏，或突然破坏现有工具调用。"],"confidence":"high","notes_for_claude_code":"对14条复审落实的判定：C-1已修核心阻断，但最小路由的精确校验集合及W1—W3回归仍属部分修；C-2已修核心墙数量矛盾，但fixture rack约束属部分修。H-3已修；H-4已修设计时序，但需确认checker到底接收归一化数组还是原值；H-5已修主要校验，仍缺基础输入schema；H-6部分修，因公共接口命名冲突且白名单未具体化；H-7已修方案口径；H-8部分修，发现run_checks.py会在基线失败后提前停止这一新阻断；M-9部分修，墙顺序缺独立真相源；M-10已修方案口径；M-11部分修，load_data.js兜底仍需显式处置；M-12已修，前提是禁止空grapheme；M-13已修；L-14已修。\n七条基础假设：1“部分成立”——W5不再被五池要求直接挡死，但weekly精确规则和历史周兼容尚未闭合；2“基本成立”——只要第3步旧墙校验明确消费normalizeIdList结果，且DATA-WALL-01状态不提前变更；3“成立”——有限三态DP可判0/1/多，但必须保证每个grapheme非空、转移严格前进；4“证据不足”——接口命名矛盾且42个调用点未分类；5“不成立”——baseline失败会令quick提前break；6“证据不足”——未附四个模板和四周数据，且着色、持久状态、兜底反解析仍有可能遗漏；7“证据不足”——load_data.js缺META兜底仍生成字符串，需审查全部loadData调用方。\n第3步旧墙校验方面：对现有单字符字符串，normalizeIdList后得到数组，for-of和includes语义仍可维持旧模型；但必须明确校验器使用归一化值，且不能在此时执行“墙等于SOUNDS全集”。第4步对第3步存在分词依赖，如果weekly仍检查三个字位和已教范围，应在步骤表中显式注明。更深复核还需读取四个weekNN.template.html、四周weekNN.data.js、tests/unit/test_assessment_contract.js及所有loadData调用点。"}