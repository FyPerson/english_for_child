{"task_type":"code-review","mode":"advice-only","summary":"基于所给差异，未发现周检词泄漏、字母越界或积木架无法摆词等数据层硬错误；但首页新音仍被渲染成无行为按钮，直接违反铁律 8。另有若干素材补齐路径、自动检查覆盖和规范自洽性问题，建议在交付前处理。","issues":[{"severity":"high","category":"edge-case","location":"week02.html / renderHome 的 hero 积木点亮墙","problem":"本次引入：无真人录音时虽然不再挂 data-sayph，但元素仍固定渲染为 button。对 c、k、e、h、r、m、d 来说，这些按钮可聚焦、可点击，却没有任何行为，仍属于铁律 8 明确禁止的“哑巴按钮”；现有浏览器冒烟只检查 data-sayph 数量，因此漏掉了标签类型。","suggestion":"像 tileHTML 一样按 canHear 分支：有录音时渲染 button，无录音时渲染 div 或 span，并保留点亮状态样式。浏览器检查应同时断言无录音积木不是 button。"},{"severity":"medium","category":"edge-case","location":"week02.html / SOUNDS.k 别名与所有 PHONEME_AUDIO[ch] 直接查询处","problem":"本次引入：SOUNDS.k 正确复用了 SOUNDS.c，但音频查询仍按字母直接查 PHONEME_AUDIO[k]。以后若只给实际音素 /k/ 注入 PHONEME_AUDIO.c，c 会点亮而 k 仍保持无录音状态；若要求调用方把同一段 base64 同时写入 c、k，又会重复载荷并容易漏配。这与“素材放进常量即可自动点亮、无需改代码”的承诺不完全一致。","suggestion":"增加统一的音频键映射，例如 phonemeAudioKey('k') 返回 'c'，所有按钮守卫和播放入口都通过该函数查询；或给 SOUNDS.c、SOUNDS.k 增加共同的 audioKey:'c'。"},{"severity":"medium","category":"edge-case","location":"week02.html / initG1 的 targetInner、G1_THEME 与 g1card 图标渲染","problem":"继承硬编码被本次换周触发：targetInner 仍只识别 roundKey === 'a' 并读取 apple，第二周轮次已经变为 c/e。即使以后补齐 camera/huh 插画，e 轮目标区域也不会自动使用新素材。G1_THEME.icon 当前指向缺失素材，所给差异未展示 artHTML 和图标父容器的完整降级逻辑，尚需确认不会留下固定尺寸的空图标槽。","suggestion":"将目标图和标题图完全数据化，例如在 G1_THEME 中显式提供 targetArt，并统一通过 hasIll 判断；无图时连同纯装饰图标容器一起省略或提供文字/SVG 降级。删除 a/apple 的周数据硬编码。"},{"severity":"medium","category":"performance","location":"tools/audio_manifest_w2.json / sounds_demo 与 week02.html 的累计 SOUNDS","problem":"本次引入：音频提取器静态收集了全部 SOUNDS.demo，但第一周旧音在第二周只通过闪卡播放音素，并没有入口重新打开旧 sound 实验室。由此 ant、apple、igloo、pig、nose、snake、sock、top 等旧 demo 单词仍进入 73 条清单；它们被提取器视为“已引用”，但实际上没有可达的单词播放入口，最终会增加单文件内嵌体积。","suggestion":"sounds_demo 提取应只收集 DAYS 中实际出现的 sound 块所指向的 SOUNDS 键，或增加显式的可达音素集合。保留旧 SOUNDS 供音素快闪使用即可，不必连带打包其 demo 单词音频。"},{"severity":"medium","category":"other","location":"tools/week-checks/check_w2_data.js 与 tools/week-checks/smoke_w2_browser.py","problem":"本次新增检查脚本把 E:/projects/english_for_child/week02.html 写死。换机器、换仓库位置或在 Claude Code 的工作区执行时会直接读取错误位置，规范所说的可复用换周流程因此不成立。","suggestion":"从脚本自身位置推导仓库根目录，并允许命令行传入目标 HTML；默认解析仓库根下的 week02.html。错误信息中打印最终解析路径，避免误审其他副本。"},{"severity":"medium","category":"missing-constraint","location":"tools/week-checks/smoke_w2_browser.py / 关键组件与功能检查","problem":"本次测试覆盖缺口：规范 §13 要求六个游戏各完整跑一轮、进入结算、写入 localStorage 并在刷新后保留；当前脚本主要检查组件、订单和积木数量，只解锁 G5，没有覆盖 G1/G2/G3/G4/G5/G6 的完整结算及刷新恢复。因此“浏览器检查全绿”不能证明铁律 6。","suggestion":"为六个游戏各增加一条最短确定性路径：驱动到结算、检查对应 state.games 字段，再刷新页面确认记录仍在。可在页面上下文中替换音频播放为立即完成的确定性桩，避免依赖真实音频时长。"},{"severity":"medium","category":"missing-constraint","location":"tools/week-checks/check_w2_data.js / usedWords、周检泄漏与字母范围检查","problem":"本次测试覆盖缺口：usedWords 只收集部分结构化块，没有把 BOOK 正文纳入已教字母检查，也没有递归检查 lead、note、table、WALL_HINT 等可见课程文本是否出现保留词。当前五个保留词从所给数据看未泄漏，但以后某个词落入表格或说明文字时，该检查仍可能全绿。","suggestion":"对 DAYS、BOOK、WALL_HINT、各游戏题库建立统一的结构化可见英文词收集器；周检 exam 自身及 RESERVED/W 的声明区显式排除。对小书正文再区分 sight words、姓名和可拼词，验证其余字母均在累计已教集合内。"},{"severity":"medium","category":"contradiction","location":"docs/声音积木周课件设计规范_20260831_v1.0.md / §4.3、§5.1、§7.3、§13","problem":"本次固化的规范与实作存在多处冲突：§4.3 和 §13 要求保留词在词卡墙锁定展示，但实作及浏览器检查要求它们完全不上墙；§5.1 写 SOUNDS、W“整体替换”，实作却必须累计保留第一周数据；§7.3 把所有四音词都规定为“从后往前接”，而 rest/sand 实作明确采用先拼前三音再补尾音。第三周按此规范执行时会出现相反实现。","suggestion":"先确定唯一口径并同步三处：建议保留词只在周检出现、不进词卡墙；SOUNDS/W 明确为“保留仍有运行时依赖的旧数据，再加入本周数据”；四音词改为按辅音群位置选择拆法——开头辅音群从词尾核心向前加，结尾辅音群先拼前三音再补尾音。"},{"severity":"medium","category":"missing-constraint","location":"tools/build_audio.py / apply_manifest_overrides 与 main 参数处理","problem":"本次引入：apply_manifest_overrides 只拒绝未知源名，却不要求 source_specs 恰好包含全部十个源，也不校验每项只能有 anchors、keys、unique 三个整数。字段拼错时可能静默保留第一周默认值。另外 --manifest 与 --target 可单独指定，容易形成清单与目标周不配套的调用，虽然引用覆盖检查通常会阻止写入，但接口本身仍有误用空间。","suggestion":"加载 manifest 时严格校验 source_specs 的源名集合、字段集合、非负整数类型，并要求非默认 manifest 与 target 成对提供；写入前再核对目标 HTML 的 KEY 周次或文件名与 manifest 的周次元数据。"},{"severity":"low","category":"readability","location":"week02.html / .g1card--c .g1__target::after","problem":"本次换色遗留：c 轮目标改为辅音青边框和背景，但下方三角仍使用 var(--ok-line)，形成无语义的绿色残留，与其余 c 轮样式不一致。","suggestion":"将三角颜色改为 var(--cons-line)，或把每轮目标色统一通过一个 CSS 自定义属性驱动，避免下周继续残留旧周语义色。"}],"recommendations":["优先修复首页无行为 button，并为 c/k 建立统一音频别名；这两项直接影响铁律 8 和后续素材热插拔。","在接受“检查全绿”作为交付门槛前，补齐六个游戏的结算、落盘和刷新恢复路径。","修订规范中的词卡墙、累计数据和四音词拆分口径，避免第三周复制时产生相反实现。","音频工具的无参数调用从代码上看仍保留第一周默认值；gen_audio.py 在模块顶层的 __main__ 块中重新赋值 MANIFEST 对普通 CLI 调用是可靠的。空源 keys==0 的放宽也仍有精确计数约束，主要缺口是 manifest schema 本身没有严格验证。"],"risks":["当前首页新音会给键盘和触屏用户提供可操作外观但无反馈。","补入 /k/ 真人录音后可能只点亮 c、不点亮 k。","73 条音频中包含不可达的旧 demo，内嵌后会无谓增大微信传输文件。","现有自动检查通过仍可能漏掉游戏不结算、不落盘，以及保留词藏在说明或表格中的情况。","规范冲突可能在第三周被当作真相源复制，造成保留词展示和课程数据裁剪策略反复。"],"confidence":"medium","notes_for_claude_code":"本审查仅依据所给差异、规范、manifest 与检查脚本文本，没有执行命令或测试。建议 Claude Code 先处理唯一的高优先级问题，再核读完整 week02.html 中 artHTML、G1 图标父容器和小书图片父容器的继承实现；这些未改代码没有完整出现在差异里，无法仅凭当前材料确认是否还存在固定尺寸空洞。"}