{"task_type":"code-review","mode":"advice-only","summary":"三态 DP 的核心判定是正确的：封顶计数不会截断候选集合，forcedWalk 也不会越过更早的可完成分叉；未发现死循环、指数回溯或词尾初值错误。但契约仍有 3 处实质性打折扣，测试还缺少若干能锁住关键性质的变异用例。","issues":[{"severity":"high","category":"missing-constraint","location":"frontend/src/shared/graphemes.js:66-76（validateSoundsTable）","problem":"SOUNDS 的自有键没有按 `^[a-z][a-z0-9_]*$` 校验；当前只检查 grapheme，因此 Object.create(null) 或 defineProperty 创建的 `__proto__` 键可被 segmentWord 接受并返回。这也使 candidates 的 ASCII 比较器前提没有真正成立。","suggestion":"在 validateSoundsTable 遍历每个 id 时调用 assertValidGraphemeId，或抛专用表结构错误；新增 `__proto__`、大写、空格及非 ASCII ID 的 segmentWord 测试，分别覆盖唯一解和歧义候选排序。"},{"severity":"medium","category":"contradiction","location":"frontend/src/shared/graphemes.js:245-277（segmentWord）及 GraphemeError 各调用点","problem":"契约称结构化错误带 `{word, offset, candidates}`，但 word-invalid、word-empty、显式分段非法、sounds/grapheme 非法等错误缺少其中部分字段；当前测试大多只断言 code，无法发现错误形状漂移。","suggestion":"先明确该三字段要求仅适用于 segment-unknown/segment-ambiguous，还是适用于 segmentWord 的全部错误；若是后者，用统一错误构造器补齐稳定字段，并在每类错误测试中断言自有属性和类型。"},{"severity":"medium","category":"ambiguity","location":"frontend/src/shared/graphemes.js:190-212（findUnknownOffset）","problem":"未知错误的 offset 使用“最长匹配贪心”诊断，不一定对应真正的未知区间；存在较长前缀走入死路、较短前缀在更后位置才失败时，结果依赖贪心选择。测试只检查 code 和 word，没有锁住 offset。","suggestion":"冻结未知 offset 的确定语义，例如取所有从词首可达状态中最早无法继续的位置，并用前向可达 DP 计算；若 offset 明确不属于契约，则不要让校验器依赖它，并补注释及测试固定这一非保证。"},{"severity":"medium","category":"other","location":"tests/unit/test_graphemes.js:全局多解、forcedWalk 与 Unicode 相关测试组","problem":"测试未直接锁住“唯一候选必须沿其真实长度前进”和“最早分叉与后续更深分叉无关”。若 forcedWalk 被改成错误步长，或错误地返回后一个分叉，现有用例未必都能精准定位该性质。","suggestion":"增加共享前缀后存在两级分叉的 fixture：第一处分叉候选均可完成，且各后缀再有分叉，断言 offset 必须取第一处；另加唯一多码元 grapheme 后再分叉的用例，锁住 `i += options[0].len`。"},{"severity":"medium","category":"edge-case","location":"tests/unit/test_graphemes.js:缺少 Unicode 分词测试","problem":"实现按 UTF-16 长度和 slice 工作，符合契约，但没有测试锁住代理对、大小写展开和组合字符的具体口径；未来若改成码点遍历或 normalize()，offset 和匹配语义可能静默变化。","suggestion":"增加 emoji/代理对前缀后的歧义 offset、`İ`.toLowerCase() 长度变化、预组字符与组合字符不等价的测试；明确当前只 lowerCase、不做 NFC/NFD 归一化。"},{"severity":"low","category":"ambiguity","location":"frontend/src/shared/graphemes.js:35-58（assertSoundsShape）","problem":"“普通映射”判据仍会接受无可枚举原型数据的 class 实例，也可能受 Symbol.toStringTag 伪装影响；虽然只读自有键降低了风险，但与注释宣称的普通对象并不完全一致。","suggestion":"若契约确实只允许对象字面量或 null 原型对象，应采用跨 realm 可行的原型终点/构造器判据并补 class 实例、Symbol.toStringTag 用例；否则把契约改成“对象型自有键映射”。"}],"recommendations":["核心算法判定可保留：count3 只需表达后缀是否可完成及是否多解，candidateIdsAt 直接扫描全部匹配边并用 `count3[target] > 0` 过滤，因此三态封顶不会漏掉第三个及之后的候选。","forcedWalk 的首个分叉就是全局最早可完成分叉：在此前每个位置只有一个下一 ID，而同一 ID 的 grapheme 长度固定，不存在被跳过的另一条可完成路径。","`count3[n] = 1` 正确，表示消费到词尾形成一个完整解析；空 grapheme 在 DP 前被拒绝，因此转移严格前进，不会自环。","重复 grapheme 和前缀重叠只增加多项式扫描量，不产生回溯；建议优先修复 ID 表校验，并补齐 Unicode、错误形状、两级分叉和未知 offset 的测试。"],"risks":["非法 SOUNDS 键可绕过字位 ID 字符集约束，并使后续 HTML 属性编码或跨端候选排序建立在错误前提上。","未知 offset 尚未冻结语义，Node 校验器若把它当精确诊断位置，可能向数据作者报告误导位置。","Unicode 行为虽与当前 UTF-16 契约一致，但缺少回归测试，重构时容易被无意改成码点或规范等价匹配。"],"confidence":"high","notes_for_claude_code":"契约逐项判断：匹配 grapheme、lowerCase、全局 0/1/多解、三态 DP、完整候选、空串/未知、explicitSegments、displayOnWall、未知 ID 统一错误、UTF-16 offset、确定排序、插入顺序无关均已落地；ID 字符集先验和结构化错误字段属于打折扣。配套着色函数及 normalizeIdList 上下文按任务说明不计漏做。总判：应先修 1 条高优先级问题，并冻结两项中优先级契约后再进入下一步。"}