{"task_type":"code-review","mode":"advice-only","summary":"静态合并审发现 1 个 high：G6 在步骤折叠后仍会后台计时、写入纪录并触发庆祝，因此目前不满足“0 high 放行”门禁。T1 的主要修复方向正确，但折叠清理、庆祝层与限时游戏组合仍有若干中等问题。","issues":[{"severity":"high","category":"edge-case","location":"week01-v3.html：toggleStep()、initFlash()","problem":"折叠 timed flash 所在步骤时，没有停止其 interval 和 30 秒 autoStopTimer。词卡挑战会在隐藏状态下自动结算、持久化成绩并弹出庆祝层；六音秒表也会持续计时。clearPageTimers() 只在切页时调用，不能覆盖同页步骤折叠。","suggestion":"为每个 flash 根节点提供不记成绩的取消钩子，例如 root._flashReset()，统一清除 timer、autoStopTimer、t0/timing/count 并恢复按钮；toggleStep() 折叠时遍历当前步骤的 flash 根节点调用该钩子。不要直接调用全局 clearPageTimers()，以免误停同页其他组件。"},{"severity":"medium","category":"edge-case","location":"week01-v3.html：bindLongPress()、toggleStep()","problem":"注释声称 G2 长按计时器因使用 laterOnce 会在折叠时同步清除，但 toggleStep() 实际只调用 AudioBus.stopAll()，并未调用 clearPageTimers() 或取消该长按 timer。在多点触控、键盘触发折叠或其他程序化折叠路径下，隐藏后的长按回调仍可能写入 confirms。","suggestion":"让 bindLongPress() 返回 cancel/dispose 函数，保存到 G2 根节点；步骤折叠、重新绘制词项和切页时显式取消。回调中的索引守卫应保留，作为第二层保护。"},{"severity":"medium","category":"edge-case","location":"week01-v3.html：showCelebration() 与 G1 作答窗","problem":"celebrate-overlay 覆盖整个视口并拦截点击，但不会暂停 G1 的 4 秒作答窗。若家长在 G1 作答期间完成同页打卡并触发日完成庆祝，孩子最多有约 2 秒无法点击答案，可能被判漏答。G1 自身结算庆祝没有该问题，但日完成入口可以与运行中的游戏重叠。","suggestion":"将庆祝改为非模态：覆盖层本身不阻断底层点击，并通过 document 捕获阶段的一次性监听实现“任意点击跳过”，使该次点击仍能继续命中 G1 答案；或者在触发庆祝前明确暂停并在关闭后恢复作答窗。不要用全局清计时器代替暂停，否则会改变题目状态。"},{"severity":"medium","category":"edge-case","location":"week01-v3.html：initG2() 的 retry 分支","problem":"点击“再试一次”只执行 renderBlocks()，没有取消正在后台执行的慢速→常速播放序列。若慢速音频尚未结束，它结束后仍会启动常速阶段，导致界面已经回到积木态却继续播放整词。","suggestion":"retry 分支先调用 AudioBus.stopAll()，再 renderBlocks()；如需保留失败提示条语义，应同时确认 stopAll 会按 P0 契约清除对应 continuation。"},{"severity":"medium","category":"contradiction","location":"week01-v3.html：initG2() 导航逻辑","problem":"prev/next 使用取模循环，最后一个词点击“下一个”会回到第一个词，没有“逐词过完即结束”的完成状态，与规格 §4.2 的完成契约不一致。","suggestion":"改为线性索引：首词禁用上一项，末词提供“完成/结束”入口或进入明确的完成视图；如果产品决定允许循环复习，应同步修正规格中的“过完即结束”，但本轮不应默认为循环。"},{"severity":"medium","category":"missing-constraint","location":"week01-v3.html：renderWall()","problem":"战利品墙没有执行 Guard.isReserved() 运行时过滤，仅凭 taughtWords 当前静态内容和构建检查保证保留词不出现。这不符合规格 §2.5 明确要求的“示例渲染/战利品墙入口过滤”，未来数据调整时可能把保留词展示并赋予可点击音频入口。","suggestion":"在 renderWall() 入口使用 taughtWords().filter(w => !Guard.isReserved(w))，并让计数、卡片渲染都基于过滤后的列表；保留现有构建断言作为第一层防线。"},{"severity":"low","category":"missing-constraint","location":"week01-v3.html：confirmWord()、unconfirmWord()","problem":"共享写入层没有校验 src 是否属于 CONFIRM_SOURCES。内部调用目前是 g2，但后续 G4/G5 若拼错来源名，会向持久化状态写入垃圾字段；unconfirmWord() 还会为未知来源写入 false。","suggestion":"两个入口均先校验 CONFIRM_SOURCES.includes(src)，非法来源 console.warn 后直接返回；这样写入层与 §6.2 清洗允许的来源集合保持一致。"},{"severity":"low","category":"other","location":"week01-v3.html：initFlash() 的 lockRecord()/fmtValue()","problem":"elapsed 使用原始毫秒精度比较和存储，却只显示一位小数。两个界面上都显示为相同秒数的成绩仍可能被判为新纪录并庆祝，用户会看到难以解释的结果。","suggestion":"在比较和保存前统一舍入到 0.1 秒，或提高显示精度；建议采用前者，使持久化、比较和展示使用同一数值口径。"}],"recommendations":["先修复 G6 折叠取消问题，再进入 T5 自测，这是当前唯一阻断 0-high 门禁的项目。","为 G1、G2、G6 统一建立组件级 reset/dispose 钩子，由步骤折叠和切页生命周期调用，避免继续依赖全局 pageTimers 间接清理。","真机彩排重点覆盖：词卡计时中折叠步骤、G2 合体播放中点“再试一次”、G1 作答窗运行时完成日打卡、确认后撤销并往返战利品墙。"],"risks":["隐藏计时写入的错误最佳纪录会持久化，elapsed 纪录尤其可能变得很难再次打破。","庆祝层若保持模态，会让任何未来新增的限时交互面临相同的计时被消耗问题。","当前审查仅基于累计 diff 与规格；P0 中的 pageTimers、runAudioStage、AudioBus.stopAll、状态清洗和 data-check 键生成完整实现未在附件中展开。"],"confidence":"high","notes_for_claude_code":"建议合入前阅读完整 week01-v3.html 中 clearPageTimers、runAudioStage、AudioBus.stopAll、状态读取清洗、checks 的 data-check 生成代码，确认组件级取消钩子不会破坏 P0 的 continuation 和题目状态。以上为静态 advice-only 审查，未执行文件、测试或行为验证。"}