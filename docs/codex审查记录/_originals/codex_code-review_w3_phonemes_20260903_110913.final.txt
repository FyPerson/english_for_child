{"confidence":"high","issues":[{"category":"other","location":"assets/phonemes/b.mp3、tools/phoneme_sources_w3.json:b、week03.html","problem":"/b/ 明确从“ba”中保留了约 80ms 元音起始，即使淡出到 -25dB，仍可能被儿童听成“buh/ba”。这直接触及“孤立辅音不能带 schwa 尾音”的硬约束，也会污染 b/d、p/b 的音素对比教学。","severity":"critical","suggestion":"在获得真正孤立的真人 /b/ 前，不要给 b 渲染听音按钮；从 PHONEME_AUDIO 移除 b，让页面按铁律 8 显示家长口令。若保留该资源，至少应先定义并执行可重复的元音尾检测门槛，而不是仅以时长和淡出电平验收。"},{"category":"other","location":"tools/phoneme_sources_w3.json:g、02_evidence.md §2","problem":"g 的 0.68–0.80 秒浊化嗡音被完全排除，只保留爆破与释放。这样可能听起来接近无声的 /k/，削弱规范中要求孩子通过喉咙振动区分 k/g 的教学目标。第二周 d 的切法不能自动证明 g 也应去掉浊化段。","severity":"high","suggestion":"重新评估并保留足够的闭塞期浊化片段，或在规格中明确 g 的目标实现是“有声闭塞+爆破”还是仅爆破；验收应针对 k/g 的可感知差异，而非只复用 d 的历史切法。"},{"category":"other","location":"assets/phonemes/README.md“怎么加新音素”第 2 步","problem":"示例仍使用 `--target week02.html --src assets/phonemes`。当前 assets/phonemes 已包含第三周 g/o/u/l/f/b，而 week02.html 不声明这些键；按 build_phonemes.py 的自检 3，该示例会因多余源文件而失败。","severity":"high","suggestion":"将示例改为与当前资源集匹配的 week03.html，或明确要求使用只含目标周新增键的临时源目录；同时说明目标 HTML 必须声明源目录中的每个文件键。"},{"category":"performance","location":"tools/prep_phonemes.py:fetch、tools/phoneme_sources_w3.json","problem":"缓存只按 sound_id 和键命名，并以“文件存在且大小大于 0”判定可复用；没有 SHA-256、Content-Length 或下载完整性校验。网络中断可能留下非空残文件，CDN 内容变化也不会被发现，导致同一 spec 在不同时间生成不同音频。","severity":"high","suggestion":"在 spec 中记录预览流 SHA-256（最好同时记录字节数和 URL），下载到临时文件后校验成功再原子重命名；缓存命中时必须验证哈希，不匹配则重新下载并报错。"},{"category":"performance","location":"tools/prep_phonemes.py:prep_one 与 tools/build_phonemes.py:process_one","problem":"prep 阶段用 soundfile/libsndfile 解码 MP3，build 阶段再用 imageio_ffmpeg/ffmpeg 解码和编码；不同解码器或版本可能产生采样、时长和峰值差异，削弱“可复现”声明。README 也只笼统写“libsndfile >= 1.1”，未固定版本。","severity":"medium","suggestion":"统一由同一套 ffmpeg 完成预览流解码，或锁定并记录 soundfile/libsndfile 版本及解码参数，并把关键输入/输出哈希纳入复现记录。"},{"category":"other","location":"tools/phoneme_sources_w3.json:f、tools/prep_phonemes.py:spectral_gate","problem":"/f/ 的擦音能量本身接近噪声，12dB 门限虽较轻，仍可能削掉高频摩擦成分或引入音乐噪声；当前只有总体峰值、时长和静音检查，无法发现频谱被挖空。","severity":"medium","suggestion":"对 /f/ 增加频带能量保留或谱质心下限等自动护栏，并限制门限增益的时间/频率平滑范围；若不满足护栏，应让该键失败而不是静默产出。"},{"category":"ambiguity","location":"tools/phoneme_sources_w3.json:u、assets/phonemes/README.md","problem":"来源描述是“uh as in Uhhh, no.”，这类语境音可能实现为 /ʌ/，也可能接近 schwa；页面标注 /ʌ/ 只能说明作者标签，不能保证实际片段符合目标音。","severity":"medium","suggestion":"在来源清单中固定目标方言和可接受的 F1/F2/时长特征，或改用明确标注 /ʌ/ 的真人来源；至少把“可能是 schwa”的不确定性列为发布阻断条件。"},{"category":"edge-case","location":"tools/prep_phonemes.py:prep_one、main","problem":"缺少输入参数校验：未检查 `noise_sample_end` 是否小于音频长度和切点、start/end 是否有限且非负、fade_in/out 是否不超过片段长度。短文件或误填参数可能触发广播异常、整段被当作噪声样本，或产生难诊断的 traceback。输出目录也不清理，旧键会被 build_phonemes.py 一并扫描。","severity":"medium","suggestion":"增加 JSON schema/数值校验和清晰的键级错误信息；对 fade 长度做截断或拒绝；建议每次使用新临时目录，或提供清理/白名单机制避免残留音频混入注入。"},{"category":"ambiguity","location":"week03.html 页脚署名、assets/phonemes/README.md 来源说明","problem":"页脚先说音素音来自 margo_heston，随后以“其中 /b/ 取自……”补充例外，读者可能误解为 /b/ 也来自前一音包。对 CC BY-NC 资源虽已写“降噪并裁剪”，但各来源的修改范围没有逐项对应。","severity":"low","suggestion":"改成“除 /b/ 外的十七段来自……；/b/ 来自……”，并在来源表或页脚明确两组资源均做过裁剪/处理；保留 CC BY-NC 的作者、标题、链接、许可证和修改说明。"},{"category":"other","location":"项目其他文档（未随附件提供）","problem":"本次只展示了 week03.html、音素 README、prep/spec 和证据；无法确认项目记忆、变更日志或冒烟脚本说明中是否仍写着“第三周六个新音待采集”等过期状态。","severity":"low","suggestion":"提交前搜索 docs、项目记忆、脚本注释和发布清单中的“第三周/待采集/无录音”引用，统一更新；这些文件需要在更深 review 时实际读取。"}],"mode":"advice-only","notes_for_claude_code":"机器自检通过并不能解除 b 的教学硬约束，也不能证明 g、f、u 的听感正确。若只能优先处理少数问题，应先阻止 b 音频发布、复核 g 的浊化段，再修正 README 的 week02 示例和下载缓存校验。","recommendations":["将 /b/ 暂时降级为家长口令，直到有无元音尾的真人录音。","把 g/k、p/b 等清浊对立纳入音频规格的教学验收，而不只检查响度、时长和静音。","为预览流加入哈希锁定、原子缓存写入，并统一或锁定 MP3 解码器版本。","修正 README 示例目标周，并为 prep 脚本补充参数、短音和残留文件校验。"],"risks":["儿童可能把 b 学成带元音尾的“buh/ba”，形成后续拼读错误。","g 若缺少浊化线索，可能与 k 的示范无法稳定区分。","CDN 或本地缓存变化会使后续重跑无法得到同一派生音频。","频谱门限对 /f/ 或“uh”片段的处理可能改变目标音质或音位。"],"summary":"本次改动的注入链和文档大体自洽，但 /b/ 带元音尾是对铁律的直接高风险偏离，g 的切点也可能破坏清浊对立教学。另有 README 示例失效、缓存无哈希及解码器不一致等可复现性问题，建议在提交前处理。","task_type":"code-review"}