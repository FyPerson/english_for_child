{"task_type":"spec-critique","mode":"advice-only","summary":"不可以按当前 v1.4 直接定稿：五审的 H-1、H-3、H-4 基本关闭，但 H-2 仍未真正闭合，且本轮暴露出“全局唯一分词”与权威规范“最长匹配”的契约冲突。建议先修复下列 2 个 high，并同步拆开 4a/4b 的依赖与验收，再进入实施；这些问题不妨碍准备第 0～3 步，但必须在第 4b/5 步接线前定死。","issues":[{"severity":"high","category":"contradiction","location":"《里程碑 2 实施方案》§3.2、步骤 0；《周课件数据层交接规范》§3“字位与积木的对应”","problem":"实施方案要求枚举完整分词并在多于一解时失败，明确禁止用最长匹配消灭较短路径；权威规范仍写“拆词一律……做最长匹配”。虽然规范后文也要求多解失败，但没有明确最长匹配只能用于剪枝，因此两个实现者可能分别做贪心最长匹配和全局唯一性检测，产生不同结果。步骤 0 当前只修订测评范围、§8 和 L/grapheme，没有修订这一处。","suggestion":"在步骤 0 增加规范修订：删除或限定“最长匹配”，明确以所有完整解析的基数判定 0 解、唯一解、多解；最长匹配仅可作为搜索优化，不得改变解析集合。把 `abc` 在 `a/ab/bc/c` 下多解的例子同时写入规范与 fixture。此项必须在第 2 步接口实现前定死。"},{"severity":"high","category":"ambiguity","location":"《里程碑 2 实施方案》§0.3 临时路由表、步骤 5","problem":"`monthly` 被定义为“仅对已迁移的 W4 执行现行 week === 4 分支内容”，但当前 `validateAssessment` 在进入该分支前还执行池类型、数量、互斥、CVC、已教字位、泄漏等公共检查。若新路由只搬 `week === 4` 分支，W4 回归会被实质削弱；若保留全部前置检查，“仅……分支内容”又不准确。H-2 因此尚未真正关闭。","suggestion":"不要引用某一行分支，直接枚举 W4 monthly 兼容检查集合。若目标是保持既有回归，应定义为“当前校验器对合法 W4 实际执行的全部检查”，并逐项列出公共检查和 W4 专项检查；同时规定仅 `assessmentMode:'monthly' && META.week===4` 可进入该兼容路径，其他 monthly 固定报里程碑 2b 前不支持。"},{"severity":"medium","category":"contradiction","location":"《里程碑 2 实施方案》步骤 4、步间依赖一览、基础假设 7","problem":"正文称 4a、4b 是两个连续提交和两个强制检查点，但步骤表仍只有合并后的“步骤 4”验收，依赖表也只写 `4 → 2`、`5 → 3,4`、`6 → 4,5`。这不能证明 4a 自身可独立提交且可判定，也允许第 5 步在只完成 4a 时被误认为依赖满足。","suggestion":"把步骤表正式拆成 4a、4b 两行，并分别给验收：4a 至少覆盖规范已先改、四周数据/fixture/校验器完成字段迁移、加载后 SOUNDS 禁止自有键 L、相关检查全绿；4b 覆盖分词、着色、游戏接线和两份清单。依赖改为 `4a→2`、`4b→4a`、`5→3,4b`、`6→4b,5`，后续依赖保持传递关系即可。"},{"severity":"medium","category":"missing-constraint","location":"《里程碑 2 实施方案》§3.1、步骤 4a 验收","problem":"“全仓四模式扫描”适合作为影响面发现手段，但不足以单独证明迁移完整：它可能漏掉计算属性访问、别名后访问、JSON 双引号键或由辅助函数间接读取。并且“全仓无 `.L` 字段消费”的门槛字面只覆盖点属性访问，与正文声称的四模式不一致。未提供四周完整 data.js，因此本次只能确认方案列出的证据，不能独立确认扫描结果完整。","suggestion":"将最终门槛改成数据与代码双保险：加载所有现役周和 fixture 后，递归断言每个 SOUNDS 条目都没有自有键 `L` 且必须有合法 `grapheme`；再用 AST 或明确覆盖 `.L`、`['L']`、`[\"L\"]`、对象键、解构、派生赋值的扫描检查可执行源码。历史归档目录以显式排除清单处理。四周 data.js 不必逐行通读才能定方案，但实施时必须由上述机器门槛覆盖。"},{"severity":"medium","category":"ambiguity","location":"《里程碑 2 实施方案》步骤 6","problem":"合成 fixture 被称为“W5/ai 测试周”，但其 `newPatterns` 与 `FIRST_TEACH_DAY` 只含 `ai`，而正式 W5 契约是 `ai` 与 `j`。这容易与步骤 5 的正式 W5 assessment fixture 混淆，并可能被误当成完整 W5 正例。","suggestion":"将其明确命名为“synthetic-ai 集成 fixture”，使用不与正式周号混淆的 fixture 标识；声明它只验证双字母链路，不证明正式 W5 数据契约。正式 W5 正例继续单独覆盖 `ai`、`j`。"},{"severity":"medium","category":"ambiguity","location":"《里程碑 2 实施方案》§5“别名元数据”","problem":"别名教学顺序仍保留二选一，导致步骤 2 的 schema、步骤 3 的审计输出和步骤 7 的墙校验尚不能确定是否需要 `aliasOf`/`wallAfter` 字段。等到第 7 步前才决定，可能迫使已完成的公共接口和审计格式返工。","suggestion":"最迟在步骤 2 前拍板，而不是第 7 步前。更简单的契约是所有上墙 ID 都必须在某周 `newPatterns` 出现，c/k 同周按数组顺序表达；只有确有“不属于任何首教周但必须上墙”的真实用例时再引入独立顺序元数据。"},{"severity":"low","category":"readability","location":"《里程碑 2 实施方案》§3.1 表格","problem":"“表面串”一行被放在影响面表结束之后，Markdown 结构上容易被误读为仍属于上一张表或成为残缺表行。","suggestion":"把“三层概念”表完整放在一起，再另起“L → grapheme 影响面”表，避免实现者误读字段层次。"}],"recommendations":["定稿门槛设为：先修复全局分词契约冲突和 W4 monthly 兼容集合两个 high；其余 medium 可作为实施前置决策或机器验收项收口。","对 7 条基础假设的判定：①部分成立，尚需把全局唯一分词修订也加入步骤 0；②部分成立，扫描可替代人工通读作发现手段，但不能替代加载后 schema 断言与源码扫描门槛；③不成立，monthly 的完整兼容行为仍不明确；④基本成立，但审计输出应包含稳定规则 ID 和可供步骤 5 直接消费的修复类别；⑤成立，显式分段的查找、校验和不可变约束已闭合；⑥大体成立，但完整性只能由调用点清单、AST/负例和集成测试证明，不能由“18 行”数量本身证明；⑦不成立，4a 尚无独立验收且依赖仍指向笼统步骤 4。","H-1 可判为设计层面基本关闭，但不能仅凭“四模式扫描”判实施影响面绝对完整；应以所有现役 SOUNDS 的运行时 schema 断言作为最终兜底。H-3、H-4 可以关闭；H-2 保持打开。","将 4a/4b 正式写入编号、验收和依赖表，避免提交粒度与文档依赖模型不一致。"],"risks":["若照当前 monthly 描述实现，W4 可能在保留页面回归的同时悄然失去测评池互斥、泄漏或 CVC 等既有校验。","若规范继续保留未限定的“最长匹配”，浏览器渲染和 Node 校验器可能采用不同分词算法，导致同一词在两端结果不一致。","若 4a 只靠文本扫描验收，残留的 `L` 数据键或计算属性读取可能直到构建或运行阶段才暴露。","别名顺序模型延迟拍板可能造成步骤 2、3、7 之间的数据 schema 和审计格式返工。"],"confidence":"high","notes_for_claude_code":"建议结论写成“不可以定稿，但只剩 2 个必须修复的 high”。其中分词契约应在步骤 0/2 前修，monthly 兼容集合应在步骤 5 前修；不必因此暂停对步骤 0～3 的准备。更深的实施核验仍需读取四周完整 `weekNN.data.js`、相关 unit/browser fixture 及构建模板，但它们不是判断上述文本矛盾所必需。"}