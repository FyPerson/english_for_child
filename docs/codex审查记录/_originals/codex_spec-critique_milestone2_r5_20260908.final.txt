{"task_type":"spec-critique","mode":"advice-only","summary":"目前不建议定稿：两个原 critical 的核心方向已修正，但仍有 4 个会直接影响实现边界或导致契约假绿的 high。尤其是权威规范仍同时要求 `L` 与 `grapheme`、最小测评路由未定义非 weekly 模式的临时行为，这两项应在动手前收口。","issues":[{"severity":"high","category":"contradiction","location":"《里程碑 2 实施方案》§3.1、步骤 0；《周课件数据层交接规范》§3“12 个常量”及“SOUNDS 单条”","problem":"实施方案拍板删除 `L`，但被声明为契约权威的规范仍把 `SOUNDS` 形状写成同时包含 `L`、`grapheme`，示例也同时保留两者。步骤 0 只要求修改测评条款，没有同步修改该 schema。按现文本实施后，代码会符合方案却违反权威规范，`DATA-SOUNDS-01` 的实现也无唯一依据。","suggestion":"把规范修改加入步骤 0：所有 `SOUNDS` schema、示例、字段说明和规则状态统一删除 `L`，明确 `grapheme` 为必需且唯一显示字段，并注明旧数据迁移要求；随后再在步骤 4 执行代码和数据重命名。"},{"severity":"high","category":"ambiguity","location":"《里程碑 2 实施方案》§0.3、步骤 5、§5“测评路由”","problem":"方案只精确定义了 weekly，却没有定义最小路由阶段遇到 monthly、stage、annual、缺失值或非法 `assessmentMode` 时的行为。若简单写成“weekly 走新逻辑，其余走旧逻辑”，W10/W20 可能按周号旧逻辑假绿；若全部拒绝，又会破坏 W4 的既有 monthly 回归。","suggestion":"写出临时路由表：weekly 执行完整 weekly 列；monthly 仅对当前已迁移的 W4 执行明确列出的兼容校验；stage、annual 在里程碑 2b 前返回固定“不支持”错误；缺失或非法值一律失败。不要用默认分支回落到旧周号逻辑。"},{"severity":"high","category":"missing-constraint","location":"《里程碑 2 实施方案》步骤 3、步骤 5","problem":"迁移前审计的五类输出不足以界定 W1–W3 首次进入 weekly 路径后的允许修改范围。当前只明确 `RESERVED` 与 `W` 覆盖，没有要求审计六个禁止常量、两个禁止块、RESERVED 泄漏位置、词数、三字位可分词性及歧义情况。步骤 5 又把允许修改范围限制为 `RESERVED` 相关常量；如果真实数据含禁止块或其他禁止常量，按现范围无法修复。","suggestion":"将步骤 3 增加逐周 weekly 契约审计表，覆盖每个必需项和八个禁止项、RESERVED 数量、W 释义、唯一分词、泄漏位置。步骤 5 的允许范围改为“仅限审计证明为 weekly 契约所必需的常量、块和对应 W 条目”，并要求超出清单即停下复审。"},{"severity":"high","category":"ambiguity","location":"《里程碑 2 实施方案》§3.2 解析上下文","problem":"`ctx = { sounds, segmentsOf(word) }` 能承载多个词各自的 segments，但 `segmentsOf` 的调用契约仍未闭合：没有规定传入原始 token 还是规范化 token、大小写词如何查 `W`、找不到词条返回什么、返回数组是否需要复制和再次校验。plain/rich 路径若自行采用不同规范化方式，仍可能丢失显式消歧。","suggestion":"把形状写为 `ctx = { sounds, segmentsOf(normalizedWord) }`，规定着色器先用唯一的 `normalizeWord` 得到查表键；返回 `undefined` 表示无显式分段，其他返回值必须交给 `segmentWord` 完整校验。增加大写 token、重复出现同一词、一个 HTML 中多个不同消歧词、未知词降级四类测试。"},{"severity":"medium","category":"missing-constraint","location":"《里程碑 2 实施方案》§2.4 墙顺序规则；《周课件数据层交接规范》SOUNDS schema","problem":"墙顺序规则要求“别名派生键按显式元数据插到本体之后”，但没有定义该显式元数据的字段名、形状、合法性或与 `audioKey` 的关系。现有 c/k 可因两者都进入 `newPatterns` 而暂时绕开，但规则对未来别名 fixture 不可实现。","suggestion":"定义独立字段，例如 `aliasOf` 或 `wallAfter`，并规定必须指向已有 ID、不得自指或成环、仅用于教学顺序，不影响录音解析；或者明确别名也必须在某周 `newPatterns` 出现，从而删除额外插入规则。"},{"severity":"medium","category":"security","location":"《里程碑 2 实施方案》§3.1“机器身份 / 外部键”、§3.3；模板与 `render-blocks.js` 的 `data-*`/ARIA 拼接","problem":"方案只约束 CSS 类不得直接拼 ID，却未规定字位 ID 的字符集，也未要求 ID、grapheme、ARIA 和 `data-*` 属性统一转义。当前模板大量直接插入 `${c}`、`${head}` 等值；若未来 ID 或字形含引号、尖括号或逗号，可能破坏属性、产生注入或让逗号分隔反解析失真。","suggestion":"优先把字位 ID 限定为稳定安全语法，例如 `^[a-z][a-z0-9_]*$`；所有可见字形和属性值仍分别走 HTML 文本转义与属性转义。对数组型 `data-*` 使用 JSON 编码，不再依赖逗号拼接。增加引号、尖括号、与号和非法 ID 失败 fixture。"},{"severity":"medium","category":"other","location":"《里程碑 2 实施方案》步骤 4","problem":"`L` 重命名与消费者接线、着色安全、模板迁移、双读兼容层被放在同一个大步骤。由于 `L` 同时涉及四周数据、派生赋值、渲染和校验器，任何遗漏都会与其他接线错误混在一起，且步骤 4 的提交难以独立审查和回滚。","suggestion":"在步骤 4 内至少拆成两个连续提交或检查点：先完成 `L`→`grapheme` 的契约、数据和所有消费者迁移并加“全仓无 `.L` 字段消费”门槛；再接入分词、着色和游戏消费者。若坚持一个步骤，也应将两个检查点分别列为强制验收。"},{"severity":"medium","category":"edge-case","location":"《里程碑 2 实施方案》§3.3、§5“模块形态”","problem":"方案要求浏览器与 Node 使用同一份实现，同时 rich text 要先解析 DOM，但 Node 环境没有原生 `DOMParser`。当前没有指定解析器来源或如何保证浏览器与 Node 的解析语义一致，实施时容易出现两份解析代码或测试环境与浏览器行为分叉。","suggestion":"在方案中指定统一的受限解析策略和依赖：要么使用同一可复用解析库并同时打包到浏览器和 Node，要么把平台适配器限定为仅提供 DOM 解析原语，而白名单校验与序列化逻辑保持共享；加入两端同 fixture 的语义等价测试。"}],"recommendations":["修完上述 4 个 high 后可以定稿并进入实施；其余 medium 可随实施收口，但应先登记为验收项。","把 `L`→`grapheme` 的全仓零残留检查扩展到属性访问、解构、字符串字段名、数据 schema、fixture、文档示例和派生赋值；未提供四周完整 data.js，因此当前不能确认影响面已列全。","步骤 3 的审计输出应机器可读并带来源位置，使步骤 5、7、9 能直接消费同一结果，避免人工抄写再次漏项。","对迁移矩阵增加“传输与编码”场景：HTML 属性转义、JSON/逗号序列化、DOM dataset、持久状态键和错误信息中的 ID 展示。"],"risks":["若按当前文本直接删除 `L`，规范与实现会立即产生已知冲突，后续审查无法判断哪一边应修。","若非 weekly 模式落入默认旧逻辑，W10/W20 可能在完整矩阵实现前被错误接受。","若步骤 5 审计范围过窄，真实 W1–W3 数据可能暴露必须改块结构的问题，却被“只准改 RESERVED 常量”的限制卡住。","若不约束 ID 语法和属性编码，未来同形多音 ID 扩展可能引入 DOM 破坏、状态键冲突或注入风险。"],"confidence":"high","notes_for_claude_code":"九条落实判定：C-1 已修；C-2 部分修，承载能力足够但 `segmentsOf` 契约未闭合；H-3 部分修，方案方向正确但权威规范未同步且影响面无法在缺少完整 data.js 时证实；H-4 部分修，仍缺别名显式元数据定义；H-5 部分修，审计项不足以覆盖 weekly 全矩阵；M-6 已修；M-7 已修，但需补 Node 解析策略；M-8 已修；L-9 已修。基础假设判定：1 基本成立，但需补非 weekly 临时路由；2 部分成立；3 不成立，至少规范 schema、文档示例、属性/字符串字段访问和完整数据文件尚未纳入可证明范围；4 不成立，别名元数据未定义；5 不成立，审计缺 weekly 禁止项与泄漏审计；6 证据不足，矩阵仍缺属性编码、数组序列化及 ID 字符集场景；7 部分成立，依赖表较完整，但步骤 4 本身过大，且步骤 0 必须先同步 `L` 契约。结论：当前仍有 high，属于必须改完才能动手的规范边界问题，暂不建议定稿。"}