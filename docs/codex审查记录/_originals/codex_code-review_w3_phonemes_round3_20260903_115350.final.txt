{"confidence":"high","issues":[{"category":"edge-case","location":"tools/prep_phonemes.py:main()，候选过滤逻辑","problem":"未使用 `--include-candidates` 时，代码直接对每个条目调用 `v.get(...)`。如果清单条目不是对象（例如数组、字符串或 null），会在进入 `validate()` 前抛出 AttributeError，而不是按设计返回带键名的拒绝错误，破坏“类型模糊一律拒绝”的失败关闭语义。","severity":"medium","suggestion":"过滤前先检查 `isinstance(v, dict)`，或先统一执行条目结构校验再决定是否跳过候选；确保所有非法条目都得到可诊断的 `SystemExit`。"},{"category":"security","location":"tools/prep_phonemes.py:prep_one() 的缓存路径和输出文件名构造","problem":"`source.sound_id` 未限制类型或内容，并直接拼入缓存路径；顶层键 `key` 也直接拼入 `<key>.wav` 路径。恶意或误填的路径片段（如 `../` 或 Windows 反斜杠）可能导致缓存或输出写到预期目录之外，甚至覆盖已有文件。","severity":"medium","suggestion":"要求 `sound_id` 为正整数，并拒绝任何路径分隔符；对 spec 键验证为安全的 basename（本项目可限制为单个小写字母），或使用 `Path(key).name == key` 等检查后再构造路径。"},{"category":"missing-constraint","location":"tools/prep_phonemes.py:validate_source()","problem":"虽然已强制 `sha256` 和 `bytes`，但 `preview`、`sound_id`、`author`、`title`、`license` 仍可为 null、数组或空字符串。错误值会延迟到 URL 请求、路径格式化或日志输出阶段才以 TypeError/网络错误失败，不能保持统一的配置诊断。","severity":"low","suggestion":"为 URL、作者、标题、许可字段增加非空字符串校验；为 `sound_id` 增加正整数校验，并在下载前报告带音素键名的配置错误。"}],"mode":"advice-only","notes_for_claude_code":"复审结论：第二轮提出的候选残留检测、README 的 --clean、哈希/字节数必填、.part 清理、guard 前置与原子 WAV 写盘、inject 和数值类型检查、nr_db=0 路径及底噪间隔约束均已按证据正确落地。无 high 及以上问题。无条件检查 candidates/ 属于合理的失败关闭策略，会增加复用旧输出时需要显式 --clean 的操作成本，但与文档和防止误注入目标一致。`validate_source()` 在 prep_one 中先于依赖音频时长的 validate() 并无实质矛盾；`.part` 的 finally 和 `.wav.tmp` 的 os.replace 逻辑也保持了失败时不覆盖旧产物。","recommendations":["修正 main() 对非对象条目的候选过滤，保证结构错误统一、可诊断地拒绝。","限制 spec 键和 sound_id 的路径语义，避免本地脚本因误配置写出目标目录或覆盖非目标文件。","补齐 source 元数据的字符串/正整数约束，以维持脚本宣称的失败关闭和可复现错误处理。"],"risks":["当前 malformed 条目可能产生 Python traceback，而不是预期的键名错误。","恶意或误填的 key/sound_id 可能造成目录逃逸和非预期文件覆盖。","source 元数据类型错误可能在网络或文件操作阶段才暴露，增加排查难度。"],"summary":"第三轮改动整体已正确修复第二轮指出的问题，当前无 high 或 critical 缺陷。仍有一个明确的 medium 边界 bug（候选过滤对非对象条目直接调用 get），以及应补强的路径安全和 source 元数据类型约束。","task_type":"code-review"}