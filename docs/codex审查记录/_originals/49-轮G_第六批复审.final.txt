{"task_type":"code-review","mode":"advice-only","summary":"本轮发现 2 个 HIGH：`check_data.js` 对认读词仍按词形全局跳过，与 H3 的来源级约束冲突；G1 pos 豁免规则在两套检查中也互相矛盾。其余主要风险集中在测试分辨力不足和校验条件/错误措辞不一致。","issues":[{"severity":"high","category":"contradiction","location":"tools/validation/check_data.js，第③节 `if (SIGHT.has(w.toLowerCase())) continue`","problem":"虽然 G1 豁免已改为记录级，但认读词仍在进入记录级判断前按词形整体 `continue`。因此某词只要属于 `SIGHT`，即使同时出现在 `words`、`wordforge-family`、`blend` 等要求可分词的来源中，也会全部免检；这正是 H3 所述“认读词身份误传染到字位操作类来源”的同类漏洞，并与 `test_grapheme_semantics.js` 中仅允许 `sight/sentences/book-page` 的规则冲突。","suggestion":"把认读词豁免也合并到记录级 `isExemptRecord`：仅当记录来源属于 `sight`、`sentences`、`book-page` 且词在累计认读词集合中时豁免；删除循环开头的整词 `SIGHT` 跳过。增加同一词同时出现在 `sight` 与 `words`、`wordforge-family` 时必须失败的真实入口测试。"},{"severity":"high","category":"contradiction","location":"tools/validation/check_data.js `isExemptRecord` 与 tests/unit/test_grapheme_semantics.js `isZeroSolutionExemptRecord`","problem":"两处对 G1 pos 桶采用了相反合同：`check_data.js` 只豁免 W1 pos，而语义测试仍无条件 `rec.kind === 'g1-rounds'`，明确豁免所有周的 pos/neg。这样同一条 W2 pos 零解记录会被一套检查拒绝、另一套检查放行，测试注释也分别宣称各自是规范。","suggestion":"先确定唯一规范；按任务简述应统一为“全部 neg + 仅 W1 pos”。让语义检查使用 `bucket` 和记录周次作相同判定，并新增 W2 pos 不豁免、W2 neg 豁免、W1 pos 豁免三组断言。"},{"severity":"medium","category":"edge-case","location":"tests/unit/test_check_data_g1_exemption_granularity.py 与 word_consumers.js 合同","problem":"本 diff 未包含 `word_consumers.js`，无法从附件确认 g1-rounds 记录确实携带正确的 `bucket`。现有 W2 pos 失败测试不能证明该字段存在，因为 `bucket` 缺失同样会得到“不豁免并失败”的结果；W1 正例仅验证真实文件整体通过，也没有直接断言抽取出的记录包含 `bucket: 'pos'`。","suggestion":"读取并核对 `tools/validation/word_consumers.js`；增加对 `collectWordConsumption` 输出结构的直接测试，分别断言 pos/neg 的 `bucket` 和 `week`。再加入含不可分词合成词的 W1 pos、任意周 neg 正例，以及 bucket 缺失时明确失败或抛结构错误的测试。"},{"severity":"medium","category":"other","location":"tests/unit/test_grapheme_semantics.js，`verifyCumulativeSightWordsAndReadingKindScope`","problem":"所谓集成测试自行创建 `cumulativeSightWords`，并在测试内部重新实现“先累计 sight、再逐条处理”的循环，没有调用真实的跨周处理入口。因此即使 `classifyStrictConsumptionWordsFromRealWeeks` 日后退回每周重建集合，这个测试仍会通过，属于由测试自身构造保证结论。场景①也只调用豁免谓词，未证明真实消费记录会最终产生失败。","suggestion":"抽取实际跨周分类流程为可传入周数据的函数，并让合成两周数据通过该真实入口执行；断言 wordforge 记录形成失败、历史 sight 在后续 book-page 被豁免，而不是在测试中复制实现算法。"},{"severity":"medium","category":"contradiction","location":"tools/validation/wall_order.js，`META.week` 校验","problem":"注释和错误语义称要求“合法正整数”，代码只检查 `Number.isInteger`。零和负整数会落入“与文件名周次不一致”分支，而不是“字段本身非法”分支；同时新增测试只覆盖 1.5，没有覆盖任务特别询问的字符串 `'1'`，也没有覆盖 0、负数和 NaN。真实数据目前即使使用数字，也不能替代对输入合同的验证。","suggestion":"类型分支改为 `typeof week === 'number' && Number.isInteger(week) && week > 0`，并为 `'1'`、0、-1、1.5、NaN 增加表驱动测试，分别校验错误码、消息和 `actual`。"},{"severity":"low","category":"edge-case","location":"tools/validation/load_data.js，`extractScriptContents`/`declaration`","problem":"使用 `scripts.join('\\n')` 已能避免两个 script 边界粘连，本轮担心的“上一段无换行导致漏匹配”并不存在。不过抽取仍把所有 script 内容拼接后取第一个同名声明；若前置的其他内联 script、注释或模板字符串中出现行首伪声明，仍可能抢在真实数据声明之前。新增 M1 测试只覆盖 script 外正文伪声明，未覆盖多 script 冲突。","suggestion":"若页面有稳定的数据 script 标识，应只解析目标 script；否则至少检测同名声明重复并结构化报错。补充“前置普通 script 含伪 RESERVED、后置数据 script 含真实 RESERVED”的测试。"},{"severity":"low","category":"readability","location":"tools/validation/check_data.js，失败消息来源汇总","problem":"失败消息把豁免和非豁免记录都折叠成 kind 集合；当同一词同时来自 G1 pos、G1 neg 或其他来源时，只显示 `g1-rounds`，无法判断究竟是哪条非豁免记录触发检查，削弱了记录级修复的可诊断性。","suggestion":"错误消息至少列出非豁免记录的 `kind/bucket/week`，并将全部来源作为补充信息，而不是只输出去重后的 kind。"}],"recommendations":["统一认读词与 G1 的豁免合同，并让 `check_data.js` 和语义检查复用同一个来源级判定函数或同一份规则表。","优先补充能区分“字段正确存在”和“字段缺失后碰巧失败”的测试，尤其是 W1 pos、W2 pos、neg 与缺失 bucket 四种情况。","把跨周语义处理抽成可注入合成周数据的真实入口，避免测试复制生产循环。","进一步审查 `tools/validation/word_consumers.js`、`SIGHT` 的构造位置及 `ENTRY_KINDS`，确认 `words` 的教学语义和所有记录字段合同。"],"risks":["同一个不可分词词汇可能因认读词身份而在词卡墙或字位操作游戏中被静默放过。","W2 及以后 G1 pos 数据可能在不同验证入口得到相反结果，造成 CI 行为和规范解释不一致。","当前测试可能在 bucket 抽取损坏或真实跨周累计逻辑回退时仍然保持绿色。","多 script 页面仍可能由较早的伪声明劫持数据抽取。"],"confidence":"high","notes_for_claude_code":"结论仅基于所给 diff；若要确认 bucket、week、words 的准确合同，需要继续读取 `tools/validation/word_consumers.js` 及相关数据结构定义。`extractScriptContents` 使用换行拼接，因此题目所问的 script 边界粘连不是问题。本轮 HIGH 数：2（2 HIGH）。"}