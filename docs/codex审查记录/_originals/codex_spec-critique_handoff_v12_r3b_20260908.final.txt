{"confidence":"high","issues":[{"category":"contradiction","location":"复审条目 C；§3 步1、步1b、步2及“节奏”","problem":"状态：部分修。步1仍把 `weekNN.data.js`列为教学设计产物，步1b又要求在步2生成的起点文件上修改同一文件，执行顺序和所有权仍不唯一。","severity":"critical","suggestion":"改成“1 教学设计工作包（不产出最终data.js）→2 生成可构建scaffold→3 数据层实现与验收”；或明确阶段A一次完成并跳过起点交接。"},{"category":"missing-constraint","location":"复审条目 H；§3“起点四处”及起点说明","problem":"状态：部分修。实测证明的是基于week04造week05时机器契约可过，并未建立可识别的“合法但未定稿”状态；文字禁止进入 `courseWeeks`/发布，无法防止误操作。","severity":"high","suggestion":"增加 `draft/scaffold` 标志或专用目录，并让 `build`、`release`、courseWeeks校验在未定稿时硬失败；定义从起点态转定稿态的必需字段和显式命令。"},{"category":"contradiction","location":"复审条目 H；§6.6 第①步与“门槛分两层”","problem":"状态：部分修。第①步仍预期 `doctor` 输出Chromium PASS，后文却允许Chromium安装失败时以替代门槛通过；同一清单给出互相冲突的成功标准。","severity":"high","suggestion":"拆出基础环境检查与浏览器检查两个结果，明确各自退出码及阶段A/B要求；在工具增加基础检查参数前，不要把含Chromium的doctor PASS写成必需预期。"},{"category":"security","location":"复审条目 H；§6.3音频键级校验、§3.2","problem":"状态：部分修。`comm`命令只比较音频路径，未验证manifest中实际变更的键是否属于 `KEYS`，也未覆盖未提交工作树中的越界文件；若manifest键到文件名存在映射/规范化，按键名直接拼路径还可能漏检。","severity":"high","suggestion":"提供权威脚本：从manifest解析键到实际文件，分别检查工作树、暂存区和提交范围，并同时校验manifest条目集合；`git add`仅接受脚本输出的具体文件。"},{"category":"edge-case","location":"复审条目 H；§6.6 第⑤⑥步","problem":"状态：部分修。worktree流程未处理目标目录已存在、路径含空格、进程中断遗留worktree或Windows文件锁；`git worktree remove --force ../dryrun-wt`可能误删既有目录。","severity":"high","suggestion":"使用唯一临时目录并先断言其不存在且位于仓库父目录；演练前后运行 `git worktree list`，失败时保留并上报，不对未经确认的路径执行强制移除。"},{"category":"readability","location":"复审条目 M；§3.3 pull后重验表","problem":"状态：已修。三类任务的复验项目已分别列出，且要求使用BASE..HEAD检查；但未区分交接基线与pull前本地提交，见下方新问题。","severity":"medium","suggestion":"补充“交接基线”和“pull前本地HEAD”两个变量的定义及各自用途。"},{"category":"missing-constraint","location":"复审条目 M；§6.6体量护栏","problem":"状态：部分修。已加入副机检测和触线停报，但仅在6.6说明，3.1/3.2/3.3任务清单没有强制调用；MB换算及course入口20MB的检查也未明确。","severity":"medium","suggestion":"把体量检查作为每个副机清单的必做步骤，明确字节到MB的换算、周文件和合并入口分别检查，以及“未判定”时禁止签署通过。"},{"category":"other","location":"复审条目 M；§6.3越权改动处置","problem":"状态：已修。已区分未推送恢复与已推送由Claude新建纠正提交，并禁止reset/强推。","severity":"medium","suggestion":"保留该规则，并增加纠正提交必须重新运行范围核验和完整验收的要求。"},{"category":"other","location":"复审条目 M；§7真相源与无同型周","problem":"状态：已修。已收敛到定稿提交中的版本化源码，并明确无同型周由Claude建scaffold、不得猜测。","severity":"medium","suggestion":"在发布门禁中引用同一“定稿提交”标识，避免源码已更新而文档状态仍指向旧提交。"},{"category":"readability","location":"复审条目 L；§6.3 Shell说明","problem":"状态：已修。已声明全文按Git Bash并补充PowerShell最小等价示例。","severity":"low","suggestion":"将所有代码块中的 `weekNN`、`<BASE>`、`<键>` 统一标注为必须替换的占位符，避免复制执行。"},{"category":"contradiction","location":"§3并行规则、§6.3及pull后范围核验","problem":"交接基线BASE固定在主机推送时，但允许主机同时提交下一周。副机pull后用 `BASE..HEAD` 会把主机并行提交一并纳入范围，导致合法的跨周提交被误判为副机越权。","severity":"high","suggestion":"区分 `HANDOFF_BASE` 与 `LOCAL_BEFORE_PULL`：副机自有改动用pull前本地HEAD到当前HEAD核验，主机回收再按提交作者/交接标识核对；或改用独立分支/临时工作树隔离。"},{"category":"missing-constraint","location":"§3起点说明、§7及发布流程","problem":"没有定义谁、何时、以什么字段把起点从“未定稿”标记为“定稿”，也没有说明 `courseWeeks` 扩展和 `release` 的先后门禁。","severity":"high","suggestion":"增加状态字段（例如 `META.status`）及状态转换表；要求只有定稿状态才能加入 `courseWeeks`，`release` 和交付命令再次拒绝draft/scaffold。"},{"category":"edge-case","location":"§6.3音频校验命令","problem":"`KEYS=\"milk sad rug\"`假定键不含空格、换行或特殊字符；命令还依赖Git Bash的`grep`、`comm`和LF换行，未给出键名约束或失败处理。","severity":"medium","suggestion":"在manifest契约中禁止空白和路径分隔符，或改用脚本/JSON输入；检查`comm`、`grep`不可用及CRLF时给出明确失败信息。"},{"category":"performance","location":"§6.6体量护栏与§2铁律10","problem":"只检查单个生成后的week HTML，未明确合并入口的20MB预算何时、由谁检查；媒体分批到位时早期字节数可能严重低估最终体量。","severity":"medium","suggestion":"主机在全部素材捕获和最终build后强制检查week与course两个预算；副机报告当前值并注明素材完成度和预计增量。"},{"category":"missing-constraint","location":"§0能力表、§3.1/§3.2命令","problem":"副机被要求自行build/check，但能力表仍写Node“未验证”；除首次6.6 dry run外，没有阶段A任务在Node不可用时的明确停机或替代路径。","severity":"medium","suggestion":"把Node 18+检查前置为所有副机任务的硬门槛；若仅配图且无法安装Node，明确禁止继续而由主机构建并提供可视化产物。"}],"mode":"advice-only","notes_for_claude_code":"本审查只依据任务简报及提供的只读文档文本，未读取或执行实际脚本；worktree、doctor、check_data及音频文件名映射仍需在真实仓库中验证。","recommendations":["先闭合§3的数据层步骤和draft/scaffold状态机，再统一project.json、courseWeeks与release门禁。","为副机提交范围建立单一脚本，并区分交接基线、pull前本地HEAD和并行主机提交。","在Windows Git Bash上实测唯一临时worktree、异常中断清理、CRLF及带特殊字符键名。","把体量检查、Node/Chromium环境判定和pull后复验分别嵌入对应任务清单。"],"risks":["阶段A可能覆盖已完成的数据层，阶段B可能从未定稿内容继续发布。","并行提交与固定BASE结合会造成越权误报或掩盖真实越权。","共享音频目录、manifest映射和未提交改动可能导致漏提交或越界提交。","Chromium/Node环境判定不一致可能错误激活或阻断阶段B。"],"summary":"复审条目中没有完全未修项，但步骤1/1b/2仍是关键流程矛盾；“合法但未定稿”缺少机器门禁，不能仅靠文字约束防止发布。Chromium、worktree、键级校验和体量护栏虽已补充，仍存在跨平台及并行提交的新缝。","task_type":"spec-critique"}