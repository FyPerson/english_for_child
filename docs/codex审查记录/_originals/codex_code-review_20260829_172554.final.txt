{"task_type":"code-review","mode":"advice-only","summary":"当前累计改动仍有 2 个高严重度问题，尚不满足“0 high = P2 收官”门禁：G4 的延迟反馈回调存在跨换单/折叠竞态，且确认后没有规格要求的撤销路径。G5 的保留词判定顺序、统一中性反馈和无单词音频路径在所给差异中未发现明显泄漏。","issues":[{"severity":"high","category":"edge-case","location":"week01-v3.html：initG4() 的 validate()、pickWord()、reset()","problem":"拼错后通过未保存、未失效保护的 `laterOnce(()=>renderPlacing(), 600)` 恢复界面。若这 600 毫秒内换订单，旧回调会在新词音频仍播放时把状态提前改成 placing，破坏“播放期锁答”；若此时折叠步骤，reset() 会把 word 置为 null，旧回调随后执行 renderPlacing() 并访问 word.length，可能直接抛出运行时异常。","suggestion":"保存该反馈计时器并引入题目代次令牌；在 pickWord()、playQuestion()、reset() 中统一清除计时器并递增令牌。回调执行前同时核对令牌、word 非空及当前仍为对应 feedback 状态。"},{"severity":"high","category":"contradiction","location":"week01-v3.html：initG4() 的 renderOrders()、validate() 和点击委托","problem":"G4 长按确认后只刷新订单上的勾号，成功区仍保留“按住 1 秒确认”，代码中也没有 `unconfirmWord(word, 'g4')` 的按钮或处理分支。因此无法完成规格 §4.4、验收 §7.2 明确要求的“已确认态 + 撤销且只清 g4 来源”。","suggestion":"让拼对后的成功视图根据 `state.games.confirms[word].g4` 分支渲染：未确认显示长按按钮，已确认显示明确的已确认状态和撤销按钮；撤销处理调用 `unconfirmWord(currentWord, 'g4')` 后重绘订单及成功区，并保留当前词一致性检查。"},{"severity":"medium","category":"edge-case","location":"week01-v3.html：initG5() 的 render()、reset() 及 toggleStep()","problem":"G5 折叠时 reset() 清空内部槽位和计时器但不重绘；重新展开也没有调用 `_g5Refresh`。因此展开后可能继续显示折叠前的旧槽位或确认界面，直到下一次有效交互才恢复一致，且 12 分钟提示计时器不会在单纯展开时重新启动。另外首次初始化时即使 G5 步骤尚未展开也会开始计时，可能在孩子第一次打开时立即显示“玩了好久”。","suggestion":"为 G5 增加明确的展开恢复钩子，例如 `_g5Resume()`：重新按 examRecorded() 渲染并从实际展开时启动计时；toggleStep() 打开含 G5 的步骤时调用它。初始隐藏状态不要启动计时，折叠时继续由 `_g5Reset()` 清理。"},{"severity":"medium","category":"contradiction","location":"week01-v3.html：initG3() 的 renderFeedback() 答错分支","problem":"答错纠正音首次失败后，提示条的 retry 只是裸调用 `WordAudio.play(target)`，没有处理其返回状态；第二次仍失败时不会重新武装提示条。这与规格 §2.3 及四审 T-2 的“重试可无限重新武装”契约不一致。","suggestion":"把纠正重播封装成完整阶段函数，或复用既有 runAudioStage；每次 failed 都重新注册同一阶段的 retry，cancelled/blocked 静默，并加入当前题令牌检查，避免旧题失败提示污染新题。"},{"severity":"medium","category":"contradiction","location":"week01-v3.html：initG4() 的 placeLetter() 与点击委托","problem":"规格 §4.4 描述的是“先点积木，再点槽位放入”，当前实现点击积木后直接写入第一个空槽；槽位点击只用于撤回，不能选择放置位置。这会让交互行为与冻结规格不一致。","suggestion":"若规格不变，应增加 selectedTile 状态：点积木只选中，点空槽后才放入；已占槽仍用于撤回。若此次确实决定简化为顺序自动入槽，应由最终决策者先同步修订规格和验收口径。"},{"severity":"low","category":"other","location":"week01-v3.html：initG3() 的 doorHTML()、stageHTML()、renderWaiting()","problem":"两扇门在等待作答前已经渲染插画，而规格把“开门 + 插画”定义为答对后的反馈；当前正确反馈仅切换 pairbtn--right 样式，没有明确的开门后揭示插画阶段。","suggestion":"等待态只展示门和单词；答对后为正确门增加独立打开状态并显示插画。若 sat/sit 共用同一插画，也应避免作答前显示，以保持反馈层次。"}],"recommendations":["先修复 G4 延迟回调竞态和撤销链路，再重新执行 P2 门禁；现有 11/11 冒烟未必覆盖“拼错后立刻换单”“拼错后立即折叠”和确认撤销场景。","补充三个定向回归：G4 拼错后 600 毫秒内换单、G4 拼错后立即折叠、G4 确认后撤销且其他来源确认仍能维持战利品点亮。","补充 G5 首次展开与折叠后再展开的界面/计时验证，以及 G3 纠正音连续失败两次的提示条重新武装验证。"],"risks":["G4 的陈旧计时器可能造成播放期间提前开放操作，或在折叠后触发空 word 异常并影响同页后续交互。","缺少 G4 撤销会让 confirms.g4 持久状态无法按来源回退，直接破坏战利品墙派生语义。","G5 展开生命周期不闭环会出现界面状态与内存状态不一致，并使软提示计时失真。","G3 二次音频失败无可见恢复入口，未满足统一播放失败契约。"],"confidence":"high","notes_for_claude_code":"本审查仅依据所给累计差异、build_audio.py 和规格文本进行静态复核，未执行命令或测试。若需继续确认计时器清理、长按取消和音频仲裁细节，应读取完整 week01-v3.html 中 laterOnce、bindLongPress、confirmWord/unconfirmWord、AudioBus、WordAudio 及步骤展开逻辑的定义；最终取舍和执行仍由 Claude Code 决定。"}