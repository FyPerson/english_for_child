{"task_type":"spec-critique","mode":"advice-only","summary":"歧义定位的核心定义已经关闭 H-1，2b 截止日问题也已关闭；四处修改中未发现阻断定稿的新矛盾。可以定稿，但建议实施前补清审计记录的文件定位字段，并把候选排序的升序及长度单位写死。","issues":[{"severity":"medium","category":"ambiguity","location":"§5「迁移前审计」最小 schema","problem":"文中称“七字段”，但当前明确列出的记录字段只有 schemaVersion、status、ruleId、findingId、line、details 六个；同时第 3 步要求发现结果“带来源位置”，仅有可空 line 无法在多周、多文件审计中唯一定位来源文件。这会使第 5/7/9 步的机器消费方自行发明 file/source 字段或外层结构。","suggestion":"明确完整 JSON schema：增加例如 sourceFile 字段，或明确 source 为包含 file、line、column 的对象；说明 schemaVersion 位于顶层还是每条记录中，并给出稳定排序键，例如 ruleId、source.file、line（null 的排序位置）、findingId。同步修正“七字段”的计数。"},{"severity":"low","category":"ambiguity","location":"§3.2 `segmentWord` 的 candidates 排序规则","problem":"“先按 grapheme 长度，再按 ID 的码点序”方向和长度单位未明确。ID 已被限制为 ASCII，ID 码点序能够形成确定全序，但实现者仍可能分别采用升序/降序、UTF-16 length/Unicode 码点数或 localeCompare，从而造成跨端结果漂移。","suggestion":"写成可直接实现的比较器：先按 grapheme 的规范化后 UTF-16 长度升序，再按 ID 的 Unicode 码点字典序升序；鉴于 ID 限定为 ASCII，也可明确禁止 localeCompare，按逐码点或普通 ASCII 字典序比较。若希望 grapheme 长度按 Unicode 码点数计算，也应明确写出。"},{"severity":"low","category":"other","location":"§3.2 歧义 DP 实现约束","problem":"现有“三态加有限候选”的复杂度约束与“返回完整 candidates 数组”可以兼容，但实现时若只保留两条见证或两个候选，仍可能漏掉三解以上的完整候选集合。","suggestion":"补一句：三态可以截断解析数量，但不得截断最早歧义位置的可完成下一 ID 集合；候选集合应从可达且可到达终点的状态边计算，而不是从有限见证解析推导。"}],"recommendations":["可以定稿；上述问题不构成新的 high 或 critical，可作为实施前的文字收口项处理。","基础假设 1：成立。以“能延伸到完整解析”的路径集合和最早分叉位置定义 offset，不依赖见证选择、键插入顺序或 DFS/DP 遍历顺序。","基础假设 2：有条件成立。ID 限定为 `^[a-z][a-z0-9_]*$` 后，明确方向的 ID 码点字典序可提供全序；当前文字还应补“升序”和 grapheme 长度单位。","基础假设 3：成立。逐项比较有序错误数组能够锁定错误内容、重复次数和顺序，强度高于集合或多重集比较；前提是错误字符串本身即当前外部契约。","基础假设 4：部分成立。枚举、findingId、规则化 details 和稳定排序方向正确，但必须补齐来源文件字段、顶层/记录层结构以及精确排序键，才能可靠支撑跨周机器消费。","M-2 可以关闭：2026-10-31 已在 frontmatter、规范修改要求、第 0 步验收和范围说明中同步，并给出了与 W10 开课时间一致的推算依据。"],"risks":["若 candidates 从有限见证而非完整可完成分支集合生成，三解 fixture 可能仍漏候选。","若排序实现使用 localeCompare，浏览器与 Node 的区域设置或 ICU 版本可能造成不必要的差异。","若审计结果没有 sourceFile，跨周同一行号的发现无法稳定回溯，第 5/7/9 步可能形成互不兼容的临时解析约定。"],"confidence":"high","notes_for_claude_code":"结论：可以定稿，H-1 与 M-2 均已关闭；本轮未发现四处修改引入新的阻断性矛盾。执行时优先把审计 schema 的 sourceFile/外层结构和排序键补成可验证 schema，并确保歧义算法只截断解析计数、不截断完整候选集合。"}