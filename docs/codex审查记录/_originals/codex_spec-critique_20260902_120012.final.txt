{"task_type":"spec-critique","mode":"advice-only","summary":"该规范的角色框架基本成立，但尚不足以让副机使用者“无需询问即可完成并回传”。主要阻塞是副机是否能操作本地文件与终端未定义、工具注入 HTML 与“GPT 不改 HTML”冲突、音频参数权限不一致，以及并行工作会同时修改 README、共享音频和 main 分支。","issues":[{"severity":"high","category":"contradiction","location":"§1、§3 步骤 7、§4.2、§6","problem":"角色表和开场提示词都禁止 GPT 修改 HTML，但步骤 7 又要求在副机执行 build_audio.py 注入并把“注入后的 HTML”作为产物提交；§6 还规定冲突时 HTML 以主机版本为准。接手方无法判断工具生成的 HTML 改动是否属于允许范围，发生冲突时也可能把刚注入的音频一起丢掉。","suggestion":"明确区分“禁止手工编辑 HTML”和“允许工具生成派生 HTML”。更稳妥的方案是副机只提交 manifest 及明确列出的音频源文件，注入只在主机步骤 8 执行；副机若必须运行注入验证，应对临时副本执行且不提交该 HTML。"},{"severity":"high","category":"ambiguity","location":"§0 能力表、§1、§3 步骤 6–7、§4 工作包","problem":"文档只说副机有“GPT、git、Python”，同时又要求 GPT 修改仓库文件、执行 Python 和提交推送。普通 GPT 会话不能直接操作本地终端；用户角色只写了“操作 GPT 会话并把产物放进仓库”。虽然第二周曾成功执行，但规范没有记录当时究竟由谁复制内容、运行命令、保存文件和执行 git。新接手者会在实际操作点卡住。","suggestion":"把步骤拆成明确操作者：GPT 生成内容或给出参数建议；用户保存指定文件并执行逐条命令；或明确副机使用的是具备本地文件和终端权限的客户端。为步骤 6、7各给一份从 pull、保存、验证到 commit、push 的可复制清单。"},{"severity":"high","category":"contradiction","location":"§2 第 6 条、§4.2 开场提示词、§5 第一条","problem":"§2 固定单词/句子使用 Jenny、小书使用 Emma；§4.2 又说“不能改声线”；§5 却把 voice 列为可调整参数，并允许在 Jenny 与 Emma 之间按用途选择。接手方可能为了修发音而把某个单词切到 Emma，违反固定声线规则。","suggestion":"选定唯一规则。建议把 voice 定义为按资源类型决定的不可调字段：单词和句子只能 Jenny，小书只能 Emma；仅允许修正错误分类，不允许作为听感调参。明确顶层默认值、逐条覆盖字段以及两者优先级。"},{"severity":"high","category":"contradiction","location":"§2 第 6 条与 §5 第一条","problem":"§2 把小书的 -30% 和静音阈值 -60 dB 写成固定规则，§5 又允许 silence_threshold_db 在 -90 到 -20 范围内逐条调整，未说明 -60 是默认值还是不可变约束。","suggestion":"将每个值标成“固定值”或“默认值”。若允许覆盖，限定可覆盖的资源类型、调整范围、触发条件及必须记录的理由；不要同时使用“固定”和“可调”两种口径。"},{"severity":"high","category":"contradiction","location":"§4.1 开场提示词、§2 第 5 条、§4.3 庆祝图验收","problem":"开场提示词要求每张图都是 1024×1024，但另外两处要求庆祝图为 384px，验收表还要求尺寸不符就退回。无法判断 384px 指 GPT 原始文件还是 embed_assets.py 规格化后的产物。","suggestion":"明确原始尺寸和派生尺寸。例如：“GPT 一律交 1024×1024；celebrate.png 经工具处理后为 384×384；验收检查处理后文件。”若庆祝图必须原始即为 384px，则应在开场提示词中明确列为唯一例外。"},{"severity":"high","category":"edge-case","location":"§3 并行规则、§6 开工/离开规则","problem":"所谓“不同周文件可以并行”并不完整：两台机器都会修改 README §1/§4，并可能触碰跨周共享的 assets/phonemes、audio_raw、audio_processed 或同词音频。两边还直接向 main 推送，后推送的一方会遇到非快进拒绝或 README 冲突，状态行也可能从 N+1 倒退到 N。","suggestion":"列出并行时允许修改的精确路径和共享文件锁。README 只由主机 Claude 更新，或改成每周独立状态文件；规定推送顺序和“推送前再次 pull”的处理流程。共享音素及跨周复用词不得与另一台机器并行修改。"},{"severity":"high","category":"contradiction","location":"§3 步骤 5、§6“离开前”","problem":"离开流程先要求提交、工作区干净并 push，下一步才要求修改 README。修改后工作区立即重新变脏，且 README 更新不会包含在刚推送的提交中。步骤 5 的“提交推送；README 更新”也有相同顺序歧义。","suggestion":"统一顺序为：更新 README → git diff/status 检查 → commit → push → 再次确认 status 干净且与 origin 同点。若没有状态变化，则不要为了“离开电脑”强制修改 README。"},{"severity":"high","category":"edge-case","location":"§6“分支与冲突”","problem":"“HTML 主机赢、PNG/mp3 副机赢、manifest 手工合并”按文件类型粗暴裁决，无法处理主机刚加入的新音频、不同周共享同一单词、派生 MP3 与 manifest 不匹配，或副机 HTML 已含新音频但被主机版本覆盖的情况。“手工合并 manifest”也没有指定由谁负责，对非工程师风险很高。","suggestion":"按真相源而非扩展名裁决：manifest 和原始素材是源，processed MP3 与 HTML 是可重建派生物；冲突由 Claude 按具体周次和键合并，随后统一重生成、注入和校验。禁止用户直接手工合并 JSON。"},{"severity":"high","category":"contradiction","location":"§3 步骤 1、spec_excerpt.md 中的校验命令","problem":"步骤 1 在 weekNN.html 装配前就要求 weekNN-data.js 通过 check_data.js，但已提供的专题节选只展示了 check_data.js 接受 HTML 的命令，没有说明它能直接校验 data.js。接手方无法机械执行该验收。","suggestion":"若校验器支持 data.js，写出完整命令及预期成功标志；若不支持，把 check_data.js 验收移动到步骤 2 装配之后，步骤 1 改为确实可执行的数据层静态检查。"},{"severity":"high","category":"missing-constraint","location":"§4.3 音频验收","problem":"用 git diff tools/build_audio.py 应为空只能发现当前未提交改动，无法发现副机已经提交的脚本篡改，也没有检查 gen_audio.py、build_phonemes.py、校验器或其他冻结文件。因此它不能证明“处理链和 tripwire 未被改”。","suggestion":"记录副机工作开始时的基准提交，并用 git diff <基准提交>..HEAD -- <冻结文件列表> 检查；更稳妥的是规定副机提交只能触碰 manifest 和指定素材路径，由主机按提交文件白名单验收。"},{"severity":"medium","category":"missing-constraint","location":"§3 步骤 8、§1 用户职责","problem":"“听检结果记入总表”没有给出总表路径、字段格式和实际写入者。用户负责听检，步骤 8 却由 Claude 执行，容易出现两方都以为对方已经登记的空白。","suggestion":"写明总表的准确路径、最小字段和流程，例如用户给出逐键结论，Claude 在步骤 8 录入并提交；或由用户按固定模板填写，Claude 只核对。"},{"severity":"medium","category":"missing-constraint","location":"§4.3 回传验收","problem":"“主体完整不出血、无文字、画风一致、词尾爆破、人耳听检”都不是可机械执行的检查，但表头和任务目标容易让接手方误以为全部可以通过命令完成。庆祝图尺寸、透明通道等客观项也未给出具体检测命令或检查对象。","suggestion":"把验收拆为“自动检查”和“人工签字”。自动项提供完整命令、检查对象和成功输出；视觉项指定由用户拍板，音频发音项指定用户逐键确认，不能用脚本全绿替代。"},{"severity":"medium","category":"ambiguity","location":"§4.1 配图包","problem":"配图包要求提供“本周角色定妆图”，紧接着又说若需要则第一步先生成定妆图；尚未生成时不可能作为输入提供。另外“文件名等于 art 键”没有在开场提示词中明确扩展名、大小写和禁止自动追加序号。","suggestion":"拆成两阶段：第一阶段只附三张风格参考图并生成定妆图；用户确认后，第二阶段把已确认定妆图作为场景参考。明确保存名为 <art键>.png，大小写严格一致，禁止中文名、空格、版本后缀和“(1)”。"},{"severity":"medium","category":"contradiction","location":"文档头部 source_of_truth 与 §7 真相源优先级","problem":"头部说与专题文档冲突时以专题文档为准，§7 却规定“上一周定稿 HTML 的实作 > 周课件规范”。这会使上一周遗留 bug 反过来覆盖工程层权威规范。","suggestion":"改为：明确写出的规范要求高于既有 HTML；上一周 HTML 只在规范未规定的实现细节上作为复制基线。同步修正文档头部和 §7，形成一份无例外歧义的优先级。"},{"severity":"medium","category":"readability","location":"§2 第 2–10 条及 §0 专题索引","problem":"多项引用无法由当前材料核对；尤其“第三周数据层交接 §4”没有在 §0 索引中给出准确文件路径，像一次性交接文档而非长期权威来源。§2 第 6、9、10 条依赖的规范章节也未在所给节选中出现。","suggestion":"所有出处使用可定位的仓库相对路径加章节号，并删除或替换一次性“第三周数据层交接”引用。长期规则应回收到课程方案或周课件规范，再由本文引用。"},{"severity":"low","category":"readability","location":"§2 第 9 条、§3 步骤 9","problem":"“codex 审 0-high”可能被理解成“零个 high”或某个名为 0-high 的等级，也没有说明 medium 是否允许交付。","suggestion":"改成明确门槛，例如“high 和 critical 均为 0；medium 必须逐条处置或由用户书面接受”。"},{"severity":"low","category":"other","location":"§5、§8","problem":"§5 大量重复专题音频规范、§2 第 6 条和 §4.2，已造成固定值与可调值互相冲突；§8 又要求每周无条件复盘并可能修改本规范，对单家庭项目会带来不必要的文档版本噪声。","suggestion":"可删去 §5 的具体参数范围和处理细节，只保留跨角色边界、真相源及专题文档链接；可删去“每周必须改本文”的暗示，改为只有职责、交接路径或验收规则实际变化时才修订。"}],"recommendations":["先确定副机的真实执行模型，并为配图、调音分别写成“GPT 做什么、用户点什么或运行什么、提交哪些文件”的线性操作清单。","把 manifest 与原始素材定义为真相源，把 processed MP3 和内嵌 HTML 定义为可重建派生物；据此重写冲突规则。","建立副机提交路径白名单，并让主机按基准提交检查文件范围，避免依赖工作区 git diff。","取消两台机器都维护同一条 README 状态行；并行期间由主机统一维护全局状态。","统一 voice、rate、silence_threshold_db 等字段的固定值、默认值和允许覆盖规则，避免 GPT 自行解释。","保留§0、§1、§3和§4.3这些交接核心内容；精简§5的技术重复和§8的例行文档流程。"],"risks":["副机可能提交工具生成的 3 MB HTML，随后在冲突处理中被覆盖，导致音频注入悄然丢失。","两台机器并行更新 main 和 README 时可能产生推送失败、状态倒退或错误手工合并。","声线及静音阈值规则不统一，可能造成同类内容跨周声线漂移。","仅检查未提交 diff 可能让已经提交的处理链修改绕过主机验收。","视觉和听检项目若没有明确人工签字人，自动检查全绿后仍可能交付错误素材。"],"confidence":"medium","notes_for_claude_code":"本审查仅依据所提供文本。要完成出处准确性和音频字段语义的深审，还需读取完整的《声音积木周课件设计规范》§3.1、§5.1、§6、§11–13，《声音积木六年课程方案》§5及§9.2，“第三周数据层交接”原文件，以及 check_data.js、gen_audio.py、build_audio.py 的命令帮助和 manifest schema。Claude Code 可优先处理所有 high 项，再决定哪些 medium 项适合纳入 v1.1。"}