{"confidence":"high","issues":[{"category":"other","location":"§3 步1/1b/2 所有权","problem":"判定：部分修。步1已改为教学设计工作包，且说明步1b是 data.js 唯一所有者；但步2产物表仍直接写“起点 data.js”，与唯一所有者表述不完全一致。","severity":"high","suggestion":"将步2产物改为“数据层起点文件（仅复制并完成四处机械替换，不负责教学内容）”，并在验收栏明确不得编辑教学内容。"},{"category":"other","location":"§3 META.status 起点态","problem":"判定：部分修。已定义 scaffold/final 和单向转换，但工具尚不识别该状态，当前过渡办法不能形成机器门禁。","severity":"critical","suggestion":"在工具实现前，发布流程必须显式解析并拒绝 scaffold；或者暂不把 scaffold 登记到 weeks、暂不生成可发布周产物。"},{"category":"other","location":"§6.6 第①步 doctor","problem":"判定：部分修。文档已列出两种可接受结果，但尚未证明 check --quick 不依赖 Chromium；因此替代门槛的可执行性仍待实现核对。","severity":"high","suggestion":"读取 project.py 和检查器实现，明确 quick 检查是否启动浏览器；必要时提供独立的无浏览器基础检查命令。"},{"category":"other","location":"§6.3 键级校验","problem":"判定：部分修。已增加允许集合、三处 diff 和 manifest 交叉核对，但第④步仍是人工 grep，不能可靠覆盖所有键形态和删除/重命名。","severity":"high","suggestion":"增加脚本解析 BASE/HEAD 两版 manifest，计算实际修改键与 KEYS 的双向差集，并按状态检查新增、删除、重命名。"},{"category":"other","location":"§6.6 第⑤步 worktree","problem":"判定：未修。临时 worktree 被追加 marker 后处于脏状态，却要求不带 --force remove；Git 通常会拒绝移除。","severity":"high","suggestion":"remove 前先撤销 marker 并确认 clean；或者明确预期 remove 失败并提供清理步骤。"},{"category":"other","location":"§6.3 pull 后重验与基线","problem":"判定：部分修。已区分 HANDOFF_BASE 与 LOCAL_BEFORE，但未规定多次 pull 后如何更新基线、如何处理未提交改动，以及如何可靠区分主机并行提交。","severity":"medium","suggestion":"每次 pull 前后记录 SHA，使用任务提交 ID 或明确 author 过滤；禁止带未提交改动 pull，或规定 stash/停工流程。"},{"category":"other","location":"§6.6 体量护栏","problem":"判定：已修。已明确纳入配图、调音、数据层三份清单，并同时检查 week/course 两个预算。","severity":"low","suggestion":"保持现有规则，并考虑让脚本自动枚举任务周，避免手工替换周号。"},{"category":"other","location":"§6.3 越权改动处置","problem":"判定：已修。已区分未推送和已推送情形，并禁止 reset、强推及副机改写共享历史。","severity":"low","suggestion":"保持现有规则。"},{"category":"other","location":"§7 真相源","problem":"判定：已修。已将版本化源码置于生成 HTML 之上，并明确 HTML 不是事实源。","severity":"low","suggestion":"保持现有规则。"},{"category":"other","location":"§6.3 Shell 说明","problem":"判定：已修。已统一说明代码块按 Git Bash 编写，并给出 PowerShell 最小等价示例。","severity":"low","suggestion":"保持现有规则。"},{"category":"other","location":"§6.3 两个基准变量","problem":"判定：部分修。定义了 HANDOFF_BASE 与 LOCAL_BEFORE，但并行提交和连续 pull 时仍可能把主机提交混入副机差异。","severity":"high","suggestion":"为每个交接包记录唯一任务提交范围；主机按提交 ID/作者及路径逐提交核对，不使用单一范围差分代替归属判断。"},{"category":"other","location":"§3 META.status 状态转换","problem":"判定：已修（规范定义层面）。已定义 scaffold→final、操作者和步9转换时机；但实现缺口仍由第2项覆盖。","severity":"medium","suggestion":"工具门禁实现后补充状态字段的 schema、迁移和回归测试。"},{"category":"other","location":"§6.3 KEYS 键名约束","problem":"判定：部分修。已声明不得含空白和路径分隔符，但校验正则仍只匹配 `[a-z]+`，两者允许范围不一致。","severity":"medium","suggestion":"明确键字符集并让生成允许集合、manifest 解析器和命令使用同一规则；不要依赖宽泛人工 grep。"},{"category":"other","location":"§6.6 week/course 护栏","problem":"判定：已修。已从仅检查 week 扩展为同时检查 course，并给出字节到 MB 的换算。","severity":"low","suggestion":"补充等于预算时的判定和安全余量规则。"},{"category":"other","location":"§0 能力表与 Node 门槛","problem":"判定：已修。已将 Node 18+ 明确为所有副机任务硬门槛，并规定不可用时停工上报。","severity":"low","suggestion":"保持现有规则，并让 doctor 输出与该门槛保持一致。"},{"category":"contradiction","location":"§6.6 第⑤步 worktree 演练","problem":"追加问题：追加 marker 后不清理就执行不带 `--force` 的 remove，流程在 Windows/Git Bash 下很可能直接失败。","severity":"high","suggestion":"在临时工作树中恢复被修改文件、确认 `git status --short` 为空后再 remove；若要测试脏树保护，应把失败作为单独测试而非成功路径。"},{"category":"missing-constraint","location":"§3 META.status 过渡办法；docs/development.md §新增一周、§发布与回退","problem":"追加问题：scaffold 可登记在 project.json.weeks 并生成独立周 HTML，而 release 描述未说明排除 scaffold；不打 tag 不能阻止手工或其他入口发布。","severity":"critical","suggestion":"在 release 工具和文档中加入逐周状态门禁，任何 scaffold 立即拒绝；工具完成前不要登记 scaffold 周或不要产生可发布产物。"},{"category":"ambiguity","location":"§6.3 HANDOFF_BASE / LOCAL_BEFORE","problem":"追加问题：未定义每次 pull 后是否更新 LOCAL_BEFORE，也未处理副机工作树有未提交改动时 pull 的行为；主机并行提交两次时作者过滤规则也不够精确。","severity":"high","suggestion":"明确“pull 前记录、pull 后更新”的状态机；要求工作树干净才可 pull，并记录任务提交 SHA 集合以隔离并行提交。"},{"category":"edge-case","location":"§6.3 manifest 第④步","problem":"追加问题：`grep -oE '\"[a-z]+\"\\s*:'` 会漏掉含数字、连字符、下划线等合法键，也可能漏检删除、重命名和嵌套字段。","severity":"medium","suggestion":"用 JSON 解析脚本比较 BASE 与 HEAD，输出修改键及双向差集；若坚持正则，则把允许字符集限制写入规范并加失败门禁。"},{"category":"edge-case","location":"§6.3 音频文件差集","problem":"追加问题：只按文件路径合并工作树、暂存区和提交差异，未定义删除、重命名、BASE 不可达及并行提交混入时的处理。","severity":"medium","suggestion":"使用 `--name-status --find-renames`，对 D/R 状态、BASE 可达性和跨周键逐项失败；按 manifest 键生成精确允许路径。"},{"category":"contradiction","location":"§6.6 doctor 与 check --quick","problem":"追加问题：有条件通过允许 Chromium 不可用，但若 check --quick 内部启动浏览器，该条件分支无法完成。","severity":"high","suggestion":"核对并引用实际实现；若 quick 含浏览器，提供无浏览器替代检查，且将完整回归固定放在主机。"},{"category":"edge-case","location":"§6.6 临时 worktree 路径","problem":"追加问题：秒级时间戳目录名在并发运行或同秒重试时可能冲突。","severity":"low","suggestion":"使用 `mktemp -d` 或循环生成并再次检查绝对路径，且明确命令须从仓库根目录执行。"},{"category":"performance","location":"§6.6 体量脚本示例","problem":"追加问题：示例把 week05 写死，复制到其他周时可能误检旧周；只用格式化后的两位小数展示也可能掩盖临界值。","severity":"medium","suggestion":"从任务周号或 project.json 自动枚举并按原始字节比较；明确等于预算是否触线，并设置安全余量。"},{"category":"readability","location":"三审逐条落地对照 / 收敛情况","problem":"追加问题：简报声称15条全部落实，但没有逐条可验证证据，且历史复审结论与本轮变更混排。","severity":"low","suggestion":"按固定编号逐条列出“已修/部分修/未修”、证据段落和剩余动作，将历史结论与本轮审查分栏。"}],"mode":"advice-only","notes_for_claude_code":"本审阅仅基于提供的简报及引用文本，未读取仓库实现，也未执行命令。要确认 META.status 是否会影响 release、check --quick 是否启动浏览器、worktree remove 的实际行为及 manifest 键结构，请进一步读取 tools/project.py、tools/build_lessons.py、tools/package 相关脚本、tools/validation/check_data.js 以及实际周 data/manifest 文件。","recommendations":["优先补上 scaffold 的真实发布门禁；在工具支持前，不要把“不进 courseWeeks、不打 tag”视为充分安全保证。","修正 worktree 演练的脏工作树清理逻辑，否则阶段B激活证据无法按文执行。","将交接基线改为任务提交集合或明确的提交归属算法，覆盖连续 pull 和主机并行提交。","把 manifest 键级核对机器化，并统一键字符集、文件名映射和删除/重命名语义。","同步修订步2产物表及 development.md 的发布规则，消除跨文档不一致。"],"risks":["未识别 scaffold 状态可能把未定稿周打包为独立课件或合并课程发布。","现行 worktree 演练大概率在 remove 步骤失败，阻断副机阶段B资格验证。","并行提交或多次 pull 可能导致越权改动误报或漏报。","人工 grep 键名校验可能遗漏合法键、删除或重命名，造成跨周音频变更未被发现。"],"summary":"第四稿已落实多数历史审查项，但“全部落实”的结论不成立：scaffold 的发布门禁仍未真正落地，worktree 演练存在直接矛盾。并行基线与键级校验虽有改进，仍需明确状态机并机器化，且应同步修订跨文档发布规则。","task_type":"spec-critique"}