{"task_type":"spec-critique","mode":"advice-only","summary":"首审 13 条中，8 条已修、5 条部分修、0 条未修。第二稿解决了大部分表述矛盾，但阶段 A/B 的 data.js 流程仍未闭合，起点文件能否通过真实契约尚无证据；dry run、Chromium 门槛、音频键级白名单也仍存在可执行性缺口。","issues":[{"severity":"critical","category":"contradiction","location":"§3 步 1、步 1b、步 2及“节奏”段","problem":"data.js 的生产顺序在阶段 A 仍然自相矛盾：步 1 已产出完整 data.js，步 1b 又要求在步 2 创建的起点 data.js 上修改，而表格顺序却是 1→1b→2。若按“阶段 A 步 1 到 5连续做完”，步 2复制起点可能覆盖步 1成果；若按阶段 B 节奏执行，则实际顺序是 1→2→1b，与编号不一致。","suggestion":"把流程显式拆成“1 教学设计文档/工作包（不写最终 data.js）→2 骨架与起点 data.js→3 数据层实现”，并分别标明阶段 A、B 的执行者。不要让步 1和步 1b都以同一个 data.js 为产物；若阶段 A确实由 Claude 一次完成，可写成阶段 A跳过起点交接步骤。"},{"severity":"high","category":"missing-constraint","location":"§3 步 2“起点 data.js”方案","problem":"“复制上一个同型周，只改 META.week 与 storageKey 即可通过 build”的结论没有规范保证。development.md 明确构建会校验字位墙、题库、测评词池、每天检查项等数据契约；若校验器包含周次—存储键一致性、课程计划字位、跨周重复、测评词或媒体键约束，旧周内容可能立即失败。即使当前能通过，也可能把语义上错误的旧周课程登记成正式新周。","suggestion":"在数据层交接规范中定义并由校验器支持明确的“起点态”：列出允许暂时沿用的字段、必须同步改动的最小字段及禁止进入 courseWeeks/发布的条件。更稳妥的是由主机生成契约合法但不可发布的 scaffold，并增加类似 draft/scaffold 标志；在没有读取并验证 check_data.js、构建契约和第五周数据要求前，不要承诺“只改两个字段必过”。"},{"severity":"high","category":"contradiction","location":"§6.6 第①步及其后的“门槛分两层”","problem":"命令预期要求 doctor 末行同时 PASS Node 与 Chromium，随后又声明副机没有 Chromium 不阻断阶段 B；而本节又规定“五步全过”才算具备阶段 B 条件。若 doctor 将 Chromium 缺失判为失败，文档仍会错误阻断激活。","suggestion":"把环境验收拆为两个机器可判定结果：阶段 B 必选的 Python/Node/quick-check 与完整回归可选的 Chromium。明确 doctor 缺 Chromium 时允许的退出码和输出，或提供只检查基础环境的参数；在确认工具实际行为前，不要把含 Chromium 的 PASS 写成阶段 B 必过预期。"},{"severity":"high","category":"missing-constraint","location":"§6.3 音频键级校验、§3.2 发音回炉命令","problem":"键级校验仍停留在原则：没有定义 manifest 键如何映射到 raw/processed 文件名、扩展名和可能的辅助文件，也没有可复制执行的命令或脚本。与此同时，§3.2 使用目录级 git add，会把共享目录内任何已有改动一起暂存并推送；主机只能在推送后发现越权文件。","suggestion":"提供权威校验脚本或明确算法，例如输入 BASE、manifest、待修键，输出允许路径集合并与 BASE..HEAD 的变更集合做差集，差集非空即失败。副机提交前必须运行同一检查，并改为按计算出的具体文件路径 git add；至少增加 git diff --cached --name-only 和键集合核对，禁止直接暂存整个共享目录。"},{"severity":"high","category":"edge-case","location":"§6.6 第③至⑤步","problem":"记录到 /tmp/base.txt 的基线后续没有被使用，因此没有真正验证“历史周哈希不变”；第⑤步只证明工作树临时只改了目标文件。直接向真实 data.js 追加内容再 git checkout 还原还依赖开始时绝对干净，并可能被换行转换、过滤器或中断留下残留。命令同时混用了 Bash 专属的 /tmp、printf、ls，而项目运行环境是 Windows，文档没有明确要求 Git Bash。","suggestion":"优先使用临时 worktree或专用 fixture完成演练；若必须改真实文件，先保存 BASE，演练后执行 git diff --exit-code BASE --所有受保护源码路径，并核验目标文件哈希恢复。明确 shell 为 Git Bash，或分别给 PowerShell 命令。不要只把 BASE 写入临时文件而不参与任何断言。"},{"severity":"medium","category":"missing-constraint","location":"§3.1、§3.2、§3.3 公用的 pull 后重验规则","problem":"公用规则把 check_data、build、check --quick 一概套给三类任务，但没有分别定义配图和调音在 pull 后需重新生成、注入、试听或重新检查哪些产物；提交后的 git status --short通常为空，也无法证明该提交范围合规。","suggestion":"为三份清单分别列出 pull 后验收矩阵：配图重新 build、embed dry-run、图像规格与提交范围；调音重新生成受影响键、build_audio、自检、试听与键级范围；数据层运行 check_data、build、check --quick。提交范围应使用 BASE..HEAD 或 HEAD^..HEAD 检查，而不是仅看工作树状态。"},{"severity":"medium","category":"contradiction","location":"§2 铁律 10、§3.1、§3.2、§3.3","problem":"首审要求在三份命令清单中加入“触线即不得提交”的明确分支，目前只在铁律和主机步 8写了责任划分。配图流程在保存图片后没有运行会检查最终内嵌体量的命令；调音流程也可能在 build HTML变大后直接提交素材。","suggestion":"在每份副机清单中写明对应的体量检测命令、判定阈值和失败动作。若现有 check只能在媒体捕获后准确计算，应明确副机无法判定时必须上报预计增量并禁止声称护栏已通过，同时由主机在合并前作最终门禁。"},{"severity":"medium","category":"ambiguity","location":"§6.3“白名单外的改动回滚”及§6.4","problem":"主机是在副机已提交并推送后检查 BASE..HEAD，此时“回滚这些文件，只保留白名单内改动”没有说明是创建修复提交、revert、还是改写已推送历史。错误操作可能破坏 main 历史或连带撤销合法改动。","suggestion":"规定已推送情况下只能由 Claude 创建新的纠正提交，并从 BASE或正确版本恢复越权文件；禁止 reset、强推或让副机自行解决。未提交阶段才允许恢复工作树文件。"},{"severity":"medium","category":"ambiguity","location":"§3 步 2、§7 真相源优先级","problem":"起点要求取“上一个同型周”，但“同型”的判定来源、找不到同型周时的处理以及由谁确认均未定义。与此同时，上一周定稿 HTML 被列为最高优先级，但 HTML现在是未入库构建物，且“上一周”未必与目标周同型。","suggestion":"在数据层规范中定义周型标识及选取规则，并规定没有同型周时由 Claude创建 scaffold、不得猜测。将最高优先级收敛到版本化源码与对应定稿提交；生成 HTML只能作为该提交构建后的核对证据。"},{"severity":"low","category":"readability","location":"§6.3 Shell说明","problem":"“PowerShell 下把 $(...) 换成 $(...) 同样可用”前后完全相同，不能提供任何转换信息；当前示例中的尖括号占位符也容易被直接复制后被 shell 解释。","suggestion":"删除无效句，分别给 Bash与PowerShell的完整最小示例，变量名直接使用 BASE_SHA，并明确示例占位值必须替换。"}],"recommendations":["首审 C“阶段 B时序死锁”：部分修。新增合法起点思路消除了“缺文件必失败”，但步骤编号、阶段 A流程及起点契约仍未闭合。","首审 H“audio_processed矛盾”：已修。三类术语和版本化理由已统一，§3.2、§6.3、§6.4基本一致。","首审 H“dry run覆盖不全”：部分修。增加了环境、清单和白名单演练，但基线未参与断言，历史周哈希并未被实际核验。","首审 H“写死 BUILT数量”：已修。三处均改为基于 project.json的稳定条件。","首审 H“音频目录级白名单过宽”：部分修。风险已明确，但缺少可执行算法或命令，副机仍目录级 git add。","首审 H“基准 SHA靠猜”：已修。显式 BASE为主、grep仅兜底；但工作包字段可再模板化。","首审 M“版本号漏改”：已修。front matter与一级标题已统一为 v1.2。","首审 M“铁律编号”：已修。编号已恢复为1至12连续。","首审 M“配图提示词边界”：已修。版本化边界和三类工作包隔离均已说明。","首审 M“pull后未重新验收”：部分修。原则已加入，但三类任务复验项没有分别定义，范围检查仍依赖工作树状态。","首审 M“环境门槛未分层”：部分修。文字上已分层，但 doctor必须含 Chromium PASS与“不阻断阶段 B”仍冲突。","首审 M“护栏责任未拆”：已修责任归属，但命令清单未落实触线分支，因此该首审项整体只能判部分修。","首审 L“manifest命名”：已修。第二周起规则及第一周历史例外均已明确。","优先修正§3的步骤模型，再决定起点 data.js是正式合法数据、草稿态还是专用 scaffold；这会直接影响 project.json、build与发布门禁。","把音频键级白名单和 dry run历史只读检查做成单一脚本或稳定命令，避免依赖人工解释路径集合。"],"risks":["阶段 A按当前表格执行可能覆盖已完成的数据层，阶段 B则可能因起点未满足隐藏契约而无法构建。","缺 Chromium时 doctor的实际结果可能与文档门槛冲突，导致阶段 B错误激活或错误阻断。","目录级暂存共享音频目录可能把其他周或未完成键一起提交并推送。","真实周文件演练若被中断、换行转换或恢复命令失误，可能污染正式课程数据。","缺少副机侧体量门禁时，大尺寸素材可能先进入 main，直到主机回收阶段才发现超限。"],"confidence":"high","notes_for_claude_code":"本复审仅依据提供的v1.2、首审稿、v1.1、development.md与project.json作文本和流程一致性审查，没有执行命令或验证工具行为。要回答“只改META.week与storageKey是否必过”，必须进一步读取check_data.js、project.py/build_lessons.py、周型与测评契约、week04.data.js及拟用的同型周数据；在此之前应将该方案标为待实测假设，而不是既定可执行保证。"}