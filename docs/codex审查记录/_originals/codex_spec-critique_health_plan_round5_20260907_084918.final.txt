{"confidence":"high","issues":[{"category":"contradiction","location":"§3.7 Release job、§4 第0期与§5 第0期验收","problem":"Release 步骤下载 artifact 后把 manifest 放在版本目录内，但命令直接执行 `gh release create ... manifest.json`，未说明当前工作目录或该文件如何复制到当前目录，实际很可能找不到文件。另有 zip 根目录结构、GITHUB_TOKEN 注入和 job 级 `contents: write` 未写成可执行契约。","severity":"high","suggestion":"明确下载后的绝对路径并在发布前 `test -f`；使用 `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`，在 release job 显式声明 `permissions: contents: write`；从同一版本目录生成 zip 与 manifest 附件，并在上传前解压 zip、按 manifest 逐文件校验。"},{"category":"contradiction","location":"§3.3 基线、§4 第1b期第6步","problem":"基线命令只定义为 `baseline --create [--force]`，且说明命令内部执行默认 `build`；第1b期却要求重取 `form: single-file` 基线，而此时默认形态已切换为引用形态。命令可能记录错误形态，导致后续基线比较失真。","severity":"high","suggestion":"增加显式 `--form single-file|reference`（或让基线命令固定调用 `build --single-file`），并让基线文件的 `form`、比较命令和验收步骤由同一参数驱动；为两种形态各加一个错误形态拒绝测试。"},{"category":"contradiction","location":"§7 风险与反向理由、§3.5 过渡指标、§4 第1b期时程","problem":"总体决定要求体量护栏无论如何不触线，但延期预案明确允许把过渡阈值临时放宽到 week 10、course 24。这是对核心约束的显式例外，且没有定义谁批准、有效到哪个提交、如何防止该例外进入长期配置。","severity":"high","suggestion":"二选一：删除“放宽阈值”预案，改为第五周暂停装配直到1b完成；或把延期版本定义成单独、带到期日期和审批记录的临时配置，CI 强制标记为非发布构建，禁止生成正式 Release。"},{"category":"contradiction","location":"§3.5 长期指标与“从构造上不随周数增长”的表述","problem":"四项指标是非累计的，只能保证不会因历史周数累加而增长，不能保证未来某一周新增媒体超过6 MB、单文件超过1 MB，或模板/门控代码超过1 MB、壳超过0.5 MB。假设C按“50周内从构造上不会触线”理解并不成立。","severity":"medium","suggestion":"把表述改为“不会因累计复习媒体而触线”；同时定义每周素材配额、单文件上限和超限处理，或在课程计划中给出可验证的最坏新增量证明。"},{"category":"edge-case","location":"§3.6 postMessage 协议","problem":"消息没有请求编号或当前周代号。iframe 在切换周次、重新加载或旧页面延迟回包时，旧的 `ready`、`saved`、`leave` 回包仍可能通过同一个 `event.source` 检查，被误套用到新周。","severity":"medium","suggestion":"每次打开周生成随机 `sessionId`/`requestId`，所有消息携带 `week` 与会话号；壳只接受当前 iframe 会话的回包，切换周时先使旧会话失效。"},{"category":"edge-case","location":"§3.6 ready 超时与 leave 超时","problem":"ready 固定2秒、leave 固定1秒，未说明慢设备、首次磁盘读取、浏览器调度延迟或回包已发出但计时器尚未处理的情况。leave 超时按不可离开会把用户卡在当前周，且没有重试/取消入口。","severity":"medium","suggestion":"把超时做成可配置并覆盖慢端测试；leave 超时显示“正在等待课件响应”，提供重试和强制返回当前周按钮，而不是永久阻塞；切换请求应可取消。"},{"category":"security","location":"§3.6 消息发送规则","problem":"只规定了接收端的 `event.source` 校验，没有规定发送端的 `targetOrigin`。本地 file:// 可能只能使用 `*`，但 http/https 下继续使用 `*` 会把清单和门控消息发送给后来导航到同一 iframe 的任意页面。","severity":"medium","suggestion":"按协议明确：file:// 使用 `*` 并依赖会话号和 source 校验；http/https 使用计算出的精确 origin；iframe 导航或 URL 变化时立即使会话失效。"},{"category":"readability","location":"§3.3 第1b期基线验收","problem":"第1b期只比较 declarationOrder 与 media，不比较 skeletonSha256，然后立即重取基线。这允许桥接改造期间出现未被发现的任意周文件代码变化，只要媒体表和声明顺序仍相同。","severity":"medium","suggestion":"为桥接插入定义允许变化的骨架差异（例如仅允许新增固定 bridge 片段），对其余骨架仍做哈希或结构白名单比较；重取基线前先保存并核对这份差异报告。"},{"category":"ambiguity","location":"§4 第1b期第1步验收","problem":"把 `build/media/` 文件数硬编码为348、总字节约6.2 MB。只要第1期素材或清单发生合法变化，验收就会误失败；同时16位哈希前缀的不同内容碰撞规则没有写明。","severity":"medium","suggestion":"把348改为由四周 manifest 计算出的去重集合断言，6.2 MB只作报告；构建前按完整 sha256 分组，若同一16位前缀对应不同完整哈希则明确失败，禁止覆盖。"},{"category":"edge-case","location":"§3.4 build/package、§3.1 build/目录","problem":"未明确每次构建是否清空或校验既有 `build/media/`。旧媒体可能残留，单独运行 `build` 或 `--single-file` 后仍被 package/manifest 纳入，形成“构建成功但发布包含陈旧文件”的状态。","severity":"medium","suggestion":"构建到全新临时目录后原子替换 `build/`，或显式删除并校验白名单；`--single-file` 输出目录必须拒绝残留 course/media，package 必须发现未声明文件即失败。"},{"category":"performance","location":"§3.7 zip 发布、§5 第1b期验收","problem":"只核对解压后的文件哈希，没有规定 zip 内文件顺序、时间戳、权限和压缩参数，因此同一提交重复发布可能产生不同 zip 字节；“可重现”目前只覆盖目录文件，不覆盖 Release 附件。","severity":"medium","suggestion":"若要求 zip 可重现，固定排序、UTC 时间戳、权限位和压缩器参数，并把 zip sha256 写入发布日志；若不要求，明确声明 zip 仅为封装，禁止把其字节一致性作为验收条件。"},{"category":"other","location":"§1 第二表 I、§3.5 单文件报告项","problem":"`--single-file` 明确“不设失败护栏，只报告体量”，因此它成为绕过长期体量指标的正式交付暗门；只发单周时可能重新出现数MB至数十MB文件，而方案目标又声称所有体量护栏不触线。","severity":"medium","suggestion":"明确单文件仅限人工导出、不属于正式 Release；至少在命令输出和 CI 中给出非零警告/显式确认，并记录单文件字节上限作为发布前检查。"},{"category":"edge-case","location":"§3.6 `hasProgress` 协议","problem":"`hasProgress` 只有布尔值，只能发现一侧有键、另一侧无键，不能发现两侧都存在但内容不同的 localStorage。Chromium 的 file:// 行为若发生部分同步，壳可能误判为一致。","severity":"medium","suggestion":"发送进度版本号或稳定摘要（例如键存在、更新时间、校验摘要），壳与周文件比较摘要；不一致时提供明确的导出/恢复提示。"}],"mode":"advice-only","notes_for_claude_code":"基础假设逐条结论：A：条件成立，仅限已实测的 Chromium/Edge/Chrome 版本；Firefox 未验证，且 file:// 策略应继续视为浏览器相关。B：条件成立，材料中的现有消费点确实把值当 img/audio 的 src；建议增加静态回归检查，防止未来新增 atob、fetch 或 Blob 解码路径。C：不成立（若“不会触线”指绝对保证）；四项指标不累计，但单周新增媒体、单个文件或代码本身仍可超阈值。D：条件成立，storageKey、checks 和周号足以计算门控，前提是门控算法在壳与周文件中完全一致并校验数据形状。E：条件成立但需补会话号、targetOrigin 策略和消息字段类型校验；source 比对本身不能处理旧 iframe 会话。F：条件成立但验收偏弱；跨形态 media 表可比，第1b期跳过骨架哈希会放过非预期代码改动。G：条件成立但依赖严格排期；现有延期预案与“护栏绝不触线”冲突，不能直接作为可接受回退。H：条件成立但应把 build 临时目录、单文件模式残留清理、artifact 下载目录和 job 权限写成可执行断言；现有实作与方案在这些点仍有明显落差。材料4未提供改造后的实际文件，因此无法确认 postMessage、build/media、zip 和新 CI 已落地；上述判断基于规范与当前旧实现的对照。","recommendations":["优先修复两个确定性契约问题：基线形态选择，以及 Release manifest/zip 的实际路径、权限和认证。","删除或严格隔离会触发过渡护栏的延期放宽预案，否则“护栏无论如何不触线”的目标不自洽。","为 postMessage 增加会话号、周号、精确的超时/重试状态机和按协议选择 targetOrigin。","将硬编码媒体数量改为从 manifest 计算，并在构建阶段检测16位产物名碰撞、陈旧输出和单文件残留。","把基础假设A、B、F的边界条件写进自动化验收，而不是只保留实测叙述。"],"risks":["Release 工作流可能在 tag 构建时因找不到 manifest、权限不足或未认证而失败。","错误形态的基线会让第1b期媒体等价性验收失去意义。","旧消息或超时处理不当可能导致切换到错误周次，或把用户锁死在当前周。","单文件模式和未来单周大素材可能绕过长期护栏，重新造成大体积交付。","build/media 残留或短哈希碰撞可能使发布包包含陈旧或错误媒体。"],"summary":"发现3条需要优先修正的 high 问题：Release 路径/权限契约、single-file 基线形态歧义，以及延期时放宽护栏与总体目标冲突；另有多条可局部修复的协议、构建和验收边角。由于 high 超过两条，当前不建议直接定稿，完成上述修订后再做一次定向复核。","task_type":"spec-critique"}