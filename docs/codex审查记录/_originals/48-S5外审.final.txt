{"confidence":"high","issues":[{"category":"edge-case","location":"tests/unit/test_render_smoke.js：runInteractionScenarios 的 G5 场景","problem":"注释与覆盖清单声称测试 G5“摆词填满 + 撤回”，实际只调用 initG5 并断言初始 HTML 非空，没有点击积木、填满槽位或撤回。该测试看似验证交互态，实际只验证初始化。","severity":"high","suggestion":"为 G5 构造真实订单/积木点击序列，断言填满后进入反馈态，再点击已填槽位并断言槽位恢复为空；若暂时做不到，应缩小注释和覆盖声明。"},{"category":"contradiction","location":"tools/validation/gen_segments.js：main() 的退出码逻辑","problem":"`--write-heuristic` 会写入 `@gen-segments-unreviewed` 标记，但此时 `hasUnwrittenCandidates` 被强制视为 false，工具返回 0；文档却定义 0 为“全部处理完毕”，而项目的 check_data.js 明确会拦截这些未经复核的标记。","severity":"high","suggestion":"明确区分“已落盘但未复核”和“已完成”：让带未复核标记的 resolved 候选仍返回非 0（可新增退出码），或把 0 的文案改为“候选均已落盘”，并在调用方契约中强制经过复核门槛。"},{"category":"edge-case","location":"tools/validation/assessment_contract.js：validateAssessment、checkConsolidation","problem":"stage/annual/不支持的 monthly 分支仍调用 `ctx()` 和 `checkConsolidation()`；当 `DAYS` 条目缺少 steps，或 consolidation 为真但 FIRST_TEACH_DAY 缺失时会抛 TypeError，而不是返回固定的“不支持/非法”错误。META 只检查 typeof object，数组也可能被当作合法 META。","severity":"medium","suggestion":"对 DAYS/day.steps/step.blocks/FIRST_TEACH_DAY 做形状防御；不支持路由应保证至少返回确定错误且不因其余字段形状崩溃；同时要求 META 为非数组对象。"},{"category":"readability","location":"tests/unit/test_render_smoke.js：G4 最终反馈断言","problem":"`!body.innerHTML.includes('g4__rack') || body.innerHTML.includes('g1__hint')` 几乎是宽松的存在性判断：空 HTML、错误页面或任意不含 g4__rack 的内容都可能通过，不能证明“填满后触发校验反馈”。","severity":"medium","suggestion":"断言具体反馈容器/文案及当前 slots 状态，并同时断言 body 非空；避免使用与 G4 无关的 `g1__hint` 作为替代条件。"},{"category":"edge-case","location":"tools/validation/assessment_contract.js：validateWeekly 的泄漏检测","problem":"当前只按归一化后的完整 token 精确匹配，并按字典键精确排除；`pit` 的复数/时态、大小写变体可按策略处理，但 `pits`、`pitfall` 等关联词不会被发现，而简单子串匹配又会误报 `spit`、`span` 等词。规范没有在代码或测试中明确这个边界。","severity":"medium","suggestion":"书面确定泄漏判据（仅词元、允许的形态变体、是否排除复合词），实现对应规范化/词形规则，并补充正反例；不要直接改成无界子串匹配。"},{"category":"other","location":"tools/legacy/port.py：第一周到第二周的一次性迁移替换","problem":"脚本仍硬编码旧的 `spit` RESERVED 行，当前 week01 已改为 `pit`，按注释所述重新运行会直接触发 sub() 断言失败。这是换词后遗留的第五层硬编码，虽是一次性工具，仍会造成不可用。","severity":"medium","suggestion":"更新迁移脚本输入字面量，或明确将其标记为不可重放并加入测试/文档，避免后续把它误当成可执行迁移工具。"},{"category":"other","location":"第 5 步基线与媒体变更记录、tests/unit/test_migration_diff.js","problem":"“媒体唯一例外”主要靠注释和总数更新表达；从提供材料看不到按原因分类或逐文件/逐键的机器断言，未来可能把引擎改造导致的媒体漂移与 pit 数据决策混在一起。音频源文件又被截断，无法独立核实删除集合是否完整。","severity":"low","suggestion":"建立带原因字段的媒体变更白名单（数据决策/引擎改造分组），对允许删除的键和其余媒体清单做精确守恒断言；补充可读的 manifest 或完整测试夹具。"}],"mode":"advice-only","notes_for_claude_code":"本审查仅基于提供的 diff 与上下文，未读取未列出的完整媒体清单、快照或其他文档；这些文件需要实际读取后才能确认 pit 音频及第五层引用是否完全清理。W1/W2/W3 unknown 的钉住断言本身可接受，但第 7 步落定时必须同步更新状态断言，最好把待办登记与结果断言分离以降低维护摩擦。","recommendations":["先修正 G5 交互测试和 gen_segments 退出码语义，这两项会造成直接的错误放行或虚假覆盖。","为 assessment 路由增加畸形输入测试，保证“不支持/非法”分支不会因数据形状崩溃。","将媒体例外与换词影响面整理成可机器验证的权威清单，并检查 legacy/迁移脚本、快照和文档引用。"],"risks":["当前测试套件可能在 G5 交互未覆盖的情况下报告全绿。","`--write-heuristic` 返回 0 可能让 CI 或人工误认为候选已完成复核。","泄漏规则边界未定义，后续加入形态变化或复合词时可能出现漏报或误报。"],"summary":"路由与 ASCII/表级 schema 等主体修法方向基本正确，但仍有一项高风险测试假覆盖（G5）和一项退出码语义矛盾，另有畸形输入崩溃与换词遗留脚本问题。修复这些问题并补齐媒体/快照清单后，才适合作为第 6、7 步的稳固地基。","task_type":"code-review"}