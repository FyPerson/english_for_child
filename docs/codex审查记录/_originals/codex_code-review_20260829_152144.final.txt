{"task_type":"code-review","mode":"advice-only","summary":"静态复审未发现高严重度问题，E1、E3–E7 的目标路径基本闭环，满足“0 high”收官门禁。仍有 2 个中等边界问题：G2 部分视图切换仅清空取消句柄而未真正取消长按，以及 elapsed 舍入后可能写入非法且不可超越的 0 秒纪录。","issues":[{"severity":"medium","category":"edge-case","location":"week01-v3.html：initG2() 的 renderBlocks、renderConfirm、renderConfirmed、renderDone 及导航切换","problem":"这些路径多处直接执行 root._g2Cancel = null，却没有先调用现有取消函数。常规单鼠标操作通常会由 mouseleave/mouseup 取消，但在多点触控或并发输入下，旧长按计时仍可能到期。尤其最后一词进入“完成”视图时 cur 未变化，myIdx 守卫仍会通过，迟到回调会写入确认并用 renderConfirmed() 覆盖完成视图。","suggestion":"增加统一的 cancelG2LongPress()，先调用 root._g2Cancel() 再置 null；在所有替换 body.innerHTML、切词、完成和重新开始路径开头调用。索引守卫继续保留，作为第二道保护。"},{"severity":"medium","category":"missing-constraint","location":"week01-v3.html：initFlash() / lockRecord() 的 elapsed 舍入逻辑","problem":"stopSounds() 只在舍入前检查 elapsed > 0，lockRecord() 随后按 0.1 秒舍入；小于 0.05 秒的值会变成 0，并被写成 elapsed 最佳纪录。这违反规格中 elapsed 必须大于 0 的清洗约束，而且“小优”比较下 0 秒将无法被正常成绩打破。","suggestion":"在舍入后再次校验 value > 0；更适合当前显示口径的实现是将任意正耗时规范为至少 0.1 秒，例如 Math.max(0.1, Math.round(value * 10) / 10)，并确保比较与写入均使用该规范值。"}],"recommendations":["E1 的折叠终止路径可保留：_flashReset 会停止 interval 和 30 秒自动停止计时器，不调用 lockRecord，重新开始时又会将索引和计数复位。","E3 的三条路径在当前实现中闭环：覆盖层不接收指针事件，捕获监听在点击时统一清理，自动结束和切页也会经过 dismissCelebration；旧的两秒回调受元素身份判断保护。","修正上述两项后，重点补测“最后一词长按未结束时进入完成视图”和“开始计时后立即停止”两个边界场景。"],"risks":["G2 迟到长按回调可能产生非预期确认并把完成视图切回已确认视图。","极短计时可能生成 0 秒永久最佳纪录，只能依赖状态清洗或手工清除恢复。"],"confidence":"high","notes_for_claude_code":"本结论仅基于所提供累计 diff、v1.3 规格及任务中转述的 Playwright 结果进行静态复审；未执行命令或测试。门禁结论为 0 high，可进入 T5，但建议在收官前一并修复这两个 medium。"}