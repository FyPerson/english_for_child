{"task_type":"spec-critique","mode":"advice-only","summary":"窄审结论：S-1 的自我取消矛盾已在契约层面消解，S-3～S-5 均已闭环；当前未发现高严重度问题，可按“0 high”门禁放行 P0 编码。§2.1 与 §2.3 仍有两处中等强度的实现边界建议在编码前冻结，以避免音频阶段竞态和二次重试失效。","issues":[{"severity":"medium","category":"edge-case","location":"§2.1 请求生命周期步骤③～④","problem":"内嵌音频失败或8秒超时后切换到 TTS 时，规格只规定沿用同一 ownerToken，没有明确停止旧 Audio、清除其定时器和注销旧阶段事件。由于两个阶段共用 token，旧内嵌阶段迟到的 ended/error 回调仍可能通过 token 校验，造成 Promise 在 TTS 真正结束前 resolve、G1/G3 提前开启作答窗，或两个物理音源短暂重叠。","suggestion":"在步骤④补充阶段级失效规则：转入 fallback 前必须暂停并归零旧 Audio、移除或失效其监听器、清除该阶段超时器；请求内部增加 phaseId 或 phase 枚举，所有回调同时校验 ownerToken 与当前 phase，只有当前阶段可完成请求。"},{"severity":"medium","category":"ambiguity","location":"§2.3 showAudioFailure 与播放阶段失败转移表","problem":"“先原子消费 continuation，再执行 retry”解决了重试清掉自身 continuation，但未明确 retry 再次返回 failed 时必须重新注册同阶段 continuation。若实现仅消费后调用一次 play，第二次失败会没有提示条，和 G1/G3“可无限重试”及各阶段重试语义冲突。","suggestion":"冻结 retry 为完整阶段处理函数，而不是裸 play 调用：每次执行后，ended 走该阶段成功续接，failed 重新调用 showAudioFailure 注册同阶段 retry，cancelled/blocked 静默结束。可抽成 runAudioStage(playFn, onEnded)，统一覆盖普通词组、G2 两阶段及 G1/G3/G4。"}],"recommendations":["按门禁规则放行 P0 编码；上述两项均不构成 high 阻断，但宜在实现前直接补入状态机契约。","为四条 S-1 核心路径各写一个确定性状态机用例：内嵌失败后 TTS 成功、TTS 也失败、fallback 中被新请求取消、同阶段连续失败两次后再次重试成功。","实现时将“请求所有权”和“物理阶段所有权”分开：ownerToken 管外部取消，phaseId 管同一请求内 embedded→fallback 的迟到回调。","S-3 的2槽、S-4 的第18张立即停表及 S-5 的 AudioBus 命名无需再改规格。"],"risks":["若只校验 ownerToken，不校验物理阶段，迟到的内嵌音频回调可能提前推进游戏状态。","若 retry 不是可重新武装的完整处理函数，连续音频失败时第二次提示会消失，题目可能停在锁答状态。"],"confidence":"high","notes_for_claude_code":"本结论严格限定于 S-1～S-5 落点及其直接冲突，没有重新审查范围外章节。附件仅含 v2 节选而非 v3 实现，因此结论是规格层审查，不代表已验证实际代码行为；Claude Code 仍负责最终取舍与执行。"}