{"task_type":"code-review","mode":"advice-only","summary":"S2 的返回码处理、差集断言和幂等注入，以及 S3 的 roundToken 三分支防护与跨卡互斥，整体实现正确。但折叠 G1 时没有终止 AudioBus 和全局失败续接，会产生隐藏播放及旧重试复活，当前不满足“0 high”放行 S4 的门禁。","issues":[{"severity":"high","category":"edge-case","location":"week01-v3.html / toggleStep()、initG1().reset()","problem":"折叠含 G1 的 step 时，_g1Reset 只递增 roundToken、清本地计时器并渲染 idle，没有调用 AudioBus.stopAll。若此时正在播题，音频会在隐藏后继续播放；若处于 audioFailed，顶部失败条及其 continuation 仍然有效，折叠后点击它会重新调用 playQuestion，令已重置的隐藏卡片复活。roundToken 无法拦截这种 continuation，因为它会创建一个新的 token。","suggestion":"把折叠定义为 G1 的完整退出：在重置卡片前对该 step 调用一次 AudioBus.stopAll，再逐卡执行 _g1Reset，确保当前音频、TTS、失败条和 continuation 一并失效。避免每张卡重复 stopAll，可由 toggleStep 收集卡片后统一调用一次。重点回归“playing 时折叠”和“audioFailed 时折叠后点击旧失败条”两条路径。"},{"severity":"medium","category":"edge-case","location":"week01-v3.html / localStorage 读取与 cleanGamesState()","problem":"JSON.parse 得到数字、字符串等真值原始类型时，代码随后访问或赋值 state.days/state.games；在当前非严格模式下赋值可能静默失败，cleanGamesState 最终会在 g.grab 处因 g 为 undefined 而中断整页初始化。字段级清洗没有覆盖顶层 state 的类型破损。","suggestion":"将 isPlainObject 提前到读取逻辑之前；解析后先执行 `if (!isPlainObject(state)) state = {days:{}}`，再保证 days 至少是可持久化的普通对象，最后运行 games 清洗。增加原始值、null、数组和损坏 JSON 四类启动用例。"},{"severity":"medium","category":"missing-constraint","location":"tools/build_audio.py / check1_uniqueness()、process_one()","problem":"文件名唯一性使用大小写敏感的 set，在目标 Windows 文件系统上无法识别 A.mp3 与 a.mp3 指向同一文件；同时没有约束 file 必须是纯文件名。带目录或 `..` 的值可能让 out_path.unlink 删除 PROCESSED_DIR 之外的文件，甚至破坏声明为只读的 raw 输入。","suggestion":"加载 manifest 时断言 file 非绝对路径、无盘符、无父目录且 `Path(file).name == file`；验证 RAW_DIR/out_path 解析后的路径仍位于各自目录内。文件冲突检查使用 Windows 语义的 normcase 或 casefold 后集合，并在任何 unlink 或 ffmpeg 调用前完成这些校验。"},{"severity":"medium","category":"edge-case","location":"tools/build_audio.py / run_ffmpeg()","problem":"subprocess.run 没有超时。56 条素材需要大量 ffmpeg 子进程，单个异常文件或子进程卡死会使构建无限等待，无法按“失败即非零退出”收场。-nostdin 只能阻止等待输入，不能覆盖解码器或进程挂起。","suggestion":"为测量、处理和复测设置合理的单条超时，捕获 TimeoutExpired 并转换为明确失败结果；处理阶段超时后继续保持该条输出已删除，最终由 check6 阻断注入。"},{"severity":"medium","category":"edge-case","location":"tools/build_audio.py / EXTRACTION_REGISTRY、SOURCE_SPECS","problem":"冻结锚点数和键数能防止多数正则退化，但部分提取器没有绑定语义容器：例如 BOOK_PAGES_BLOCK_RE 可匹配任意 pages 数组，DEMO_BLOCK_RE 也未限定 SOUNDS。若目标结构失配而别处恰好出现同形同数量数据，计数、manifest 子集和孤儿差集仍可能共同假绿。","suggestion":"先唯一定位 `const BOOK`、`const SOUNDS`、`const DAYS` 等顶层常量的边界，再只在对应块内提取字段；分别断言容器锚点恰好一个。继续保留现有数量和差集断言作为第二层防线。"}],"recommendations":["先修复 G1 折叠时的 AudioBus 与失败 continuation 清理，再决定 S4 放行。","针对 G1 补充跨卡连点、播放中折叠、作答窗折叠、失败后折叠及重开轮次的定向验收。","在 manifest 校验阶段集中完成路径、大小写冲突和结构约束，之后才创建或删除 processed 文件。"],"risks":["失败场景下旧 continuation 可在隐藏界面重新启动 G1，并产生用户看不到的音频和状态变化。","Windows 大小写不敏感和未约束路径可能造成输出覆盖或误删目录外文件。","损坏但可解析的 localStorage 值可能让整页启动中断。"],"confidence":"high","notes_for_claude_code":"本结论仅基于所给 build_audio.py 全文、累计 diff 和规格静态审查，未执行命令或测试。更深复核还需读取未省略的 week01-v3.html 中 WordAudio.play、AudioBus、showAudioFailure/hideAudioFailure、laterOnce、clearPageTimers 的完整实现，以及 tools/audio_manifest.json 的实际文件名与分组。预筛修复中，ffmpeg 返回码处理、unreferenced 精确差集、标记对幂等替换、roundToken 回调前统一校验和开新轮时的跨卡 reset 均未发现新的高等级问题。"}