{"confidence":"high","issues":[{"category":"other","location":"仓库根目录；.gitignore/.gitattributes 与提交文件列表","problem":"材料中的代码 diff 未显示删除或移出 Git 的 `course.html`、`week01.html` 至 `week04.html`。文档却声明根目录不再有 HTML，若这些文件仍被跟踪或留在磁盘，将继续造成旧产物与 `build/` 产物并存。","severity":"high","suggestion":"确认五个根产物已从 Git 索引删除并从工作树移除；补充删除 diff，并增加目录布局测试断言根目录不存在这些文件。"},{"category":"other","location":"tools/build_lessons.py：build()","problem":"实现按文件直接写入 `build/`，没有按方案要求先在同文件系统临时目录完成全部产物，再整体原子替换目录；也不会清理旧目录中的残留文件。写入中断可能留下半套产物，陈旧文件会继续存在。","severity":"high","suggestion":"构建到 `build` 同级临时目录，完成全部校验后将旧 `build` 改名、把临时目录原子改名为 `build`，失败时恢复旧目录并清理临时目录；补充陈旧文件和写入失败测试。"},{"category":"edge-case","location":"tools/build_lessons.py：build(week=...)；tools/project.py：release/serve","problem":"`--week` 只写指定周、课程和入口，却不清理其他周。随后 `package()` 会把 `build/` 中仍存在的旧周一起打包，可能形成源码新旧混合的发布目录；`reproducible()` 只比较当前 `project.json` 列出的文件，旧文件名仍在清单时未必能发现。","severity":"high","suggestion":"要么删除单周构建接口，要么规定其只能输出到显式临时目录；任何面向发布的构建必须生成完整目录并清理旧产物，且 package 前检查目录内容与完整白名单完全一致。"},{"category":"contradiction","location":"tools/project.py：main() 的 release 分支","problem":"`release` 先显式运行一次 `build_lessons.py`，随后 `run_checks.py` 又执行默认 build 和临时目录重建，实际在 package 前完成三次构建；方案要求 release 由 check 通过后直接 package `build/`，不再额外构建。","severity":"high","suggestion":"release 分支直接运行 `run_checks.py`，由 check 的第①步负责写 `build/`，通过后立即调用 `package()`；serve 若需要单独 build 可保留。"},{"category":"missing-constraint","location":"tools/project.py：package()；tests/unit/test_build.py：test_release_is_reproducible_allowlisted_and_hashed","problem":"package 只检查必需文件是否存在，静默忽略 `build/` 中的其他文件；现有测试还明确把 `build/secret.txt` 视为可接受。方案要求 package 只接受白名单，其他文件存在即失败，这会让直接 package/serve 路径绕过残留文件守卫。","severity":"medium","suggestion":"在 package 开始时递归枚举 `build/`，将允许的顶层文件（以及后续 `media/` 白名单）与实际文件集合精确比较；多余文件立即失败，并把测试改为断言失败。"},{"category":"readability","location":"docs/工程与交付规范_20260905_v1.0.md；tools/build_lessons.py","problem":"文档仍写 `tools/build_lessons.py` 支持 `--check` 且该参数“只比较、不写出”，但代码已删除 `--check` 参数。接手者按文档执行会得到参数错误。","severity":"medium","suggestion":"删除该旧参数描述，统一改为 `python tools/project.py check` 或 `build_lessons.py --output-dir` 的现行用法，并全仓搜索 `build_lessons.py --check`。"},{"category":"other","location":"docs/声音积木六年课程方案_20260902_v1.3.md §9.1；docs/项目记忆/周课件生产线.md","problem":"方案 §6 要求第0期同步六年课程方案 §9.1 与项目记忆中的流程和路径，但材料2未显示这两个文件的对应修改；现有文档可能继续描述旧的根目录产物流程。","severity":"medium","suggestion":"逐文件核对并补充 `→ build`、`build/` 路径和 clone 后先构建的说明；若确实无需修改，也应在提交说明或验收证据中明确核对结果。"},{"category":"security","location":"tools/verify_manifest.py：verify()","problem":"路径校验没有建立严格的规范化 POSIX 路径和大小写唯一性约束。Windows 下大小写不敏感时，`week01.html` 与 `WEEK01.HTML` 等清单键可能解析到同一文件；驱动器相对路径、符号链接也未明确拒绝，可能导致“清单覆盖全部文件”的判断与实际目录不一致。","severity":"medium","suggestion":"要求键为规范化相对 POSIX 路径，拒绝驱动器前缀、`.`/空组件、符号链接，并按平台 `casefold()` 检测重复解析路径；同时校验 digest 为 64 位十六进制字符串。"},{"category":"edge-case","location":"tools/project.py：package()","problem":"package 先在最终 `dist/soundblocks-*` 目录逐个写文件和 manifest，再调用 `verify()`。若写入或自检失败，dist 可能留下半成品目录，后续候选扫描还可能看到这个不完整版本。","severity":"low","suggestion":"先写入 dist 同级临时发布目录并完成 manifest 自检，再原子改名为最终目录；失败时删除临时目录，不让半成品进入版本候选。"}],"mode":"advice-only","notes_for_claude_code":"CI 的 artifact 路径通常会在下载后保留 `soundblocks-*` 一级目录，`find` 唯一性断言可覆盖空目录和多目录情形；tag push 时远端 tag 已存在，`--verify-tag` 与 job 级 contents: write、step 级 GH_TOKEN 的组合原则上可用。`reproducible()` 当前会捕获 build/ 根下直接的 `.tmp` 或其他多余项；主要缺口是构建目录未整体原子替换及单周构建残留。","recommendations":["优先修复根目录旧产物、整体原子构建、单周构建残留和 release 重复构建四项高严重度问题。","让 package 对 build/ 内容执行严格白名单校验，并同步修正对应单元测试。","完成 §6 所列文档逐文件核对，特别是六年课程方案 §9.1、项目记忆生产线和过期的 `--check` 用法。","补充 Windows 路径大小写、符号链接和半成品发布目录的验证测试。"],"risks":["旧根目录 HTML 若仍被跟踪，用户或部署脚本可能继续误用未由当前源码生成的版本。","非原子构建或 `--week` 后直接发布可能产生新旧周次混合的课件包。","manifest 路径别名或符号链接处理不严时，发布完整性核验可能在不同操作系统上出现不一致。"],"summary":"实现已覆盖默认 build、临时重建比较、基线读取和 CI 发布核验的大部分主流程，但仍有数项违反方案硬契约：根产物删除证据缺失、构建目录非整体原子替换、单周构建可残留旧文件，以及 release 额外构建一次。文档联动也疑似漏改，建议先处理高严重度问题后再复审。","task_type":"code-review"}