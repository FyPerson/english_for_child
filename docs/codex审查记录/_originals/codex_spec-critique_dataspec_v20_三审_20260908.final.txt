{"task_type":"spec-critique","mode":"advice-only","summary":"第三稿对复审 27 条的落实度较高，可判 23 条已修、4 条部分修、0 条完全未修，但仍不宜激活。最重要的新问题是 `instructionMode` 仍无法表达“复习旧内容并教授新技能”的混合周，且若干标为“已实现”的规则实际上只支持旧字符 schema，或会在新 `BOOK` 分支直接异常。","issues":[{"severity":"critical","category":"contradiction","location":"§4.3「周型：正交维度」及第一年对照表 W10、W20","problem":"`instructionMode` 仍是互斥枚举，却同时承担“是否教新技能”“是否为复习周”“点亮墙模型”“是否要求音素实验室”等多种职责。W20既复习43块又首次引入 Magic e 概念：填 `review` 会丢失“教新技能”语义，填 `skill` 又会丢失复习语义。W10的“综合；B段末”更接近纯复习，当前标为 `skill` 缺少课程依据。文档声称三维正交，但此维度内部仍混合了两个可同时成立的属性。","suggestion":"把该维拆为至少两个字段，例如 `teachingMode:'phoneme'|'skill'|'none'` 与 `consolidation:true|false`，并分别定义约束；或者新增明确的组合值并给出完整矩阵。按现有课程表，W10应倾向 `teachingMode:'none', consolidation:true`，W20应为 `teachingMode:'skill', consolidation:true`。随后重新定义点亮墙、首教日和每日块要求，避免继续由单一枚举推导。"},{"severity":"high","category":"contradiction","location":"§3「点亮墙与首教日：唯一模型」","problem":"新模型消除了 `FIRST_TEACH_DAY` 的直接矛盾，却造成墙内容随周型跳变：`phoneme` 周只显示本周新字位，紧接着的 `skill/review` 周突然显示全部累计字位，再到下一个 `phoneme` 周又缩回两个新字位。这与“点亮墙”“展示已掌握积木”以及累计学习体验不一致，也未说明渲染器是否把 `wallLetters` 当“本周新增”还是“墙上全部可见项”。","suggestion":"先固定字段语义。若它表示墙上完整集合，则所有周都应等于 `Object.keys(SOUNDS)`，另用 `newPatterns` 标本周点亮项；若它只表示本周动画新增项，则建议改名为 `wallNewPatterns`，并让墙的累计集合由 `SOUNDS` 推导。不要让同一字段按周型改变集合语义。"},{"severity":"high","category":"missing-constraint","location":"§4.1 状态表：DATA-RACK-01、DATA-PAIR-01、DATA-RESERVED-02","problem":"这三条仍标“已实现”，但第三稿已把契约改成字位 ID 与统一分词。现有 `check_data.js` 中 `rack.split('')` 对数组会直接报错，G3 与周检长度仍使用字符串长度或逐字符比较。因此它们在第三稿规定的合法输入上并未实现，而不只是“以后优化”。","suggestion":"在里程碑2落地前，将三条状态降为“部分实现”，明确“仅支持旧单字符 schema，新 ID 数组输入不符合当前实现”。统一分词迁移和对应正反 fixture 合入后再改为“已实现”。"},{"severity":"high","category":"edge-case","location":"§3「BOOK 的三种结构」与 check_data.js 引用完整性、visibleText","problem":"规范新增 `reread` 和 `none` 后，当前校验器仍无条件执行 `for (const p of BOOK.pages)` 和 `BOOK.pages.forEach(...)`。`BOOK=null` 会触发空值异常，`reread` 无 `pages` 也会异常；因此合法 schema 分支无法得到规则化失败信息，甚至无法继续其他检查。","suggestion":"把 BOOK 访问全部按 `bookMode` 分支，并优先做结构校验：`new` 才遍历 pages，`reread` 校验来源字段并解析引用，`none` 只接受 null。将这一缺口同时记入 `DATA-BOOK-01` 的实现范围和三分支 fixture。"},{"severity":"high","category":"missing-constraint","location":"§4.1 DATA-RESERVED-01 状态及 check_data.js usedWords/visibleText","problem":"`DATA-RESERVED-01` 标为“已实现”过强。当前 `usedWords` 不收集 `wordforge`；`visibleText()` 也没有遍历块的全部字段，只枚举 `html/note/head/rows/items/words/pairs`，因此保留词可通过 `wordforge.families` 等合法字段泄漏而不报错。新 BOOK 分支还会导致扫描异常。","suggestion":"将状态降为“部分实现”，或建立统一的结构化曝光词抽取器，覆盖每种合法块及 BOOK 分支；至少增加保留词出现在 `wordforge`、`initialpick`、`sentences`、BOOK、G1 文案中的失败 fixture。"},{"severity":"high","category":"ambiguity","location":"§4.1 PED-NOVELTY-01「两套词集」","problem":"“历史曝光词”宣称覆盖全部孩子可见或操作过的词，但枚举遗漏了 `lead/list/note/table` 中的英文例词、SOUNDS 的 demo/cue 等可见字段、WALL_HINT、G1_THEME 文案等位置。这与 DATA-RESERVED-01 对可见文本的广泛口径不一致，会把孩子在说明或示范中看过的词误判为陌生词。","suggestion":"定义一个共享的“孩子可见词”抽取层，再由规则按用途筛选。历史曝光至少应包含全部渲染给孩子的结构化英文；若某些家长文案明确不算曝光，应逐字段列入排除表，而不是依赖不完整的包含清单。"},{"severity":"high","category":"ambiguity","location":"§4.1 PED-NOVELTY-01/02「内容词」与归一化口径","problem":"`sentences` 和测评短文只统计“内容词”，但规范没有给出可执行的内容词判定：哪些功能词排除、认读词是否排除、缩写和连字符如何处理均不明确。仅有词形归一化规则不足以让 fixture 和不同实现得到相同结果。","suggestion":"提供机器可读的功能词/认读词排除来源及 token 化规则，明确撇号、连字符、大小写和非字母字符的处理；最好让抽取器输出可审计的词种及排除原因。"},{"severity":"medium","category":"contradiction","location":"§3 FIRST_TEACH_DAY 与 §2「新字位落到 FIRST_TEACH_DAY」","problem":"§2表述“新字位落到 FIRST_TEACH_DAY”，§3则规定 skill/review 为空，基本一致；但当未来采用“复习+新技能”组合周时没有问题，而若课程出现复习周补教字位，当前模型无法表达。更直接的问题是 `phoneme` 周 `wallLetters` 仅等于新字位，与“累计字位数”和点亮墙通常含义未建立关系。","suggestion":"在解决周型拆分时明确：`FIRST_TEACH_DAY` 只记录本周首次教授的字位，历史首教日不要求在本周文件重复；墙的累计集合单独推导，避免两个概念再次耦合。"},{"severity":"medium","category":"missing-constraint","location":"§4.1 PED-NOVELTY-01「独立文件模式 history manifest」","problem":"虽补了哈希与版本字段，但仍未定义信任根：如果数据文件和 manifest 都由同一执行方提供，攻击者可同时改内容与重算普通哈希，所谓“篡改失败”无法成立。","suggestion":"明确 manifest 必须由主机从受信任提交生成，并由校验器通过仓库固定公钥签名、HMAC或主机提供的不可编辑基线验证；至少定义哈希算法、规范化序列化、覆盖文件集合及版本不匹配行为。"},{"severity":"medium","category":"contradiction","location":"§4.3「每天四类块」与块类型表","problem":"“输出”类别只包括 pair、initialpick、g1/g4/g5、wordforge；现有 `assessment_contract.js` 却检查不存在于合法表中的 `output` 块。第三稿正确描述了目标契约，但未在状态表中给“每天四类块”独立规则 ID 或实现状态，§8又要求每个规则 ID映射代码与fixture，导致该硬约束无法进入总体验收映射。","suggestion":"为四类块约束新增独立规则 ID（如 `DATA-DAY-CATEGORIES-01`）并标待实现；迁移时删除 `assessment_contract.js` 对虚构 `output` 类型的依赖，按块类型类别映射逐日检查 D1–D6。"},{"severity":"medium","category":"ambiguity","location":"§4.1 DATA-GRADIENT-01","problem":"“新字位当天不启用本周新词形”没有定义“本周新词形”的机器来源。课程方案的技能重点是自然语言，`META.noveltyPolicy` 只描述陌生词数量，无法据此判断 rain、tail 等属于哪个新词形。","suggestion":"在课程配置或 META 中增加结构化技能/词形标识及适用词集合或判定规则，例如 `newWordPatterns`，并定义仅禁止目标教学活动还是所有曝光位置；否则该规则无法可靠实现。"},{"severity":"medium","category":"ambiguity","location":"§4.1 DATA-IMAGE-01","problem":"“每个词首次出现”未明确跨周还是本周内、同一天多个块如何排序、标题/说明/SOUNDS.demo/G1等曝光是否参与首次出现判定。它与历史曝光词口径也可能冲突。","suggestion":"定义严格遍历顺序（天、步骤、块、块内索引）和参与曝光的字段；明确规则检查的是本周首次教学呈现，还是孩子终身首次曝光，并复用统一曝光抽取器。"},{"severity":"medium","category":"missing-constraint","location":"§3 SOUNDS.audioKey","problem":"只要求 `audioKey` 指向现有键，没有禁止自指或别名环，也没有明确链式别名是否允许。渲染或音频解析若递归跟随可能无限循环，若只跟随一层则链式别名行为不一致。","suggestion":"规定 `audioKey` 必须直接指向不再含 `audioKey` 的主记录，禁止自指与环；增加自指、二节点环、悬空引用和合法别名 fixture。"},{"severity":"low","category":"readability","location":"§4.3 W6–W7 externalReading","problem":"课程方案写的是W6“第1+级到位”，把W6起标 true合理；但规范未说明“到位”当周是否必然有外部读物，还是表示基础设施可用。若只是能力上线，布尔值可能被误解为每周实际安排。","suggestion":"明确 `externalReading` 表示“本周课程确实安排外部可解码读物”，而不是引擎能力可用；若W6可能因资源延期没有实际读物，应由逐周配置显式取值。"}],"recommendations":["先修正周型建模，再确定 W10/W20 映射；否则点亮墙、每日块和测评 fixture 会建立在不稳定语义上。","建立统一 tokenizer、字位分词器和曝光词抽取器，供 PATTERN、RESERVED、NOVELTY、IMAGE、ASSESS 等规则共同使用，避免各规则维护不同的字段清单。","把 DATA-RACK-01、DATA-PAIR-01、DATA-RESERVED-02、DATA-RESERVED-01 按第三稿新 schema 重新降级，直到合法 ID 数组和三种 BOOK 分支均有正反 fixture。","为“每天四类块”补独立规则 ID；否则它无法满足 §8 所要求的规则到代码及 fixture 的闭环。","在激活前拍板 W6 回退机制。该决定不影响 schema 正确性，但会显著影响首次交接失败时的交付风险。"],"risks":["若直接按当前 `instructionMode` 生成 W20，执行方无论选 `skill` 还是 `review` 都会违反另一部分课程语义。","合法的 `bookMode:'reread'` 或 `bookMode:'none'` 输入会使当前校验器异常退出，而非给出可操作的契约错误。","新 ID 数组 schema 与当前 rack、最小对立和周检长度实现不兼容，却仍显示“已实现”，容易产生错误激活判断。","历史曝光范围遗漏说明、示范和主题文案后，陌生词与测评隔离指标可能出现假绿。","没有可信签名或受信任基线的 history manifest 可被连同普通哈希一起重写，无法证明历史未被篡改。"],"confidence":"high","notes_for_claude_code":"复审27条逐项结论：1 点亮墙模型＝部分修（直接矛盾消除，但字段语义随周型跳变）；2 rack ID数组＝已修；3 两套词集＝部分修（核心拆分已完成，但“全部曝光”清单仍漏可见字段）；4 W31–W40＝已修；5 phase与测评块＝已修；6 M1三规则入表＝已修；7 DATA-PATTERN-01降级＝已修；8 词形归一化＝已修；9 §8总体验收＝已修；10 assessmentMode矩阵＝已修；11 复现率显式字段＝已修；12 strategy约束＝已修；13 v1.2与dry run＝已修；14 RESERVED释义＝已修；15 W6回退＝部分修（已记录但未决策）；16 rack容量口径＝部分修（验收要求已写，上限仍待定）；17 DATA-SOUNDS-01降级＝已修；18 DATA-SIGHT-01口径与降级＝已修；19 initialpick及未知块＝已修；20 BOOK三模式＝已修（规范层面；当前校验器不兼容是新发现）；21 状态与代码同提交＝已修；22 history manifest细节＝已修（但信任根仍缺）；23 四类块fixture＝已修；24 派生物白名单＝已修；25 dependency/deadline机器字段建议＝已修；26 noveltyPolicy覆盖相关周＝已修；27 规则ID用于错误与fixture＝已修。上述判断仅基于所提供的规范和代码文本静态审阅；未执行命令或验证仓库行为。更深复审仍需读取 load_data.js、normalizeWeekType、render-blocks.js、实际 week01/week03/week04 数据、里程碑0分组方案及三方分工v1.2草案。"}