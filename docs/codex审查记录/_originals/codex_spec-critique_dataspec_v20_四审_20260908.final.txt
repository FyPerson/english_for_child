{"task_type":"spec-critique","mode":"advice-only","summary":"第四稿已实质修复三审的大多数问题，关于 `output` 块的反驳成立；但仍存在 `reread` 模型无法表达 W4、同形多音无法仅靠最长匹配分词、曝光排除口径自相矛盾等高优先级缝隙，因此目前仍不适合激活。","issues":[{"severity":"high","category":"contradiction","location":"§3「BOOK 的三种结构」、§4.3 W4、课程方案 §5.3 W4、render-blocks.js 的 book 分支","problem":"W4 要“复读三本”，但 `bookMode:'reread'` 的 `BOOK` 结构只能引用一本书；同时渲染器的 `book` 分支直接访问 `BOOK.pages`，并没有解析 `sourceWeek/sourceBookId`。第四稿只要求校验器按分支处理，尚未定义渲染端如何取得复读正文，也未规定 reread 周应挂载几个 `book` 块。","suggestion":"将 reread 结构改成可表达多个来源，例如 `BOOK={sources:[{sourceWeek,sourceBookId},...]}`，并明确挂载规则、顺序、来源存在性、循环引用及渲染解析方式；或者明确 W4 不通过通用 `book` 块呈现，并给出实际块结构。为单本、多本、悬空来源、循环来源分别建立契约用例。"},{"severity":"high","category":"ambiguity","location":"§3「字位与积木的对应」、DATA-PATTERN-02、DATA-PAIR-01、DATA-STRATEGY-01","problem":"“按已教字位集合最长匹配”无法区分 `oo_short` 与 `oo_long`、`th_unvoiced` 与 `th_voiced`：它们的 `grapheme` 完全相同，单凭词面和最长匹配会得到多个同长候选。规范虽然要求同形多音使用不同 ID，却没有规定逐词消歧来源，导致分词、最小对立、积木架和策略校验无法确定唯一字位序列。","suggestion":"增加受信任的逐词字位序列，例如 `W[word].segments:['b','oo_short','k']`，或建立独立发音词典；自动最长匹配只用于不存在歧义的词。校验器应在多解时失败并要求显式消歧，不能按对象键顺序任选。"},{"severity":"high","category":"contradiction","location":"§4.1 PED-NOVELTY-01「排除表」与其后「归一化口径」第 5 条","problem":"排除表声明“只有这些不算曝光”，只列测评池、`how/warn`、专名三项；后文却又规定功能词与认读词从累计 `sight` 集合排除。两处对排除项数量和语义直接冲突，实施者可能把 sight 词计入或排除，产生不同结果。","suggestion":"把排除逻辑拆成两层：曝光抽取排除项与陌生度统计排除项。认读词可以仍算“历史曝光”，但在内容词或陌生真词统计时排除；将每条规则在哪一层生效写进同一张表。不要把“功能词”与“认读词”视为同义词，需明确非 sight 的可解码功能词如何处理。"},{"severity":"high","category":"contradiction","location":"§1「SOUNDS 和 W 是累计的」与 §3「12 个常量」的 W 行","problem":"§1 要求此前各周 `W` 词条“原样保留”，而 §3 又只要求“保留仍被引用的旧词”。这会改变历史词典及陌生度计算结果，也会影响校验器究竟能否把缺失旧词视为错误。","suggestion":"选定唯一规则。若 `W` 是累计账本，应要求所有历史条目原样保留，并校验不可删除或修改；若允许裁剪，应明确历史曝光绝不能从当前 `W` 推导，并给出可删除条件。考虑到陌生度和审计需求，建议采用严格累计、不可变语义。"},{"severity":"high","category":"missing-constraint","location":"§4.3 assessmentMode 契约矩阵、课程方案 §5.3 W40","problem":"`annual` 的矩阵与 `stage` 完全相同，无法表达 W40 额外的“年度解码 20 词”和年度测评专属数据或块。按现有契约，W40 即使完全缺少年度 20 词也可能满足 `annual`。","suggestion":"为年度测评定义明确常量和挂载块，例如 `ANNUAL_DECODING` 及对应块类型或 `assessment` 模式，并规定数量、隔离、字位合法性、复测关系和出现次数；若由现有常量承载，也要明确哪一项必须为 20 词以及与其他池是否互斥。"},{"severity":"medium","category":"ambiguity","location":"§4.1「曝光词抽取器」","problem":"“遍历全部块并自动纳入新增块类型”没有定义如何区分块内的英文教学内容、枚举值、资源键、CSS 类名、字位 ID、图像键及控制字段。若递归扫描所有字符串，`mode:'family'`、`tone:'warn'`、`art` 等元数据会被误当成孩子见过的词；若仅扫描已知展示字段，则又不是真正的新增块自动纳入。","suggestion":"让每种块在 schema 中声明可见文本路径或提供统一的 `extractVisibleTokens(block)` 注册项；未知块没有抽取定义时直接失败。对 SOUNDS、G1_THEME、WALL_HINT 等非块对象同样列出可见字段，不要递归收集所有字符串。"},{"severity":"medium","category":"missing-constraint","location":"§3 META 示例、DATA-GRADIENT-01","problem":"规则依赖 `META.newWordPatterns`，但 META schema 示例未包含该字段，也未定义允许值、词形语法、空值规则以及与 `teachingMode` 的关系。","suggestion":"把 `newWordPatterns` 正式加入 META schema，并定义机器可判定的模式枚举或语法、哪些教学活动参与检查，以及没有新词形时必须为空数组。"},{"severity":"medium","category":"contradiction","location":"§4.1 DATA-PHASE-01","problem":"规则要求 `post-assessment` 严格位于当天最后一个测评块之后，但测评类别表包含 `assessment` 和 `baseline`，此处却只列 `exam/retest/probe`，遗漏测评短文和基线块。月测日若最后还有 `assessment` 或 `baseline`，所谓测后步骤仍可能排在它们之前。","suggestion":"直接复用统一的“测评类别”映射，避免在规则正文重新手列类型；明确 `baseline` 是否属于必须先完成的测评。如果不是，应从类别表中拆成独立类别。"},{"severity":"medium","category":"ambiguity","location":"§4.3「每天四类块」","problem":"`output` 只是家长指引文本，却被归为输出活动；反之 `pair/g1/g4/g5` 等是真实活动。若只放一个 `output` 文案即可满足规则，仍不能保证孩子实际进行了输出。第四稿虽记录当前实现差距，但目标契约仍允许把说明文字当活动。","suggestion":"将 `output` 改为“输出指引”而非独立满足输出类别，要求同一步骤或同一天同时存在至少一个真实输出活动块；或者为 `output` 增加结构化 `activityKind` 并验证对应活动存在。"},{"severity":"medium","category":"edge-case","location":"§3 点亮墙唯一模型、SOUNDS.audioKey","problem":"`wallLetters === Object.keys(SOUNDS)` 会把所有音频别名记录都当成独立积木，但规范没有明确 `audioKey` 条目必然也是独立字位。未来若 audioKey 被用于纯发音别名或兼容键，点亮墙会出现不应展示的积木。","suggestion":"明确 SOUNDS 每个键都必须对应可展示字位，或增加 `displayOnWall`/记录类型并让墙集合从可展示记录推导；同时规定对象键顺序是否决定墙面顺序，若顺序有教学意义应使用显式数组。"},{"severity":"low","category":"readability","location":"§4.3 assessmentMode fixture 说明","problem":"W20 fixture 仍写成“`review` + `new` + `stage` 混合”，但第四稿已经删除 `review` 枚举，容易让实现者误以为仍存在旧周型。","suggestion":"改为明确字段组合：`teachingMode:'skill', consolidation:true, bookMode:'new', assessmentMode:'stage'`。"},{"severity":"low","category":"ambiguity","location":"§2 七天节奏、§4.3 每天四类块、DATA-TIME-01","problem":"第 7 天同时被称为 `rest:true`、休息日和周检日，且仍要求 `checks` 块。`rest` 实际语义更像“不受 30 分钟约束”，不是没有课程，字段名容易诱导执行方生成空日。","suggestion":"明确 `rest:true` 的精确定义及第 7 天仍必须包含的块；后续可迁移为 `timingMode:'untimed'` 或类似字段。"}],"recommendations":["先补齐 reread 多书引用与渲染解析契约，再讨论激活；这是当前最直接的 schema 不可执行点。","把字位分词定义升级为“无歧义时自动最长匹配、同形多音必须逐词显式消歧”，并让所有相关规则共享同一字位序列。","将“可见曝光”“目标教学词”“内容词”“陌生度排除”拆成不同层级，分别列出包含和排除规则，避免一张排除表承担互相冲突的语义。","逐项建立规则到代码入口、通过用例、失败用例的映射时，额外覆盖 W4 多书复读、W13/W16 同形多音、W40 年度 20 词三个场景。"],"risks":["若按当前 reread schema 制作 W4 或类似复读周，数据无法表达多本复读，通用 book 渲染还可能访问不存在的 pages。","同形多音没有显式消歧时，分词结果可能随实现或对象顺序变化，进而使多条校验互相矛盾。","曝光与排除口径冲突会让陌生词、测评隔离和小书复现率出现假绿或假红。","年度模式缺少专属契约会使 W40 在遗漏核心年度测评数据时仍通过结构验收。"],"confidence":"high","notes_for_claude_code":"三审 14 条结论：1 周型拆分已修；2 点亮墙语义已修，但仍需明确墙顺序及 alias 资格；3 三条状态降级已修；4 BOOK 校验异常部分修，校验范围已记录但多书 reread 与渲染解析未解决；5 RESERVED-01 降级已修；6 统一曝光抽取部分修，默认递归抽取缺少字段级协议；7 token 化部分修，基础规则已补但排除表与 sight/功能词规则冲突；8 FIRST_TEACH_DAY 解耦已修；9 manifest 信任根已修；10 四类块规则 ID 已修，但 output 文案能否单独满足输出仍需调整；11 DATA-GRADIENT-01 部分修，新增字段未进入正式 META schema；12 DATA-IMAGE-01 已修；13 audioKey 环检测已修；14 externalReading 语义已修。对“删除虚构 output 依赖”的反驳成立：所给 render-blocks.js 确实存在 `output` 分支，原三审对其称谓不准确；第四稿把真实问题改述为类别映射不完整是正确方向。更深复审仍应读取实际 week01 至 week04 数据、load_data.js、项目配置、里程碑 0 方案及三方分工 v1.2，以确认累计不可变性、BOOK 来源解析和独立文件 manifest 的可执行性。"}