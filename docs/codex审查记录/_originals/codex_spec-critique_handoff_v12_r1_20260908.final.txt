{"task_type":"spec-critique","mode":"advice-only","summary":"两阶段思路本身可以解除职责归属的循环依赖，但当前 v1.2 仍存在一个阶段 B 流程阻断，以及“派生物不提交”、dry run 证据范围和固定构建计数等关键自相矛盾。建议先修正阶段 B 的骨架交接顺序与派生物定义，再将本文作为激活证据。","issues":[{"severity":"critical","category":"contradiction","location":"§3 步 1b、步 2及其“节奏”段","problem":"阶段 B 明确要求主机先完成步 1、2并推送，副机随后执行步 1b；但步 2已经把新周加入 project.json，并要求运行 build 成功，此时 weekNN.data.js 尚未由副机创建。根据 development.md，“新增一周”需要 data.js、template、七份媒体声明和 project.json 周号共同存在，因而阶段 B 的步 2验收很可能在交接前就因缺少 data.js 失败。","suggestion":"把阶段 B 拆成“2a 骨架准备但暂不加入正式 weeks/不要求全量 build”“1b 副机写 data.js”“2b 主机或副机完成登记并全量构建”，或者由主机先创建一个明确允许的最小占位 data.js，再规定副机只能整体替换其内容。若采用占位文件，需说明占位文件能否提交、由谁创建，以及副机 build 前 project.json 已具备什么状态。"},{"severity":"high","category":"contradiction","location":"§6.3“没有派生物要提交”、§3.2、§6.4","problem":"“没有派生物要提交”与文内规则直接冲突：§3.2要求 git add resources/assets/audio_processed，§6.4又明确把 resources/assets/audio_processed 定义为派生物。当前准确结论只能是“build/ 下的 HTML 等构建产物不提交”，不能泛化为没有任何派生物提交。","suggestion":"将表述改为“build/、dist/ 等构建/发布产物不提交；audio_processed 虽可重生成，但按当前仓库策略仍在白名单内提交”。同时解释为何 audio_processed 需要版本化；若实际上也应停止入库，则应从白名单和 git add 命令中同步删除。"},{"severity":"high","category":"missing-constraint","location":"§6.6；数据层交接规范 §8 第5条","problem":"五条 dry run 只能证明同步状态、环境、现有项目构建检查和构建后工作树干净，不能证明激活条件中明确要求的“只读历史周、目标源码白名单、构建产物清单”。它也没有实际创建或修改一个受控目标 data.js，因此无法证明副机对阶段 B 唯一目标文件的写入、检查和提交范围可控。","suggestion":"增加一次隔离的演练周或 fixture 演练：记录变更前基线；只改指定 data.js；验证历史周哈希不变；列出 build 前后生成文件并与允许清单比较；用机器可判定的路径检查确认 git diff 仅包含目标文件；最后恢复演练改动。若不希望在真实周文件演练，可提供专用 dry-run fixture 和验证脚本。"},{"severity":"high","category":"contradiction","location":"§3 步2、§3.3、§6.6","problem":"多处把“六个 BUILT”写成固定预期，但 project.json 当前有4个 weeks，六个输出很可能对应当前4周加两个入口；新增第5周后输出数量会增长。文档一方面删掉写死的校验通过项数，另一方面又写死构建产物数，阶段 B 每周照跑时会迅速失真。","suggestion":"改为“所有 project.json 声明的周与入口均显示 BUILT，失败0项”，或让 project.py 输出稳定的汇总条件供检查，例如“FAILED 0”。仅在 §6.6 针对当前仓库快照保留具体数量时，也应注明该数字只适用于当前 project.json，并附日期或配置基准。"},{"severity":"high","category":"security","location":"§6.3 副机路径白名单","problem":"audio_raw 和 audio_processed 以整个共享目录作为白名单，无法落实表格中的“本周新增或回炉的键”。副机提交任意其他周或跨周复用键的变更，在路径级核对中仍会被视为合法；这也削弱了并行规则对跨周音频不得并行修改的约束。","suggestion":"为提交范围增加键级校验：从本周 manifest 和待修键列表计算允许文件集合，再将 git diff --name-only 与该集合比较。若工具与目录结构允许，优先按周或 manifest 所属范围拆分音频路径；至少不要把共享目录路径检查描述成足以验证“本周键”。"},{"severity":"high","category":"edge-case","location":"§6.3 主机核对命令","problem":"BASE=$(git log -1 --format=%h --grep=Claude-Session) 依赖提交信息必须包含 Claude-Session，但本文没有把该标记规定为主机提交的强制格式。找不到匹配时 BASE 为空；历史中存在较旧匹配时又会把过多提交纳入检查。该命令还是 Bash 语法，而文档适用于以 Windows 为主的环境，却未声明应在 Git Bash 中执行。","suggestion":"在主机推送交接时显式记录基准 SHA，并把它作为工作包字段交给副机/主机验收，例如 git diff --name-only <base-sha>..HEAD。补充 PowerShell 与 Bash 两种写法，或明确所有代码块要求在哪种 shell 中运行。不要通过非强制提交信息猜基准。"},{"severity":"medium","category":"contradiction","location":"文档 front matter、文件名和一级标题","problem":"文件名与修订记录均为 v1.2，但 front matter 的状态仍写 v1.1，一级标题也仍是“三方分工与交接规范 v1.1”。这会使引用方无法判断当前权威版本，并可能导致数据层规范 §8第5条无法被严格认定为“已升 v1.2”。","suggestion":"将 front matter 状态和一级标题统一改为 v1.2；确认所有反向引用使用同一文件名和版本号。"},{"severity":"medium","category":"readability","location":"§2 一致性铁律清单","problem":"编号10重复：体量护栏是10，数据层规范是11，随后“素材可以分批到位”又编号10。后续章节引用“铁律10”时虽然目前意指体量护栏，但重复编号会造成审查、issue和自动提取歧义。","suggestion":"把最后一条改为12，并全文检查所有按编号引用。"},{"severity":"medium","category":"contradiction","location":"§4.1 配图包开场提示词、§3.1","problem":"开场提示词说“只能碰 resources/assets/weekNN/”，但配图工作开始前按§3.1必须运行 build，工具虽预期只写被忽略的 build/，字面上仍然触碰了该目录之外的文件系统路径。更重要的是，这个提示词若与阶段 B 数据层工作合并使用，会错误禁止修改 data.js。","suggestion":"将“只能碰”限定为“可提交的版本化文件只能是 resources/assets/weekNN/**；允许工具生成被忽略的 build/**”。并明确配图、调音、数据层必须使用各自独立工作包提示词，不能把配图提示词复用于阶段 B 综合任务。"},{"severity":"medium","category":"edge-case","location":"§3.1、§3.2、§3.3 的同步与推送命令","problem":"命令先提交、再 fetch 检查 behind；若发现落后再执行 git pull --no-rebase，会生成合并提交，且即使没有文本冲突，也可能把远端对白名单外文件的变化混入本地历史。文档没有要求 pull 后重跑对应 dry-run、自检或 check，再直接 push 会让先前验收结果失效。","suggestion":"优先在生成和提交前执行 git fetch并确认不落后；若提交后发现远端推进，pull成功后必须重新运行该任务的全部验收并重新检查 diff 范围，再允许 push。将这一规则写入三个命令清单，而不只写“无冲突再 push”。"},{"severity":"medium","category":"ambiguity","location":"§0、§3.3、§6.6","problem":"§0说 doctor 验出 Node 可用后限制自动解除，但§6.6把 Chromium也列为 PASS environment 的必要条件；阶段 B的数据层 build/check --quick 究竟最低需要 Node，还是同时必须安装 Chromium并不清楚。development.md又说明 Playwright用于浏览器测试，普通构建仅依赖Python标准库与Node。","suggestion":"分开定义阶段 B 最低环境门槛与完整回归门槛：例如数据层编写需 Python最低版本和 Node最低版本，check --quick所需依赖单列；Chromium若仅供完整回归，则不应成为副机数据层交接的阻断条件，除非明确说明 quick检查确实调用它。"},{"severity":"medium","category":"missing-constraint","location":"§2 铁律10、§3 步8","problem":"把体量护栏列为“三方共同遵守”可以提醒副机，但副机只能改素材和参数，无法调整预算、模板或长期媒体架构；文档没有规定副机在 build/check 命中体量上限时必须停止，也没有规定由谁决定压缩、删图、拆包或提升预算。","suggestion":"保留三方可见性，但把责任拆成“副机负责发现后停止并上报；Claude负责归因和工程处理；用户批准可能影响画质或交付形态的取舍”。在§3.1、§3.2、§3.3加入触线即不得提交的明确处理分支。"},{"severity":"low","category":"other","location":"§3 步3及§3.2","problem":"音频清单统一写成 audio_manifest_wN.json，但提供的仓库事实显示首周使用 audio_manifest.json，第二、三周才使用带周号命名。虽然本文适用于第三周以后，仍应明确 wN命名是否是未来周的强制规则，避免执行方凭现有文件推断错误。","suggestion":"补一句“第三周及以后新建清单统一命名为 audio_manifest_wN.json；首周 audio_manifest.json 为历史例外”，或指向 manifest 命名的权威规则。"}],"recommendations":["先修复阶段 B 的依赖顺序，使主机交出的骨架在没有最终 data.js 时也有明确定义且可验证的状态。","统一术语：区分“被忽略的构建产物”“可重生成但仍版本化的处理素材”“不可重生成的源素材”，不要统称为派生物。","把白名单验收从目录级提升到文件或音频键级，并使用显式交接基准 SHA。","将命令预期改为稳定条件，避免写死随周数增长的 BUILT 数量；所有 pull 后必须重新验收。","基础假设1：成立，但仅在修复阶段 B 的骨架/data.js时序后才真正可执行。","基础假设2：证据不足。路径类别基本覆盖，但共享音频目录过宽，且无法证明“仅本周键”；更深审查需读取音频工具的实际输出路径与 git 跟踪状态。","基础假设3：不成立。build/ HTML不提交成立，但 audio_processed 在本文中既被定义为派生物又要求提交。","基础假设4：不成立。按当前顺序，主机把新周加入 project.json并验收 build 时，副机尚未创建 data.js。","基础假设5：不成立。现有五条没有覆盖激活条件要求的历史周只读、目标源码白名单和构建产物清单。","基础假设6：证据不足。参数形式已有自查说明，但固定“六个 BUILT”、Shell语法、pull后不重验等使“每周照抄执行”不成立；需读取 project.py、doctor、构建输出及目标平台Shell约定才能完整确认。","基础假设7：成立一部分。护栏应让三方可见，但执行责任与触线处置应主要归Claude，副机职责应明确为停止、保留证据并上报。"],"risks":["阶段 B首次启用时可能在主机骨架构建阶段直接失败，导致交接无法开始。","“没有派生物提交”的错误表述可能诱发误删或漏提 audio_processed，造成跨机结果不一致。","目录级音频白名单可能让其他周或共享键被意外覆盖而未被验收发现。","基准提交推断失败可能漏检副机越权改动，或把无关历史提交误判为副机改动。","固定构建数量和pull后不重验会产生错误停止或未经重新验证的推送。"],"confidence":"high","notes_for_claude_code":"本审查仅依据提供的文档、project.json和任务说明进行文本一致性评审，未读取或执行工具源码。若要确认所有命令与输出，应进一步读取 tools/project.py、tools/embed_assets.py、tools/gen_audio.py、tools/build_audio.py、tools/capture_lesson_media.js、.gitignore、project构建清单实现及相关验证脚本。优先处理两个阻断项：阶段 B 的步2/步1b时序，以及 audio_processed 与“没有派生物要提交”的矛盾。"}