{"confidence":"high","issues":[{"category":"edge-case","location":"scratchpad/inject_w4_cards.py：card() 与 main() 写入阶段","problem":"[1] 部分修。五个卡片在全部键校验完成前就分别写入 `GEN/*_192.png`；媒体文件和 data 文件也通过两个连续的 `replace` 写入，第二次替换失败时仍可能留下半完成状态。因此注释中的“原子”不成立。","severity":"medium","suggestion":"把规格化 PNG 也留在内存或临时目录，全部键、媒体正文和 data 正文校验成功后再统一提交；至少在失败时清理临时成品，并明确这是“尽量避免半成品”而非跨文件事务原子操作。"},{"category":"edge-case","location":"tools/theme_palette.py：oklch_to_srgb()、oklch_L_for_lab_L()、week()","problem":"[2][3] 的常规色相已修，但极端 chroma 未受约束。`oklch_to_srgb()` 会逐通道裁剪到 sRGB 范围后再计算 CIELAB L*，二分函数始终返回结果，却不报告目标是否只能靠裁剪近似；极端 chroma 可能导致不可预测的 L*、黑白通道饱和或验证结果失真。CLI 也未拒绝非有限值或异常 chroma。","severity":"medium","suggestion":"对 hue 做有限值与归一化，对 chroma 设明确范围并拒绝 NaN/无穷；在裁剪前检测是否越出 sRGB，若越界则降 chroma 或直接报错。二分结束后重新计算最终 sRGB 的 L*，并对目标不可达情况显式失败。补充多组 chroma（含高、低及边界值）的测试。"},{"category":"missing-constraint","location":"tools/theme_palette.py：SEMANTIC_SOFT 与 validate()","problem":"[3] 的阈值断言已加入，但语义软色底列表是手工硬编码，未与模板中的实际选择器和令牌引用建立契约。它可能检查了当前版本不会使用的组合，也可能漏掉未来模板实际将 `ink` 或 `ink-2` 放置的背景；语义色一旦在模板中漂移，验证仍可能使用旧值。","severity":"medium","suggestion":"建立模板选择器到背景/前景令牌的显式映射，并让验证读取或校验这些令牌值；至少增加测试，逐项确认 `vowel-soft`、`cons-soft`、`accent-soft`、`ok-soft` 的常量与四周模板一致，且覆盖所有实际文字组合。"},{"category":"edge-case","location":"tools/theme_palette.py：block_spans()、apply_to_text()","problem":"块尾使用 `text.index('}', start)`，只取从起点到第一个右花括号。当前十六进制令牌块可工作，但模板注释、CSS 函数、字符串或未来自定义属性值包含花括号时会截断块并错误改写。","severity":"low","suggestion":"改用按花括号深度扫描的块解析器，并验证扫描后的结束位置；同时对三个目标块分别检查唯一性和完整闭合。"},{"category":"edge-case","location":"scratchpad/inject_w4_cards.py：规格化验收","problem":"[4] 部分修。边界连通去底、内部透明洞和四边留白检查已覆盖，但没有主体像素损失比例或参考图差异检查；`fill_interior_holes()` 也会对所有内部透明区域填色，无法区分应保留的透明细节与误抠区域。由于 `unify_card_outlines.py` 实现未随材料提供，仍无法确认其不会引入额外挖空。","severity":"medium","suggestion":"对去底前后记录主体像素面积、最大连通域和透明区域变化，设置损失上限；至少对 Dan/Nat 做参考图差异或人工可审计的像素指标，并复核 `unify_card_outlines.py` 的透明处理。"},{"category":"edge-case","location":"scratchpad/inject_w4_cards.py：card() 的长边与连通性检查","problem":"[5] 部分修。四边最小留白、长边比例和无内部洞已加入，但仍未约束主体是否碎片化、最大连通域占比或质心严重偏移；多个小碎片组成的外接框仍可能通过现有门槛。","severity":"low","suggestion":"增加最大连通域占比、连通组件数量、主体质心偏移和非透明像素触边断言；若规范只要求外接框，则应在文档中明确不保证主体连贯性。"},{"category":"other","location":"工具入库与资源管线","problem":"[6] 部分修。主题色工具和测试已入库，但补图生成/规格化/注入仍依赖 scratchpad 脚本与硬编码路径，第四周素材目录还必须避免运行现有全量嵌入工具；因此任意检出位置无法完整复现媒体产物。","severity":"medium","suggestion":"在 adopted 资源管线落地前，至少把路径改为仓库根目录或显式参数，并提供只读校验入口；后续让规格化 PNG 成为媒体生成的确定性输入，移除 README 中的人工绕行警告。"},{"category":"readability","location":"frontend/src/weeks/week01.template.html 至 week04.template.html：--shadow-*","problem":"[8] 未修。蜜桃等暖色主题仍使用旧蓝灰阴影基色，可能造成表面与阴影的冷暖不一致。","severity":"low","suggestion":"在四周浅色/深色页面上做组合对照；若偏差可见，新增受控阴影令牌并在规范中明确其是否随周主题变化。"},{"category":"ambiguity","location":"course.html 壳层与周模板主题边界","problem":"[9] 未修。缺少壳层与四周主题、手动/系统深色切换的组合验收，无法确认壳层固定色是否会与新糖果色内容产生明显断层。","severity":"low","suggestion":"增加壳层到周内容的浅色、深色和系统主题矩阵截图或断言，并明确壳层保持品牌中性还是应继承周主题。"},{"category":"other","location":"tests/browser/smoke_w2_browser.py：主题断言","problem":"冒烟测试部分修。令牌对照能验证页面确实采用自身 `--ground`，但删除了“第二周应为紫色”的事实断言；若模板和令牌一起被错误改成另一色，冒烟测试本身不会发现。现有单测对方案 A 的覆盖可部分弥补，但两者职责未明确衔接。","severity":"low","suggestion":"保留运行时令牌一致性断言，并在主题契约测试中断言 week02 使用方案 A 的生成值（或显式色相/调色板标识），避免在浏览器测试中重新硬编码 RGB。"}],"mode":"advice-only","notes_for_claude_code":"以上判定仅依据提供的四份材料；未读取完整 week04.data.js、模板词卡逻辑、WORD_ILL、unify_card_outlines.py 或实际基线文件，也未运行测试。无 high 级阻断项，可按当前范围放行，但建议优先处理注入脚本副作用、调色板越界处理和软色背景映射。","recommendations":["[1] 部分修；幂等的 art 替换逻辑已覆盖 null/同键重跑，但写入阶段仍不是严格跨文件原子。","[2] 已修；已改为以最终 sRGB 计算的 CIELAB L* 为目标并加范围断言。","[3] 已修；对比度和线条间隔已有硬断言，测试也覆盖多组色相；软色映射契约仍需补强。","[4] 部分修；连通域去底、洞和留白已处理，但缺少主体像素损失验证。","[5] 部分修；四边留白等核心门槛已落实，碎片化和偏心仍未限制。","[6] 部分修；主题工具已入库，补图生成与规格化管线仍不可独立复现。","[7] 已修（基于材料中的代码事实与 34/34 墙面验收）；若要防回归，应把这些入口契约加入自动测试。","[8] 未修；阴影基色仅记录未改。","[9] 未修；壳层与周主题边界仍缺组合验收。","冒烟改读令牌为部分修：运行时一致性更稳健，但紫色事实应转移到主题契约测试而非完全删除。"],"risks":["调色板 CLI 允许异常 chroma 或非有限输入时，sRGB 裁剪可能掩盖不可达色域并造成错误的 L* 结论。","补图脚本在后续键冲突或文件替换失败时可能遗留 `_192.png`、媒体和 data 的不一致状态。","继续以内嵌 base64 追加媒体会使当前约 8 MB 预算的余量快速下降。"],"summary":"九条首审意见中，[2]、[3]、[7]可判定已修；[1]、[4]、[5]、[6]及冒烟为部分修；[8]、[9]仍未修。当前没有 high 级阻断，但调色板越界/裁剪、软色背景映射和注入脚本写入副作用应作为后续优先修复项。","task_type":"code-review"}