{"task_type":"spec-critique","mode":"advice-only","summary":"不可以定稿。H-1 已关闭，但 H-2 未真正关闭：W4 monthly 兼容集合遗漏了现有校验器在 `week === 4` 分支中继续执行的测评短文、认读词和复用隔离检查，因此基础假设 2 不成立。","issues":[{"severity":"high","category":"missing-constraint","location":"《里程碑 2 实施方案 v1.5》§0.3“W4 monthly 兼容集合逐项枚举”","problem":"所谓“当前校验器对合法 W4 实际执行的全部检查”并不完整。`assessment_contract.js` 在 W4 专项的巩固周和墙数量检查之后，还会校验 `ASSESS_TEXT` 至少 60 词、`TAUGHT_SIGHT` 精确集合、短文与测评池冲突、内容词不得进入教学词表、字位必须已教、整句不得复用以及全文不得复用。方案列出的十项完全遗漏了这些检查。若按该表重构兼容路径，W4 会出现实质性校验退化。","suggestion":"把 W4 兼容集合补成现有 W4 路径实际执行的完整清单，至少新增上述七类短文与隔离检查；更稳妥的实现约束是先把现有 W4 行为提取为独立函数并用“重构前后错误集合完全一致”的特征测试锁定，再接入 `assessmentMode:'monthly' && week===4`。"},{"severity":"medium","category":"ambiguity","location":"《里程碑 2 实施方案 v1.5》§0.2 第1条及§7","problem":"方案要求在规范 §8 为“里程碑 2b”写独立截止日，但全文没有给出具体日期或明确的取值来源。第 0 步因此无法机械验收，也可能由实施者自行拍定排期。","suggestion":"定稿前补充明确日期，或写明必须由用户拍板且在未取得日期前第 0 步不得完成；同时把该日期加入第 0 步验收条件。"},{"severity":"medium","category":"contradiction","location":"《里程碑 2 实施方案 v1.5》§0.2、§3.1","problem":"§0.2 中删除 `L` 的规范修改实际是第5条，但§3.1 影响面表仍写“第 0 步先改，见 §0.2 第 4 条”。第4条是最长匹配修订，会误导实施与审查定位。","suggestion":"将交叉引用统一改为“§0.2 第5条”，并把文档机械核对增加“编号型交叉引用目标摘要匹配”，避免只验证步骤号集合而漏掉条目号漂移。"},{"severity":"medium","category":"missing-constraint","location":"《里程碑 2 实施方案 v1.5》§3.2、§5“分词”","problem":"方案规定 DP 只保留“0解/唯一解/多解”及有限候选，但没有规定结构化错误中 `offset` 与 `candidates` 在全局多解时如何确定。像 `abc` 的两条解析在不同首段处分叉，不同实现可能报告不同 offset 和候选，浏览器与 Node 虽都判失败，错误契约却不一致。","suggestion":"规定歧义错误的确定性定位规则，例如报告两条见证解析的最早分歧 UTF-16 offset，候选 ID 按稳定排序输出；测试同时断言错误码、offset 和候选集合，而不只断言抛错。"},{"severity":"low","category":"readability","location":"《里程碑 2 实施方案 v1.5》§5“迁移前审计”及任务简述第四节","problem":"任务简述称“第3步审计输出的规则 ID 化”已登记为验收项，但正文只要求覆盖 weekly 矩阵并输出机器可读来源位置，没有明确规定审计记录必须携带哪些稳定规则 ID、字段 schema 或版本号。","suggestion":"在第3步验收中明确机器可读格式至少含 `schemaVersion`、`ruleId`、`week`、`status`、`sourceFile`、`line`、`details`，并将 weekly 各项映射到稳定规则 ID，避免实施时临时设计格式。"}],"recommendations":["明确结论：本轮不能定稿，卡在 H-2；先补全 W4 monthly 兼容检查集合并锁定重构前后错误集合等价。","基础假设1：成立。第0步已同时覆盖全局唯一分词语义和 `SOUNDS` 删除 `L`，执行顺序也早于第2步与4a。","基础假设2：不成立。十项枚举遗漏 `assessment_contract.js` 的 W4 测评短文、认读词、词表隔离及句子复用检查。","基础假设3：基本成立。4a、4b 在步骤表、验收和依赖表中已分开；但应修正 §3.1 指向 §0.2 第4条的错误引用。","基础假设4：基本成立。运行时 schema 断言与六类源码扫描能覆盖主要迁移面；建议扫描器同时锁定扫描根目录、文件类型和显式排除清单，避免新增目录逃逸。","基础假设5：当前范围内成立。`c`、`k` 可通过同周 `newPatterns` 顺序表达；未来出现无首教周的内部或展示别名时，按方案另立契约即可。","基础假设6：就正文列出的主步骤依赖而言成立；里程碑 2b 截止日仍缺少确定值，不应视为第0步已完全可执行。","待修复上述 high 后，ID 字符集与属性编码、Node 侧 DOM 解析、审计输出规则 ID 化可以留在实施阶段收口，但必须在对应步骤开始前先落成可执行验收和失败 fixture，不能仅保留为说明文字。"],"risks":["若按当前十项表直接重写 monthly 路由，W4 的测评短文可能不足60词、复用教学句子或含不合规内容词而仍然通过。","缺少里程碑2b明确截止日会使规范第0步出现不可验证的自由裁量。","歧义错误定位未确定可能导致浏览器与Node返回不同的 offset 或候选列表，削弱“同一错误契约”的目标。"],"confidence":"high","notes_for_claude_code":"本结论仅基于所提供的方案、规范及代码内容进行文本审查，未执行命令或测试。优先修复 W4 兼容集合遗漏；修复后再做一次从 `validateAssessment` 控制流逐分支映射到方案表项的机械核对。"}