{"task_type":"code-review","mode":"advice-only","summary":"整体修复方向基本正确，③ 的异常已被捕获，因此从补丁看不会阻止后续检查继续执行；但 G1/认读词豁免仍存在按“词”聚合导致的跨来源误放行，且 `load_data.js` 新增了若干解析回归风险。建议至少修正豁免粒度和 HTML 脚本提取后再合入。","issues":[{"severity":"high","category":"contradiction","location":"tools/validation/check_data.js，第③节 `wordSourceKinds` 循环","problem":"代码先按词聚合全部来源，再以 `kinds.has('g1-rounds')` 跳过整个词。只要某个拼写曾出现在 G1，该词在 `book-page`、`wordforge-family` 等严格来源中的零解也会被一并豁免，并非注释所称的“按记录来源精确匹配”。同理，`SIGHT.has(...)` 也会跳过该拼写的所有消费来源。","suggestion":"按 `collectWordConsumption(box)` 返回的每条记录逐条校验，或仅当该词的全部来源都属于允许豁免的来源时才整体跳过；错误去重可以放在校验结果层完成，不能在豁免判断前丢失记录粒度。"},{"severity":"high","category":"contradiction","location":"tools/validation/check_data.js 第③节；数据层规范 `DATA-PATTERN-01`","problem":"规范只明确豁免“G1 干扰词（含第一周正例桶）”，实现却对所有周次的全部 `g1-rounds` 记录豁免，包括未来周的正例桶。注释声称新旧覆盖范围完全一致，但未来出现不可解码的 W2+ 正例时，新实现会放行而旧规则不会。","suggestion":"保留并使用记录的 `bucket` 和周次：豁免所有 `neg`，以及仅第一周的 `pos`；如果产品规则确实已改成所有 G1 正反例均只听不读，应先同步修改规范，不要用当前数据恰好可解码来掩盖规则差异。"},{"severity":"high","category":"ambiguity","location":"tests/unit/test_grapheme_semantics.js，`isZeroSolutionExemptRecord` 与 `sightWordsThisWeek`","problem":"认读词豁免同时可能过宽和过窄：按拼写无视来源会让同名词在 `wordforge-family`、`blend` 等必须操作字位的来源中也获豁免；但集合只从“本周 `kind === 'sight'`”收集，先前周已教、后续周继续出现在阅读材料中的认读词又可能无法获豁免。当前正反测试没有覆盖这两个边界。","suggestion":"建立累计已教认读词集合，并按消费语义限定允许豁免的来源；例如阅读文本来源可按词豁免，而 `wordforge-*`、`blend`、`pair` 等字位操作来源仍须可分词。新增“认读词同拼写出现在 wordforge 必须失败”和“历史周认读词出现在后续 book-page 可通过”两类测试。"},{"severity":"medium","category":"edge-case","location":"tools/validation/load_data.js，`extractScriptContents` 与 `loadData`","problem":"META 探测在抽取后的脚本文本上执行，但真正的 `declaration(raw, name)` 仍扫描整份原始 HTML。只要脚本内存在一个合法 META，正文中更早、恰好位于行首的 `const META =` 或其他声明就可能被 `declaration()` 选中；现有正文负例前面带有“示例代码：”，没有覆盖这一情况。","suggestion":"当 `html === true` 时，将抽取出的脚本文本同时用于 META 探测和所有 `declaration()` 调用；增加正文以换行开头、声明位于行首且脚本内另有合法 META 的测试。"},{"severity":"medium","category":"edge-case","location":"tools/validation/load_data.js，`extractScriptContents`","problem":"脚本提取正则区分大小写，因此浏览器认可的 `<SCRIPT>...</SCRIPT>` 或混合大小写标签会被当成没有脚本，形成相对旧实现的新拒绝路径；正则也不能可靠处理属性引号中含 `>` 的合法标签。","suggestion":"最低限度给正则增加不区分大小写标志，并补大小写变体测试；若输入不保证由固定模板生成，应改用 HTML 解析器提取脚本内容。"},{"severity":"medium","category":"missing-constraint","location":"tools/validation/wall_order.js，`gatherWeekRecordsUpTo`","problem":"新增断言只在 `box.META` 为真时比较周次；META 缺失时不会抛 `teaching-order-week-mismatch`，不满足“文件内容必须声明并等于文件名周次”的完整约束。当前测试仅覆盖值为 99，不覆盖缺失或非整数。","suggestion":"显式要求 `box.META` 存在且 `META.week` 为预期整数；缺失、类型错误和数值不匹配均抛结构化错误，并分别增加测试。"},{"severity":"medium","category":"other","location":"tests/unit/test_build_course.py，`test_real_built_course_html_payload_has_no_close_tag_sequences`","problem":"所谓端到端测试依赖仓库中预先存在的 `build/course.html`，既会在干净检出或未先构建的测试环境中失败，也可能检查旧产物而不是当前 `build_course.py` 生成的结果，因此不能可靠证明本次实现。","suggestion":"测试中用当前代码在临时目录生成产物后检查，或将该用例明确移到要求先构建的集成测试阶段并校验产物与当前源码的新鲜度。"},{"severity":"low","category":"other","location":"tools/validation/check_data.js 第③节；tools/validation/assessment_contract.js，`assertCvcByGraphemes`","problem":"成功调用 `segmentWord` 后再检查返回 ID 是否属于与其同源的 `SOUNDS` 键集合，当前结构下属于恒真分支。真正的不存在字位会在分词阶段抛错，因此新增测试只能证明“分词失败”，无法证明 `含未教字位` 分支具有判别力。","suggestion":"若没有独立的“累计已教字位集合”，删除或明确标注该防御性分支；若业务确实需要区分“已知但尚未教”的字位，应让分词使用完整字位表，而 `taught` 使用累计教学集合，并增加能独立触发该分支的测试。"},{"severity":"low","category":"edge-case","location":"tests/browser/smoke_w3_browser.py，`data-grapheme-id` 定位","problem":"虽然不再依赖显示文本，但字位 ID 仍直接插入 CSS 属性选择器，没有进行 CSS 字符串转义。含引号或反斜杠的合法 ID 仍可能令选择器语法错误，原评论声称已消除特殊字符问题并不完全成立。","suggestion":"通过 DOM `dataset.graphemeId` 比较、参数化页面求值或标准 CSS 转义构造选择器，并补一个包含引号或反斜杠 ID 的定位测试。"}],"recommendations":["优先把零解豁免改为逐消费记录判定，并明确认读词在阅读来源与字位操作来源中的不同契约。","让 HTML 模式下的 META 探测和声明提取使用同一份脚本文本，避免“探测安全、实际提取仍扫正文”的不一致。","补充真正具有判别力的交叉来源、跨周认读词、缺失 META 和干净构建产物测试。","`\u0003c` 全量转义本身不会把原有字面量反斜杠序列双重解码：JSON 序列化会先保护反斜杠，后续替换只处理真实的 `<`；仍建议增加原始字符串含字面量反斜杠加 `u003c` 的直接回归用例。"],"risks":["严格来源中的不可分词词可能因同拼写出现在 G1 或认读词集合而静默通过。","后续周的 G1 正例会被实现永久豁免，与当前书面规范发生漂移。","合法 HTML 的脚本大小写变体可能被错误拒绝，正文伪声明也可能污染实际加载结果。","单元测试依赖陈旧或缺失的构建产物，导致假绿或环境相关失败。"],"confidence":"high","notes_for_claude_code":"本复审仅基于提供的差异文本，未执行命令或测试。③ 对 `idsForWord` 的异常有逐词 `try/catch` 并 `continue`，所以从补丁结构看不会阻断④、⑤等后续检查。`declaration()` 的新正则不会匹配 `const METADATA =`，因为 `META` 后必须立即出现空白或等号。weekly 与 monthly 的 `taught` 初始化未完整出现在差异上下文中，合入前仍应读取 `assessment_contract.js` 两个函数的完整定义确认确实同源。进一步审查还应读取 `word_consumers.js`，确认记录是否保留周次、桶和消费语义字段，以便实现精确豁免。"}