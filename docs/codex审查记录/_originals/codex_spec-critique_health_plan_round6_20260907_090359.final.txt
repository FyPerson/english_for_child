{"confidence":"high","issues":[{"category":"contradiction","location":"§3.3“去桥接骨架哈希”；第1b期第1、2、6步","problem":"当前定义只替换两个 SOURCE 标记之间的脚本文本，但保留外层 script 标签；旧基线是在加入桥接前生成的，因此替换后仍会多出脚本包装，无法等于旧 skeletonSha256。第1b期第1步又写明“本步尚未加桥接”却要求执行去桥接哈希，而该算法要求两个桥接标记各出现一次。","severity":"high","suggestion":"明确一种可执行算法：要么去除整个桥接注入块（包括 script 标签和 SOURCE 标记）后再计算哈希，要么先生成包含桥接的专用基线并取消与旧基线的直接相等要求；同时分别定义第1b期第1步（无桥接的普通骨架）和第2步（带桥接的去桥接骨架）验收条件。"},{"category":"ambiguity","location":"§3.7 Release job命令与验收","problem":"命令仍包含 `soundblocks-<tag>.zip`、`<值>` 这类占位符，没有说明如何计算并传入实际 zip 路径和 sha256；因此所谓“可执行契约”仍不能直接复制执行。","severity":"high","suggestion":"在步骤中定义变量，例如 `ZIP_PATH=...`、`ZIP_SHA=$(sha256sum \"$ZIP_PATH\" | cut -d' ' -f1)`，后续 `test -f`、解压核对和 `gh release create` 全部引用变量，并明确 Release notes 必须包含该变量的实际值。"},{"category":"ambiguity","location":"§3.3与§3.4 check契约","problem":"§3.3要求 check 额外执行一次 `build --single-file` 获取基线比对对象；§3.4却只列出默认形态的两次构建，没有说明单文件构建属于哪个阶段、哪些文件参与比较、临时目录何时清理。不同实现可能漏做基线比对或重复/错误比较 course.html。","severity":"medium","suggestion":"把 check 明确列为编号步骤：默认形态可重现构建、单文件形态基线构建、基线三项比较、其余检查，并写明单文件仅比较四个周文件且临时目录必须清理。"},{"category":"ambiguity","location":"§3.6会话号、消息校验与发送目标","problem":"虽要求字段缺失或类型不符即忽略，但没有给出各消息的完整类型约束，也没有明确壳必须校验 `message.week === selectedWeek`，HTTP 场景也未明确是否同时校验 `event.origin`。仅校验 source 和 sessionId 会让协议边界依赖实现者猜测。","severity":"medium","suggestion":"为五种消息列出字段类型、允许值和接收条件；壳侧同时校验当前周号，非 `file:` 时校验 `event.origin === location.origin`，`file:` 才仅使用 source 加 sessionId。"},{"category":"edge-case","location":"§3.6 leave 超时与重试流程","problem":"只描述了超时后显示“重试/留在本周”，没有定义等待状态、重试是否复用还是生成新请求、点击其他周时如何取消目标周和旧计时器，以及迟到的 leave 回包如何处理。实现容易出现重复切换或旧回包改变 UI。","severity":"medium","suggestion":"定义显式状态机（空闲、等待离开、超时、已取消），每次请求绑定 sessionId/请求序号；重试先取消旧计时器并新建请求，留在本周清除等待态，任何非当前请求的回包一律丢弃。"},{"category":"contradiction","location":"§0、§3.5长期指标","problem":"文档宣称体量护栏从构造上不随周数累计增长，但 shell 的 payload 会随周数量线性增加；`media/` 总字节虽只是报告项，也与“无累计指标”的表述并列，容易让读者误以为所有体量都不增长。","severity":"medium","suggestion":"改成“受护栏约束的对象均为单文件或单周新增指标”，单独说明 shell 随周数增长但受总上限约束，并把累计总字节明确标为观察性报告而非护栏。"},{"category":"readability","location":"§5各期验收，尤其第0期和第1b期","problem":"多处写“反例测试红”“前缀碰撞测试红”“构建残留测试红”，与第0期要求 `project.py check` 全绿相冲突。读者无法判断“红”是被测命令应返回非零，还是测试套件真的允许失败。","severity":"medium","suggestion":"统一改为“反例断言通过（被测命令按预期返回非零）”；若需要记录失败输出，说明它是测试内部断言，不是 CI job 失败。"},{"category":"edge-case","location":"§3.6 progressDigest","problem":"`progressDigest` 的存储读取、字符串长度定义和读取异常未规定。localStorage 访问可能抛异常或返回非法 JSON；若父子侧异常处理不同，可能误报或中断桥接。","severity":"low","suggestion":"规定 digest 直接针对原始存储字符串计算，`length` 使用 UTF-16 JavaScript 字符长度；读取异常统一映射为 `exists:false,length:0,hash:0` 或明确错误状态，并为异常场景增加协议测试。"}],"mode":"advice-only","notes_for_claude_code":"上一轮13条判定：H-1部分修（命令仍有占位符）；H-2部分修（check阶段未明确）；H-3已修；M-4部分修（“非累计”表述仍过宽）；M-5部分修（消息字段和周号/来源校验未完全定义）；M-6部分修（超时重试状态机未定义）；M-7已修；M-8未修（去桥接哈希与第1b期步骤存在实质矛盾）；M-9已修；M-10已修；M-11已修；M-12已修；M-13部分修（异常和长度语义未定义）。当前没有发现必须推倒重来的 critical；两条 high（哈希算法、Release占位符）都可局部修复，修完即可定稿。","recommendations":["先修正去桥接哈希的规范和第1b期步骤顺序，再据此固定基线重取条件。","把 Release shell 命令中的所有占位符替换为变量化、可复制执行的命令，并验证 notes 使用实际 zip 哈希。","补一段 check 的编号化执行流程和消息协议字段表，减少实现者自行解释。","将第5节所有“测试红”改成反例断言语义，避免与全绿验收冲突。"],"risks":["若不修哈希矛盾，第1b期验收可能在理论上无法通过，导致基线无法重取。","Release 命令保留占位符会导致 tag 发布步骤直接失败或发布错误附件。","未定义的异步离开状态和消息校验可能造成切周竞态、旧回包污染当前界面或进度提示误报。"],"summary":"v1.2 已覆盖上一轮大部分问题，但去桥接骨架哈希与第1b期步骤存在实质性矛盾，Release 契约仍含不可执行占位符。两条 high 都能局部修复；修完即可定稿。","task_type":"spec-critique"}