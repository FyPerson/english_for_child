{"task_type":"spec-critique","mode":"advice-only","summary":"第五稿已基本落实四审 12 条，但仍有 5 个高优先级契约缝隙，其中 `audioKey` 被误当作“不上墙别名”、W5 测评 fixture 与矩阵直接冲突、`rereadSources` 无法证明合集正文确为旧内容。当前仍不适合激活，但未发现必须判为 critical 的问题。","issues":[{"severity":"high","category":"contradiction","location":"§3「点亮墙与首教日：唯一模型」；§3 SOUNDS 单条；render-blocks.js 的 sound 分支","problem":"规范把所有带 `audioKey` 的记录定义成“不单独上墙的别名”，但现有 `c`/`k` 模型中 `k` 是需要单独展示和教学的字位积木，只是与 `c` 共用录音。渲染器也把这类记录作为“一音多形”的不同积木分别渲染。复用音频不等于同一个字位；若按第五稿排除 `k`，点亮墙将漏掉已教字位，`newPatterns`、`FIRST_TEACH_DAY` 和累计字位数也会与墙集合失配。","suggestion":"不要用 `audioKey` 推导上墙资格。将其语义限定为“录音解析键”，所有具有独立字位 ID 和 `grapheme` 的记录默认上墙；若确有纯内部别名，新增显式字段如 `displayOnWall:false` 或独立记录类型。补充 `c`/`k` 正例 fixture，验证两个 ID 都上墙但共享录音。"},{"severity":"high","category":"contradiction","location":"§4.3「assessmentMode 契约矩阵」后的 fixture 段","problem":"矩阵规定所有 assessmentMode 都必须有 `RESERVED`，但紧接着要求 W5“不带任何测评常量应通过”。按字面执行，W5 缺少必需的五个周检词；文档同时还要求第 7 天有 `exam`。","suggestion":"把 W5 fixture 改成“仅带 `RESERVED`，不带 `RESERVED_RETEST`、`ASSESS_TEXT`、`GLOBAL_RESERVED`、`PROBE_A/B`、`ANNUAL_DECODING`，并且无 `retest`/`probe` 块时通过”。为 weekly 模式漏 `RESERVED` 增加失败 fixture。"},{"severity":"high","category":"missing-constraint","location":"§3「BOOK 的三种结构」及 PED-NOVELTY-02","problem":"`rereadSources` 只能声明来源，不能证明手工拼入 `BOOK.pages` 的正文确实来自这些旧书，也没有定义来源书的稳定标识、页面映射、顺序或内容一致性。执行方可以填写三个合法来源，却放入改写或全新正文，从而让陌生度规则错误地把新正文视为旧内容。当前 schema 示例中的历史 BOOK 也未展示 `bookId` 字段，`{week,bookId}` 可能无法解析。","suggestion":"先确认历史周是否存在稳定 `bookId`；若没有，使用可解析的 `{week}` 加页范围，或正式为 BOOK 增加稳定 ID。为每个合集页增加来源映射，例如 `source:{week,page}`，并校验规范化后的 `line` 与历史源页一致；若允许改写，则改写页必须按普通本周正文计算新词，不能因合集级 `rereadSources` 整体豁免。补充合法三书合集、悬空来源、正文不一致和重复来源 fixture。"},{"severity":"high","category":"contradiction","location":"§3「12 个常量」的 W/RESERVED 定义；DATA-ASSESS-01；check_data.js 引用完整性","problem":"规范一处要求 W 包含“保留词”，DATA-ASSESS-01 又写 W4 起保留词缺释义即失败，但现有校验器明确用 `META.week >= 4 || W[w]` 豁免 W4 以后的 RESERVED。`segments` 只存在于 W，因此允许 RESERVED 不在 W 会使双字母字位周的“三个字位”校验无法获得显式消歧信息和释义。","suggestion":"确定唯一契约：建议所有 RESERVED 必须存在于 W，并可携带 `segments`，删除 `META.week >= 4` 豁免。明确 G3、G4、G5 中哪些词必须在 W：G3/G5 已有要求，G4 当前通过 usedWords 间接要求，最好显式写入规则；G1 干扰词可继续豁免。为含双字母和同形多音的 RESERVED/G3/G4/G5 各加分词 fixture。"},{"severity":"high","category":"ambiguity","location":"PED-NOVELTY-01、PED-NOVELTY-02「内容词」「历史曝光词」定义","problem":"两层排除表修复了 sight/proper/G1 的冲突，但“内容词”仍没有机器可执行定义。仅给出 ASCII token 化和去屈折规则，无法判断句子中的哪些词属于内容词；同时“历史曝光词＝全部词减去排除表”未明确减的是曝光层排除还是统计层排除。不同实现会对 sight、专名及普通功能词产生不同复现率。","suggestion":"把集合推导写成明确流水线：先生成 `visibleExposureTokens`，只应用曝光层排除；再生成 `statisticalContentTypes`，在前者基础上应用统计层排除。避免依赖自然语言词性判断，可将“内容词”正式定义为“归一化 token 减 sight、proper、G1 干扰和测评池”，或为无法自动分类的词增加显式元数据。审计输出需同时列出原始位置、归一化 lemma、曝光层结果和统计层排除理由。"},{"severity":"medium","category":"missing-constraint","location":"§3 META.newWordPatterns；DATA-GRADIENT-01","problem":"词形枚举虽已加入 schema，但没有定义如何从字位序列计算 C/V 模式，尤其 `qu`“永远两块一起”、同形多音、Magic e 和多音节可能产生多种解释。`multisyllable` 也不是与 CVC 等同一层级的形状枚举，可能与其他模式重叠。","suggestion":"定义唯一分类函数：先取得 segments，再依据 `SOUNDS[id].type` 映射 C/V；明确 `CVCe` 如何识别静音 e，`multisyllable` 如何优先于或组合其他模式，以及 `qu` 的两个 ID 各自类型。对无法唯一分类的词要求显式 `wordPattern`，并添加边界 fixture。"},{"severity":"medium","category":"missing-constraint","location":"DATA-TIME-01 状态；§2 七天节奏；check_data.js「每天时长与打卡」","problem":"DATA-TIME-01 标为已实现，但现有实现用 `d.rest || total === 30`，任何第 1–6 天都可设置 `rest:true` 绕过 30 分钟要求；也未禁止第 7 天之外出现 rest。因而“前 6 天各 30 分钟”尚未完整实现。","suggestion":"将该规则状态降为“部分实现”，或同步要求实现：D1–D6 的 `rest` 必须不是 true 且分钟数恰为 30；D7 必须 `rest:true`、含 checks 与 exam。增加 D3 错设 rest 的失败 fixture。"},{"severity":"medium","category":"missing-constraint","location":"DATA-BOOK-01；§3 BOOK 结构","problem":"`rereadSources` 的结构约束没有完整写入 DATA-BOOK-01：未规定仅 `bookMode:'new'` 可出现、数组是否允许为空、来源周必须小于当前周、来源是否可重复，以及普通新书是否禁止携带该字段。","suggestion":"明确 `rereadSources` 要么缺省，要么为非空且去重的历史来源数组；每个来源必须满足 `1 <= week < META.week` 并可解析。普通新书不应填写该字段，避免借标记绕过小书新词上限。"},{"severity":"medium","category":"contradiction","location":"§4.3「每天四类块」；§3 块类型表","problem":"块类型表仍把 `output` 标为“输出”类别，但后文规定它不能单独满足输出类别。若实现只读取类别列，仍会错误放行。这里实际混用了“展示分类”和“满足每日活动门槛资格”两种语义。","suggestion":"在表中把 `output` 类别改为“输出指引（不计门槛）”，或新增明确的 `countsForDailyCategory:false` 属性。DATA-DAY-CATEGORIES-01 只统计真实输出块集合，并单独校验 output 指引与真实活动共存。"},{"severity":"low","category":"readability","location":"§3 标题「BOOK 的三种结构」","problem":"正文已删掉 reread 分支，只剩 `new` 与 `none` 两种结构，但标题仍写“三种结构”。","suggestion":"将标题改为“BOOK 的两种结构”，避免执行方误以为还有未列出的第三个分支。"},{"severity":"low","category":"readability","location":"§5 交付前自查清单","problem":"清单要求“上一周的 SOUNDS / W 逐条比对，确认一字未改”，但新契约明确 W 会按当前引用裁剪并新增词，无法对 W 整体做到“一字未改”。","suggestion":"改成“上一周仍被本周保留的 W 条目不得修改；允许删除不再引用的条目并新增本周条目”，并明确是否允许修正历史词条错误及其审批流程。"}],"recommendations":["先修正文档内部的两处直接冲突：`audioKey` 上墙语义和 W5 weekly fixture；这两处会让不同实现得到相反结果。","为 `rereadSources` 增加页级来源与正文一致性规则，否则它只能作为未经验证的自报标签，不能参与陌生度判定。","统一规定所有需要分词或释义的题库词必须能解析到 W 条目；不要保留 W4 起 RESERVED 缺项豁免。","把陌生度逻辑正式拆成“可见曝光集合”和“统计内容词集合”两个有名字、有审计输出的阶段。","重新核对所有标“已实现”的规则是否覆盖完整规范语义，尤其 DATA-TIME-01；实现了部分检查不应标为已实现。"],"risks":["若按 `audioKey` 排除墙项，c/k 等一音多形字位会从完整点亮墙中消失。","若合集级 `rereadSources` 被无条件信任，新正文可伪装成旧正文，导致小书陌生词上限假绿。","若 RESERVED 可不在 W，同形多音周的三字位校验与释义校验无法可靠执行。","若内容词口径继续留给实现者解释，不同校验器会对测评历史词比例和小书新词数给出不同结论。","状态列高估现有校验覆盖面时，规范激活验收可能在 fixture 不充分的情况下误判完成。"],"confidence":"high","notes_for_claude_code":"四审 12 条复核：1 reread 多书表达＝部分修（数组能列来源，但不能验证页内容与来源一致，且 bookId 稳定性未证实）；2 同形多音 segments＝部分修（核心消歧已补，但 RESERVED 是否必在 W 与现有豁免冲突）；3 曝光/统计两层＝部分修（两层表已补，内容词及集合推导仍不够形式化）；4 W 累计/裁剪＝已修（明确当前引用表并指出历史必须跨周抽取）；5 annual 常量＝已修（已定义专属常量、数量与互斥要求）；6 字段级抽取协议＝已修（明确路径声明并禁止递归扫全部字符串）；7 newWordPatterns＝部分修（已入 META，但模式分类算法仍缺边界定义）；8 PHASE 复用类别＝已修（已复用类别并排除 baseline 基准作用）；9 output 单独计输出＝部分修（正文已禁止，但类别表仍标输出，机器映射可能误用）；10 墙集合与顺序＝部分修（顺序已明确，但把 audioKey 等同不上墙别名不成立）；11 fixture 字段组合＝部分修（组合写清，但 W5“不带任何测评常量”与矩阵冲突）；12 rest:true＝部分修（语义写清，但现有校验仍允许 D1–D6 用 rest 绕过计时）。更深确认还需要读取 week01 至 week04 的完整数据、load_data.js、project.json、历史 BOOK 是否存在稳定 bookId，以及里程碑 0 和三方分工 v1.2。"}