{"task_type":"spec-critique","mode":"advice-only","summary":"五审列出的修复大多已落到正文，但仍有三处会直接削弱交接门禁的硬伤：体量脚本不会以失败码退出且代码块多出一个“PY”，音频允许路径无法由键校验输出生成，以及未定稿标题可能早于状态转正被移除。另有一项审计输入缺失：简报声称五审有 11 条，落实表实际只列出 10 条，无法完成第 11 条的逐条核验。","issues":[{"severity":"critical","category":"contradiction","location":"§6.6“体量护栏在副机侧怎么判”代码块","problem":"脚本虽然设置了“bad”标志，却没有执行“sys.exit(1 if bad else 0)”，因此出现“OVER”或“MISSING”时 Python 仍返回成功；而 heredoc 结束后又单独出现一行“PY”，Git Bash 会把它当作命令执行并报“command not found”。这与正文声称“出现 OVER 或 MISSING 就是没通过”及简报声称已补失败信号不一致。","suggestion":"在 Python 代码末尾加入“import sys”和“sys.exit(1 if bad else 0)”，并删除注释后的第二个孤立“PY”。正文同时明确以退出码非零为失败依据。"},{"severity":"high","category":"contradiction","location":"§3 META.status 人工闸门段","problem":"文档规定“META.status”只能由主机在步 9 从 scaffold 改为 final，但又规定在步 1b 定稿内容时去掉“[未定稿]”前缀。这样从步 1b 到步 9 之间，状态仍是 scaffold、工具门禁仍未实现，唯一能随被拷走文件保留的警示却已消失，与“三道人工闸门缺一不可”的表述冲突。","suggestion":"把标题前缀的移除与“META.status”转为 final 绑定到同一个步 9 提交；步 1b 只完成内容，不移除前缀。若确需步 1b 移除，则必须同时调整状态转换规则并增加等效门禁。"},{"severity":"high","category":"missing-constraint","location":"§6.3 键级校验脚本及随后 allowed.txt/actual.txt 比对步骤","problem":"校验脚本的“files”只打印 manifest 中的裸文件名，例如“an.mp3”，但后续要求 allowed.txt 包含“resources/assets/audio_raw/an.mp3”和“resources/assets/audio_processed/an.mp3”这类完整路径，并称这些路径由脚本的 files 行得到。照文档复制输出无法得到格式一致的允许集合，comm 会把合法音频改动判为越界。","suggestion":"让脚本直接输出可机器消费的完整允许路径，每个变更条目分别展开为 raw 与 processed 两条路径，并单独提供纯路径输出模式；不要要求操作者从 Python 列表文本手工改写 allowed.txt。"},{"severity":"high","category":"contradiction","location":"§0 Node 18+ 硬门槛及紧随其后的替代路径表","problem":"正文先写“副机装不上 Node 时不要继续任何任务”，随后又允许副机继续出图和运行纯 Python 的 gen_audio.py。两个指令对同一条件给出相反动作，执行者可能错误停工或错误继续完整流程。","suggestion":"改成“Node 不可用时不得执行完整副机任务或声称交付通过；仅可执行下表列出的前置子任务，后续构建、截图和验收由主机补齐”。"},{"severity":"medium","category":"readability","location":"任务简报“落实对照”","problem":"简报称五审共有 11 条并要求逐条判定，但落实表只有 10 行对应项：5 条高、4 条中、1 条低；缺少第 5 条中等级问题的原文或映射，因此无法确认所谓“11 条全部落实”。","suggestion":"补充缺失的五审条目及其落点，或把总数更正为 10。当前可判定：表内 10 项中，体量失败信号仅部分修复；其余 9 项从所给正文看已修，但音频路径集合修复又引入了新的高风险问题。"},{"severity":"medium","category":"contradiction","location":"§6.3“恢复方式按是否已推送分两种”","problem":"未推送场景建议副机执行“git checkout <BASE> -- <越权路径>”，但 BASE 的语义在文中可能是 LOCAL_BEFORE、pull 前 SHA 或其他任务基准；若越权路径在 BASE 后包含主机合法更新，该命令也会把合法更新回退。","suggestion":"把命令中的 BASE 明确限定为“该越权改动发生前且已包含最新主机合法提交的提交”，否则停下交主机处理；不要沿用文中泛化的 BASE 占位符。"},{"severity":"low","category":"readability","location":"§6.6 dry run 及全文脚本输出","problem":"所给可执行代码中的判定词已基本使用 ASCII；未发现“✓”“✗”“→”“—”出现在 print/echo 的判定输出中。仍有中文 echo/print，按文档自身口径可能乱码但不会因这些常用汉字必然崩溃。","suggestion":"保留现状即可；若希望日志跨终端稳定，可仅将所有机器判定行统一为 ASCII，中文解释放在代码注释或正文。"}],"recommendations":["定稿前只做三项收口修正：修正体量脚本退出码与多余 heredoc 标记；让键校验脚本直接生成完整 allowed 路径；把“[未定稿]”前缀移除与步 9 状态转正绑定。","五审落实判定建议记录为：音频 comm 格式已修；manifest 解析已修但允许路径生成未闭环；工作区 manifest 比对已修；LOCAL_BEFORE 时机已修；META.status 展示已修但标题移除时机仍冲突；步 1 验收已修；步骤顺序已修；Node 替代路径部分修；护栏失败标志部分修；护栏退出码未修。第 11 条因简报未列出而无法判定。","建议记入已知缺口的三条：第一，尽快实现 scaffold 的 courseWeeks、package、release 机器门禁；第二，把音频键级与文件级范围核对做成仓库内正式工具，避免手工拼 allowed.txt；第三，为 doctor 增加只检查 Python、Node 和基础依赖且不启动 Chromium 的模式。"],"risks":["体量超限或产物缺失可能显示失败文字却返回成功，自动或人工流程会继续提交。","合法音频文件可能因允许集合格式错误被误判；反过来，操作者手工拼路径也容易漏掉 raw 或 processed 文件。","scaffold HTML 在最终审核前可能失去可见警示，被误拷到上课电脑后展示上一周内容。","Node 缺失时职责边界不清，可能造成盲画、缺少构建验证或无谓停工。"],"confidence":"high","notes_for_claude_code":"本审查仅依据提供的 v1.2 正文、简报、manifest、run_checks.py 与 project.json 做静态评审，未执行命令或验证仓库行为。建议优先处理三个会让门禁失效的硬伤后按用户决定定稿；其余可进入已知缺口，不必继续扩写流程。"}