{"task_type":"spec-critique","mode":"advice-only","summary":"不可以定稿，当前卡在歧义错误定位规则仍不具备完全确定性：存在三条及以上解析时，“两条见证解析”如何选择未定义，浏览器与 Node 仍可能得到不同的偏移量。W4 兼容集合按现有 `validateAssessment` 控制流核对确为 17 项，没有发现第 18 项；里程碑 2b 截止日尚未拍板也使第 0 步按方案自身门槛不能完成。","issues":[{"severity":"high","category":"ambiguity","location":"《里程碑2实施方案》§3.2“歧义错误的定位必须确定”","problem":"规则规定偏移量取“两条见证解析的最早分歧点”，但多于两条完整解析时没有规定选哪两条见证。不同 DP、Trie 或遍历顺序可能选择不同见证对，进而产生不同偏移量；候选排序稳定并不能消除见证选择的不确定性。基础假设 3 不成立。","suggestion":"不要以任意见证对定义错误位置。改为：偏移量取所有能够延伸到完整解析的路径中，最早出现两个及以上不同下一字位 ID 的位置；候选为该位置所有能够延伸到完整解析的下一 ID，按明确比较器排序。再增加至少三解、不同分叉深度且打乱 `SOUNDS` 插入顺序的 fixture，浏览器与 Node 同时断言错误码、偏移量和完整候选数组。"},{"severity":"medium","category":"missing-constraint","location":"《里程碑2实施方案》§0.2 第 1 条、§4 第 0 步","problem":"里程碑 2b 截止日仍待用户拍板，而第 0 步验收明确规定未取得日期就不算完成。因此文档可以继续作为待决实施草案，但不能按自身标准标为最终定稿。","suggestion":"用户拍板后写入规范 §8，并同步更新方案页首“待用户拍板”状态；在此之前明确标记为“技术审查通过但决策未闭合”。该事项不妨碍先做独立的第 1–3 步，但会阻止第 0 步完成，并通过依赖关系阻止第 4a 步及后续实施。"},{"severity":"medium","category":"ambiguity","location":"《里程碑2实施方案》§0.3 W4 特征测试、§4 第 5 步","problem":"“重构前后错误集合完全一致”没有说明比较集合、重集还是有序数组。当前校验器可能对重复词或多处冲突产生重复错误；若测试用普通集合比较，会吞掉重复次数和执行顺序变化，不能严格证明行为原样保留。","suggestion":"明确兼容门槛。若错误顺序属于外部行为，逐项比较有序错误数组；若顺序不属于契约，至少比较错误多重集，并另行固定错误码及关联字段。不要使用会去重的普通集合。"},{"severity":"medium","category":"missing-constraint","location":"《里程碑2实施方案》§4 第 3 步、§5“迁移前审计”","problem":"七个审计字段只定义了字段名，尚未定义 `status` 枚举、`ruleId` 命名空间、同一规则多条发现的身份键、行号未知时的表示方式及 `details` 的稳定结构。第 5、7、9 步若要直接机器消费，仅有字段存在性仍可能导致临时解析约定和格式漂移。基础假设 4只能部分成立。","suggestion":"在第 3 步开始前补最小 schema：固定 `schemaVersion` 值、`status` 枚举、记录唯一键或 `findingId`、`line` 的可空规则、`details` 的规则级对象结构，以及稳定排序规则；加 schema 正反 fixture 和重复记录检测。"},{"severity":"low","category":"edge-case","location":"《里程碑2实施方案》§4 第 6 步 synthetic-ai fixture、§2.4 墙顺序取数规则","problem":"合成 fixture 要求墙包含 `r/ai/n`，但 `newPatterns` 与 `FIRST_TEACH_DAY` 只有 `ai`。若该 fixture 被当作没有历史上下文的单周记录，它无法说明 `r`、`n` 从哪一周进入累计教学顺序；第 7 步启用墙契约后复用此 fixture 时可能产生歧义。","suggestion":"明确该 fixture 带有规范化的前置历史记录，其中 `r`、`n` 已在早期 `newPatterns` 出现；或者明确它只验证第 6 步消费者链路且不得直接用于 `DATA-WALL-01`。更稳妥的是为墙校验另建带连续周记录的 fixture。"}],"recommendations":["W4 的 17 项映射可以接受：入口公共检查对应 1–8，巩固周分支对应 9，W4 分支及其余分支体对应 10–17；现有控制流中未发现第 18 项。","基础假设 1：基本成立，但特征测试应比较有序数组或多重集，不能用去重集合。","基础假设 2：成立。与字位语义有关的字符级处理已覆盖 `assessment_contract.js` 中 CVC 正则、周检/复测已教范围展开和测评短文未教字位展开；`tokens()` 的字母级词法切分属于单词抽取，不是字位分词遗漏。","基础假设 3：不成立；必须消除任意见证解析选择带来的不确定性。","基础假设 4：部分成立；七字段是可用骨架，但不足以保证跨步骤稳定机器消费。","基础假设 5：成立；除 2b 日期外，其余第 0 步修订具有明确的机械核对目标。","“ID 字符集与属性编码”和“Node 侧 DOM 解析”可以留到实施阶段，但应在第 2 步接口/schema 实现及第 4b 步渲染接线开始前分别冻结具体编码函数、解析适配边界和失败 fixture，不能拖到接线完成后再定义。"],"risks":["任意见证对导致浏览器与 Node 报错位置不一致，直接违反本轮试图修复的确定性契约。","未拍板的里程碑 2b 日期会使第 0 步无法验收，并阻断依赖第 0 步的第 4a 步。","若 W4 特征测试对错误去重，重构可能减少或增加重复诊断而仍被判等价。","审计输出缺少稳定枚举和记录身份时，第 5、7、9 步可能各自实现不同的解析约定。"],"confidence":"high","notes_for_claude_code":"建议先只修改歧义定位定义及其三解 fixture，再送一次极小范围复核；这是当前唯一技术性 high。2b 截止日是外部决策门槛：不必阻止第 1–3 步准备工作，但在日期写入前不要宣称第 0 步完成或整份方案正式定稿。"}