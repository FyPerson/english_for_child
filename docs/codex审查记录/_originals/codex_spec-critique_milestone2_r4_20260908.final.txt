{"task_type":"spec-critique","mode":"advice-only","summary":"v1.2 已修复三审的大部分问题，但仍有 2 个阻断级契约缺口：测评最小路由遗漏规范矩阵中的禁止项，着色接口无法把 `W[word].segments` 传给分词器。基础假设 3 可作确定判定：不成立，模板和共享渲染代码仍存在矩阵未明确覆盖的 `SOUNDS[].L`、硬编码字位串及按表面值传递身份等消费点。","issues":[{"severity":"critical","category":"contradiction","location":"实施方案 §0.3、§4 第 5 步、§5「测评路由」；交接规范 §4.3 assessmentMode 契约矩阵","problem":"方案称 weekly 周“只允许 RESERVED”，但精确禁止集合只列 `RESERVED_RETEST`、`PROBE_A`、`PROBE_B`、`GLOBAL_RESERVED`，遗漏规范明确禁止的 `ASSESS_TEXT`、`ANNUAL_DECODING`、`retest` 块和 `probe` 块。因此按方案实现后，weekly 周仍可能携带测评短文、年度池或测评块而通过。","suggestion":"把最小路由写成完整矩阵子集：weekly 必需且仅允许 `RESERVED`；明确禁止其余所有模式专属常量及 `retest`、`probe` 块。为每个禁止项至少提供参数化失败用例，不要只测“任一现有四常量”。"},{"severity":"critical","category":"missing-constraint","location":"实施方案 §3.2、§3.3、§3.4","problem":"`segmentWord` 支持 `explicitSegments`，但三个上层着色接口只有 `(word/text, sounds)`，没有 `W`、segments 或解析回调参数。遇到 `oo_short`/`oo_long` 时，严格着色和句子着色无法消费 `W[word].segments`，会把规范要求的可消歧数据重新变成歧义错误；所谓“本次冻结消费接口”尚未真正完成。","suggestion":"为所有会拆词的上层入口定义统一解析上下文，例如 `{sounds, segmentsOf(word)}` 或 `{sounds, lexicon}`；`colorStrictWord` 必须读取显式 segments，plain/rich 路径中的每个 token 也必须能查询对应词条。补充同形多音词在 strict、plain、rich 三条路径均正确着色的用例。"},{"severity":"high","category":"missing-constraint","location":"实施方案 §2.2「显示 / ARIA」「机器身份 / 外部键」；render-blocks.js 的 sound、tileHTML、initFlash；四个模板的首页墙","problem":"迁移矩阵没有明确登记 `SOUNDS[].L` 这一套既有显示来源。实际代码中 sound 块把 `SOUNDS[f].L` 传给 `tileHTML`，flash 显示 `s.L`，首页及多个 ARIA 又直接使用 ID。若 `L`、ID、`grapheme` 不再相同，前者会把字形误当查表 ID，后者会泄漏稳定 ID。笼统写“直接显示 ID”不足以覆盖这条独立数据流。","suggestion":"在矩阵增加“旧显示字段 L”一行，逐项列出 `forms.map(...SOUNDS[f].L...)`、flash 的 `s.L`、首页墙、G1 目标、G4/G5 槽位与 ARIA。规定 `tileHTML` 接收 ID、内部用 `graphemeLabel` 显示，禁止调用方先转成 `L`；同时明确 `L` 是删除、兼容别名还是与 grapheme 强制相等。"},{"severity":"high","category":"edge-case","location":"实施方案 §2.4、§4 第 7 步","problem":"墙顺序来源虽不构成逻辑循环，但尚未定义在“当前被校验文件”和历史仓库周同时存在时如何取数，也未处理别名键可能已经出现在 `newPatterns` 中的重复插入。单文件 fixture、历史缺周、当前周覆盖仓库同周、`displayOnWall:false` 和别名本体跨周等情况会产生不确定结果。","suggestion":"定义纯函数输入为按周排序的规范化周记录；当前输入替换同周仓库记录，不得重复计入。要求周号连续且唯一；按 `newPatterns` 追加并去重时将重复视为错误。只有不在 `newPatterns` 中的派生别名才按明确元数据插入，不能仅靠 `Object.assign` 语法猜测。第 7 步启用校验时同时迁入 W1–W4 的 `newPatterns` 数组。"},{"severity":"high","category":"contradiction","location":"实施方案 §4 第 5 步、§5「测评路由」","problem":"第 5 步只声明给四周补 `assessmentMode`，但新的 weekly 路径还会首次强制执行“RESERVED 必须在 W”及三个字位等检查。现行 `check_data.js` 对 W1–W3 明确存在 `META.week >= 4` 豁免，说明历史数据未被证明满足新要求。若不满足，第 5 步无法按现有范围独立全绿。","suggestion":"把第 5 步范围改为“补 assessmentMode，并修复由首次启用 weekly 契约暴露的 W1–W3 数据缺口”，列明允许修改的常量；或者在第 3 步先做只读审计并把结果写成第 5 步前置条件。不得仅以新增正例 fixture 代替真实四周数据回归。"},{"severity":"medium","category":"ambiguity","location":"实施方案 §3.5、§3.8、§4 第 4 与第 8 步","problem":"兼容层按“字段类型与来源上下文”决定旧格式，但没有给来源上下文一个机器可判定的标志；HTML 兜底最终又允许“支持或拒绝二选一”，导致第 8 步的完成条件不是唯一的。调用方数量核对也不能证明没有新的字符串构造或字段别名。","suggestion":"在方案中拍板唯一行为：建议第 8 步拒绝缺 META 的旧 HTML，并使用固定错误码。第 4 步给旧入口显式 schemaVersion 或适配器函数，不允许普通消费者自行猜类型；第 8 步以字段级负例和全仓字符串构造清单验收，而不只依据 7 个 loadData 调用方。"},{"severity":"medium","category":"edge-case","location":"实施方案 §3.3","problem":"富文本规则写“危险实体”但没有定义实体解码后的校验顺序。若先检查源码后由 DOMParser 解码，实体可能改变文本或属性语义；浏览器 DOMParser 与 Node 替代实现的序列化结果也未规定是否要求逐字节一致。","suggestion":"规定先解析、再检查解析后的元素与属性、最后由受控序列化器输出；拒绝注释、命名空间元素和非白名单节点类型。测试比较规范化 DOM 语义而非浏览器间逐字节序列化，并加入实体编码属性、嵌套标签和畸形标签用例。"},{"severity":"medium","category":"missing-constraint","location":"实施方案 §2.2、四个 weekNN.template.html 的首页与打印路径","problem":"模板仍有多处硬编码教学字位或按字符含义生成的展示，例如品牌文字、首页说明、示例墙字面串以及打印路径中的 `colorWord`。其中部分可能是静态文案而无需数据驱动，但方案没有逐项声明保留理由，42 个 `colorWord` 清单也不能覆盖不经过 colorWord 的硬编码字位展示。","suggestion":"除 42 个着色调用点清单外，再产出“模板内字位展示来源清单”，逐项标记为数据驱动、刻意静态文案或必须迁移。至少覆盖四个首页 hero、品牌周标题、墙、声音块、flash、打印小书和 ARIA。"},{"severity":"low","category":"readability","location":"实施方案章节顺序","problem":"正文先出现 §0.3 后出现 §0.2，且 §3 缺少 §3.6。虽然不影响代码语义，但在十步实施和多轮审查背景下容易造成引用误读。","suggestion":"按编号重排为 §0.1、§0.2、§0.3，并补齐或重编号 §3.6–§3.8；重新运行步骤引用和章节锚点核对。"}],"recommendations":["基础假设逐条结论：1 部分成立；跳过单一基线作业可实现，但第 5 步真实历史数据是否能全绿尚未证明。2 部分成立；不存在逻辑循环，但跨周数据装载、同周替换和别名插入规则未闭合。3 不成立；仍遗漏 `SOUNDS[].L` 数据流及不经过 colorWord 的硬编码字位展示。4 不成立；三个接口不能传递 `W[word].segments`，且 42 个调用点清单尚未实际产出。5 不成立；这些回归未覆盖 weekly 禁止 `ASSESS_TEXT`、`ANNUAL_DECODING` 和块级约束，也未证明真实 W1–W3 数据满足新检查。6 证据不足；7 个调用方只覆盖装载入口，不能证明字段消费者和字符串重建已清零。7 不成立；第 5 步依赖历史数据修复、第 7 步依赖跨周 newPatterns 装载、第 9 步还依赖受版本控制派生物先提交，表中未全部写成前置条件。","第 1 步的开关应在构造 jobs 后仅过滤名称严格等于 `single-file baseline` 的作业；不要修改失败 break 集合，也不要跳过 `baseline contract`。单测应验证后续作业实际执行，而不只是最终退出码。","三审 10 条落实判定：C-1 已修；H-2 已修；H-3 部分修；H-4 已修；H-5 部分修；M-6 已修；M-7 已修；M-8 部分修；M-9 部分修；L-10 已修。","将第 3 步扩为迁移前审计门：除差分结果外，输出真实 W1–W4 的 RESERVED/W、newPatterns、墙顺序、旧格式入口和所有显示字段消费清单，后续步骤以该审计结果作为明确前置条件。"],"risks":["weekly 最小路由若按当前文字实现，会允许本应禁止的测评内容进入普通周。","同形多音出现后，渲染层可能无法使用显式 segments，导致合法生产数据在浏览器中失败。","把 `L` 或 grapheme 当 ID 传递会在单字母阶段保持假绿，到稳定 ID 与字形分离时集中爆发。","墙校验若直接读取仓库历史，独立 fixture 可能受工作树现状影响而不可复现。","W1–W3 首次进入 weekly 路径可能迫使第 5 步临时修改历史词表，使“每步独立提交”的范围扩大。"],"confidence":"high","notes_for_claude_code":"本审查仅依据提供的文档与代码文本，未执行命令、测试或修改文件。建议优先修复两个 critical，再把迁移矩阵补上 `SOUNDS[].L`/grapheme/ID 的完整调用链；基础假设 3 应按“不成立”处理，而不是继续视为已覆盖。"}