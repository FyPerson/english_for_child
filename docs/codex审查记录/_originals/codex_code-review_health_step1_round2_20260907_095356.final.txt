{"confidence":"high","issues":[{"category":"other","location":"H-1：baseline.validate()、check()","problem":"状态：已修。顶层键、固定四周、七类声明、键唯一性、MIME 白名单、摘要格式及 CELEBRATE_NAT 约束均已加入，check() 也不会再以空覆盖成功。","severity":"low","suggestion":"保留现有严格校验；建议补充对 declarationOrder 类型的显式检查，避免畸形值触发未捕获的 TypeError。"},{"category":"contradiction","location":"H-2：baseline.create()、模块说明","problem":"状态：部分修。create() 已在构建后复查 git 状态并拒绝脏树，但仍直接调用默认构建器并读写仓库根目录，没有落实“临时目录中显式构建 single-file”的核心隔离契约；当前只是按文档声明的第 0 期过渡实现。","severity":"medium","suggestion":"在第 2 步及第 1b 期按计划改为临时 output-dir 和显式 --single-file；在过渡期应在 CI/方案中明确默认构建形态切换前必须完成该替换。"},{"category":"other","location":"M-3：media_declarations.js 的 scanObject()","problem":"状态：已修。求值前逐行静态限制普通属性、字符串值、注释和键序，并拒绝重复、计算键、展开、访问器、嵌套对象、数字键，多项测试覆盖了这些情形。","severity":"low","suggestion":"继续以真实第 1 至 4 周产物做回归；当前正则仍会拒绝带转义字符或 Unicode 裸标识符的合法 JavaScript 写法，若未来产物采用这些写法需升级扫描规则。"},{"category":"security","location":"M-4：media_declarations.js 的 VM 求值","problem":"状态：部分修。静态扫描基本消除了 getter、表达式和展开等代码执行面，Python 子进程也有 120 秒超时；但实现仍依赖 vm.runInNewContext()，并未真正改为 AST 解析，直接调用该 JS 工具时也没有整体超时。","severity":"medium","suggestion":"后续改为 AST 纯数据提取；在过渡期保持静态语法白名单，并为独立 CLI 调用增加进程级超时/资源限制。"},{"category":"other","location":"M-5：baseline.analyze()、.gitattributes","problem":"状态：已修（依据任务说明）。analyze() 明确拒绝 CR，任务说明称仓库已配置 eol=lf；但 .gitattributes 内容不在提供材料中，无法独立确认该契约覆盖所有相关文件。","severity":"low","suggestion":"复审时读取并确认 .gitattributes 对 HTML、模板、JS、JSON 的覆盖，且构建检查与基线都按字节语义执行。"},{"category":"other","location":"M-6：tests/unit/test_baseline.py","problem":"状态：已修。测试独立计算文件摘要，按精确占位符计算骨架摘要，并断言声明顺序等于指定顺序。","severity":"low","suggestion":"保持这些独立期望值，避免测试复用被测实现中的常量或转换逻辑。"},{"category":"other","location":"M-7：创建、只读和失败路径测试","problem":"状态：已修。已有成功创建、force 覆盖、生成字段、失败 check 保持 fixture 字节和 mtime、仓库 fixture 缺失即失败及两个入口只读测试。","severity":"low","suggestion":"补充一次真实构建器的集成测试，以覆盖 mock 未覆盖的文件变更行为。"},{"category":"edge-case","location":"M-8：baseline.check(week_paths=...)","problem":"状态：已修。注入路径分支要求四周映射完整，真实路径分支也要求 project.json 列出四周。当前额外 week_paths 键会被静默忽略，和“完整映射”语义略不一致。","severity":"low","suggestion":"可选择拒绝多余键，或在 docstring 中明确只消费 COVERED_WEEKS，避免调用者误以为额外路径会被检查。"},{"category":"readability","location":"L-9：baseline.differences()","problem":"状态：已修。现在分别报告新增、删除、换序位置、MIME、字节摘要变化，并输出声明顺序的期望值和实际值。","severity":"low","suggestion":"无需进一步调整；可在文档中固定这些错误信息作为 CI 诊断接口。"},{"category":"ambiguity","location":"L-10：baseline.py、project.py 参数解析","problem":"状态：已修。两个入口均在 --force 非 --create 时明确报错，且测试覆盖。","severity":"low","suggestion":"保持两入口帮助文本和错误措辞同步。"},{"category":"missing-constraint","location":"baseline.check()、differences()","problem":"创建的 fixture 包含 course.sha256，但 check() 从未重新计算或比较 course.html；课程文件发生变化时，基线检查仍可能报告成功。","severity":"medium","suggestion":"在 check() 中根据 load_config()['entry'] 计算课程文件摘要，并将其与 data['course']['sha256'] 比较；差异信息应明确指出 course.sha256。"},{"category":"edge-case","location":"baseline.validate()","problem":"对 declarationOrder 直接调用 sorted()，未先确认其为列表且元素为字符串。某些畸形 JSON（例如包含不可比较元素）会抛出 TypeError traceback，而不是统一的 baseline fixture invalid 错误。","severity":"low","suggestion":"先检查 isinstance(value, list) 及 all(isinstance(x, str))，再检查长度和排列；对其他字段也采用统一的失败路径。"},{"category":"edge-case","location":"media_declarations.js：DATA_URI、push()","problem":"DATA_URI 只检查字符集合，不检查严格 Base64 填充和规范长度；Node Buffer.from() 对部分畸形字符串会宽松解码，可能把无效数据 URI 当作有效媒体并生成摘要。","severity":"low","suggestion":"增加规范 Base64 校验（长度、填充位置及必要时重新编码一致性），再进行解码。"},{"category":"edge-case","location":"tests/unit/test_baseline.py：make_fixture()","problem":"test_create_refuses_when_the_build_dirties_the_tree 使用 mock 的 build() 和预设 git side_effect，证明的是 create() 对返回状态的响应，不证明真实 build_lessons.py 实际会产生并被检测到的改动。","severity":"medium","suggestion":"保留该单元测试，同时增加真实临时项目或集成构建测试，让 build() 实际写入一个受控文件后验证创建被拒绝且 fixture 未写入。"}],"mode":"advice-only","notes_for_claude_code":"本复审仅基于提供的 diff、两个文件全文和上一轮结论，未读取实际 fixture、四个周文件、.gitattributes、构建器或 CI。上一轮 10 项中 H-2、M-4 属于部分修，其余按材料可判定为已修；未发现需要阻断放行的 high，但课程摘要未参与 check 是新增的 medium 缺口。","recommendations":["将 H-2 的临时目录和显式 single-file 构建按计划里程碑落实，不要让过渡实现长期存在。","把 course.sha256 纳入 check 差异比较，并补一条课程文件变更的失败测试。","补充真实构建器集成测试，同时保留现有 mock 单元测试。","在 validate() 和媒体数据 URI 校验中增加类型及严格 Base64 检查。"],"risks":["默认构建形态切换前若未完成 H-2 后续改造，基线可能读取错误形态或污染仓库工作树。","课程文件变化目前不会触发基线失败。","直接调用 media_declarations.js 时仍存在 VM 执行和缺少进程级超时的残余风险。","未核实 .gitattributes 与真实产物格式，静态扫描规则可能在未来合法语法变化时误拒。"],"summary":"上一轮 10 项中 8 项已修、H-2 与 M-4 部分修；当前未见 high 严重度阻断项，可有条件放行。新增最具体缺口是 fixture 中记录的 course.sha256 未在 check() 中比较，另有 mock 集成覆盖不足及少量畸形输入处理问题。","task_type":"code-review"}