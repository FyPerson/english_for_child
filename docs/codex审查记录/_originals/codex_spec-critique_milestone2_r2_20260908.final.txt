{"task_type":"spec-critique","mode":"advice-only","summary":"首审 13 条在文档层面均有落点，但仍存在 3 个会阻断实施的高风险矛盾：延后 DATA-ASSESS-01 与现有 W5 校验器不兼容、ai fixture 的“墙显示 1 块”违反墙全集模型、步骤 3 在旧数据上无法同时启用新墙校验并保持全绿。分词歧义定义也只处理局部同长度冲突，尚未满足规范要求的全局“多解即失败”。","issues":[{"severity":"critical","category":"contradiction","location":"实施方案 §0.1、§4 第4步；assessment_contract.js validateAssessment","problem":"方案把 DATA-ASSESS-01 延后到里程碑 2b，却要求里程碑 2 的 W5/ai fixture 跑通全链路。现有 validateAssessment 仅对 META.week<4 跳过，W5 会立即要求 RESERVED_RETEST、PROBE_A、PROBE_B、GLOBAL_RESERVED 等数组，与规范矩阵中 weekly 只允许 RESERVED 直接冲突。因此在不先改 assessment_contract.js 的情况下，W5 fixture 和第3至第6步的 check --quick 不能按方案通过。","suggestion":"明确选择一种过渡方案：要么把 assessmentMode 最小路由修复保留在里程碑2，只把完整矩阵及全部 fixture 延后到2b；要么让里程碑2的 ai fixture 使用不进入 validateAssessment 的专用测试入口，并明确它不算“全链路 check_data”。若坚持完整全链路 W5 fixture，则 DATA-ASSESS-01 不能整体拆出。"},{"severity":"critical","category":"contradiction","location":"实施方案 §4 第4步、§5“新能力”；规范 §3“点亮墙与首教日：唯一模型”","problem":"ai fixture 同时要求分解 rain 为 r/ai/n，又要求“墙显示1块”。若 SOUNDS 至少包含 r、ai、n，规范要求 wallLetters 等于全部可上墙的 SOUNDS 键，墙至少应显示3块；若 SOUNDS 只有 ai，则 rain 无法分词。这里把“墙全集”与“本周新点亮1块”混为一谈。","suggestion":"把验收改成“墙显示全部可上墙字位，且仅 ai 具有本周新点亮态”。fixture 至少声明 r、ai、n；wallLetters 应与其可展示 SOUNDS 键一致，newPatterns 为 ['ai']，FIRST_TEACH_DAY 的键也仅为 ai。"},{"severity":"high","category":"missing-constraint","location":"实施方案 §3.2 segmentWord 契约","problem":"“最长匹配优先；同长度多解抛歧义”只覆盖当前位置存在多个同长度候选的局部冲突，不能检测完整单词的多种合法分词。例如存在 grapheme a、ab、bc、c 时，abc 可分为 ab+c 或 a+bc；首项长度不同，但全局仍有两解。规范要求的是“出现多解且无 segments 即失败”，不是只检查同长度候选。","suggestion":"把契约改为：求完整串的所有可行分词，0解报未知，1解返回，多于1解报歧义；最长匹配只能用于剪枝或在已证明全局唯一后选择，不能消除合法的较短起始路径。增加“不同首段长度但存在两种完整解析”的失败 fixture。"},{"severity":"high","category":"contradiction","location":"实施方案 §3.5、§4 第3步与第5步；check_data.js ⑤","problem":"第3步要求消费者和校验器切换到新字位及墙语义，但数据仍保持旧字符串和旧墙语义。新 DATA-WALL-01 若在此时启用，W2/W3 的 wallLetters 仍分别只有7和6项，不等于 SOUNDS 全集，且当前 FIRST_TEACH_DAY 互检逻辑也不符合新模型，因此 check --quick 不可能同时全绿。","suggestion":"将墙消费者的双读与墙契约的严格启用拆开：第3步只接入兼容读取和新渲染接口，暂保留旧模型校验；第5步迁移数据时，同一提交启用 DATA-WALL-01 新校验及正反 fixture。更稳妥的做法是给 schemaVersion，由版本显式决定旧、新墙契约，迁移完成后删除旧版本。"},{"severity":"high","category":"missing-constraint","location":"实施方案 §3.2 explicitSegments","problem":"显式 segments 目前只要求 surfaceOf(explicitSegments) 等于规范化单词，没有明确校验数组形状、ID 是否存在、ID 是否属于本周累计 SOUNDS、空数组、displayOnWall:false 是否影响可分词资格，以及错误的 offset/candidates 如何报告。不存在的 ID 可能退化为 undefined 或产生难定位异常。","suggestion":"规定 explicitSegments 必须为非空字符串数组，每个 ID 必须是 sounds 的自有键；surfaceOf、graphemeLabel、soundType 遇到未知 ID 均抛统一结构化错误。明确 displayOnWall 只影响墙展示，不影响分词资格。为未知ID、非数组、空数组、非字符串元素分别加失败测试。"},{"severity":"high","category":"edge-case","location":"实施方案 §3.3 colorTokenizedText；render-blocks.js colorWord 调用点","problem":"“不能分的原样输出”未定义 HTML 转义和已有允许标记的处理边界。函数结果会进入 innerHTML；如果把数据字符串原样拼回，可能引入标签或实体解释差异。另一方面，直接转义又可能破坏规范允许的 <b>、<span class=\"en\">、<br>。","suggestion":"把纯文本着色与受控富文本渲染分成两个接口：纯文本必须逐段 HTML 转义；允许富文本的字段先通过既定白名单解析，再只对文本节点着色。禁止用正则直接在整段 HTML 上分词，并增加尖括号、&、引号及允许标签 fixture。"},{"severity":"high","category":"missing-constraint","location":"实施方案 §4 第2步“对四周所有被消费的词”","problem":"差分测试没有定义“所有被消费的词”的权威枚举入口。现有 check_data.js 明确漏掉 wordforge、BOOK.pages 等位置；若差分测试复用它的 usedWords 收集逻辑，仍会在声称全覆盖时漏测。五项差分也没有覆盖槽位状态、surfaceOf 查表、显示 grapheme 和句子着色。","suggestion":"在方案中指定共享的词消费抽取器或逐场景清单作为测试语料来源，并断言各生产入口均被覆盖。把旧数据等价测试与新能力测试分开：旧数据差分覆盖所有消费位置；合成 fixture 覆盖槽位、表面串、显示、歧义及句子降级。"},{"severity":"high","category":"contradiction","location":"实施方案 §4“每步 check --quick 全绿”、第7步基线重取；baseline.py","problem":"消费者接线和共享解析器进入单文件产物后，skeletonSha256 必然变化。如果 check --quick 包含 baseline --check，则第3步开始直到第7步重取基线都会失败；如果 quick 不包含基线，方案没有明确说明，当前“每步全绿”表述不可验证。","suggestion":"明确列出 check --quick 是否运行基线作业。若包含，应在迁移分支对预期基线差异采用独立非放行审计，或调整提交策略；若不包含，改写为“每步 quick 全绿且基线差异被记录，第7步后 full check 全绿”。"},{"severity":"medium","category":"missing-constraint","location":"实施方案 §2.4、§4 第3至5步；check_data.js ⑤","problem":"方案指出旧墙互检失效，却没有把新 DATA-WALL-01 的精确断言写入设计：wallLetters 应等于按教学顺序过滤 displayOnWall!==false 的 SOUNDS 键；FIRST_TEACH_DAY 键应与 newPatterns 完全相等；每个首教日还需落到对应 DAYS[].sounds。仅替换 includes 检查不足以保证顺序、缺项、额外项和重复项。","suggestion":"在 §3 或第5步验收中写出上述三条集合及顺序断言，并增加缺项、额外项、乱序、重复项、displayOnWall:false、FIRST_TEACH_DAY 与 newPatterns 不等的失败 fixture。"},{"severity":"medium","category":"other","location":"实施方案 §3.1“三层概念”","problem":"ID、grapheme、表面串覆盖了主要数据转换，但没有明确覆盖音频解析键、持久状态键、DOM data-* 传输及 CSS 标识符。这些位置若误用 grapheme 或表面串，同形多音会共享确认状态，特殊 ID 也可能形成非法或难选择的 CSS 类。","suggestion":"补一条“机器身份与外部键”规则：确认状态、槽位、DOM 数据和业务比较使用稳定 ID；词级状态继续使用规范化表面串；音频必须经 audioKey 解析；CSS 类不得直接拼接未经转义的 ID。将这些消费方式列入迁移矩阵。"},{"severity":"medium","category":"edge-case","location":"实施方案 §3.5 normalizeIdList","problem":"normalizeIdList(value) 对任意字符串按单字符展开，无法区分合法旧格式、误传的普通单词和已经含双字母的新字符串。兼容期可能把错误数据静默解释成另一组合法 ID，与“反解析侧能区分新旧格式”的要求尚无具体机制。","suggestion":"不要靠值内容猜版本；由字段类型加 schemaVersion/来源上下文决定解析方式。旧字符串展开后还应逐项验证 ID 存在。第6步除单元测试外，对 HTML 反解析和数据层JS两条入口分别增加拒绝旧格式测试。"},{"severity":"medium","category":"performance","location":"实施方案 §3.2、§6 体量风险","problem":"若按正确的全局多解检测实现，朴素回溯在字位前缀高度重叠时可能指数增长；同时错误文本和兼容代码会被复制进每个单文件产物。方案只允许底层“可以”预编译，却未给复杂度或缓存边界。","suggestion":"要求使用按 offset 记忆化的动态规划或 trie，最多保留“0、唯一、多个”三态及有限候选用于报错，避免枚举全部解。浏览器构建可使用精简错误码，Node 校验器再补充详细位置说明。"},{"severity":"medium","category":"readability","location":"实施方案 §4 第5、6步与 §5“体量”","problem":"第5步记录的产物字节增量不是最终值，因为第6步删除字符串兼容分支仍会改变产物和哈希。该记录可用于定位迁移成本，但不能作为最终容量结论。","suggestion":"保留逐提交增量，同时在第6步完成后重新记录最终五个产物大小、相对里程碑前净增量和剩余余量；基线只基于该最终形态重取。"},{"severity":"low","category":"readability","location":"实施方案 §3.2 导出清单","problem":"文档同时称 normalizeIdList 为六个长期配套导出，又在第6步“删兼容层”。容易被理解为删除整个函数，而实际应只删除字符串分支，数组校验/规范化接口可能仍被消费者依赖。","suggestion":"明确第6步是将 normalizeIdList 收紧为 assertIdList，或保留函数名但只接受数组；列出调用方是否需要迁移，避免实现者误删公共入口。"}],"recommendations":["先解决两个阻断性矛盾：确定 DATA-ASSESS-01 的过渡边界，并把 ai fixture 的墙验收改为“全集展示、仅 ai 新点亮”。","把分词器定义提升为全局唯一解析契约，而不是局部最长匹配；用动态规划验证零解、唯一解和多解。","重新划分步骤3与步骤5：兼容消费者可以先接线，但新墙契约只能与墙数据迁移同一提交启用。","明确每一步实际运行的检查集合，区分 quick、浏览器回归、基线差异审计和最终 full check。","补充词消费语料的权威枚举及机器身份类消费场景，避免迁移矩阵再次因搜索模式或手工清单漏项。"],"risks":["W5 在现有 assessment_contract.js 下会因缺少非 weekly 测评常量而失败，直接否定“拆出后不阻塞装配”的假设。","若按当前局部最长匹配实现，同形多音或前缀重叠数据可能被确定性地分错，而不是按规范报歧义。","若第3步提前启用墙唯一模型，四周旧数据会使 quick 检查变红；若不启用又可能出现文档宣称已完成而契约仍未生效。","富文本着色若直接操作 innerHTML，可能造成注入风险或破坏允许标签。","未定义最终体量复测时，第5步的容量数据会被第6步代码删除所改变。"],"confidence":"high","notes_for_claude_code":"首审13条落实判定：C-1 已修；C-2 部分修（文档切分已写，但现有W5测评校验形成实施阻断）；H-3 部分修（接口清楚，但歧义仅按局部同长度定义不足）；H-4 部分修（主要调用点已纳入，富文本边界未定）；H-5 部分修（改为差分正确，但消费词全集来源和差分维度不足）；H-6 部分修（纳入检测与fixture，但全局多解算法未写对）；H-7 部分修（双读生命周期明确，但缺schema/version判别及反解析细则）；H-8 已修；M-9 已修；M-10 部分修（需第6步后最终净增量）；M-11 已修；M-12 部分修（验收目标明确，但共享文件的浏览器/Node封装方式未指定）；L-13 部分修（矩阵显著改善，但仍漏机器身份、audioKey、DOM/CSS及富文本边界）。七条基础假设判定：1 成立——W2应为13、W3应为19，但“本周新点亮”只能看newPatterns；2 不成立——现有assessment_contract.js会阻塞W5；3 证据不足——三层覆盖核心转换，但未覆盖音频、持久状态、DOM/CSS等身份消费；4 证据不足——可删除，但需schema判别、两条反解析入口测试及调用方清单；5 不成立——按当前“墙显示1块”要求无法同时分解rain并满足墙全集模型，且W5测评校验也会阻断；6 不成立——第3步新墙校验与旧数据冲突，且若quick含基线，第3至6步也无法全绿；7 不成立——至少还缺全局分词歧义、富文本安全、audioKey、持久状态键、DOM/CSS身份传输等消费方式。更深核对仍建议补看四个weekNN.template.html中墙渲染片段，以及project.py/run_checks对check --quick是否包含baseline的调度段。"}