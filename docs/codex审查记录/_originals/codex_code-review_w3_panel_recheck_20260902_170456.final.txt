{"confidence":"medium","issues":[{"category":"edge-case","location":"fix_week02.diff 中 bindLongPress / .parent-panel__danger","problem":"触摸事件改为 passive 且不阻止默认行为后，contextmenu 拦截只能处理已派发的菜单事件，不能保证阻止 Windows 触屏长按期间的文本选择、系统调用提示或放大镜行为。入口 `.parent-gate` 有 `user-select:none`，但面板重置按钮 `.parent-panel__danger` 没有同等约束。","severity":"medium","suggestion":"为所有 bindLongPress 目标统一加 `user-select:none`、`-webkit-user-select:none`、`-webkit-touch-callout:none`；必要时补 `selectstart` 取消处理。保留 passive touchmove 以允许滚动，并在真实 Windows 触屏浏览器验证长按 1.5 秒行为。"},{"category":"security","location":"fix_week02.diff 中 openParentPanel()","problem":"面板模板把 `state.startDate` 直接插入 `value=\"...\"` 的 innerHTML。若本地存储被手工篡改或被同源脚本写入恶意字符串，可能造成属性注入甚至 XSS。当前 diff 未证明加载阶段始终把该字段限制为严格日期格式。","severity":"medium","suggestion":"先以固定模板创建 input，再通过 `input.value = isValidDateStr(state.startDate) ? state.startDate : ''` 赋值；或对 HTML 属性做可靠转义，并在状态加载处统一拒绝非 `YYYY-MM-DD` 值。"},{"category":"contradiction","location":"插画生成提示词_第三周_20260902_v1.0.md §0.6 与 §4 fog.png","problem":"§0.6 的关系图适用清单没有 `fog`，但 `fog.png` 明确要求同时出现松树和不透明雾块，违反 §1 通用前缀的“Single subject”。这会让出图模型收到互相冲突的约束。","severity":"medium","suggestion":"把 `fog` 加入 §0.6 适用清单，并明确树与雾是一个紧凑图标中的主体与物件；或者把提示词改成真正的单一雾形。"},{"category":"accessibility","location":"fix_week02.diff 中 bindLongPress() 的 onMouseDown()","problem":"对 mousedown 调用 `preventDefault()` 通常会阻止按钮获得浏览器默认焦点。用户鼠标按过入口后再尝试直接用键盘操作，可能无法进入键盘焦点路径；现有冒烟测试是显式 `focus()`，覆盖不到这一点。","severity":"medium","suggestion":"验证目标浏览器行为；若确实失焦，在取消默认行为后显式调用 `el.focus()`，或改用不会阻断焦点获取的事件策略，同时继续抑制拖拽和文本选择。"},{"category":"edge-case","location":"renderHome() / closeParentPanel() 的生命周期","problem":"closeParentPanel 覆盖了关闭、设置日期、重开面板三条显式路径，但 diff 未展示所有会重绘或替换 footer 的入口。若其他路径直接替换面板 DOM，`panel._resetCancel` 可能未调用；实现只能依赖 laterOnce 被 pageTimers 清理。","severity":"medium","suggestion":"审计所有 renderHome、路由切换和整页重绘入口，统一在替换 footer 前取消面板长按；或把面板取消函数注册到明确的页面生命周期清理点，避免只依赖隐式计时器清理。"},{"category":"readability","location":"fix_week02.diff 中页脚 foot__credits 标记","problem":"原来的 `<p class=\"foot__credits\">` 改成了包含多个块级元素的 `<div class=\"foot__credits\">`。若现有 CSS 使用 `p.foot__credits`、`footer > p` 或基于段落默认 margin 的选择器，页脚样式会回归。","severity":"low","suggestion":"全文检查针对 `p.foot__credits` 和 footer 直接子元素的选择器；必要时补 class 级样式并显式设置 margin/display，或调整 DOM 结构避免依赖标签选择器。"},{"category":"edge-case","location":"tools/week-checks/smoke_parent_panel.py 的 Storage.prototype.removeItem 模拟","problem":"临时 monkeypatch 没有放在 `try/finally` 中。测试中途异常时原型方法不会恢复，虽通常随浏览器进程结束，但会污染同一进程后续用例或调试状态。","severity":"low","suggestion":"将替换、失败路径断言和恢复放入 `try/finally`，并在 finally 中无条件恢复原方法、删除临时全局变量。"}],"mode":"advice-only","notes_for_claude_code":"上一轮核对表：H-1：部分成立，主键过滤、位移取消、键盘路径和 contextmenu 已补，但触摸原生选择/调用提示及重置按钮的 CSS 仍需闭环；H-2：成立，同步 removeItem 后读回确认，正常情况下 location.reload 不会给其他任务插入机会；H-3：成立，独立生成数、落盘复制和逐字节检查已对齐；H-4：部分成立，关系场景覆盖条款有效，但 fog 未列入适用清单；M-1：基本成立，显式重开、关闭和设日期路径都保存并调用取消函数，仍需审计其他重绘入口；M-2：基本成立，Enter/空格、repeat、keyup、blur 和 aria-expanded 已覆盖，但 mousedown preventDefault 可能损失鼠标后的焦点获取；M-3：基本成立，测试增加了三类输入、右键、滑出、失败删除和导航等待，建议补 finally 与原生 contextmenu/选择行为断言；M-4：成立，gap/fit/leg/fog 提示词的可辨认性约束已具体化；M-5：从文档文字看自洽，实际是否由 embed_assets.py 调用 unify_card_outlines 仍需以脚本实现为准；L-1：成立，面板和失败路径都写明“无法恢复”。\n\n推送门禁：当前不建议直接推送。必须先修：fog 的 §0.6 约束矛盾、所有长按目标的触摸选择/系统行为约束，以及确认 startDate 未经转义插入 innerHTML（若加载清洗已严格保证，也应在代码或测试中明确证明）。可以带着走：foot__credits 标签样式审计、Storage monkeypatch 的 finally 清理，以及对非主战场触屏行为的额外实机验证。","recommendations":["上一轮 H-1：部分成立，输入过滤和位移取消正确，但触摸原生行为仍需验证并补齐所有长按目标的不可选择样式。","上一轮 H-2：成立；removeItem 后 getItem 判空足以确认同步删除，未见明确竞态。","上一轮 H-3：成立；独立生成、复制落盘、逐字节比对和数量检查自洽。","上一轮 H-4：部分成立；关系场景覆盖有效，但 fog 未纳入覆盖清单。","上一轮 M-1：基本成立；显式面板替换路径已调用取消函数，需继续核对所有重绘入口。","上一轮 M-2：基本成立；键盘长按和 ARIA 已补，需处理 mousedown preventDefault 对焦点的影响。","上一轮 M-3：基本成立；冒烟测试覆盖明显扩大，但 monkeypatch 应使用 finally，触摸监听最好加直接观测。","上一轮 M-4：成立；四个提示词已提高主体可辨认度。","上一轮 M-5：文档层面成立；需要以 embed_assets.py 实际调用链作最终确认。","上一轮 L-1：成立；不可恢复提示已写入面板和失败反馈。","先补齐触摸长按的 CSS/浏览器验证，再决定是否保留 passive touchstart。","将 startDate 改为 DOM 属性赋值或严格 HTML 转义，并增加篡改 localStorage 的回归用例。","把 fog 加入 §0.6 清单，避免模型同时收到 Single subject 与双元素构图要求。","审计 `p.foot__credits`、`footer > p` 等标签选择器，确认 `<p>` 改 `<div>` 不改变页脚布局。"],"risks":["触摸兜底路径可能出现系统选择或长按调用提示，导致长按确认体验不稳定。","恶意或损坏的本地存储值可能通过 innerHTML 属性插值造成页面注入。","若存在未展示的 footer 重绘入口，面板重置计时器可能在 DOM 脱离后继续运行。","现有触摸冒烟主要验证最终 UI 状态，未直接证明没有被浏览器原生手势抢占。"],"summary":"大部分修法已落地且逻辑方向正确，但 H-1、H-4 和状态值插值仍有可执行的边界问题。修复这些门禁项并补生命周期/样式审计后再推送；其余测试稳健性问题可随后跟进。","task_type":"code-review"}