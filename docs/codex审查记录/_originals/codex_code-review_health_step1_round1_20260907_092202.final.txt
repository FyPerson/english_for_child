{"confidence":"medium","issues":[{"category":"missing-constraint","location":"tools/baseline.py：load()、check()","problem":"基线结构没有被完整校验。一个包含 schemaVersion=1、form=\"single-file\"、weeks={} 的 fixture 会通过 load()；check() 随后把所有配置周列为 not covered、differences() 返回空，并以“BASELINE OK: 0 weeks”成功退出。media 缺少某个本应为空的声明时也可能因 get(decl, []) 而静默通过。这会让基线门禁在 fixture 被截断或误改后完全失效。","severity":"high","suggestion":"增加严格的 fixture schema 校验：至少强制包含 week01.html 至 week04.html；每周必须包含 sha256、skeletonSha256、declarationOrder、media；declarationOrder 必须是 NAMES 的完整排列；media 必须恰好包含七类声明；逐条验证键、MIME、64 位十六进制摘要及键唯一性。若仍允许未来新增周不覆盖，应把“固定要求覆盖的四周”和“项目新增周”明确区分，而不能把所有缺失周都视为可跳过。"},{"category":"contradiction","location":"tools/baseline.py：create()、check()","problem":"实现没有遵守 §3.3 中“显式构建单文件形态到临时目录”的契约。create() 调用当前默认 build_lessons.py 并写仓库根目录，check() 也直接读取根目录产物；FORM 却无条件记录为 single-file。当前默认形态恰好是单文件，因此暂时可用，但默认形态切换为引用形态后命令会失效，也无法保证基线行为与默认构建解耦。构建还可能在最初的干净树检查之后改动受控产物，使 sourceCommit 不再准确描述所取字节。","severity":"high","suggestion":"先为构建器提供 --single-file 和 --output-dir，再让 create()、check() 始终使用 TemporaryDirectory 中的显式单文件产物；不要写仓库根目录。若暂时不能增加这些参数，至少在构建后再次检查 git 状态并拒绝捕获被构建改脏的工作树，同时明确设置这是过渡实现且必须在默认形态切换前替换。"},{"category":"edge-case","location":"tools/validation/media_declarations.js：extract()","problem":"通过 VM 求值后使用 Object.entries() 会静默折叠重复键。例如同一对象中两次声明 cat，只会保留最后一个值，基线无法发现被遮蔽的媒体项；计算属性、展开属性、Symbol 键和非枚举属性同样会绕开“按源码键序完整冻结”的含义。拒绝纯数字键只解决了整数键重排，未解决这些情况。","severity":"medium","suggestion":"使用 JavaScript AST 解析器读取对象字面量，明确只接受普通静态属性和字符串值，并在求值前拒绝重复键、计算属性、展开、访问器和 Symbol。若暂时保留 VM，至少用 Reflect.ownKeys() 和属性描述符拒绝 Symbol、访问器及非枚举属性，但重复键仍必须靠源码解析检测。"},{"category":"security","location":"tools/validation/media_declarations.js：vm.runInNewContext()、Object.entries(value)","problem":"Node 的 vm 不是不可信代码安全边界，声明中的表达式会被执行。尤其对象 getter 在 vm.runInNewContext() 返回后由 Object.entries() 触发，不受该次 2000 毫秒 timeout 约束，可让本地检查或 CI 无限挂起；恶意或误写表达式还可能造成高内存消耗。","severity":"medium","suggestion":"改为静态 AST 提取，不执行声明。过渡期至少拒绝所有访问器和非常规属性，并在独立子进程中执行提取器，配置整体超时和资源限制；不要仅依赖 vm 的脚本执行超时。"},{"category":"edge-case","location":"tools/baseline.py：analyze()；tools/build_lessons.py：--check 分支","problem":"analyze() 对原始字节和骨架保留 CRLF，而 build_lessons.py --check 使用 read_text() 比较，会把 CRLF 规范化为换行符。因此在启用自动换行转换的 Windows checkout 中，生成文件检查可能通过，但基线骨架仍因行尾不同而失败。材料未提供 .gitattributes，无法确认仓库是否已经强制 LF。","severity":"medium","suggestion":"若行尾属于“字节不变”契约，在 .gitattributes 中对 HTML、模板、JS 和 JSON 明确 eol=lf，并让构建检查按字节比较；若行尾不应参与骨架语义，则在 skeletonSha256 前显式规范化并把这一点写入方案。需检查仓库现有 .gitattributes 后决定。"},{"category":"missing-constraint","location":"tests/unit/test_baseline.py：test_order_media_and_hashes()、test_plain_text_change_alters_skeleton_not_media()","problem":"核心哈希测试断言过松。sha256 只断言与 skeletonSha256 不相等，没有与输入文件的独立 SHA-256 对照；skeletonSha256 只验证文本变化会导致变化，没有证明七段声明被替换为规定的精确占位符。错误的常量、删除声明而非替换等实现仍可能通过部分测试。换序测试也只断言顺序“不相等”，未断言结果恰为给定顺序。","severity":"medium","suggestion":"在测试中独立计算原文件 SHA-256；构造使用精确 /*@MEDIA NAME@*/ 替换后的期望骨架并计算摘要，与结果做相等断言；换序测试直接断言 declarationOrder 等于 reversed(NAMES)。"},{"category":"missing-constraint","location":"tests/unit/test_baseline.py：FixtureGuardTests、ReadOnlyBehaviourTests","problem":"没有正向覆盖干净树创建、--force 覆盖、生成 schema/sourceCommit/course 哈希，也没有覆盖失败的 check 仍保持 fixture 字节和 mtime 不变。只读测试调用的是 build_lessons.py 和 baseline.py --check，并未证明 project.py check 的完整接线只读。fixture 缺失时 skipTest 还会让关键契约测试在基线意外丢失时消失，虽然 run_checks 中更早的基线任务通常会失败。","severity":"medium","suggestion":"用临时项目或完整 mock 增加 create 成功与 force 覆盖测试，并校验生成字段；增加故意不匹配时 fixture 仍不变的测试；将完整 project.py check 的只读行为放到不会递归执行自身的独立集成测试中。对仓库必备 fixture 应直接失败，或完全使用测试临时 fixture，不应 skip。"},{"category":"edge-case","location":"tools/baseline.py：check(fixture, week_paths)","problem":"传入 week_paths 时，缺失的映射会静默回退到 ROOT 下真实周文件，使测试可能混用临时样本和工作树产物。当前测试又没有覆盖该注入入口，因此路径隔离是否正确不可证伪。","severity":"medium","suggestion":"只要 week_paths 非空，就要求它完整提供所有待比较基线周，缺项立即报错；或把路径解析封装为显式的构建目录参数。增加包含四周临时路径以及缺失映射失败的测试。"},{"category":"readability","location":"tools/baseline.py：differences()","problem":"差异输出能定位到声明和媒体键，但 keys differ 只显示数量，不列出新增、删除或换序键；declarationOrder 也不显示期望值和实际值，MIME 与字节变化被合并。这会增加人工确认“是否为有意改动”的成本。","severity":"low","suggestion":"分别报告新增键、删除键、首个换序位置、MIME 变化和摘要变化；声明顺序输出期望与实际列表，摘要可显示足够区分的前缀。"},{"category":"ambiguity","location":"tools/baseline.py：main()；tools/project.py：baseline 子命令","problem":"--force 可与 --check 一起传入并被静默忽略，和帮助文本“仅与 --create 配合”不完全一致。","severity":"low","suggestion":"解析参数后若 args.force 且非 args.create，明确非零退出；在 project.py 和 baseline.py 两个入口保持相同行为。"}],"mode":"advice-only","notes_for_claude_code":"偏离项判断：a 对当前四周混合语法有现实必要性，但 VM 方案只有在补上重复键检测及执行安全边界后才适合作为基线基础；b 只有在严格锁定原始四周为必覆盖集合后可接受，否则当前实现能以零覆盖成功；c 提前接入 check 可接受，但会形成“教学代码提交先红、随后单独重取基线才绿”的两提交窗口，需要确认 CI 只要求分支最终头通过并把流程写清；d 因创建要求干净树而拆成工具提交与 fixture 提交是合理的，sourceCommit 指向前一个工具提交也符合取样时的 HEAD。更深复审需要查看完整 fixture、四个实际周文件、.gitattributes、check_data.js、CI 配置及 project.py 的 run() 实现；仅凭 fixture 前 40 行无法确认四周 760 条目是否完整或摘要是否对应真实字节。","recommendations":["先修复 fixture 严格校验和显式临时单文件构建这两个高严重度问题，再复审基线门禁。","把基线覆盖范围定义成明确的不可为空集合，例如固定原始四周；未来周是明确豁免还是必须扩充基线，应写入方案和命令输出。","补齐独立期望值测试、正向 --force 测试、失败路径只读测试和完整入口集成测试。","确认并固化跨平台行尾策略，避免 Windows checkout 与基线生成环境产生假阳性。"],"risks":["当前 fixture 一旦被误改为空 weeks 或漏掉空媒体声明，检查可能成功但实际没有提供完整保护。","默认构建形态切换后，现有 create/check 不再满足 single-file 契约，可能直接阻断后续里程碑。","提前接入基线门禁会让需要重取基线的变更产生中间红提交，若 CI 按每个提交强制通过会与既定两提交工作流冲突。","VM 求值使原本应为数据校验的步骤具备代码执行和拒绝服务面。"],"summary":"当前实现已覆盖七类声明、媒体字节摘要、形态拒绝和基本只读行为，但存在两个必须先修的问题：fixture 可在零覆盖时成功，以及 create/check 未固定使用临时单文件构建。其余主要风险集中在 VM 求值、跨平台行尾和测试可证伪性不足。","task_type":"code-review"}