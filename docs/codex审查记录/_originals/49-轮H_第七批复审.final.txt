{"task_type":"code-review","mode":"advice-only","summary":"本批修复整体方向成立，H2 的三组扰动对布尔返回值已有足够判别力，`\\bpit\\b` 也不会误删 `pits`。但最重要的 M4 测试仍不能证明实际界面未泄漏，并存在缺少构建产物时静默跳过的问题，因此本轮有 1 个 HIGH。","issues":[{"severity":"high","category":"other","location":"tests/unit/test_w4_ten_words_not_exposed.js","problem":"测试只检查 `WORD_AUDIO`、`WORD_ILL` 和 `collectWordConsumption` 的结构化记录，没有执行真实渲染。若未来生产代码新增 `Object.keys(W)` 并直接渲染词条，而共享抽取器未同步覆盖，该测试仍会通过；文件头的人工枚举审计无法形成持续约束。另外，构建产物不存在时以退出码 0 静默跳过，会让单独执行或流水线调度、清理顺序变化时出现假绿。","suggestion":"增加运行时渲染断言：在未进入测评流程的初始页面及主要游戏、词墙、小书路径中检查十词均不可见，并单独验证进入测评后才允许出现。构建产物缺失时应失败，或由测试自己在临时目录生成产物；不要依赖“任务列表中 build 排在前面”这一隐含前提。若必须共享流水线产物，应显式传入本轮构建路径并校验新鲜度。"},{"severity":"medium","category":"edge-case","location":"tools/validation/migration_audit.js：isLikelyCommentMatch / findFirstNonCommentMatch","problem":"注释过滤仅能识别整行去空白后以 `//` 开头以及简单的未闭合块注释。`代码; // tileHTML(...)` 形式的行尾注释不会被识别，字符串、模板字符串或正则字面量中的同形文本也可能被当成真实消费点；当前诱饵恰好只覆盖最容易识别的行首 `//`。因此审计仍可能被注释或字符串诱饵错误判为 pass。","suggestion":"优先用 JavaScript 解析器或词法扫描器定位真实调用表达式；至少补充行尾 `//`、多行块注释、字符串和模板字符串四类诱饵，并让过滤逻辑识别匹配位置是否位于词法注释或字符串区间。`enclosingAnchor` 也应在同一词法过滤后匹配，不能只凭字符距离。"},{"severity":"medium","category":"edge-case","location":"tools/validation/assessment_contract.js：visitRedactingSelfWord","problem":"`\\bpit\\b` 只会挖掉独立的 `pit`，不会挖掉 `pits`，因为 `t` 与 `s` 之间没有单词边界。这避免了误做子串替换，但也意味着若测评隔离要求同时禁止复数、时态等屈折形式，`zh:'pit 的复数是 pits'` 中的 `pits` 不会作为 `pit` 泄漏被命中。当前规则究竟按精确词元还是按词形家族判断并不明确。","suggestion":"先明确契约：若只禁止测评词的精确词元，增加 `pit` 与 `pits` 的边界测试并在注释中说明 `pits` 允许；若屈折形式也属于泄漏，应在统一分词或词形归一化层处理，而不是扩大这里的字符串替换，以免误伤 `spit` 等合法词。"},{"severity":"medium","category":"compatibility","location":"frontend/src/weeks/week04.data.js：ASSESSMENT_WORDS 派生声明","problem":"新增的 `Object.fromEntries` 需要 Safari 12.1 及以上；该文件看起来是直接作为浏览器脚本执行，没有从差异中看到转译或兼容垫片。若项目兼容 Safari 12 或更早版本，页面会在加载数据时直接失败。对一个被说明为零生产消费者的常量引入这项兼容要求尤其不划算。","suggestion":"核对项目明确的 Safari 最低版本和构建转译配置。若不能确认支持 `Object.fromEntries`，改用兼容的循环或 `reduce` 构造对象；如果确实零消费者，优先评估删除该常量及对应加载白名单，而不是继续维持派生副本。"},{"severity":"medium","category":"edge-case","location":"frontend/src/weeks/week04.data.js：`W[w].zh`；tools/validation/check_data.js：缺失 W 词条诊断","problem":"派生表达式会在模块加载阶段立即读取 `W[w].zh`。真实 W4 数据一旦漏掉某个 RESERVED 或 RESERVED_RETEST 条目，会先抛出 TypeError，`check_data.js` 新增的“周检词/复测词不在 W 里”精确诊断可能根本没有机会执行。现有缺词反例基于另一份 fixture，未证明真实 week04 数据能到达该诊断。","suggestion":"避免派生常量在非法输入上抢先崩溃，例如先过滤缺失项或使用安全读取，让校验器负责报告缺词；同时增加基于真实 week04 数据删除一个复测词的反例，断言得到预期的校验消息而非加载异常。"},{"severity":"low","category":"readability","location":"tests/unit/test_migration_audit.js：expectMigrated 与诱饵测试","problem":"多项“独立复核”实际复用了生产代码导出的 `isLikelyCommentMatch`、`findFirstNonCommentMatch` 和同一份 `spec.pattern`，只能证明调用方与辅助函数一致，不能独立证明辅助函数语义正确。`enclosingAnchor` 也只是邻近文本，可能位于注释、字符串或相邻表达式中。","suggestion":"保留共享函数用于定位，但测试预期应使用固定的合成源码和明确的期望行号或 AST 节点作为独立判据；分别覆盖真代码、各种诱饵和只有诱饵而无真代码的失败场景。"}],"recommendations":["H2 无需再增加更多布尔扰动：渲染结果逐项等于包装函数实际返回值，配合至少两组不同返回向量，足以抓住常量、自算逻辑、取反及其他布尔非恒等变换；`!wallTileLitState(c)` 会直接与记录值相反而失败。建议只补一个显式取反实现的破坏测试以固化这个结论。","从所示 `build_jobs` 看，新增七项位于公共任务列表，因此 `--quick` 和全量都会执行；全量只是在其后追加浏览器套件，没有发现只挂错档的问题。但应阅读完整 `tools/run_checks.py`，确认任务是串行执行、构建失败会阻止后续、且不存在并行竞态。","`POST_BASELINE_NAMES` 看起来是 `run_checks` 契约测试用来描述基线任务之后的固定任务序列，而不是另一条实际执行档位；新增任务同步加入它是合理的。仍需结合 `tests/unit/test_run_checks.py` 完整逻辑确认 `skip_baseline`、快速档和失败恢复分支的语义。","核对完整的 `tools/validation/load_data.js`：确认 `ASSESSMENT_WORDS` 已在白名单中、派生声明会在 `W` 初始化后求值，并确认加载器不是按白名单顺序逐个执行声明。当前真实构建产物测试能覆盖正常路径，但不能替代对缺词异常路径的检查。","把“数据结构里没有十词”和“用户界面没有十词”拆成两个命名清晰的测试，避免当前日志声称“任何非测评源码位置”而实际只覆盖共享抽取器已知的十五类来源。"],"risks":["新增测评词以后，未来出现新的 W 全量渲染消费者时，现有 M4 测试可能持续假绿。","低版本 Safari 可能因 `Object.fromEntries` 在数据脚本加载阶段白屏。","真实 W4 缺词时可能退化为无上下文的加载异常，而不是新加入的精确校验消息。","迁移审计仍可能被行尾注释、字符串或模板字符串中的同形文本欺骗。","若流水线任务并非严格串行，依赖固定 `build/week04.html` 可能读取旧产物或直接跳过。"],"confidence":"high","notes_for_claude_code":"本轮 HIGH 数：1。该结论仅基于所提供的差异文本；未执行命令、测试或行为验证。进一步定案需阅读完整的 `tools/run_checks.py`、`tests/unit/test_run_checks.py`、`tools/validation/load_data.js`、`tools/validation/word_consumers.js`、项目浏览器兼容矩阵及实际渲染入口。"}