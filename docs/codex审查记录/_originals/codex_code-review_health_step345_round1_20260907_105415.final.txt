{"confidence":"high","issues":[{"category":"edge-case","location":"tools/run_checks.py main()；tools/size_budget.py check()","problem":"`run_checks` 调用 `size_budget.py` 时未传入可重现构建的临时目录，检查目标固定为 `build/`。若 `reproducible(temp)` 不是严格逐字节比较，临时产物可能未经过体量护栏；即使当前比较保证一致，代码意图也不明确。","severity":"medium","suggestion":"显式对可重现构建目录执行 `size_budget.py --dir <temp>`，或在比较后同时检查 `build/` 与临时目录，并增加一个目录不同步时会失败的测试。"},{"category":"edge-case","location":"tools/project_config.py:size_budgets()","problem":"`float('nan')` 和正无穷会通过 `value <= 0` 检查；这不符合“正数”约束，且无穷预算在错误格式化时可能触发 `OverflowError`，使配置错误变成未处理异常。Python 的 JSON 解析默认也可能接受 `NaN`/`Infinity`。","severity":"medium","suggestion":"使用 `math.isfinite(value)` 并要求 `value > 0`；补充 NaN、Infinity 的配置反例测试。"},{"category":"edge-case","location":"tools/size_budget.py:check()","problem":"预算允许浮点数，但超限错误使用 `int(limit)`，会截断小数预算并报告错误的字节上限；例如非整数 MB 预算可能显示为较小的整数。","severity":"medium","suggestion":"要么把预算限制为可精确换算的整数 MB，要么按实际浮点/Decimal 字节上限格式化，确保错误消息与比较使用同一数值。"},{"category":"edge-case","location":"tools/project.py:node_version_check()","problem":"仅捕获 `FileNotFoundError/OSError`。`subprocess.run` 若因超时、解码失败或其他 `SubprocessError` 异常退出，doctor 会抛出 traceback，而不是按约定输出可读的失败信息。","severity":"medium","suggestion":"为 `TimeoutExpired`、`UnicodeError` 及适用的 `SubprocessError` 增加转换为 `SystemExit` 的错误路径，并保留异常实际信息。"},{"category":"readability","location":"tools/project.py:node_version_check()","problem":"非零退出时使用 `stdout or stderr`，只要 stdout 有内容就完全丢弃 stderr；当版本命令把诊断写入 stderr 时，用户看不到完整实际输出。","severity":"medium","suggestion":"合并并标注 stdout/stderr，或至少在两者均存在时同时输出；为空时也应给出明确的无输出提示。"},{"category":"edge-case","location":"tools/project.py:node_version_check()","problem":"正则 `^v(\\d+)\\.` 没有结束约束，诸如 `v18.0.0garbage` 会被接受为有效版本，和“格式异常失败”的要求不一致。","severity":"low","suggestion":"使用完整版本格式的 `fullmatch`（例如 `v\\d+\\.\\d+\\.\\d+`），并增加尾随垃圾字符、空白及多行输出的反例。"},{"category":"contradiction","location":"tools/project_config.py:load_config()；tests/unit/test_build.py（材料未提供）","problem":"`load_config` 现在强制所有配置声明 `sizeBudgetMB`。第 1、2 步或既有构建边界测试若使用缺少该字段的手写临时配置，会在进入被测逻辑前失败；材料未展示这些 fixture 已同步。","severity":"medium","suggestion":"检查并更新所有临时 project.json/字典 fixture；若某些测试只验证旧配置兼容性，应明确改为调用专用解析路径，避免静默绕过正式配置校验。"},{"category":"other","location":"第 5 步迁移记录；docs/项目记忆/外部归档清单_20260907.md","problem":"清单记录了路径、字节数和 sha256，但材料没有可复用的校验脚本、归档仓库提交标识或“目标目录存在且复制校验完成后才删除”的持久证据。Markdown 清单本身无法证明 109 个文件确实逐文件复制并核对后才执行 `git rm`，换机验证也依赖人工定位 worklog。","severity":"high","suggestion":"增加机器可读清单及验证命令/脚本：输入归档根目录，逐项检查存在性、字节数、sha256，并校验条目总数；记录归档仓库提交或不可变版本。将该验证作为迁移前置步骤并保留结果。"},{"category":"other","location":"方案 §5；docs/architecture.md、docs/development.md、resources/README.md","problem":"方案明确要求同步第四周设计文档，但材料 2 没有该文档的 diff；仅凭提供的三份文档无法证明不存在 `resources/reference/` 或 `docs/archive/screenshots/` 的残留引用，也无法确认清单完整的 109 条记录。","severity":"medium","suggestion":"对完整仓库执行并保留限定范围的引用扫描结果，检查第四周设计文档；若无需修改，补充明确的审查记录，否则同步更新并在 CI 或校验脚本中防止旧路径回归。"},{"category":"edge-case","location":"tests/unit/test_doctor.py:test_real_node_on_this_machine()","problem":"该测试无条件调用本机 Node。它把 PATH、Node 安装和版本直接变成 Python 单测结果；在仅运行 Python 契约测试或缺少 Node 的环境中会在 doctor 的“缺失 Node”行为尚未被测试前就整体失败。","severity":"low","suggestion":"将真实环境记录改为显式 doctor/验收步骤，或在 Node 不可用时跳过并打印原因；桩测试继续覆盖缺失、非零和版本门槛。"}],"mode":"advice-only","notes_for_claude_code":"本审查仅依据题目提供的 diff、清单前 30 行和方案摘录，未读取实际仓库文件、完整归档清单或执行命令。优先复核体量检查目录、配置 fixture、迁移验证证据及第四周设计文档。","recommendations":["先修正迁移验证的可审计性，并补齐完整仓库引用扫描与第四周文档核对。","将 size budget 的有限数值策略、浮点格式化规则和临时构建目录纳入单元测试。","完善 doctor 的异常与双流输出处理，避免环境错误以 traceback 或丢失诊断信息呈现。"],"risks":["归档副本若有遗漏或校验错误，历史清理后可能无法恢复原始素材。","体量护栏若未检查实际可发布目录，超限产物可能在 CI 中漏检。","严格新增配置字段可能使旧测试 fixture 或外部构建调用突然失败。"],"summary":"主要功能路径已覆盖方案中的预算、Node 门槛和文档联动，但体量检查目录、非有限预算、doctor 异常处理以及迁移验证可审计性仍有缺口。迁移校验证据和配置 fixture 兼容性应在后续复审前明确解决。","task_type":"code-review"}