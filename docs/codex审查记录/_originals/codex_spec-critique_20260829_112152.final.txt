{"task_type":"spec-critique","mode":"advice-only","summary":"R-1、R-3～R-8 的落点基本闭环，R-2 的题目停留与分母口径也已修正；但 R-2 与 R-9 组合后产生了同一播放请求被仲裁器自我取消的 high 级矛盾。当前不满足“0 high”放行条件，建议修正音频令牌所有权后再进入 P0 编码。","issues":[{"severity":"high","category":"contradiction","location":"§2.1 异步契约、§2.2 仲裁","problem":"规格要求 WordAudio.play、speakFallback 都在开播前调用 Audio.stopAll，同时 stopAll 又会使所有未决 play 令牌失效。内嵌音频失败后转入 speakFallback 时，fallback 再次调用 stopAll 会取消仍在等待兜底结果的父请求，使其无法按约定最终返回 ended；如果 WordAudio.play 先分配新令牌再调用 stopAll，还可能在首次开播前取消自身。这直接破坏“调用方全程只等一个 Promise”和“兜底成功等价 ended”的契约。","suggestion":"区分“开始一个外部播放请求”和“同一请求内部切换音源”：顶层 beginPlay 先取消旧请求和物理音源，再分配本次 ownerToken；内嵌音频转 TTS 时沿用同一 ownerToken，speakFallback 不得调用会使该 token 失效的完整 stopAll。也可给 stopAll 增加 preserveToken 参数。把调用顺序、谁创建/终结 Promise、内嵌转 TTS 是否保留 token 写成明确伪代码或状态表。"},{"severity":"medium","category":"ambiguity","location":"§2.1～§2.3、§4.2～§4.4","problem":"失败条由谁创建、retry continuation 通过什么接口注册仍未落定；同时只完整定义了 G1/G3 的主提示音失败。G2 第二遍整词、G3 答错后的纠正重播、G4 出题音以及普通 data-say 的词组接续失败后，是重放当前阶段、重跑整段流程，还是允许继续，规格没有逐项确定。“任何新播放使 continuation 失效”也未说明点击失败条时应先原子取出 continuation，避免重试自身先将其清掉。","suggestion":"明确唯一所有者，例如 WordAudio.play 只返回结果、不直接渲染失败条，由调用方执行 showAudioFailure({token,retry})；或在 opts 中正式定义 retry。补一张播放阶段表，至少覆盖普通词→词组、G2 慢速→常速、G3 出题→纠正重播、G4 出题，并规定每个阶段 failed 后的锁答、重试范围及是否推进。重试点击应采用“先消费并保存 continuation，再启动新播放”的原子顺序。"},{"severity":"medium","category":"contradiction","location":"§4.5 G5 造词工坊、§10 自检 ⑤","problem":"G5 只有 3 槽和 4 槽，但其真词白名单被要求严格等于 A 组 18 词，其中 at、it、in、an 是 2 字母词。因此这四个被声明为合法 G5 真词，却无法通过规定的槽位构造，构建断言也无法发现这类不可达数据。","suggestion":"优先增加 2 槽模式，并将 2/3/4 槽都纳入门禁；如果产品上明确不允许 2 槽，则把 G5 可构造白名单改成 A 组中长度为 3 或 4 的词，并同步修改 §2.5、§4.5 和 §10 的等值断言。"},{"severity":"medium","category":"edge-case","location":"§4.6 G6 计时闪卡、§6.2 读取清洗","problem":"flash_words 的成绩定义为 30 秒内“前进的张数”，但没有规定到第 18 张后是否停止、封顶或循环；清洗规则却只接受 0～组容量。若渲染器沿 v2 风格循环翻卡，正常操作可能得到大于 18 的成绩，刷新后又被清洗删除。","suggestion":"冻结为每张最多计一次：不循环，达到第 18 张立即停表或保持结束页，成绩上限 18；或者若允许循环，就取消组容量上限并重新定义成绩语义。将该行为加入 G6 正常路径门禁。"},{"severity":"medium","category":"other","location":"§2.2 仲裁、附件 3 Phone 模块","problem":"规格将仲裁器命名为全局 Audio.stopAll，但浏览器中的 Audio 已是原生构造器，附件代码还依赖 new Audio(src)。实现者若用 const Audio 定义仲裁模块会遮蔽原生构造器并导致音频创建失败；若给原生构造器追加静态方法，又形成非显然的全局修改。","suggestion":"将仲裁器明确命名为 AudioBus、AudioCoordinator 等，统一调用 AudioBus.stopAll；创建媒体元素时保留 window.Audio 或 new Audio 的原生含义。"}],"recommendations":["修正 §2.1～§2.2 的 ownerToken 与内部音源切换规则后再做放行判断；这是目前唯一明确的 high 门禁项。","把音频流程冻结成“一个逻辑请求、一个 Promise、一个 ownerToken、可包含 embedded→TTS 两个物理阶段”，所有外部新请求才取消旧 ownerToken。","为所有多阶段播放补失败转移表，并用验收覆盖“内嵌失败但 TTS 成功”“TTS 也失败”“fallback 期间被新播放打断”“失败条自身重试”四条路径。","补齐 G5 两字母词可达性和 G6 第 18 张后的行为，再进入相应 P2/P1 实现。"],"risks":["兜底播放可能被错误返回为 cancelled，或 Promise 无法按预期完成，导致 G1 卡题并使验收 #4 失败。","失败 continuation 若没有唯一所有者，普通词卡与各游戏可能出现不同的重试语义或旧提示条重放错误流程。","G5 的四个两字母白名单词可能成为永远无法在工坊中触发的死数据。","G6 快速翻完全部词卡后可能写入一个刷新即被清洗掉的合法成绩。","仲裁器遮蔽原生 Audio 时可能导致所有内嵌音频在运行期失效。"],"confidence":"high","notes_for_claude_code":"本轮结论基于所提供的 v1.2 全文、v1.1 复审记录和 v2 节选；未覆盖节选之外的实际实现。R-1、R-3、R-4、R-5、R-6、R-7、R-8 可视为已按上一轮意见闭环；R-2 的 G1/G3 续接已经明显改善，R-9 的统一仲裁方向正确，但二者组合产生了上述 high 矛盾。修正后建议只针对音频令牌/兜底状态机做一次窄范围确认审查。编码时还需读取完整 week01-v2.html、tools/audio_manifest.json，以及实际 build_audio.py/gen_audio.py 才能核实数据形状和构建原子性。"}