{"confidence":"high","issues":[{"category":"security","location":"§3.6、§4 第0期第5步","problem":"`json.dumps` 后把 `<!--` 替换为 `<\\!--` 会生成非法 JSON 转义。JSON 只允许有限的反斜杠转义，浏览器执行 `JSON.parse` 时会失败，因此该步骤与验收要求直接矛盾。","severity":"critical","suggestion":"改用合法转义，例如把 `<` 编码为 `\\u003C`（至少覆盖 `</script` 和 `<!--`），并增加直接调用 `JSON.parse` 的回归测试。"},{"category":"security","location":"§4 第0期第7步“历史清理”","problem":"门槛仍未核对所有远端 heads、tags 和其他 refs；`git log --all` 的路径枚举又安排在删除若干 refs 之后，可能漏掉仅存在于被删分支中的历史文件。另有 `git-filter-repo` 可能移除 origin 远端配置，后续直接 `git push origin` 不一定可执行。","severity":"critical","suggestion":"删除 refs 前先保存并核对完整的 `refs/remotes/*`、远端 tags 和 heads 清单；先从 bundle 或完整 refs 枚举路径，再删除临时 refs。明确 filter-repo 后重新设置并验证 origin，强推前再次核对远端状态。"},{"category":"contradiction","location":"§4 第0期第7步“清除路径”","problem":"命令 `git log --all --diff-filter=A --name-only -- '*.html'` 会匹配 `docs/design/design-doc.html` 等文档 HTML，不仅是根目录生成物，配合 `--invert-paths` 可能误删应保留的历史设计文档。","severity":"high","suggestion":"只接受仓库根目录的产物名（例如过滤无 `/` 的路径），单独列出需要清理的旧产物名；对文档 HTML 做保护性断言。"},{"category":"contradiction","location":"§3.4、§4 第0期第2步、§5 验收","problem":"方案要求“再构建一次到临时目录”并比较五个文件，但现有 `build_lessons.py --check` 只读取并比较根目录文件，没有输出目录参数；若先 build 再 check，check 可能只是比较刚生成的文件，不能验证独立重建。","severity":"high","suggestion":"定义明确的临时输出接口，例如 `build --output-dir <temp>`，两次构建分别写入两个临时目录并比较五个 SHA-256；同步规定 `--check` 是否写盘及 fresh clone 的行为。"},{"category":"missing-constraint","location":"§3.7、§5 第0期验收","problem":"check job 计划上传 `manifest.json`，但当前构建流程只在 `dist/<版本>/manifest.json` 生成它，`project.py check` 不会生成根目录 manifest。计划也没有指定 artifact 名称、`if-no-files-found: error`、下载步骤或实际的 Release 创建 action/命令。","severity":"high","suggestion":"明确 artifact 的实际文件路径和唯一名称；成功构建后上传存在的五个 HTML 及 manifest，缺文件必须失败。release job 下载该 artifact 后用固定 action/API 创建 Release，并验证下载字节与构建字节一致。"},{"category":"missing-constraint","location":"§3.2 媒体清单 schema","problem":"固定单引号生成格式没有约束键的字符集。键若包含单引号、换行或反斜杠会破坏生成的 JavaScript；JSON 重复键在常见解析器中会被静默覆盖，当前“同一声明内键不得重复”无法被实现验证；未知顶层声明键也未规定是否拒绝。","severity":"high","suggestion":"限制键为明确的 ASCII 标识符集合，或规定 JS 字符串转义；使用拒绝重复键的 JSON 解析；拒绝未知顶层键和未知声明字段，并在 schema 校验中覆盖这些情况。"},{"category":"edge-case","location":"§3.3 基线与“字节不变”","problem":"`skeletonSha256` 的替换边界和占位符内容没有形式化定义，也没有保留七个声明的位置/名称序列。若交换声明块、改变标记附近结构或制造解析器未识别的重复键，骨架哈希与逐键媒体哈希可能仍相等。","severity":"high","suggestion":"定义基于语法解析的七个声明区间、固定占位符和声明顺序校验；验收同时比较声明名称/位置序列、键计数、键序、MIME 与解码字节，不能只依赖自由文本正则。"},{"category":"contradiction","location":"§3.1、§3.2、§4 第1期第7步","problem":"目标树把每周清单放在 `frontend/src/media/weekNN/manifest.json`，数据流却写成 `resources/assets → resources/adopted + manifest.json`；素材工具表又未说明清单到底写入哪个目录。`WALL_ILL` 的目标目录 `adopted/wall/` 与现有工具 `wall-nat → WORD_ILL` 的映射也不一致。","severity":"high","suggestion":"为每个声明键指定唯一清单路径和真相源，并逐一列出五类插画到 adopted 子目录的映射；明确 WALL_ILL 是否独立产物，或删除该目录/声明的歧义。"},{"category":"other","location":"§4 第1期第1步、第7步","problem":"导出阶段规定同字节文件可复用、无源文件才写入 adopted，但没有定义跨来源同哈希文件的稳定路径选择、已有 adopted 文件冲突、文件名碰撞和旧 provenance 更新规则。不同实现可能产生不同 manifest 路径，导致后续提交不稳定。","severity":"medium","suggestion":"规定按周序/声明序/键序选择 canonical 文件名，碰撞直接失败或使用确定的别名规则；写入前校验已有文件哈希，PROVENANCE 更新必须原子且覆盖全部 adopted 文件。"},{"category":"contradiction","location":"§3.2 第5、6条、`load_data.js` 兼容性","problem":"固定格式与现有 `declaration()` 对普通 base64 内容基本兼容，但方案没有规定生成声明中禁止注释、额外常量或非标准行尾；`load_data.js` 仍是基于首个 `};` 的正则解析器，未来合法键值或注释变化可能截断声明。","severity":"medium","suggestion":"把生成格式限制为无注释、固定 LF、固定结尾，并在 manifest 校验测试中直接用 `load_data.declaration()` 解析所有七类声明；长期应改为结构化解析或至少加入嵌套/截断反例。"},{"category":"other","location":"§4 第1期顺序（并存→等价→删旧）","problem":"步骤顺序总体成立，但第2步新增 `@media` 支持后没有定义旧 `@include` 与新指令同时存在时的冲突处理、未知指令行为和重复声明行为。切换模板后若某周漏改一个 include，可能静默保留旧媒体。","severity":"medium","suggestion":"规定两种指令可并存但同一声明只能出现一次；未知或重复声明直接失败；切换验收增加全仓扫描，确保四周模板恰好各有七个 `@media` 且没有旧 media include。"},{"category":"readability","location":"§2、§3.3、§4 第1期第1步","problem":"基线示例中的 `weeks` 只展示 `week01.html`，但实施要求四周；`sourceCommit` 使用占位符，未规定完整 SHA-1/SHA-256 格式和构建版本。执行者可能生成字段不全或无法追溯的基线。","severity":"medium","suggestion":"给出四周完整 schema，规定 `sourceCommit` 为固定长度十六进制提交号，并记录构建工具版本、Python/Node 版本及哈希算法。"},{"category":"other","location":"上一轮 C-1","problem":"状态：已修。§3.3 已加入周文件整哈希、骨架哈希、逐键媒体哈希和 course 前后哈希，已覆盖上一轮基线缺口。","severity":"low","suggestion":"按本轮对基线规范化和声明位置的补充继续收紧即可。"},{"category":"other","location":"上一轮 C-2","problem":"状态：部分修。已增加工作区、远端同点、tag/stash/未推送提交、副机停工、bundle 和恢复验证，但仍遗漏完整远端 refs、删除 refs 后的路径枚举以及 origin 重建等不可逆操作细节。","severity":"critical","suggestion":"采纳本审查关于完整 refs 快照、路径枚举时序和远端配置的补充门槛。"},{"category":"other","location":"上一轮 H-3","problem":"状态：已修但仍有边界缺口。§3.2 已明确七键、路径安全、MIME、空对象、别名、键序和固定输出格式。","severity":"low","suggestion":"补充键字符集、重复 JSON 键、未知字段和实际解析器兼容测试。"},{"category":"other","location":"上一轮 H-4","problem":"状态：部分修。§3.4 已提出可重现构建和临时目录比较，但没有落到现有脚本可执行的输出目录接口，仍与当前 `--check` 语义冲突。","severity":"high","suggestion":"先固定临时目录构建契约，再同步 `project.py`、`run_checks.py`、README、development.md 和 CI。"},{"category":"other","location":"上一轮 H-5","problem":"状态：已修。第1期已拆出导出、指令支持、模板切换、验收、测试和删旧机制的顺序。","severity":"low","suggestion":"补充并存期间的重复/漏改失败规则。"},{"category":"other","location":"上一轮 H-6","problem":"状态：已修但仍有实现歧义。三个工具已有参数、输入输出、幂等、原子和 dry-run 规格，并增加端到端测试要求。","severity":"low","suggestion":"统一工具清单写入位置、WALL_ILL 映射和多文件失败后的清理规则。"},{"category":"other","location":"上一轮 H-7","problem":"状态：已修。第2期已列出 srcdoc、localStorage、HTTP fetch 的验证表、失败回退和验收场景。","severity":"low","suggestion":"保留现有实测要求，无需提前到第0期。"},{"category":"other","location":"上一轮 H-8","problem":"状态：已修。`resources/adopted`、导出 provenance、原始与派生边界及基线导出前置校验均已写入。","severity":"low","suggestion":"补充 canonical 文件名和冲突规则。"},{"category":"other","location":"上一轮 M-9","problem":"状态：已修。迁移步骤已规定复制校验、相对归档说明、文件清单和文档同步。","severity":"low","suggestion":"同步明确 architecture.md 迁移后不再保留 reference 的最终树形。"},{"category":"other","location":"上一轮 M-10","problem":"状态：已修。单位、计算对象、严格大于预算失败及第1期阈值切换均已明确。","severity":"low","suggestion":"把阈值切换作为自动验收断言，而不只写文字要求。"},{"category":"other","location":"上一轮 M-11","problem":"状态：部分修。已说明直接嵌入字符串、`</` 和 `<!--` 转义、模板去除 atob，但 `<!--` 的转义写法非法，故 JSON.parse 回归无法通过。","severity":"critical","suggestion":"采用合法 JSON 转义并覆盖脚本标签、注释开头、中文和反斜杠内容。"},{"category":"other","location":"上一轮 L-12","problem":"状态：已修。已规定 `node --version`、主版本解析、缺失/非零/异常格式/低版本失败及桩测试。","severity":"low","suggestion":"验收中记录实际版本输出。"},{"category":"other","location":"上一轮 H-13","problem":"状态：部分修。已增加 check/release job、tag 触发、最小权限和同一批字节的目标，但 artifact 路径、上传成功条件和 Release 创建步骤仍未闭环。","severity":"high","suggestion":"补齐具体 workflow 步骤与 artifact 契约，并在 tag 场景自动校验下载 SHA。"},{"category":"other","location":"上一轮 M-14","problem":"状态：已修。§6 已按文件列出 README、开发指南、架构、交接规范、课程方案、工具 README、配置和技能文档的联动范围。","severity":"low","suggestion":"以全仓搜索结果作为每期验收证据，避免清单与实际残留不一致。"}],"mode":"advice-only","notes_for_claude_code":"本复审基于题面提供的 v0.2、脚本内容、测试和文档节选；未读取完整四周模板、全部素材工具实现、现有 project.py CI 运行结果及未附文档。应优先处理两个 critical：非法 JSON 转义和历史重写 refs/路径保护；随后补齐可重现构建、artifact/Release 闭环与清单 schema 的严格校验。","recommendations":["先把 course payload 的转义改成合法 JSON，并用真实 `JSON.parse` 测试包含 `</script>`、`<!--`、中文和反斜杠的内容。","将重写操作拆成“完整 refs 快照与核对→bundle 恢复验证→精确根目录路径枚举→重写→远端配置/引用核对”的可执行清单。","为 build 增加明确的临时输出目录契约，使 reproducible build 不依赖当前根目录生成物。","在 CI 中明确成功 artifact、下载方式、Release 创建动作和权限，并移除不存在的根目录 manifest 路径。","冻结清单键语法、重复键处理、声明位置和 adopted 路径映射后，再执行并存迁移和删除旧 JS。"],"risks":["非法的 `<\\!--` 转义会使合并入口在构建后无法解析 payload，属于直接可用性故障。","历史重写若遗漏远端 refs 或误删文档 HTML，可能造成不可恢复的历史数据损失。","reproducible build 契约未落地时，fresh clone 可能因没有根目录 HTML 而检查失败，或把刚生成文件自检为通过。","CI artifact 路径与实际产物不一致会造成主分支检查通过但没有可下载 Release。","清单键和路径规则不严格会产生不同实现、不同字节结果，使“字节不变”验收失去确定性。"],"summary":"上一轮 14 条大多已落实，但 C-2、H-4、M-11、H-13 仍是部分修复；其中 M-11 的 `<\\!--` 写法会让 JSON.parse 直接失败。当前还存在历史重写路径过宽、可重现构建未落地、CI artifact 不存在以及清单路径/键约束不足等高风险缺口，建议这些阻断项解决后再执行第0期。","task_type":"spec-critique"}