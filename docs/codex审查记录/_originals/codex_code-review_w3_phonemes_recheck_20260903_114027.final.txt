{"confidence":"high","issues":[{"category":"edge-case","location":"tools/prep_phonemes.py:main；assets/phonemes/README.md“候选目录”","problem":"候选残留仍可能进入提升流程。脚本只检查 `<out>/*.wav`，不检查 `<out>/candidates/*.wav`；README 的 b 提升命令又未带 `--clean`。如果 `tmp/b_only/candidates` 留有另一个、且同样被 week03 声明的旧候选，后续 `build_phonemes.py --src tmp/b_only/candidates` 会把它与 b 一并注入。这会绕过“候选逐个听检后提升”的隔离意图。","severity":"high","suggestion":"当使用 `--include-candidates` 时，也对 `<out>/candidates/*.wav` 按 `produced` 集合执行残留检测；同时把 README 的提升命令改为带 `--clean`，确保 `--key b` 最终只留下 b。"},{"category":"missing-constraint","location":"tools/prep_phonemes.py:validate、fetch","problem":"H-3 对当前清单有效，但脚本没有强制 `source.sha256` 和 `source.bytes` 必填。`validate()` 的必填列表遗漏了这两个字段，而 `fetch()` 接受 `None`，因此未来新增来源时漏填完整性字段会静默退化为不校验，与脚本说明和可复现性目标矛盾。","severity":"medium","suggestion":"把 `sha256`、`bytes` 纳入必填校验；要求 sha256 为恰好 64 位十六进制字符串，bytes 为正整数且拒绝布尔值。`fetch()` 也可改为非可选参数，避免调用端绕过。"},{"category":"edge-case","location":"tools/prep_phonemes.py:fetch","problem":"`.part` 只在下载完成但哈希或字节数不符时删除；`urlopen`、`r.read()`、写盘或网络中断抛异常时没有清理逻辑，可能留下部分文件。原缓存不会被覆盖，这一点是安全的，但“所有失败路径干净”尚未完全成立。","severity":"medium","suggestion":"用 `try/finally` 包住下载与校验，仅在原子替换成功后保留结果，其他异常统一删除 `.part`；下载前也先删除同名旧 `.part`。"},{"category":"edge-case","location":"tools/prep_phonemes.py:prep_one","problem":"分带护栏是在 `write_wav()` 覆盖目标文件之后才判定。若护栏失败，代码会删除刚写的文件，也同时丢失该路径原先可能存在的合格输出；未使用 `--clean` 的失败重跑因此不是保留旧成果的原子操作。","severity":"medium","suggestion":"在创建或覆盖输出文件前完成 guard 计算与判定；或者先写同目录临时文件，全部检查通过后再用 `os.replace()` 原子替换。"},{"category":"missing-constraint","location":"tools/prep_phonemes.py:validate","problem":"`inject` 使用 `bool(it.get(...))` 转换，字符串 `\"false\"` 会被当成 true，使本应进入 candidates 的资源写到顶层。另有 `guard`、`source` 未先验证为对象，`guard.band` 元素也未验证为有限数值，配置错误时可能产生 TypeError，而非带音素键的可诊断错误。","severity":"medium","suggestion":"要求 `inject` 必须是 JSON 布尔值；要求条目、source、guard 为对象，并逐项校验 band 两端为有限非布尔数值，再进行范围比较。候选控制应采用拒绝模糊值的失败关闭策略。"},{"category":"edge-case","location":"tools/prep_phonemes.py:validate、spectral_gate","problem":"即使 `nr_db=0`、完全不使用底噪样本，仍强制 `noise_sample_end≤start`，会误拒绝开头立即发音但不需要降噪的来源。另外在启用降噪时，STFT 使用中心填充，`noise_sample_end == start` 仍可能让最后几个底噪帧的窗函数覆盖音素起始约半个 FFT 窗长。当前第三周切点间隔充足，不受影响。","severity":"low","suggestion":"仅在 `nr_db>0` 时要求并校验底噪区间；启用降噪时要求底噪终点与 start 至少相隔 `N_FFT/(2*采样率)`，或改为显式的 `noise_start/noise_end` 区间并按实际覆盖帧取样。"},{"category":"contradiction","location":"assets/phonemes/README.md“怎么加新音素”末段","problem":"前文正确说明早前周次不能直接使用当前跨周累计目录，末段却又说“给第二周注入时，本目录同时放着第一周的六个也不会出问题”。这在历史上仅含前两周文件时成立，但对目前还含第三周文件的实际目录容易造成误解。","severity":"low","suggestion":"改成“源目录若只含第一、二周键，第一周文件会原样透传；当前累计目录还含第三周键，给第二周重注入仍须使用筛选后的临时目录”。"}],"mode":"advice-only","notes_for_claude_code":"复审结论：C-1 的当前产物处置正确——所给 diff 中 PHONEME_AUDIO 只新增 g/o/u/l/f，b 未出现；`inject:false` 默认跳过并在显式包含时写入 candidates，build 只扫指定目录顶层，页面降级与文档路径也相互对应。H-2 的主要修正正确，仅 README 末段仍有轻微语境矛盾。H-3 的当前 spec 已具备哈希、字节数、缓存命中复核和校验失败后的原缓存保护，但必填约束及异常清理仍需补齐。对 H-1，基于所给“释放后约 10 ms 已出现低频谐波、与 c 的送气噪声有差异、且与既有 d 同型”的具体依据，没有足够的新证据要求改变用户已拍板的切点。文档中的 17 段、约 60.5 KB、b 未注入在所给 README、diff、项目记忆和 spec 中一致。","recommendations":["优先封闭 candidates 残留路径，并让 README 的单候选提升命令默认带 `--clean`。","把来源哈希与字节数从约定提升为脚本强制约束，并为下载异常统一清理 `.part`。","让 guard 检查先于目标文件覆盖，所有配置类型校验采用失败关闭。"],"risks":["旧候选可能在提升 b 时被一起注入，绕过逐项批准流程。","未来清单漏填哈希或字节数时，脚本会在没有完整性保护的情况下继续处理。","护栏失败可能删除此前已有的合格阶段产物，网络异常也可能遗留 `.part`。"],"summary":"首轮 C-1、H-2 的核心问题已修正，H-1 的不采纳理由在现有证据下可以接受；H-3 对当前清单已生效但实现仍未强制完整性字段。修订引入了一个 high：候选目录残留未检测且 README 提升命令未清理，可能把未经批准的旧候选一并注入；未发现 critical。","task_type":"code-review"}