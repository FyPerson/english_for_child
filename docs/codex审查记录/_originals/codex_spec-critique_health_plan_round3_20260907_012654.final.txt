{"task_type":"spec-critique","mode":"advice-only","summary":"v0.3 已正确修复上轮大部分问题，尤其 JSON 转义、构建输出目录、清单标量形态和精确历史路径都有实质收紧；但历史重写的最终提交顺序、adopted 文件命名、PROVENANCE/prune 语义以及基线生成入口仍有会阻断实施的缺口。建议先修正这些高风险契约，再进入第 0 期。","issues":[{"severity":"high","category":"contradiction","location":"§4 第0期第7步子步骤9—10、§5 第0期验收","problem":"子步骤9先强推并核对远端，子步骤10才把 commit-map 写入对照文件并更新含旧提交号的文件。如果这些文件位于仓库内，步骤10会产生尚未提交、尚未推送的修改，导致“远端已完成重写”的核对立即失效；方案也没有后续 commit、push 和主副机同点检查。","suggestion":"把 commit-map 和仓库内提交号更新移到最终强推之前：完成 filter-repo→生成并核对映射→更新文件→提交该批更新→最后一次 force push→重新核对本地 main、origin/main 与远端 main 同点。若部分“归档”位于仓库外，应逐项标明，无需混入 Git 提交流程。"},{"severity":"high","category":"edge-case","location":"§4 第1期第1步 canonical 文件名规则、§3.2 第3条键字符集、§3.1 adopted/words 映射","problem":"方案规定无源文件的 canonical 文件名取“首次出现的键”，但合法键允许空格、逗号、句点、感叹号和问号。问号在 Windows 文件名中非法，句点或空格结尾也不安全；WORD_AUDIO 的键还可能是整句。项目主要在 Windows 上运行，因此导出可能直接失败，或在不同系统生成不同路径。","suggestion":"不要直接把业务键当文件名。定义确定性的跨平台编码规则，例如规范化安全短前缀加内容哈希，或仅使用 `[A-Za-z0-9_-]` 的转义文件名；明确大小写碰撞、保留名、尾随点/空格及最大路径长度处理，并为 `?`、空格、大小写冲突和长句键增加测试。"},{"severity":"high","category":"contradiction","location":"§3.1 PROVENANCE.json、§4 第1期第1步与第7步、§5 第1期验收","problem":"“PROVENANCE.json 覆盖 adopted 下每个文件”若按字面包含 README.md 和 PROVENANCE.json 本身，会出现自哈希或无意义记录；若仅指媒体文件，也未明确。更严重的是，`--prune` 只删除“PROVENANCE 与四周清单都不引用”的文件，而初次导出又要求 PROVENANCE 覆盖全部 adopted 媒体，因此后来从清单移除的孤儿仍会被旧 provenance 引用，永远无法被 prune。三个素材工具也只写明更新文件和 manifest，没有规定同步更新 provenance。","suggestion":"明确 PROVENANCE 只记录 adopted 下的媒体文件，不含 README 和自身。把四周 manifest 定义为存活引用真相源：prune 删除“不被任何 manifest 引用”的媒体，并同步移除 provenance 条目；素材工具成功提交文件与 manifest 后也必须原子更新 provenance，或明确 provenance 由独立重建命令从 manifest 生成。"},{"severity":"high","category":"missing-constraint","location":"§4 第1期第7步“幂等与原子”、失败后的清理","problem":"“文件先写 `.tmp` 再 rename；清单最后写；失败时清单不写”只保证单文件原子，不保证整批操作一致。若前几个文件已替换、后续处理失败，旧 manifest 仍可能指向已被覆盖的新字节；`--prune` 只能清孤儿，不能恢复被覆盖的已引用文件。","suggestion":"规定整批先写入独立 staging 目录并完成所有校验，已有目标若被 manifest 引用且哈希不同则在提交前失败；确认无误后再按受控顺序发布文件、provenance 和 manifest。至少要保证失败不会改变任何现有 manifest 已引用文件的字节。"},{"severity":"high","category":"ambiguity","location":"§3.3 基线生成、§4 第0期第1步","problem":"文档称基线“由 `python tools/project.py build` …生成”，但同一命令在日常流程中的契约只是构建课件。若普通 build 每次都生成或更新入库基线，会把基准随被测输出一起漂移；若不会生成，则第0期第1步缺少实际创建 baseline 的入口。","suggestion":"增加一次性、显式的基线命令或参数，例如独立 `baseline create`，要求工作区干净、目标 fixture 不存在或必须显式覆盖，并记录当前 HEAD。普通 build/check 只能读取和比较基线，绝不更新它。"},{"severity":"high","category":"missing-constraint","location":"§3.7 CI 与 Release","problem":"表格写了 release job 仅在 `v*` tag push 时运行，但没有给出同一 workflow 内的 job-level `if` 条件；当前顶层触发包含所有 push、PR 和 workflow_dispatch。若实现者只照表拆 job，release job 可能在普通 push 或 PR 上尝试执行 `gh release create`。此外“下载 artifact 后对目录内全部文件执行命令”没有固定下载目录和文件展开规则。","suggestion":"在 release job 明定 `if: startsWith(github.ref, 'refs/tags/v')`，并固定 download-artifact 的 `path`。随后先定位且断言恰好一个 `soundblocks-*` 目录、校验 manifest，再用明确文件列表创建 Release；避免依赖含空格或多目录时不稳定的 shell 展开。"},{"severity":"medium","category":"contradiction","location":"§4 第0期第7步子步骤7与9、§5 第0期验收","problem":"子步骤7先要求 `git for-each-ref` 只剩 main，随后重新添加 origin 并 fetch；fetch 后通常会重新产生 `refs/remotes/origin/main`，因此该断言只在 fetch 前成立。子步骤9又要求 `git ls-remote origin`“只剩 main”，但输出通常还包含指向 main 的符号引用 `HEAD`，按字面验收可能永远失败。","suggestion":"把断言写成精确 ref 集合：重建 origin 前本地仅有 `refs/heads/main`；fetch 后允许且要求 `refs/remotes/origin/main`。远端验收应断言 heads 集合仅为 `refs/heads/main`、tags 为空，并单独允许符号 `HEAD`。"},{"severity":"medium","category":"edge-case","location":"§3.2 第4条 manifest path","problem":"路径虽要求解析到 `resources/` 下并禁止 `..` 与绝对路径，但没有规定 JSON 路径必须使用 `/`，也没有明确目标必须是普通文件。Windows 上的反斜杠路径可能本机通过而 Linux CI 失败；指向目录或特殊文件时错误位置也不明确。","suggestion":"规定 manifest 使用仓库相对 POSIX 路径，只允许 `/` 分隔符，并要求解析后目标是位于真实 `resources/` 树内的普通文件；加入反斜杠路径、目录路径和符号链接逃逸反例。"},{"severity":"medium","category":"missing-constraint","location":"§3.3 skeletonSha256","problem":"使用 `declaration()` 定位和固定占位符的方向正确，但现有 `declaration()` 只返回首次匹配，无法自行证明“每个声明名恰好命中一次”。如果基线工具只调用七次该函数，重复声明可能不会被计数；并存规则的重复检测也不能依赖它。","suggestion":"基线生成和构建分别先枚举所有 `^const <NAME> = ` 起点并断言每名恰好一次，再调用同一解析器确定结束边界。增加重复声明、声明换序、缺失结尾、声明外普通文本变化四类可证伪测试。"},{"severity":"medium","category":"performance","location":"§3.4、§3.7 check job、§4 第0期第2步","problem":"CI 调用 `project.py release`，release 先 build，随后完整 check 又按新契约执行根目录 build 和临时目录 build，相当于同一提交至少构建三次；完整浏览器回归只运行一次，因此额外两次主要用于同一进程链内的字节比较。对当前规模可行，但流程和故障定位偏重。","suggestion":"保留一次根目录构建和一次独立临时构建比较即可。可让 release 复用 check 已验证的根目录产物后直接 package，或让 check 返回/固定已验证输出目录；不要再增加第三次等价构建。"},{"severity":"low","category":"readability","location":"§3.1 数据流","problem":"数据流写成 `build → 根目录 HTML → release → dist/`，而 release 的当前语义实际还会再次 build 和完整 check。结合 §3.7“用 release 产物上传”，容易让实现者误以为 release 只负责复制根目录文件。","suggestion":"把流程文字改成“project release 调用 build/check，通过后 package 到 dist”，并标明 dist 中还包含 index.html 与 manifest.json，而“五个产物”仅指 course 加四个周文件。"}],"recommendations":["上轮12条状态：JSON合法转义已修；完整refs与origin重建部分修；根目录历史路径保护已修；`--output-dir` 与 fresh clone 行为已修；artifact/Release闭环部分修；schema严格校验已修；骨架定义部分修；声明与adopted映射已修；canonical与冲突规则部分修；固定声明格式与解析测试已修；并存期重复规则已修；四周基线与版本字段已修。","第0期执行前优先修订历史重写子步骤9—10的顺序，并把远端、本地跟踪引用和符号HEAD的验收分别写清。","第1期前冻结跨平台文件名算法、manifest路径格式、PROVENANCE生命周期及批量失败回滚语义；这四项会直接决定Windows副机能否稳定工作。","给基线建立显式的一次性生成入口，禁止普通build自动更新基准。","CI可保留完整浏览器回归，但应固定tag条件、artifact下载目录和Release文件清单，并减少重复构建。"],"risks":["历史重写后的对照文件若在最终推送之后才更新，会造成主机与远端再次分叉，并让副机重新clone到不完整版本。","直接用媒体键生成文件名可能在Windows上失败，阻断首次导出。","旧provenance持续引用孤儿文件会让prune失效，adopted目录继续累积。","批量素材处理失败后可能留下“manifest路径不变但文件字节已变”的隐蔽不一致。","普通build若能重写baseline，会让字节不变验收退化为自我确认。"],"confidence":"high","notes_for_claude_code":"本审查仅基于题面提供的v0.3及所附脚本、测试和文档内容，未执行命令或验证实现。八项已拍板决定未重新讨论。建议将上述四个high级数据一致性问题视为实施前门槛；其余可在对应阶段编码前补入规格。"}