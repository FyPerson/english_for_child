{"task_type":"code-review","mode":"advice-only","summary":"基于所给差异的静态审查，CVC 判据改按字位处理的方向正确，但“已教”集合的语义和显式分词数据来源仍有实质性契约缺口。四模板共享函数还存在跨 script 调用时序未被现有冒烟测试真实覆盖的问题。","issues":[{"severity":"high","category":"contradiction","location":"tools/validation/assessment_contract.js:166-186；tests/unit/test_assessment_contract.js:310-376","problem":"显式分词从 d.W[normalized].segments 读取，但任务说明明确指出周检保留词按规范只进入 RESERVED、不进入 W。rain 正例通过把 rain 同时写进 W，可能只在不符合生产数据契约的测试夹具中成立；对真正需要消歧的保留词，生产路径拿不到 explicitSegments。","suggestion":"为测评词定义契约内的分词载体，例如 RESERVED_SEGMENTS[word] 或结构化 assessment.words 条目，并由校验器与数据检查脚本共同读取。测试应增加“词只存在于 RESERVED、存在重叠字位且必须消歧”的真实形态；若 segmentWord 的自动分词足以覆盖 rain，也应另选一个确有多解的词验证显式消歧通道。"},{"severity":"high","category":"missing-constraint","location":"tools/validation/assessment_contract.js:约 weekly 路径中 taught 的定义及 175-176","problem":"taught 是由 Object.keys(d.SOUNDS) 得到的字位 ID 集合，不是字符集合；因此多字母 ID 如 ai 的成员检查本身正确。但它把“出现在 SOUNDS”直接等同于“已经教学”。若 SOUNDS 预置隐藏字位、未来字位或 displayOnWall:false 的辅助条目，这些字位仍会被判为已教，当前 untaught 检查甚至因 segmentWord 与 taught 使用同一键空间而基本恒真。","suggestion":"从明确的教学范围字段构造 taught，例如累计已教字位列表、META.wallLetters 或独立 taughtPatterns，并确认隐藏但已教的字位如何表达。segmentWord 可以使用更宽的可识别字位表，但教学成员检查必须使用独立集合；增加“SOUNDS 中存在但尚未教学的 ai”反例。"},{"severity":"medium","category":"ambiguity","location":"tools/validation/assessment_contract.js:139-186；frontend/src/shared/graphemes.js（未提供）","problem":"差异未提供 soundType 的实现，无法确认它优先读取 SOUNDS[id].type、type 缺失时是否由 grapheme/ID 猜测，或直接抛错。当前只补齐了合成 fixture 的 type，未覆盖 W5 数据缺 type、非法 type、适配器只补 grapheme 不补 type 等情况；代码注释中“schema 本来就要求合法 type”的结论尚未由本批测试证明。","suggestion":"进一步审阅 frontend/src/shared/graphemes.js、tools/validation/sounds_grapheme_adapter.js 及真实 W5 数据。补三类测试：type 缺失、type 非 c/v、双字母元音 ID 的 type=v；明确期望是契约失败而非字符猜测。如果 soundType 有字符级回退，应判断该回退是否会重新引入本次要消除的误判。"},{"severity":"medium","category":"edge-case","location":"frontend/src/weeks/week01.template.html:882；week02.template.html:932；week03.template.html:945；week04.template.html:929；tests/unit/test_render_smoke.js 的 extractScripts 路径","problem":"四份模板都改成 key => bookArtHTML(key)，因此都有相同的跨 script 时序条件：包装只延迟了赋值时的解析，若早期 script 在共享 render-blocks.js 所在 script 执行前调用 bookArt，仍会 ReferenceError。冒烟测试把多个 script 提取后拼成一个 vm.Script，函数声明可能跨原 script 边界被整体提升，从而掩盖真实浏览器中的时序错误。","suggestion":"逐份核对完整模板中 media include、render-blocks include 和首次 renderHome/renderDay/bootstrap 调用的实际 script 顺序。更稳妥的是让共享函数在所有消费者及启动调用之前加载；测试应按原 script 标签顺序分别执行 vm.Script，而不是拼成一个脚本，并在每段执行后保留同一上下文。"},{"severity":"medium","category":"other","location":"tests/unit/test_render_smoke.js：T3-1 接线验证","problem":"源码级断言只在整份文件中搜索精确文本 const lit = wallTileLitState(c);，并不能证明该调用位于 hero 渲染的有效执行路径。注释、死代码或另一个无关函数包含该文本都能让测试通过，属于新的结构代理/恒真式变体；同时它对无语义影响的格式变化很脆弱。","suggestion":"在运行 renderHome 时替换或包装 wallTileLitState，记录调用 ID，断言调用集合与 META.wallLetters 一致，并检查渲染结果的点亮状态。若仍保留源码检查，应把匹配范围限制到 renderHome 的对应 AST 节点，而非全文正则。"},{"severity":"medium","category":"missing-constraint","location":"tests/unit/test_assessment_contract.js:310-376","problem":"新增五例主要覆盖字位数量，没有覆盖恰好三个字位但类型不是 C-V-C 的分支，也没有覆盖 soundType 抛错、SOUNDS 中存在但未教、显式 segments 含未知 ID或与词拼写不一致等关键边界。","suggestion":"至少增加 V-C-V 或 C-C-C 的三字位反例、缺失/非法 type、可识别但未教学的字位，以及非法显式 segments。对每例断言具体错误类别，避免只以 errors 非空作为通过标准。"},{"severity":"low","category":"readability","location":"frontend/src/weeks/week01.template.html:1652-1701；frontend/src/shared/games.js","problem":"initWordForge 被从其他周模板复制到 week01，而非共享；既然目标是四模板统一调用同一初始化全集，保留多份逐字复制实现会继续产生实现漂移风险。","suggestion":"确认四周实现及依赖完全一致后，将 initWordForge 收敛到 shared/games.js；若周间确有差异，抽共享核心并把差异改为数据或小型配置项，同时增加四模板运行时接线测试。"},{"severity":"low","category":"other","location":"tests/unit/test_migration_audit.js:348-349","problem":"迁移审计通过硬编码行号跟随新增 26 行手工调整，任何无语义注释变化都会再次破坏测试，且不能证明消费点逻辑正确。","suggestion":"用稳定的 finding ID、函数名或 AST/模式定位代替绝对行号；行号可作为诊断输出，不宜作为契约断言。"}],"recommendations":["优先解决测评词显式 segments 的合法数据来源，并将“可分词字位集合”与“已教字位集合”分离。","按真实浏览器的独立 script 标签执行顺序补时序测试，确认四份模板首次调用共享素材函数时定义已存在。","补齐 CVC 类型、缺失 type、未教但存在于 SOUNDS、非法显式分词等反例。","用运行时调用观测替代 T3-1 的全文源码正则接线断言。"],"risks":["符合规范、只出现在 RESERVED 的多字母字位周检词可能无法获得显式分词，导致误拒或依赖不稳定的自动分词。","预置在 SOUNDS 但尚未教学的字位可能被错误接受为“已教”。","拼接脚本的 vm 冒烟测试可能全绿，但真实浏览器仍在首次渲染时发生 ReferenceError。","多份 initWordForge 复制实现今后可能再次产生跨周行为差异。"],"confidence":"medium","notes_for_claude_code":"本结论仅基于提供的 roundD.diff，未执行命令或测试。要把 soundType 与跨 script 时序结论提升到高置信度，需要继续读取 frontend/src/shared/graphemes.js、tools/validation/sounds_grapheme_adapter.js、四份模板展开后的完整 script/include 顺序、真实 W5 数据及 RESERVED/W 的 schema 校验规则。monthly 路径的孪生正则按任务要求未列为本轮问题。"}