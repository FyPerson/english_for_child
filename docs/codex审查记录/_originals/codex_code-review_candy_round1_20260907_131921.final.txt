{"task_type":"code-review","mode":"advice-only","summary":"三个提交的职责划分和主要事实链基本合理：颜色差异只涉及规定的第一层令牌，两个深色块一致，补图数量与基线增量也能对应。需要优先修正补图脚本名义上“幂等”但实际无法安全重跑的问题，并澄清规范中的 L* 与脚本 OKLCH L 混用；其余风险适合在下一提交周期补验证和可复现性。","issues":[{"severity":"high","category":"edge-case","location":"inject_w4_cards.py：main() 中媒体写入及 DATA 替换循环","problem":"脚本注释宣称幂等，但第二次运行时五个 data.js 条目已不是 art:null，正则替换次数会全部为 0 并触发退出。更严重的是 MEDIA 已在此之前写入，脚本没有先完成全部校验再提交写入；任何后续键匹配失败都可能留下半完成状态。因此它只对 WORD_ILL 键替换近似幂等，对整个操作并不幂等或原子。","suggestion":"先在内存中生成 media_out 和 data_out，并接受 art 为 null 或已等于对应 key：null 时替换，已等于 key 时视为成功，其他值才报冲突。确认五个键均唯一、两个输出结构均合法后，再统一写文件；同时把注释改成准确描述。修复后复审重复执行和单键冲突场景。"},{"severity":"medium","category":"contradiction","location":"palette_gen.py：week()；设计规范 §3.1","problem":"规范使用“L* 约 93–95、深色约 10–12”，脚本实际参数是 OKLCH 的 L=0.955/0.19，两者不是同一个标度。浅色输出从观感和数值上大体接近规范，但深色输出如 #0A1711、#15121C、#1C110C 换算成传统 CIELAB L* 很可能低于 10；因此目前不能用输入的 0.19 直接证明满足“L* 10–12”。","suggestion":"明确规范所指的是 CIELAB L* 还是 OKLCH L。若坚持 CIELAB L*，在生成脚本中计算最终裁剪后 sRGB 的 CIELAB L* 并设断言范围；若规范本意是 OKLCH L，则改写规范术语和范围，避免后续周主题误用。"},{"severity":"medium","category":"missing-constraint","location":"palette_gen.py：checks()","problem":"自动检查仅覆盖 ink/ground、ink-2/ground、ink/surface 和 line 与 ground 的亮度差，没有断言阈值，也没有覆盖文字实际可能出现的 surface-2、sunk、语义软色背景。脚本打印结果不等于生成失败，未来调色即使跌破 7:1 或 4.5:1 仍会正常产出并被应用。","suggestion":"对最终 sRGB 结果添加硬断言：ink/ground 与 ink/surface 至少 7:1，ink-2 在其实际背景上至少 4.5:1；根据模板选择器补齐 ink/ink-2 与 surface-2、sunk、vowel-soft、cons-soft、accent-soft、ok-soft 的真实组合。为 line 可见度定义可执行下限，而不是只打印 dL。"},{"severity":"medium","category":"edge-case","location":"inject_w4_cards.py：card_data_uri()；tools/unify_card_outlines.py：key_out_flat_bg()（实现未提供）","problem":"是否会误抠除 Dan 的白鞋带、眼白等细节，取决于 key_out_flat_bg 是仅移除与画布边界连通的近白背景，还是全局删除近白像素。当前仅凭左上角 alpha 是否为 255 决定调用去底，且长边指标无法发现人物内部被挖空；肉眼检查 192px 成品降低了风险，但不能证明不存在小面积透明孔洞。","suggestion":"读取并复核 key_out_flat_bg 的实现；应采用从边界开始的连通域抠底，并限制颜色距离和羽化范围，避免全局色键。增加规格化前后内部透明孔洞、非边界连通透明区和主体像素损失比例检查，至少对 Dan、Nat 做像素级差异检查。"},{"severity":"medium","category":"missing-constraint","location":"inject_w4_cards.py：card_data_uri() 的 m[\"long\"] 门槛","problem":"长边 85%–95% 对高瘦人物比面积门槛更合理，但它只约束外接框最长边，无法阻止另一边极窄、主体碎片化、偏心或边缘被裁切。注释称其对应“8px 留白”，但 95% 长边只保证总余量约 9.6px，若主体偏心，单侧可能明显少于 8px。","suggestion":"保留长边门槛，同时增加四边最小透明留白、主体质心偏移、最大连通域占比及触边检查。若规格要求每边 8px，应直接断言包围盒 left/top ≥ 8 且 right/bottom ≥ 8，而不是用总长边间接推断。"},{"severity":"medium","category":"other","location":"palette_gen.py、apply_palette.py、inject_w4_cards.py 的存放与路径配置","problem":"三个脚本位于 scratchpad 且 apply/inject 硬编码 E:/projects/english_for_child。当前提交结果可以审计，但下一周无法从仓库和任意检出位置稳定复现调色、规格化与注入过程；这与工程健康化第 1 期准备建立 resources/adopted 真相源的方向不一致。","suggestion":"下一提交周期把通用生成、规格化和校验脚本纳入仓库，以仓库根目录或显式命令行参数定位路径；将五张规格化 PNG 导出到 resources/adopted/week04，并让媒体构建流程从该目录确定性生成 word_ill.js。迁移前可接受当前媒体声明作为真相源，但应记录来源提示词、生成模型、原始图与规格化参数。"},{"severity":"medium","category":"ambiguity","location":"week04.data.js：认读词及 taughtWords()/showWordPicture() 相关逻辑","problem":"材料足以确认 pan、tip、tap 从 null 改为现有同名 art，也能确认五个新键用于造词工坊；但没有提供 taughtWords()、34 词清单、词卡墙过滤逻辑和 showWordPicture() 实现，因此无法仅凭当前材料证明“真正无图的只有三词”以及 i、a、see、the、is、to、it、on 不配图在所有入口都成立。尤其 it、on 是否属于可拼词并会进入词卡墙，需要代码事实支持。","suggestion":"复审时读取 week04 模板中 taughtWords()、词卡墙渲染和 showWordPicture()，以及完整 W 表、WORD_ILL 键表；建立断言：墙上需图词均能解析到媒体、明确列入免配图集合的功能词不会显示破图、工坊可生成词若配置 art 则媒体键必须存在。"},{"severity":"low","category":"readability","location":"四个 weekXX.template.html：--shadow-*","problem":"第四周换成蜜桃色后，阴影 rgba 仍沿用旧蓝灰基色 rgba(35,43,54,...)；其他周也继续使用旧主题的固定阴影基色。它们不属于规范列出的第一层换周令牌，所以本次未改不构成越界，但第四周暖色表面上可能出现轻微冷灰阴影，削弱色系统一。","suggestion":"先做实际页面视觉对照；若冷暖偏差可见，把阴影颜色抽成受控的周主题阴影令牌，并同步更新规范，而不要在本次提交中偷偷扩大“第一层令牌”范围。"},{"severity":"low","category":"other","location":"course.html 壳层与周模板主题边界","problem":"course.html 未改色本身不违反 §3.1，但壳层固定色与各周饱和度更高的新糖果底色切换时可能产生明显断层。目前材料没有壳层截图或壳与周模板的组合方式，无法判断是否属于设计缺陷。","suggestion":"用四周浅色、深色及系统/手动主题切换矩阵检查壳层到内容区的过渡。若壳层应保持品牌中性，明确写入规范；若应跟周变化，则通过周标识传递令牌，避免复制四套壳颜色。"}],"recommendations":["颜色提交可保留：diff 中每周浅色块均修改了 11 个规定令牌，未改 vowel、cons、accent、ok 等语义色；两个深色入口逐项一致，浅色 tile-face 均仍为 #FFFFFF，未发现漏改或越界修改。","对比度抽查结果可信：例如第一周 #1B2A23 对 #D5FAE9 约为 13:1，#51665C 对该底色约为 5.5:1，分别高于 7:1 和 4.5:1；仍应把阈值改成自动断言。","五张新增卡约增加 0.23 MB，最终 7.61 MB 距 8 MB 仅约 0.39 MB。若造词工坊确实会展示 dan、nat、nap、rag、pats，补齐反馈是合理的；但后续不宜继续直接堆叠 base64，应尽快完成 adopted 资源迁移并设置更早的预警线。","媒体摘要中的“6 行新增、1 行删除”与净增 5 个键并不矛盾：很可能是原末项 tub 为追加逗号产生一删一增，再新增五键；应以完整键级 fixture 确认，而不是把 tub 误算为新卡。","基线重取理由充分，且按 §3.3 单独提交优于与补图提交合并。sourceCommit 指向前一内容提交 9eb2ee3 是合理的；四周骨架哈希因模板令牌变化而全变，第四周媒体 307→312 与五张新卡一致。","第五周开始应在仓库中定义主题生成入口、色相选择规则和验收断言；不能长期依赖 scratchpad 中的 SETS 与人工复制，否则设计规范与实际令牌会再次漂移。"],"risks":["最终包体距 8 MB 护栏只剩约 0.39 MB，后续少量图片或音频增长即可触线。","深色周底与既有 vowel-soft、cons-soft、accent-soft、ok-soft 的组合未在现有 checks 中覆盖，局部控件可能出现色相冲突或边界不清。","若去白底算法执行全局近白删除，人物内部白色细节可能形成透明孔洞，192px 肉眼检查可能漏掉小缺陷。","当前补图流水线缺少仓库内原始资产和生成元数据，未来重新构建、调整压缩或追溯版权与来源会比较困难。","仅有 fixture 摘要而无完整测试实现，无法判断骨架哈希是否把纯主题变化与意外结构变化区分开，也无法确认媒体字节校验覆盖所有注入异常。"],"confidence":"high","notes_for_claude_code":"建议把 inject_w4_cards.py 的幂等性和写入顺序列为 high 必修并修后复审；其余 medium 可进入下一提交周期。更深复审需读取完整 frontend/src/weeks/week04.data.js、四个模板中的 taughtWords/词卡墙/showWordPicture 实现、frontend/src/media/week04/word_ill.js、tools/unify_card_outlines.py、test_media 及 baseline fixture 校验代码。以上结论只基于所给材料，没有宣称执行脚本、测试或修改文件。"}