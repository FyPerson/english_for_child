{"task_type":"code-review","mode":"advice-only","summary":"四份模板的主干接线总体一致：hero 均读取 `META.wallLetters`，G2/G4/G5 与小书均调用共享初始化，前三周的 `wallTileLitState` 调用形态相同。主要漂移是 week04 硬编码点亮、week01 的素材降级与音素别名处理落后于后三周；由于共享运行时和周数据正文未提供，尚不能完整核实 `segments`、游戏内部字位拆分及小书翻页实现。","issues":[{"severity":"medium","category":"contradiction","location":"frontend/src/weeks/week04.template.html:renderHome 的 hero 墙；对照 week01.template.html:1343、week02.template.html:1407、week03.template.html:1416","problem":"【漂移】前三份均以 `const lit = wallTileLitState(c)` 计算点亮态，week04 却写死 `const lit = true`。即使第四周没有新字位、当前结果预期全部点亮，硬编码仍绕过共享函数对历史字位、元数据缺项和未来数据变化的统一判定，也与“今天的 M1 点亮态抽成共享函数”和“四模板改造”的目标不一致。","suggestion":"week04 也调用 `wallTileLitState(c)`；若第四周确实要求无条件点亮，应把“无新字位时历史字位为已点亮”的规则放进共享函数并由 `META.newPatterns`、`FIRST_TEACH_DAY` 等元数据驱动，同时补一条第四周全部返回已点亮的测试。"},{"severity":"medium","category":"edge-case","location":"frontend/src/weeks/week01.template.html:bookArt、celebrateNat、printBook；对照 week02..04.template.html 的同名函数","problem":"【漂移】week01 的 `bookArt` 直接回退到 `ART[key]`，`celebrateNat` 无条件输出庆祝图片，`printBook` 无条件输出 `BOOK_IMG[pg.art]`；week02 至 week04 分别使用空串兜底、庆祝图存在性判断和打印图片存在性判断。素材缺失或键错误时，week01 可能渲染 `undefined`、空 `src` 或破图，而后三周会安全降级。小书翻页初始化本身四份均调用 `initBook()`，但素材异常路径并不一致。","suggestion":"把 week02 至 week04 的三处存在性守卫同步到 week01，最好进一步抽成共享的绘本图片、打印图片和庆祝图片渲染辅助函数，避免模板继续漂移。"},{"severity":"medium","category":"other","location":"frontend/src/weeks/week01.template.html:Phone.say；对照 week02..04.template.html:phAudioKey、Phone.say","problem":"【漂移】week01 的 `hasPhoneme` 会通过 `phAudioKey(ch)` 判断录音，但 `Phone.say` 又直接用原始字位 `k` 查询 `PHONEME_AUDIO` 和建立缓存；后三周则统一先计算 `ak = phAudioKey(k)`。如果第一周或以后回移的数据使用 `audioKey` 别名，页面会渲染可点击按钮，但点击后找不到录音。","suggestion":"令 week01 的 `Phone.say` 与后三周一致，查询、缓存均使用 `phAudioKey(k)`，重试仍保留原字位参数。更稳妥的做法是把 Phone 实现移入共享运行时。"},{"severity":"low","category":"other","location":"frontend/src/weeks/week01.template.html:renderDay；对照 week02..04.template.html:renderDay","problem":"【漂移但当前结果等价】week01 直接渲染 `d.sounds.map(...ipa)`，后三周用 `Set` 去重。第一周当前没有 c/k 这类同音多形，因此显示结果通常无差别，但同一个“今日音”逻辑存在两个版本。","suggestion":"统一采用后三周的去重实现，或抽成共享的“今日音标签”函数，避免第一周未来加入同音字位后重复显示音标。"},{"severity":"low","category":"contradiction","location":"frontend/src/weeks/week03.template.html:文件头设计假设；对照同文件 PHONEME AUDIO 注释及 renderHome 发音说明","problem":"【陈旧说明】文件头仍称第三周真人孤立音与助记插画“尚未就位”，后文却明确写六段新音已注入、插画已嵌入且可离线使用。两种说明互相矛盾，容易误导后续审查或发布判断。","suggestion":"更新文件头，使其与当前媒体注入状态一致；若仍有个别缺失素材，应明确列出具体键，而不是保留整周未就位的旧描述。"},{"severity":"low","category":"readability","location":"frontend/src/weeks/week04.template.html:PHONEME AUDIO、BOOK、G3、G4、G5 段落注释；对照 week03.template.html 同名段落","problem":"【漂移】week04 多处注释原样描述“第三本书”、第三周的 hot/hut 题组、g 订单和累计 111 词白名单，但第四周任务是巩固、基线和月测。实际常量由 `week04.data.js` 注入，注释很可能已经与数据不符。","suggestion":"按 week04 实际数据重写这些段落说明，至少写明第四周各游戏是否复用第三周题库、基线词是否隔离，以及 BOOK 是三本复读集合还是单一本书。"},{"severity":"medium","category":"ambiguity","location":"四份模板的 renderDay 初始化调用及构建期 include","problem":"【材料边界】四份均调用 `initG2()`、`initG3()`、`initG4()`、`initG5()`、`initFlash()`、`initBook()`、`initG1()`；但 week01 缺 `initWordForge()`，week02 缺 `initInitialPick()`，week03、week04 两者都有。该差异可能是合法周内容差异，也可能是消费者接线遗漏，仅凭模板中被省略的周数据和未提供的共享渲染器无法定性。同样，`segments`、按字符切词及小书翻页的核心实现位于共享文件，附件只能确认模板调用，不能确认实现一致性。","suggestion":"对照四个 `weekNN.data.js` 中是否存在 `wordforge`、`initialpick`、book 块，再核对 `render-blocks.js` 的块到 DOM 映射和 `games.js` 的初始化器是否可安全空调用。若初始化器幂等且空调用安全，可四份统一调用全集，减少按周漏接线风险。"}],"recommendations":["优先消除 week04 的 `const lit = true`，让四份 hero 墙统一经过 `wallTileLitState`。","将 Phone、绘本素材降级、庆祝素材降级和“今日音”格式化迁入共享层，模板只保留周数据和文案。","增加构建期静态检查：禁止模板出现用于字位拆分的 `.split('')`、`[...word]` 和 `word.length`，并校验所有墙字位来自 `META.wallLetters`。附件可见部分未发现这些按字符切词写法；唯一 `.split` 是打卡键的 `key.split('-')`，与字位无关。","增加四周矩阵测试：hero 字位顺序与标签、可听字位的 `data-sayph`、G2/G4/G5 初始化、书页前后翻页、缺图降级、同音多形录音别名和第四周历史字位点亮。","附件可见范围内未发现 hero 墙硬编码字母列表；`sat`、`cat`、`dog` 示例词、周号、存储键、打印证书序号属于合法周内容差异。"],"risks":["week04 绕过共享点亮函数后，共享规则或元数据发生变化时不会得到相同修复，可能掩盖 `META.wallLetters` 配置错误。","week01 的图片与音频别名降级路径可能只在素材缺失、构建漏注入或新增同音多形时暴露，常规成功路径不容易发现。","未读取共享运行时和周数据正文前，无法排除 `games.js` 或 `render-blocks.js` 内仍按 Unicode 字符拆词，也无法确认缺少的两个初始化调用是否确属合法。","week04 的陈旧注释可能使维护者错误修改音频提取清单、题库或基线隔离规则。"],"confidence":"medium","notes_for_claude_code":"模板层材料足以确认上述显式漂移，但不足以完成任务要求中的 `segments`、三个游戏内部字位显示、小书翻页和初始化差异定性。建议继续读取 `frontend/src/shared/render-blocks.js`、`frontend/src/shared/games.js`、`frontend/src/shared/graphemes.js`，以及 `frontend/src/weeks/week01.data.js` 至 `week04.data.js`；若要核实第四周点亮语义，再一并查看 `wallTileLitState` 使用的元数据定义和相关测试。"}