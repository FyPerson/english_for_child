{"task_type":"code-review","mode":"advice-only","summary":"未发现 high/critical 问题，按“0 high”门禁可放行。实现整体符合温和指路纪律；主要改进点是日历日差的极端时区边界，以及日期控件的失败反馈与可访问性。","issues":[{"severity":"medium","category":"edge-case","location":"week01-v3.html：todayDayN()","problem":"当前通过两个本地时间对象的毫秒差除以 86400000，再用 Math.round 推算日序。普通一小时夏令时切换通常能被 round 正确吸收，但这并非严格的日历日计算；遇到午夜时区跳变、跨国际日期变更线式的历史规则或较大的时区偏移变化时，仍可能偏差一天。","suggestion":"按本地年月日提取字段后，用 Date.UTC(年,月,日) 生成纯日历序号再相减；这不是解析 UTC 日期字符串，因此不会把用户输入解释成 UTC 日历日，同时可消除 DST 和本地午夜长度变化的影响。"},{"severity":"low","category":"edge-case","location":"week01-v3.html：data-startdate-act=\"confirm\" 分支","problem":"日期为空或无效时，确认分支直接静默返回，界面没有任何反馈。用户可能误以为已保存；在不完整支持 type=date 的浏览器或手工清空输入时更明显。","suggestion":"在控件内显示简短错误文本或将确认按钮在空值时禁用；reveal 后主动聚焦输入框。仍应保持温和语气，不引入催促信息。"},{"severity":"low","category":"readability","location":"week01-v3.html：开课日期动态控件","problem":"动态替换出的日期输入框没有可关联的 label，首页与页脚入口替换后也未主动转移焦点；仅依赖视觉上下文的用户较难理解当前控件用途。","suggestion":"为输入框添加唯一 id 和可见或屏幕阅读器标签，例如 aria-label=\"Day1 开课日期\"；替换 innerHTML 后调用 input.focus()。"},{"severity":"low","category":"other","location":"week01-v3.html：state.startDate/state.startDatePromptDismissed 读取清洗","problem":"非法日期删除和 dismissed 类型复位只修改内存中的 state，当前片段未显示清洗后立即持久化，因此损坏值可能继续留在 localStorage，并在每次启动时重复清洗。运行期行为仍正确，但持久化状态不完全规范化。","suggestion":"若现有 games/days 清洗已有统一的脏标记与回写机制，应把这两个字段纳入；否则记录 cleaned 标志，在全部 schema 清洗结束后统一保存一次，避免清洗过程中多次写入。"}],"recommendations":["保留 dismissed 与 startDate 相互独立的设计，并补齐两条清除语义测试：首次设置后清除应重新出现提示；曾选择“不用了”后通过页脚设置再清除，提示仍不复现。","为跨年、闰日、普通夏令时切换、未来日期、超过第 7 天和系统日期前后调整补充 todayDayN 单元测试；系统时钟错误只允许影响角标，不得写入进度状态。","确认完整点击委托中不存在更早的通用 button、data-goto 或 return 分支截获 data-startdate-act；当前节选不足以独立证明整个委托顺序无冲突。","维持当前安全边界：startDate 经严格数字格式校验后才进入 innerHTML，且日期结果仅控制装饰性角标，不参与解锁、跳转或完成状态。"],"risks":["用户系统日期设置错误时，“今天”角标会跟随错误日期；由于实现不写进度、不锁定也不催促，影响被限制为临时指引错误。","极少数具有非标准午夜时区跳变的地区可能触发当前毫秒差算法的日序偏差。","本次仅依据所附 diff 和规格进行静态复核，无法独立确认完整全局事件委托及 save/初始化调用顺序。"],"confidence":"medium","notes_for_claude_code":"建议重点读取完整 week01-v3.html 中 document.click 委托的全部前置分支、save()、启动渲染入口及后续 games 清洗代码。用户提供的 8/8 与 38/38 属参考结果，本复核未独立执行测试或验证行为。"}