{"task_type":"code-review","mode":"advice-only","summary":"共享豁免判据和真实分类入口的方向正确，但 `check_data.js` 仍只传入本周认读词，导致跨周认读词规则在两个消费方之间继续不一致。本轮发现 1 个高严重度问题，另有若干契约与兼容性风险。","issues":[{"severity":"high","category":"contradiction","location":"tools/validation/check_data.js，第③节豁免过滤","problem":"`isExemptConsumptionRecord` 接收的 `cumulativeSightWords` 实际是当前文件的 `SIGHT`，而语义套件会累积此前各周的 sight 记录。因此历史周认读词在后续周 `sentences` 或 `book-page` 中出现且零解时，语义套件会豁免，单周 `check_data.js` 却会报错；两个生产含义并未真正统一。现有真实四周通过只能说明当前数据没有触发该分支，不能覆盖第五周及以后。","suggestion":"让 `check_data.js` 根据 `META.week` 加载并合并截至当前周的认读词，且与语义套件共用同一个“按周构建累计认读词集合”的辅助函数。补充入口级测试：第一周声明 sight、第二周仅在 book-page 引用且不可分词，第二周 CLI 应通过；同时保留非阅读来源不豁免的反例。"},{"severity":"medium","category":"contradiction","location":"tools/validation/exemptions.js:isExemptConsumptionRecord；tests/unit/test_word_consumers.js 的 M1 测试","problem":"M1 明确断言 `g1-rounds` 记录携带 `week`，但共享判据完全忽略 `rec.week`，只信任调用方的 `options.week`。两者不一致时可错误豁免或错误报红；`options.week` 缺失、字符串化或非法时，pos 桶还会静默返回 false，而不是暴露调用契约错误。","suggestion":"确定唯一周次来源。优先使用并校验 `rec.week`，若同时传入 `options.week` 则要求二者是相同的正整数，不一致时抛结构化错误；或者删除记录上的 `week` 契约及相关测试，并对 `options.week` 做严格正整数校验。增加缺失、非法和二者不一致的测试。"},{"severity":"medium","category":"edge-case","location":"tools/validation/load_data.js:countDeclarationOccurrences 与 loadData 的重复声明普查","problem":"正则只能识别“行首带缩进的 const”，不能判断真正的顶层作用域或脚本类型。函数体中的局部 `const W`、模块脚本中独立作用域的同名声明，或非课程数据脚本中的合法局部变量，都可能被计为重复并拒绝整个文件。这里实际实施的是“所有 script 文本中任何行首同名 const 均禁止”，比注释所称的“顶层常量唯一”更宽。","suggestion":"优先通过明确的 script 标识只选择课程数据脚本；否则逐个 script 解析 AST，仅统计目标数据脚本的 Program 顶层声明，并明确处理 `type=\"module\"`、JSON 脚本和普通脚本。至少增加“辅助函数内部存在同名局部 const 不应触发重复”的兼容性测试。"},{"severity":"medium","category":"edge-case","location":"tools/validation/check_data.js 调用 isExemptConsumptionRecord 的 records.filter","problem":"共享判据对缺失或非法 bucket 会抛错，但该调用位于现有分词错误处理之外。当前直接函数测试只验证错误对象，未覆盖 CLI 如何呈现此错误；若没有外层统一捕获，检查器可能输出未处理异常栈，而不是稳定、可定位的校验失败。","suggestion":"在 CLI 顶层统一捕获结构化校验错误并输出 code、word、kind、bucket、week 后以约定退出码结束，或在过滤阶段转换成 `ok(false, ...)`。补充一个入口级测试验证输出和退出码。"},{"severity":"low","category":"readability","location":"tools/validation/wall_order.js 的非法 META.week 消息及 test_wall_order.js 表驱动测试","problem":"`JSON.stringify(NaN)` 返回 `null`，所以 NaN 用例的错误消息会把实际值显示成 null。新表驱动测试又移除了原先对 `caught.actual` 的断言，因此无法发现消息失真或结构化字段回归。","suggestion":"使用能区分 NaN、Infinity、undefined 和字符串的格式化函数，并在每个表项中声明期望 actual，逐例断言 `caught.actual`；NaN 使用 `Number.isNaN`。"},{"severity":"low","category":"readability","location":"tools/validation/check_data.js:nonExemptDescr","problem":"非豁免来源按原始记录直接拼接，没有去重或计数。同一词大量重复出现时，错误消息可能非常长，反而掩盖关键来源；而“全部来源”又只保留 kind，定位粒度不对称。","suggestion":"按 `kind/bucket/week` 去重并附出现次数，必要时限制展示条数，同时保留总数。"}],"recommendations":["先修复跨周认读词集合的同源构建，再把共享判据视为唯一权威实现；否则只是共享了布尔函数，没有共享其关键输入语义。","按任务给定规则，`words` 和 `wordforge-*` 属于字位操作来源，认读词在其中要求可分词是预期行为，不应因为“认读词通常不规则”而恢复整词豁免；应在数据规范中明确这一约束，并加入模拟第五周的正反测试。","现有 M2 已让合成场景和真实四周包装调用同一个 `classifyConsumptionRecordsForWeeks`，足以消除测试内复制循环；无需以“删除共享函数后仍能否通过”作为必要反证。更有价值的是对周次乱序进行拒绝或排序，避免累计集合产生时间倒流。","十例表驱动的期望值来自任务明示规则，并包含多个 false 分支，不属于由实现动态计算期望值的恒真测试；所述将判据强改为恒 true 能使负例失败，判别力基本成立。","重复声明扫描对约二十个名称进行线性正则遍历，针对普通周文件通常不是实质性能风险；应优先审查作用域和脚本选择的正确性。"],"risks":["第五周及以后复用历史认读词时，单周检查器可能产生与语义套件相反的结果并阻断合法数据。","多个周次来源未校验一致性，会使 G1 pos 豁免取决于调用方偶然传入的值。","对全部 script 文本做正则普查可能把合法局部声明或非数据脚本识别为重复声明，形成兼容性回归。","结构化 bucket 错误若未在 CLI 边界捕获，可能破坏既有错误格式和退出码约定。"],"confidence":"high","notes_for_claude_code":"本审查仅依据所给差异文本。若要进一步确认，应阅读 `check_data.js` 中 `SIGHT` 的完整构造位置、`word_consumers.js` 的 G1 记录生成逻辑、`load_data.js` 的完整 script 提取正则，以及未来周数据约定。本轮 HIGH 数：1。"}