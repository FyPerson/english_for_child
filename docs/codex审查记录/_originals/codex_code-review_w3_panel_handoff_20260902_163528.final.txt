{"confidence":"medium","issues":[{"category":"edge-case","location":"[A] bindLongPress_excerpt.js：bindLongPress.start/cancel；panel_week02.diff：.parent-gate","problem":"触摸开始后没有监听移动或重新命中检测，且入口设置了 `touch-action:none`、`touchstart` 又调用 `preventDefault()`。因此手指从按钮上开始滑动时页面不能正常滚动，滑出按钮也不会取消计时，停留满 1.5 秒仍可能打开面板或触发重置；鼠标路径还未限制主键，长按右键也能触发。双击和长按页脚其他位置本身不会触发。","severity":"high","suggestion":"优先改成统一的 Pointer Events 实现：只接受主指针及鼠标左键，记录起点，`pointermove` 超过小阈值或离开命中区域即取消，并处理 `pointercancel`、`lostpointercapture`、页面隐藏。仅在确有需要时阻止默认行为；同时增加真实触摸拖动测试。"},{"category":"edge-case","location":"[A] panel_week02.diff：resetWeekAndReload()","problem":"`localStorage.removeItem(KEY)` 的异常被静默吞掉，之后仍然刷新。如果隐私模式、存储策略或浏览器异常导致删除失败，旧状态会在刷新后完整恢复，但界面刚刚向家长表示已经执行了重置。","severity":"high","suggestion":"删除后读取并确认 `localStorage.getItem(KEY) === null`，只有成功时才刷新；失败时留在当前页并显示明确错误。不要空 `catch`，至少向面板反馈“未能清除，请重试”。"},{"category":"ambiguity","location":"[B] 插画文档 §2.3、§4、§5、§8","problem":"`dog.png` 被要求复用 `book/_char_dog.png`，`hug.png` 被要求复用 `book/danHug.png`，但注入器按目录和精确文件名配对，文档没有明确要求生成后复制为这两个词卡文件。副机可能只保留原文件，最终缺少两个 `W[].art` 资源；“50 张”也混淆了生成次数与最终文件数。","severity":"high","suggestion":"增加明确映射步骤：将最终 `_char_dog.png` 原样复制为 `word-cards/dog.png`，将最终 `book/danHug.png` 原样复制为 `word-cards/hug.png`；分别写清“独立生成图数量”和“落盘文件数量”，并把这两项加入 dry-run 前检查。"},{"category":"contradiction","location":"[B] 插画文档 §1 通用风格前缀与 §4/§5 各主体提示词","problem":"通用前缀强制 `Single subject`、`facing the viewer`、`no background scenery`，但 `lap`、`big`、`mud`、`fog`、`gap` 以及多页小书明确需要多角色、物体关系或侧向动作。`fog` 又要求对象“faded behind”雾层，容易引入透明渐变，直接违背“无渐变”。作为副机唯一依据，这些冲突会造成不可预测的构图。","severity":"high","suggestion":"保留既有风格文字时，另设“关系词卡/小书场景覆盖条款”，明确允许一个紧凑场景内多个主体、允许侧视动作，并声明覆盖 `Single subject` 与 `facing the viewer`。把 `fog` 改为用边缘清晰的纯色雾块遮住物体下半部，禁止淡出和半透明渐变。"},{"category":"edge-case","location":"[A] panel_week02.diff：openParentPanel() 与 close/setdate 分支","problem":"`bindLongPress()` 返回的取消函数没有保存。若重置计时已经开始，而面板随后通过第二指针、键盘或其他重绘路径被清空，旧 DOM 虽然消失，计时器仍可能到点执行重置。普通 `renderHome()` 重绘不会造成长期重复绑定，但“绑定时正在计时”的节点不能仅靠垃圾回收保证安全。","severity":"medium","suggestion":"保存面板重置按钮的取消函数，在 `close`、`setdate`、再次打开面板以及任何替换面板 DOM 的路径调用；同时确认 `renderHome()` 开头确实执行 `clearPageTimers()`，并加入“按住重置时关闭或重绘面板不会清空”的测试。"},{"category":"other","location":"[A] 家长设置入口和重置按钮的可访问性属性","problem":"两个原生按钮对键盘只有普通 click，而 click 被有意忽略，因此 Enter/Space 完全不可达；入口也没有 `aria-expanded`、`aria-controls`，关闭后不恢复焦点。现有 `aria-label` 与视觉上的 1.5 秒要求一致，但没有表达展开状态。","severity":"medium","suggestion":"至少支持 Enter/Space 按住 1.5 秒的 `keydown`/`keyup` 路径，或提供同等强度的键盘二次确认；补充 `aria-expanded`、稳定的面板 id 与 `aria-controls`，关闭时将焦点还给入口。若决定不支持键盘，应把该设备范围作为明确的已接受限制。"},{"category":"edge-case","location":"[A] smoke_parent_panel.py；panel_tests_spec.diff 中 smoke_w3_browser.py 第 ⑬ 段","problem":"冒烟只用鼠标事件，无法发现触屏拖动仍触发的问题。主题断言 `localStorage.getItem('soundblocks-theme') !== undefined` 永远为真，因为不存在时返回 `null`；而测试前还执行了 `localStorage.clear()`，根本没有建立“主题应被保留”的前置状态。重载只靠固定等待，也可能产生时序性误判。","severity":"medium","suggestion":"重置前写入一个确定主题值，重置后断言严格等于该值；增加触摸或 Pointer Events 用例，覆盖滑出、滚动、取消、非主键和多指重绘；用导航等待或页面初始化标记替代固定等待，并同时断言旧进度消失、默认首页已完成初始化。"},{"category":"ambiguity","location":"[B] 插画文档 §4：fog、gap、fit、leg","problem":"几张图的语义仍可能让 7 岁孩子认错：透明底上的“缺失木板”不容易表现为 `gap`；`fit` 的双手点赞更像“很好”；`leg` 的迈步动作更像“走”；`fog` 的房屋树木组合可能被认成天气场景而非雾。","severity":"medium","suggestion":"将 `gap` 明确为五个固定槽位中只有四块木板、中央留出显著窄空隙；让 `fit` 用合身衣服与明显过大衣服作紧凑对比；将 `leg` 改为单独、清晰的腿部指示构图；`fog` 使用被不透明雾块遮住大半的高对比物体，并要求缩至 112px 复核。"},{"category":"contradiction","location":"[B] 插画文档 §0、§7、§8","problem":"§0 声称 `embed_assets.py` 已负责统一 3px 描边，§7 又把 `unify_card_outlines.py` 描述为补救手段，§8 却要求无条件运行且未给出参数、作用范围和顺序。副机无法机械判断它是必跑步骤还是仅在描边不合格时使用，也无法判断重复处理是否会加粗描边。","severity":"medium","suggestion":"核对脚本后统一为一种管线：若注入器已幂等处理描边，就从必检项删除独立脚本；若必须运行，则给出完整命令、运行顺序、输入目录和幂等说明。"},{"category":"readability","location":"[A] openParentPanel() 中重置按钮及说明文案","problem":"整体语气中性，没有对孩子说教或教育家长的问题；但“本周从头再来”比实际行为温和，说明虽列出清除范围，却没有直接说数据无法恢复。","severity":"low","suggestion":"保留用户拍板的按钮名，在旁边补一句“清除后无法恢复”；存储删除失败时则不得显示成功或刷新。"}],"mode":"advice-only","notes_for_claude_code":"静态对照中，B 的 6 个音素 art 键、36 个非空词卡 art 键、6 个 BOOK art 键均与 week03-data.js 对上，未发现漏键、多键或拼写错误；`let` 为 null 的处理一致。C 的清单在所给文本内自洽：111 个 A 组词，加 C 组 `to`、D 组 `fish`、13 个 E 组 demo 词和 6 个 F 组句子，合计 132；7 个 unreachable 也恰好是仅属于旧音素卡 demo 且不在其他可达源中的词，F 组覆盖参数一致，未见必须修改项。无法仅凭现有材料确认通用风格前缀是否与第二周文档逐字一致，也无法验证运行时可达性、64 条复用音频及清理生命周期；进一步审查需读取第二周插画文档、完整 week01/02/03.html、laterOnce/clearPageTimers/save 初始化代码、音频提取报告及相关处理脚本。门禁结论：当前不建议直接推送。必须先修长按移动取消、删除失败处理、复用图片的落盘映射和提示词硬冲突，并补上能覆盖这些行为的测试；键盘替代、焦点属性及文案微调可在明确接受限制后带着走。","recommendations":["必须先修：完善长按的移动取消和主指针限制；删除失败时不得刷新；明确 `dog.png`、`hug.png` 的复制映射；消除通用前缀与关系场景的冲突。","推送前测试应覆盖真实触摸拖动、活动计时器遇到面板关闭或重绘、存储删除失败、主题值精确保留以及重载完成状态。","C 可按当前清单进入下一步，但提交前仍应由实际 week03.html 的提取报告逐键比对，并抽听 F 组句子和新增单词，避免仅凭元数据判定音频正确。","可以带着走：在明确目标设备只使用触摸和鼠标的前提下，键盘替代可登记为已知限制；`aria-expanded`、焦点恢复和“无法恢复”文案仍建议尽快补齐。"],"risks":["孩子从家长按钮位置开始滑动或停顿时，可能在无明确确认意图下打开面板或执行重置。","存储删除失败会刷新并恢复旧数据，造成家长误以为重置成功。","副机若按当前文档机械执行，可能缺少 `dog.png` 和 `hug.png`，或因互相冲突的构图指令生成不可用插画。","现有主题保留断言属于假绿，鼠标冒烟也掩盖了最关键的触屏手势缺陷。","未读取完整引擎和第二周提示词真相源，仍存在活动计时器清理、前缀逐字一致性及运行时音频可达性方面的残余风险。"],"summary":"当前不建议直接推送：A 中触屏拖动仍可能触发长按且删除失败会被伪装成成功，B 中两份复用图片的落盘方式及通用前缀存在会直接影响副机交付的歧义。C 的数量、分组、参数和不可达键在所给文本内自洽，未发现阻断问题。","task_type":"code-review"}