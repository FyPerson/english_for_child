{"task_type":"code-review","mode":"advice-only","summary":"整体方向正确，但仍有数个会削弱迁移安全性的实质缺口：默认写回可能在仍有歧义词时返回成功、严格着色会随字位表扩充产生新的歧义、共享 schema 的全局问题没有被 check_data 消费。建议修完高优先级问题后再作为第 7 步地基。","issues":[{"severity":"high","category":"edge-case","location":"tools/validation/gen_segments.js：main / needsHumanExit","problem":"默认 --write 不写回 resolved 歧义词，但 needsHumanExit 只统计 tie、unknown、error。若文件只有 resolved 项，命令会返回 0，CI 或调用者可能误认为全部处理完成，实际仍有必须人工决定的词。现有测试依赖 pqr/zap 产生非零，未覆盖这个场景。","suggestion":"当未启用 --write-heuristic 且存在 resolved 时也返回非零，或定义独立的“存在未落盘候选”退出码；新增仅含 unique+resolved、没有 tie/unknown/error 的 fixture，分别断言 dry-run、--write、--write-heuristic 的退出码。"},{"severity":"high","category":"missing-constraint","location":"frontend/src/shared/graphemes.js：colorStrictWord；frontend/src/shared/render-blocks.js：严格着色调用点","problem":"“当前可唯一解码”不是稳定的数据契约：以后向 SOUNDS 增加 at、ai 等 ID，原本唯一解析的旧词可能变成 segment-ambiguous，严格渲染会突然白屏。仅给包含多字母字位的词补 segments 不够，受新 ID 影响的既有纯单字母词也必须固定教学分词。","suggestion":"第 7 步扩充 SOUNDS 前，对所有 strict 消费词按扩充后的完整字位表重新分析；凡存在多解都必须在 W 中声明 segments。增加回归测试：先以旧表验证唯一，再加入重叠字位 ID，确认显式 segments 仍使所有 strict 调用可渲染。"},{"severity":"high","category":"missing-constraint","location":"tools/validation/check_data.js：soundsIssuesByKey 分组与 Object.keys(SOUNDS) 循环","problem":"validateSoundsSchema 返回的表级问题使用 id:null，例如 sounds-prototype-chain、sounds-invalid；check_data 只遍历 SOUNDS 自有键并读取 soundsIssuesByKey[k]，这些全局问题永远不会传给 ok()。新增 poisoned 原型单测只测校验器本身，没有证明生产校验入口会失败。","suggestion":"单独收集 issue.id == null 的问题并执行一次 ok(globalIssues.length===0, ...)，再处理逐键问题；为 check_data CLI 增加带污染原型或非法 SOUNDS 容器的端到端 fixture。"},{"severity":"medium","category":"edge-case","location":"frontend/src/shared/games.js：surfaceOfSafe","problem":"该守卫 catch 所有异常，除预期的空槽/陈旧状态外，也会吞掉 SOUNDS 缺项、非法 sound 类型和实现错误；这与 colorLenientWord 采用错误码白名单的修复原则不一致，会把真实缺陷表现成一次无声的不匹配。","suggestion":"先显式检查 slots 是否为数组且是否含 null，命中则返回 null；完整槽位直接调用 surfaceOf。若确需捕获，只白名单捕获代表空槽或非法槽状态的结构化错误码，其余异常重抛。"},{"severity":"medium","category":"security","location":"frontend/src/shared/games.js、frontend/src/shared/render-blocks.js、frontend/src/weeks/week01.template.html：HTML 模板插值","problem":"本批补齐了英文词和部分属性的转义，但多处数据文本仍裸插入 innerHTML，例如 d.zh、demo 的 zh、句子 zh、s.ipa/s.cue、WALL_HINT 的 en/zh、BOOK.zh 和 BOOK_IMG[pg.art] 属性。若这些字段并非构建期完全可信，仍存在 HTML/属性注入；即使可信，当前“纯文本路径均转义”的安全边界也不一致。","suggestion":"明确字段信任模型。所有声明为纯文本的字段统一走 escapeHtmlText，URL/属性字段走 escapeHtmlAttribute 并对 URL 做协议或资源键约束；若某字段允许富文本，只能走完成后的 colorRichText 白名单解析。还需读取其余三个模板全量内容才能确认同构改动没有遗漏。"},{"severity":"medium","category":"missing-constraint","location":"tests/unit/test_render_smoke.js：HARNESS / makeSandbox","problem":"测试只直接调用静态 blockHTML，querySelectorAll 恒为空，因此 G1 meaningChip、G2 stage/reveal、G3 门、G4/G5 rack/slot/feedback、flash drawCard、book draw 等初始化后或交互态渲染完全没有执行。它能拦住 sound demo 的 C1，却不能支持“全部渲染路径冒烟”的结论。","suggestion":"把名称和说明收窄为 blockHTML 数据冒烟；另增加最小状态驱动测试，至少实例化并触发 G1 正误反馈、G2 单词切换、G4/G5 填满/撤回/确认、flash word/sound 两类卡面和 book 页面。优先把内部 HTML 生成函数抽成可直接测试的纯函数。"},{"severity":"medium","category":"missing-constraint","location":"tests/unit/test_grapheme_migration.js：scanForLResidue / scanForLegacyTrue","problem":"白名单 count 实际统计的是“命中的行数”，不是调用或模式出现次数；同一行新增第二个 legacy:true 或 .L 命中不会增加计数，因此可绕过所谓精确命中数门槛。配额还按文件顺序消耗，不能证明被豁免的是预期位置。","suggestion":"对每行使用全局匹配统计全部 occurrence，或登记稳定的行内容哈希/精确片段；至少加入同一行出现两次 legacy:true 和两次 .L 的反例，证明额外出现会被报告。"},{"severity":"medium","category":"edge-case","location":"frontend/src/shared/graphemes.js：colorizeToken / WORD_TOKEN_PATTERN","problem":"按 graphemeLabel.length 切原串只对规范化前后 UTF-16 长度不变的 ASCII 输入可靠。当前 ID 和 token 正则把生产路径基本限制在 ASCII，因此断言能拦截而不是静默错位；但 Unicode 组合字符、全角字母、部分大小写映射会被降级或触发 length mismatch，接口注释却容易被理解为通用文本支持。","suggestion":"把“可着色词仅支持 ASCII A-Z/a-z，grapheme 也必须为 ASCII”写进 schema 并校验，或改为基于规范化映射保存原文边界。为组合字符、全角字符、土耳其 İ 等加入明确的拒绝/降级测试，固定预期行为。"},{"severity":"low","category":"readability","location":"tests/unit/test_word_coloring.js：stripTags 与大小写往返断言","problem":"stripTags 正则只能证明可见字符串近似保留，不能验证生成 HTML 结构合法、实体解码后的文本相同，也不能发现多余属性或错误嵌套；当前用例也未覆盖显式 segments 与多字母字位的大小写切片。","suggestion":"增加 ai 等多字母字位及 explicit segments 用例，并用可信 HTML/DOM 解析后的 textContent 和元素结构断言；至少精确断言期望 HTML，而不是只剥标签比较。"},{"severity":"low","category":"other","location":"frontend/src/shared/graphemes.js：colorRichText；ctx_33.md §3.3","problem":"规格要求定义并实现受限富文本安全边界，但接口当前固定抛错。材料称现有调用点不使用它，因此本批可不阻塞；不过 assessment_contract 已暗示未来可能出现标签，若数据先变化就会产生运行时故障或诱使调用方绕过接口。","suggestion":"在 check_data 中显式拒绝当前纯文本字段包含标签，直到 colorRichText 完成；同时为未来实现保留白名单 DOM 语义测试，避免数据演进早于渲染能力。"}],"recommendations":["先修复 gen_segments 的成功退出语义和 check_data 对表级 schema 问题的漏报，这两项直接影响自动化门槛可信度。","把第 7 步验收条件改为“扩充后的完整 SOUNDS 下，所有 strict 消费词均唯一或有显式 segments”，不要只枚举已经含多字母 ID 的词。","将渲染测试明确拆成静态 blockHTML 冒烟与交互状态渲染两层，并补真实多字母字位 fixture。","取得 week02、week03、week04 完整 diff 或文件内容后逐份比较；仅凭 diffstat 无法核实四模板一致性。"],"risks":["第 7 步新增重叠字位后，既有严格着色词可能因新歧义导致整页渲染失败。","生成工具可能以退出码 0 掩盖尚未处理的启发式候选。","共享校验器虽然发现表级异常，生产 check_data 入口却可能静默丢弃。","现有冒烟测试的绿色结果可能被误解为游戏交互态渲染已覆盖。"],"confidence":"high","notes_for_claude_code":"本审查仅依据所附 diff、ctx_33.md 与任务书，没有读取仓库其他文件，也未运行测试。深入核实 D/G 需读取四份完整模板、escapeHtmlText/escapeHtmlAttribute 实现、segmentWord/surfaceOf 实现、load_data.js、assessment_contract.js 及各周真实数据。总评：这两个 commit 的架构方向可以保留，但在退出码、表级 schema 漏报和扩表后 strict 歧义契约修复前，不建议视为第 5/7 步的稳固地基。"}