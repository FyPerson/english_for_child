{"confidence":"high","issues":[{"category":"contradiction","location":"“模块形态”与方案②-a","problem":"②-a 在浏览器使用原生 DOMParser、Node 使用手写解析器，严格说是两套实现；即使 fixture 语义相同，也不能满足“一份实现同时被浏览器与 Node 使用”的字面要求。","severity":"high","suggestion":"选择②-b，让浏览器与 Node 调用同一字符串扫描/白名单实现，并把非法结构统一判为构建错误。"},{"category":"ambiguity","location":"“着色安全”验收判据","problem":"当前数据只来自受版本控制的仓库和 AI，未跨越终端用户输入边界，因此“富文本必须解析并对文本节点着色”不是当前威胁模型下的最低安全要求，更像防误配置和未来扩展的纵深防御。","severity":"medium","suggestion":"明确区分“当前安全最低线”（纯文本必须转义、富文本必须拒绝未知结构）与“功能要求”（是否给富文本着色）；不要把两者混成同一不可协商的安全条件。"},{"category":"security","location":"富文本渲染及构建流程","problem":"即使来源可信，AI 仍可能误写事件属性、危险标签或未闭合结构；直接 innerHTML 会使本地打开的课件执行这些内容。","severity":"high","suggestion":"无论是否着色，都在构建期对富文本做严格白名单校验并拒绝未知标签、属性、注释、命名空间和畸形嵌套；不要把“没有终端用户输入”当作免检理由。"},{"category":"edge-case","location":"方案②-b 的字符串扫描器","problem":"最易出错的是嵌套/未闭合标签、`&lt;` 或 `&amp;` 等实体、属性引号变体，以及把文本中的 `<` 误当标签；错误处理不一致会造成漏着色或重新产生 HTML 注入。","severity":"high","suggestion":"先冻结严格语法（仅接受规范规定的标签和属性写法），对任何畸形输入整体拒绝；测试实体、嵌套、缺闭合标签、额外属性和尖括号文本。"},{"category":"edge-case","location":"方案②-a 的跨解析器比较","problem":"在当前规范化输入上，两端差异主要是理论风险；但浏览器 DOMParser 可能自动修复畸形嵌套或解码实体，而手写解析器通常会拒绝，AI 偶发偏离规范时就会变成实际差异。","severity":"medium","suggestion":"若仍采用②-a，必须先定义“规范语法”并在两端都拒绝非规范输入；仅比较通过 fixture 不能证明任意输入等价。"}],"mode":"advice-only","notes_for_claude_code":"结论：选②-b。当前威胁模型并不要求富文本一定着色，但建议保留该功能并将白名单校验定位为纵深防御；若愿意牺牲富文本着色，‘构建期白名单验证 + 富文本原样不着色’是更简单的第三方案。","recommendations":["采用②-b，维护一份浏览器/Node 共用的严格白名单扫描实现。","把富文本语法限定为规范化、可拒绝的形式；不要尝试兼容浏览器自动修复行为。","将非法富文本作为构建失败处理，并加入实体、嵌套、畸形标签和额外属性测试。","在文档中明确：纯文本路径承担转义安全，富文本路径承担白名单验证；富文本着色是功能选择而非当前输入边界下的唯一安全方案。"],"risks":["扫描器对实体和嵌套边界处理错误，可能导致文本漏着色或 HTML 注入。","未来若数据来源扩展到用户输入，当前“可信仓库”假设失效，需重新评估并加强运行时隔离。","若允许规范外的标签写法，②-a 的跨解析器差异会从理论问题变成可见行为不一致。"],"summary":"结论：选择②-b。它满足浏览器与 Node 共用同一实现，避免②-a的双解析器不一致；在当前可信仓库输入模型下，富文本“解析后只对文本节点着色”并非最低安全必需，但仍适合作为防 AI 误写和未来扩展的纵深防御。","task_type":"spec-critique"}