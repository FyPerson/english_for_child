{"task_type":"code-review","mode":"advice-only","summary":"本批主线方向合理，但仍有两个高优先级缺口：P16 的入口校验只约束了 RESERVED，未约束同样被补入 W 的 RESERVED_RETEST；新的墙面接线测试保留原返回值，仍可能被“调用了共享函数但渲染继续使用重复逻辑”绕过。仅凭所给 diff 无法确认新增十个 W 条目是否会被枚举型消费者展示给儿童。","issues":[{"severity":"high","category":"missing-constraint","location":"tools/validation/check_data.js「① 引用完整性」及 tests/unit/test_check_data_reserved_in_w_week4.py","problem":"校验器仍只执行 `for (const w of RESERVED) ok(W[w], ...)`，没有要求 RESERVED_RETEST 存在于 W；新测试也只删除 RESERVED 中的 fib。与此同时，本批注释、W4 数据迁移和月测 CVC 校验都把 RESERVED 与 RESERVED_RETEST 十词作为同一组依赖 W 的词处理。将来复测词需要显式 segments 时，仍可能因缺少 W 条目而出现分词歧义或运行时读取失败。","suggestion":"明确契约是否覆盖 RESERVED_RETEST。若覆盖，应对 `[..., RESERVED, ...RESERVED_RETEST]` 统一校验，并增加一个仅删除复测词 W 条目的 CLI 反例；同时让 migration_audit 的同类规则与入口校验使用相同词池。若规范确实只约束 RESERVED，则应删掉“十词都必须入 W”的误导性注释，并单独说明复测词入 W 的依据。"},{"severity":"high","category":"other","location":"tests/unit/test_render_smoke.js：wallCallTrackingForWeek","problem":"猴子补丁只是调用 `original(id)` 并原样返回，因此只能证明 wallTileLitState 在 renderHome 期间被调用，不能证明返回值真正驱动了 class。模板即使丢弃该返回值、另用一份重复表达式计算点亮态，只要两者当前结果相同，observed 与 renderedLit 仍会全部一致，测试会错误通过。","suggestion":"把补丁改成主动扰动输出，例如对每个 ID 返回原结果的反值，或按 ID 产生预设真假交错结果，然后断言渲染 class 随扰动结果变化；最好分别运行至少两组互补返回映射。这样才能证明数据流接线，而不只是发生过调用。"},{"severity":"medium","category":"edge-case","location":"tests/unit/test_render_smoke.js：wallCallTrackingForWeek","problem":"observed 使用 Map，只保留每个 ID 的最后一次结果。集合相等不能证明日志所称的“每块恰好调用一次”，重复调用不会被发现。此外，renderedLit 只检查是否含 tile--wallon；当预期为 false 时，即使元素同时缺少 tile--wallon 和 tile--walloff 也会被当作 false 而通过。","suggestion":"额外记录每个 ID 的调用次数并断言均为 1；解析 class 后显式断言 tile--wallon 与 tile--walloff 恰有一个，再比较其状态。"},{"severity":"medium","category":"contradiction","location":"tools/validation/assessment_contract.js：collectTextParts/validateMonthlyW4","problem":"注释声称“测评词出现在另一个词条中仍应查出”，但 excludeDictionaryKeys 会整条跳过所有 owner 键。由于 owner 还包含 GLOBAL_RESERVED，如果一个测评词出现在另一个同样属于 owner 的 W 词条释义中，该词条整体被排除，交叉引用不会被检测。当前反例只使用非 owner 键 other，未覆盖这个边界。","suggestion":"先明确 W 词典条目是否一律不属于儿童可见教学内容。若只应忽略词条自身造成的自匹配，应收集被排除条目的值并检测其中的其他 owner 词，仅忽略“键与自身词相同”这一项；补充“一个保留词出现在另一个保留词释义中”和“GLOBAL_RESERVED 键中的交叉引用”测试。若整条排除才是预期，则修改注释，避免宣称所有其他词条引用都会被查出。"},{"severity":"medium","category":"other","location":"frontend/src/weeks/week04.data.js：新增十个 W 条目与 ASSESSMENT_WORDS","problem":"所谓“复用 ASSESSMENT_WORDS 原值”实际表现为再次写入相同的 zh 字面量，而不是建立单一真相源。今后修改 ASSESSMENT_WORDS 或 W 任一处都可能产生释义漂移；diff 中也没有对应一致性断言。","suggestion":"让 ASSESSMENT_WORDS 从 W 派生，或用共享常量构造两者；如果受声明顺序或静态数据格式限制，至少增加测试，逐词断言 RESERVED/RESERVED_RETEST 在 W 与 ASSESSMENT_WORDS 中的中文释义一致。"},{"severity":"medium","category":"other","location":"frontend/src/weeks/week04.data.js：W 新增十词的消费者影响面","problem":"本 diff 只证明了泄漏扫描会排除这些键，没有证明所有 W 消费者都是按课程引用取词。若词卡、搜索、音频清单、调试词典或候选生成器通过 Object.keys(W) 等方式枚举，新增十词可能直接进入儿童可见界面或资源清单，违背测评词隔离目的。","suggestion":"审计所有 W 的枚举点，重点检查 word_consumers、渲染块、词卡/词墙、音频清单生成和 wordforge 候选来源；增加从真实 W4 构建产物出发的负向测试，断言这十词不出现在非测评 DOM、教学块和儿童可浏览词表中。不要只依赖 assessment_contract 的文本收集器。"},{"severity":"low","category":"other","location":"tests/unit/test_migration_audit.js：expectMigrated","problem":"期望行号使用生产代码导出的同一 L_FIELD_CONSUMER_SPECS.pattern 和 lineOf 重新计算，与 auditWeek 的定位逻辑高度同源。它消除了脆弱的绝对行号，但对“pattern 本身错误地匹配了注释、死代码或错误消费点”几乎没有独立检错能力。","suggestion":"保留动态行号计算，同时增加独立语义断言，例如验证匹配行位于预期函数范围且包含实际 grapheme 消费表达式；更稳妥的是让测试 fixture 同时包含诱饵注释和真实消费点，确认审计选择真实代码。"}],"recommendations":["优先补齐 RESERVED_RETEST 的入口约束及反例，确保 check_data、assessment_contract 与 migration_audit 对测评词池的定义一致。","将 wallTileLitState 插桩改为返回受控扰动值，并校验调用次数及 wallon/walloff 互斥性。","在合并前完成 W 全部枚举消费者的静态审计，并为真实 W4 构建产物增加“测评词不进入非测评界面”的回归断言。","明确词典泄漏检测对“两个测评词词条之间交叉引用”的策略，并使实现、注释和测试一致。"],"risks":["RESERVED_RETEST 缺少 W 条目时仍可能绕过入口校验，直到月测分词或界面读取阶段才失败。","墙面接线可能退化为重复逻辑，而当前测试仍显示通过。","新增 W 键可能被枚举型消费者自动纳入儿童可见词表或音频资源。","W 与 ASSESSMENT_WORDS 的重复释义会随维护产生静默漂移。","扩大排除集合可能形成新的泄漏检测盲区。"],"confidence":"high","notes_for_claude_code":"结论仅基于 roundF.diff 的文本审查，未执行命令或测试。要完整回答“十词是否可见”，还需读取所有 W/Object.keys(W) 消费点、word_consumers.js、音频清单生成逻辑、render-blocks.js、W4 模板测评块以及构建后的 W4 HTML。分段执行的五个既有调用点在所给 diff 中均已改为复用同一 sandbox/context，extra 是在全部真实脚本之后执行的测试桥接代码，这部分未见明显遗漏。"}