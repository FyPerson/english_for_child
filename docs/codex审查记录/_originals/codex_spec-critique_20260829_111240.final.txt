{"task_type":"spec-critique","mode":"advice-only","summary":"v1.1 已实质修正二审中的大多数问题，但 H-2、H-3、H-4、H-5 仍存在可直接导致实现分叉或验收矛盾的缺口。当前仍有 4 项 high，不满足“复审 0 high”门禁，建议修订后再放行 P0 编码。","issues":[{"severity":"high","category":"contradiction","location":"§3 flash 映射、§4.6 G6、§6.1 flash schema、附件 3 Day6 数据","problem":"§4.6 声称第一周 timed 组均为“读完整组用时”并采用 elapsed，但附件 3 中第二个 timed 组明确是“30 秒挑战：能读出几个”，应采用 count；同时 §3 又要求 v2 玩法不变。H-5 虽引入了两种 metric，却没有为两个实际组确定各自度量，原矛盾尚未真正解决。","suggestion":"在规格中写死第一周映射，例如“Day6 六音快闪＝elapsed；Day6 词卡 30 秒挑战＝count”，并为两组定义稳定组键、成绩取值时点和比较方式。不要使用会随 DOM 顺序意外变化的临时 uid 作为持久化组键。"},{"severity":"high","category":"ambiguity","location":"§2.1–§2.3、§4.1 G1、§4.3 G3","problem":"播放失败后的控制流尚未闭环。play() 已经以 failed resolve 后，顶部提示条再次调用同 key，并不能自动恢复此前 G1/G3 调用方的状态机；系统 TTS 兜底成功时最终应返回 ended 还是仍返回 failed 也未定义。若同一题永久失败，“不计入分母”又没有说明是停留重试、跳过、补题还是降低分母，和固定“8 题对 6”“10 题对 7”无法唯一对应。","suggestion":"冻结一个统一协议：建议游戏调用方进入 audioFailed 状态，由提示条持有本次题目的重试 continuation；重试成功后同题进入作答窗，失败则继续停留，切页或新播放使 continuation 失效。若启用 TTS 兜底，play() 应在兜底结束后才最终 resolve，兜底成功返回 ended，全部失败才返回 failed。另明确每轮必须形成 8/10 个已判分题，永久失败时是补抽题还是允许退出重开。"},{"severity":"high","category":"missing-constraint","location":"§2.4 朗读入口迁移表、§10 自检 ③/⑤","problem":"“引用覆盖”只扫描静态 data-say/data-phrase 字面值和 BOOK 行，无法覆盖附件 3 中由模板插值动态生成的入口，例如 words、sight、sentences 的 data-say=\"${w}\"；词卡墙、WALL_HINT、音素 demo 等也不是静态属性字面值。因而自检 ③不能兑现§2.4 所称的“键覆盖保证”，即使运行时引用缺音频也可能全绿。","suggestion":"明确 build_audio.py 从哪些结构提取运行时键。至少覆盖 DAYS 的 words/sight/sentences、词卡墙及 WALL_HINT、音素 demo、BOOK、G1/G3/G4 题库和 G5 白名单；更稳妥的是在页面中建立一个可静态读取的 AUDIO_REFERENCES 注册表，由所有渲染器与构建脚本共同使用，并断言其全集是 manifest 键的子集。"},{"severity":"high","category":"missing-constraint","location":"§0 D5、§4.4 G4、§4.5 G5、§6.1、§7.2","problem":"H-4 的存储结构已支持按来源撤销，但 G4、G5 契约只规定长按确认，没有规定用户从哪里执行撤销；对应门禁也未验证 g4/g5 的 unconfirmWord。只有 G2 明确提供“撤销”按钮，因此 D5“凡读出状态一律可撤销”仍未完整落到交互与验收。","suggestion":"为 G4、G5 的已确认态明确显示来源内撤销入口，分别调用 unconfirmWord(w,'g4') 和 unconfirmWord(w,'g5')；补充门禁，验证撤销只清除当前来源，其他来源仍为 true 时战利品保持点亮，全部来源为 false 时才回到剪影。"},{"severity":"medium","category":"contradiction","location":"§6.1、§7.2 G2 门禁","problem":"§7.2 写成“G2 撤销后战利品墙该词回剪影”，但 §6.1 规定只要 g2/g4/g5 任一来源为 true 就保持点亮。同一个词若已由 G4 或 G5 确认，撤销 g2 后不应回剪影。","suggestion":"把 G2 门禁改为两种明确前置条件：无其他来源时撤销后回剪影；存在其他来源时撤销 g2 后仍点亮。"},{"severity":"medium","category":"missing-constraint","location":"§6.2 读取清洗","problem":"数值字段只要求“有限数字”，因此负数、超出题数的 best、elapsed=0 等都会被接受；elapsed 的历史最好若被清洗为 0，之后将永远无法破纪录。games、flash、confirms 是否必须为普通对象也未明确，数组在 JavaScript 中仍会被视为对象。","suggestion":"增加范围规则：grab best 为 0–8 整数，doors best 为 0–10 整数，count 为非负整数且不超过对应组容量，elapsed 必须大于 0；games、flash、confirms 仅接受非 null、非数组的普通对象。非法值恢复默认或删除。"},{"severity":"medium","category":"ambiguity","location":"§5 积木点亮墙、§3 DAYS 数据","problem":"点亮墙按“该音所属天”派生，但 Day6 的 sounds 又包含全部六音。若实现按 DAYS.sounds 建映射并被后项覆盖，六块积木可能都要到 Day6 才点亮，与“按引入日逐步点亮”的意图不一致。","suggestion":"直接冻结首次教学日映射：s/a→Day1、t→Day2、i→Day3、p→Day4、n→Day5，或明确算法为取包含该音的最小 day 编号。"},{"severity":"medium","category":"missing-constraint","location":"§10 自检 ⑥、§1 后处理要求","problem":"“裁静音+响度归一已执行”没有可判定标准。仅解码成功和时长大于 0 无法证明完成了裁剪或归一，构建脚本也无法据此稳定给出通过或失败。","suggestion":"指定可执行判据，例如统一目标响度或峰值及容差、首尾静音阈值和最大允许长度；或者规定后处理由 build_audio.py 自身强制执行，自检只验证处理后的测量值。"},{"severity":"medium","category":"ambiguity","location":"§2.2、§2.4 入口 7、附件 3 Phone 模块","problem":"§2.2 要求任何新播放都先停止其他声音，但迁移表对 Phone.say 写“真人音不动”，而附件 3 的现有 Phone.say 不会停止 WordAudio 或系统 TTS。若“不动”被按模块实现理解，点击音素时仍可能与单词音重叠，违反验收 #5。","suggestion":"澄清“不动”仅指六个真人音频资产和 data-sayph 教学行为不变，Phone.say 仍必须接入统一声音仲裁器；由 Phone.say、WordAudio.play、speakFallback 和切页清理共同调用同一个 stopAll/令牌失效入口。"}],"recommendations":["暂不按“0 high”放行 P0；先修正 G6 两组度量映射、播放失败续接协议、动态音频引用自检、G4/G5 撤销闭环四项。","修订时把二审处置表中的 H-2、H-3、H-4、H-5 标为“需再次落定”，其余 C-1、H-1、H-6、M-2、M-6、L-1 等可视为已达到可执行程度。","将所有音频调用方统一套用同一结果处理约定，至少明确 ended、failed、cancelled、blocked 对游戏状态是否推进，避免各游戏自行解释。","在编码前把持久化键、成绩范围、确认撤销和构建引用来源写成小型静态表，减少两天工期内的临场决策。"],"risks":["若按当前 G6 文本实现，Day6 词卡的 30 秒数量挑战可能被错误改成整组计时，且历史成绩语义无法兼容。","若全局重试条与游戏状态机没有 continuation，用户点击“再试一次”可能只播放声音而题目仍卡死或错误推进。","当前构建自检可能在动态 data-say 缺键时仍报告全绿，直到课堂点击具体词卡才暴露问题。","缺少来源级撤销交互会造成确认误操作无法按 D5 恢复，或开发者直接清掉该词全部来源。"],"confidence":"high","notes_for_claude_code":"本复审仅依据所附 v1.1、二审记录和 v2 节选进行文本契约核对，未执行命令、测试或行为验证。上述 high 均可由现有附件直接确认；进入实现时还应读取完整 week01-v2.html，并在生成后检查 tools/audio_manifest.json 与 tools/build_audio.py 是否真正覆盖所有动态键源。"}