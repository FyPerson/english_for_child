{"task_type":"code-review","mode":"advice-only","summary":"静态审查未发现四周迁移数据的累计数量或顺序错误，但测试证明链仍有三个高风险缺口：六类墙失败场景多数未证明真实 CLI 接线、真实周语义扫描会漏掉未声明 segments 的歧义词、gen_segments 的退出码优先级与文档承诺矛盾。建议修完高优先级问题后再将第 7 步视为外审通过。","issues":[{"severity":"high","category":"other","location":"tests/unit/test_wall_order.js:约128-234；tools/validation/check_data.js:约214-280","problem":"所谓“六个失败 fixture”中，缺项、额外项、重复项、displayOnWall:false、FIRST_TEACH_DAY 键集合不等主要只直接调用纯函数；真实 check_data.js CLI 的改坏副本仅覆盖 wallLetters 乱序。因而当前证据只证明纯函数能诊断六类问题，以及 CLI 接通了“乱序”这一条，不能证明其余五条在真实入口没有参数接错、条件漏接或被别的错误抢先终止。这符合任务中担心的“测试声称验证接线，实际只验证辅助函数”模式。","suggestion":"为六类场景分别复制合法周 fixture、只做一个最小变异并运行真实 check_data.js CLI；逐例断言非零退出、摘要正常打印、目标诊断文案出现，并断言没有更早的语法/schema/教学顺序异常抢先失败。纯函数测试可以保留，但不能代替入口级测试。"},{"severity":"high","category":"edge-case","location":"tests/unit/test_grapheme_semantics.js:collectMultiLetterWordsFromRealWeeks 与 realWeekDataSemanticsSuite","problem":"扫描器只收集“已经存在 entry.segments，且 segments 中已有多字母 ID”的词。未来 W5 若某个严格消费词因为 ai 加入而变成多解、却恰好漏写 segments，该词会被直接跳过，套件甚至可能输出语料为空。因此“W5 起自动激活”以及“枚举真实数据全部相关词”的声明不成立，也没有落实方案要求的“扩表后所有严格消费词均唯一解或有显式 segments”。","suggestion":"语料来源应改为共享的严格消费词抽取器，逐词在完整 SOUNDS 下调用分词：零解单独统计，多解且无显式 segments 必须失败，有显式 segments 则验证表面串和 ID；同时断言执行数、各类跳过数与总数守恒。不要以已有 segments 作为进入检查的前提。"},{"severity":"high","category":"contradiction","location":"tools/validation/gen_segments.js:约455-484；tools/gen_segments.py:退出码说明","problem":"文档反复承诺“只要最终磁盘仍有 @gen-segments-unreviewed，三种模式重跑必须一直返回 4”，但代码先判断 needsHumanExit 和 hasUnwrittenCandidates，再判断标记。因此文件同时存在旧标记与 tie/unknown/error 时返回 1，同时存在旧标记与未落盘 resolved 候选时返回 3，而不是 4。新增测试只覆盖没有并存问题的场景，未覆盖退出码优先级。","suggestion":"明确单一优先级契约。若“磁盘有未复核标记必为 4”是机器协议，应把标记判断置于 1/3 之前，并补“标记+tie”“标记+未写候选”测试；若希望 1/3 优先，则必须收回“必须一直是 4”的文档与日志承诺，并说明调用方如何同时获知残留标记。"},{"severity":"medium","category":"edge-case","location":"frontend/src/weeks/week01.template.html、week02.template.html、week03.template.html:renderHome 点亮态表达式","problem":"当前条件 FIRST_TEACH_DAY[c] != null ? dayDone(...) : true 对合法数据是正确的：本周待学字位都有首教日，仍由 dayDone 决定；只有历史字位默认点亮。但它把“历史字位”和“本周 newPatterns 漏配 FIRST_TEACH_DAY 的坏数据”合并处理，后一种坏数据会在页面上静默显示为已点亮。check_data 虽会报集合不等，但渲染逻辑自身是失败开放。","suggestion":"按 META.newPatterns 判断是否为本周字位：历史字位才直接点亮；本周字位若缺 FIRST_TEACH_DAY，应保持未点亮或显式抛出可诊断错误。可抽成共享函数，避免三份模板继续复制该判断。"},{"severity":"medium","category":"contradiction","location":"roundA_方案切片.md §2.4；四个 weekNN.template.html","problem":"方案 §2.4 明写 W2/W3 新出现的历史积木为“未点亮态”，而实现和任务说明采用“上周学过的恒已点亮”。后者从教学逻辑看更连贯，但当前材料中的权威文字彼此冲突，未来维护者可能按方案把实现改回去。","suggestion":"确认最终教学口径后同步修正文档；若历史字位应亮，明确写成“历史字位恒亮，本周字位按首教日逐日点亮”，并为历史、本周已完成、本周未完成三种状态各加一条渲染测试。"},{"severity":"medium","category":"other","location":"tests/browser/smoke_w3_browser.py:约74-90","problem":"新逻辑用 wall_letters.index(ch) 找位置，再只比较该位置元素的 tagName。它间接要求 META 中存在本周新音，但没有核对该 DOM 元素的 data-sayph、显示字形或无障碍标签确实对应 ch。若模板在该位置渲染了错误字位，而错误字位与目标字位恰好都有录音或都无录音，测试仍会通过。因此它证明的是位置上的元素类型与预期相同，不完整证明“本周新音的那块元素与录音状态正确配对”。","suggestion":"逐个 NEW_SOUNDS 按 DOM 身份属性定位，例如精确匹配 data-sayph 或专用 data-grapheme-id，再断言唯一存在、tagName 与 hasPhoneme(ch) 一致、显示文本等于 graphemeLabel(ch)。不要仅靠 META 索引映射到 DOM 位置。"},{"severity":"medium","category":"missing-constraint","location":"tools/validation/check_data.js:约198-220；tests/unit/test_check_data_array_guards.py","problem":"新增守卫只验证三个字段的外层是数组。数组中若含 null、数字、空串或非法 ID，后续 normalizeIdList 仍可能抛异常并中止剩余检查；现有测试只把整个数组改回字符串，没有覆盖畸形数组元素。这没有完全兑现“校验器应清晰报错并继续执行”的目标。","suggestion":"在 check_data.js 对每个字段调用可捕获错误的 assertIdList/normalizeIdList 包装器，失败时通过 ok(false, 明确消息)记录并回退到空数组；为 null、数字、空串、非法 ID、重复 rack 合法性分别补入口级用例。"},{"severity":"medium","category":"edge-case","location":"tools/validation/wall_order.js:gatherWeekRecordsUpTo 约91-115","problem":"MED-7 中两项确有静态证据：使用 (META.newPatterns || []) 会把字段缺失静默当作合法空数组；读取历史周文件时的 ENOENT、解析错误等没有结构化区分，统一被上层包装成“无法计算教学顺序”。对已迁移的 W1-W4 和正常 W5 不会立即算错，但会让新增周或分支工作树中的缺文件、漏字段产生级联墙差异而非直接指出根因。","suggestion":"gatherWeekRecordsUpTo 应要求每条记录显式拥有数组型 newPatterns，缺失即抛 teaching-order-missing-patterns；文件不存在、读取失败、解析失败分别包装为带 week/file/cause 的结构化错误码。空数组只能由显式 [] 表达。"},{"severity":"medium","category":"other","location":"tools/validation/wall_order.js:computeTeachingOrder 与 gatherWeekRecordsUpTo","problem":"“连续”只从输入中的 minWeek 开始验证，因此 computeTeachingOrder([{week:2},{week:3}]) 会通过；同时校验产物当前周时，历史记录取自源码树而不是产物自身。这两点在当前 W1-W4 数据上不造成错误，但使单独校验 W5 产物依赖外部源码树状态，也可能接受缺失开头周的调用方输入，削弱独立产物与 fixture 的可复现性。国庆维持档是否会导致 project.json 周号缺口，附件没有足够信息判断。","suggestion":"让调用方显式传入预期起始周或完整 project 周序，并验证记录精确等于截至当前周的前缀。若需要校验构建产物，应从同一次构建的规范化周快照读取全部历史记录，或明确将该检查限定为源码树校验，避免混合产物与源码。"},{"severity":"low","category":"readability","location":"tests/unit/test_migration_audit.js:约533-540","problem":"注释仍写 newPatterns-presence 的“status 不变仍是 unknown”，但同一批代码已经改为 not-applicable。这会误导后续审查，并与紧邻的期望计数矛盾。","suggestion":"将残留注释同步改为“由 unknown 改为 not-applicable”，并解释 finding 数量不变只是状态变化。"},{"severity":"low","category":"other","location":"tests/unit/test_gen_segments.py:test_rerun_after_write_heuristic_still_exits_4","problem":"测试把标记恰好出现 4 次钉死在提示注释当前重复书写方式上；将来仅调整提示文案就会导致无语义回归的失败。","suggestion":"按被标记词或声明块计数，或只断言标记数非零且各重跑前后保持不变；不要依赖每条注释中同一标记恰好出现两次。"}],"recommendations":["攻击方向 1：已按 diff 静态验算。W1 为 s/a/t/i/p/n；W2 依次追加 c/k/e/h/r/m/d，得到 13 项；W3 再追加 g/o/u/l/f/b，得到 19 项；W4 newPatterns 为空且保持同一 19 项。四份 wallLetters 的数量、集合和顺序均正确，rack 数组也保留了原字符串的顺序与重复项。","攻击方向 2：合法数据下不会提前点亮本周待学字位，因为本周字位有 FIRST_TEACH_DAY 时仍调用 dayDone；问题仅在坏数据缺首教日时失败开放。另需消除方案中“历史字位未点亮”与实现“历史字位已点亮”的文字冲突。","攻击方向 3：纯函数六类案例本身的变异原因基本明确；M-6a 同步倒序 newPatterns 与墙后，预期只命中新加的首教日排序断言；孤儿 SOUNDS 条目也刻意构造成合法 schema。主要缺陷是六类并未逐一通过真实 CLI 证明接线。","攻击方向 4：按 META 索引定位避免了原先取前六块的错位，但仍未验证 DOM 元素身份，只验证对应位置的标签类型，建议补 data-sayph/字形精确配对。","攻击方向 5：三条被称为“数学上恒真”的判断在既定前置契约下基本成立；它们可作为正向不变量保留，但不应继续宣称具有独立分辨力。真正需要修的是现实语料枚举入口，不能只扫描已有多字母 segments。","攻击方向 6：外层字符串守卫确实能避免该特定旧格式直接进入 normalizeIdList；乱序改坏副本也确实走了真实 CLI。不过数组元素形状仍可能异常退出，且真实 CLI 只证明了乱序分支接线。","攻击方向 7：W5 正常接入且 project.json 连续时，minWeek 连续性不会立即出错；缺字段被 || [] 吞掉和历史文件错误缺少结构化诊断会影响 W5 数据编写体验。源码/产物混合取数会影响独立产物验证。国庆维持档是否造成非连续周号或特殊周来源，当前 diff 未包含 project.json 和 W5 设计，无法作确定结论。","在送下一轮外审前，优先补齐六类真实 CLI 变异测试、重构真实周严格消费词扫描，并明确 gen_segments 退出码优先级；其余中等级问题可与 W5 开工门一起处理。"],"risks":["当前测试可能在 check_data.js 某些 DATA-WALL-01 分支未接通时仍保持绿色。","W5 漏写 segments 的新歧义词可能被 realWeekDataSemanticsSuite 完全跳过，形成静默假绿。","自动化调用方若依赖退出码 4 表示磁盘存在未复核标记，在并存其他问题时会收到 1 或 3，可能采取错误处理路径。","历史源码树与当前产物不一致时，同一产物的墙校验结果可能随检出环境变化。","模板在无效数据下会把缺首教日的本周字位错误显示为已点亮。"],"confidence":"medium","notes_for_claude_code":"本审查仅基于所附 diff 与方案切片进行静态分析，没有执行命令或测试。材料足以确认四周数组迁移、累计墙顺序和上述代码级缺口；若要提高到高置信度，还需读取 normalizeIdList/segmentWord/validateSoundsSchema 的完整实现、共享严格消费词抽取器、project.json、四周 FIRST_TEACH_DAY 与 DAYS 全量数据、run_checks 的完整作业控制流，以及 W5/国庆维持档的数据规范。建议 Claude Code 将前三条 high 作为本轮外审阻塞项，其余按 W5 开工风险排序处理。"}