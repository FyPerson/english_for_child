{"task_type":"spec-critique","mode":"advice-only","summary":"方案的总体方向正确，但当前五步拆分不能完整兜住迁移风险：实施范围漏掉了以“字符长度、字符串拼接、显示文本”为核心的运行时语义，也与上游规范中“里程碑 2 同期完成测评契约、墙模型和歧义失败”的范围存在冲突。建议先补齐统一的“字位序列—显示字形—单词表面串”接口及迁移矩阵，再决定是否采用双模式过渡。","issues":[{"severity":"critical","category":"missing-constraint","location":"实施方案 §2.2、§4 第3步；games.js G4/G5","problem":"改动清单只列出 rack 拆分、逐字匹配等位置，却漏掉 G4/G5 的核心状态仍以字符串语义运行：G4 使用 `word.length` 创建槽位、用 `slots.join('') === word` 判对，并在长按守卫中再次比较；G5 用 `slots.join('')` 生成词并查询白名单、保留词和确认状态。`rain` 即使 rack 已改为 `['r','ai','n']`，G4 仍会创建四槽，无法完成；将来 `oo_short` 等 ID 加入槽位后，`join('')` 也不等于表面词。","suggestion":"为每道目标词预先取得 `targetSegments`，槽数使用其长度，判定使用字位 ID 数组逐项比较；另设 `surfaceOf(ids, SOUNDS)` 通过 `SOUNDS[id].grapheme` 生成显示/查词字符串。G5 同样区分 `slotIds` 与 `surfaceWord`，所有白名单、保留词、W 和 confirms 的键使用明确规定的表面词或稳定词 ID，不再依赖 `join('')` 偶然成立。"},{"severity":"critical","category":"contradiction","location":"实施方案 §0、§3.3、§4；交接规范 §3、§4.1 DATA-ASSESS-01、§8","problem":"方案声称以上游规范 §3 为契约来源，却把规范已经明确要求的行为排除或遗漏：规范要求多解且无 `segments` 时校验失败；墙迁移要求用 `newPatterns` 驱动新点亮动画；`DATA-ASSESS-01` 又被明确列为“里程碑 2 同期”。当前方案只改 `assessment_contract.js:48` 的墙断言，并明确不实现歧义处理，因此即使五步完成，也不能满足规范自己定义的里程碑 2 和激活条件。","suggestion":"在方案开头给出逐条范围映射并作出明确选择：要么把歧义检测、`segments` 消费路径、墙的新旧点亮语义及 assessmentMode 矩阵纳入本里程碑；要么修改上游规范，将它们拆成有独立截止日期的后续里程碑。不能同时宣称以上游为权威又局部排除其强制要求。"},{"severity":"high","category":"ambiguity","location":"实施方案 §3.1","problem":"`tokenize(word, patterns)` 的契约不充分：`patterns` 是字位 ID 数组、字形数组还是映射没有定义；若函数只收到字符串模式，就无法在同形多音时返回稳定 ID；若从 `SOUNDS` 取全部键做最长匹配，键如 `oo_short` 又不是单词中实际出现的字形。此外没有定义大小写、非字母字符、空串、重复 grapheme、未知片段、错误类型和确定性顺序。","suggestion":"将公共接口定义为例如 `segmentWord(word, sounds, explicitSegments?) -> 字位ID数组`，匹配依据固定为 `SOUNDS[id].grapheme`。规定规范化、最长匹配、同长度多解失败、未知区间失败、显式 segments 的表面串一致性校验，并返回带词和偏移位置的结构化错误。底层纯函数可另收预编译索引，但不要暴露含义不明的 `patterns`。"},{"severity":"high","category":"edge-case","location":"实施方案 §2.2 #8–#9；render-blocks.js `colorWord`、`wordforge`","problem":"改动面漏掉多处字符假设。`wordforge` 除 `candidate.charAt(0)` 外还有 `from.slice(1)` 和 `head + tail`；`colorWord` 被用于单词、句子、书页以及可能包含空格和标点的文本，不能简单对整个字符串调用严格分词。`tileHTML`、G1 的目标字位、G4/G5 rack 按钮目前直接显示 ID，而契约要求显示 `SOUNDS[id].grapheme`；同形多音 ID 会直接暴露成 `oo_short`。","suggestion":"不要继续只按 `split/charAt/[i]` 搜索。按语义审计所有“长度、切片、拼接、首项、末项、显示、ARIA、CSS 元音分类、音频键、DOM dataset、序列化”路径。拆出 `graphemeLabel(id)`、`segmentWord`、`colorTokenizedText` 等统一接口；句子和正文先保留分隔符地分词，再只对可解码词着色。"},{"severity":"high","category":"other","location":"实施方案 §4 第2步","problem":"“四周 check_data 输出逐字节不变”只能证明当前四份单字母数据在一次端到端输出上的表面回归，不能证明分词函数语义等价。当前输出主要是计数和失败消息；不同内部字位序列可能产生完全相同输出，而且现有数据不覆盖重复 rack、最长匹配冲突、未知片段、显示转换和槽位状态。异常消息或遍历顺序变化还会造成无关的字节差异。","suggestion":"保留输出快照作为附加回归，但把安全垫改成双实现差分测试：对四周所有被消费的词，逐词断言旧字符数组与新字位 ID 数组完全相等；分别断言 canSpell、首字位、字位数、最小对立差异数和元音分类结果一致。再加入合成数据覆盖 `rain`、重复积木、最长匹配、未知片段和同形多解。"},{"severity":"high","category":"edge-case","location":"实施方案 §3.3、基础假设 6","problem":"“没有生产数据就无法验证消歧”不成立。上游契约已经规定多解必须失败；可以用合成 SOUNDS 和 fixture 验证 `oo_short`/`oo_long`。若现在的函数只返回最长匹配结果而不保留候选集、显式 segments 入口及歧义错误，W13 前很可能需要修改公共 API、所有调用方和缓存结果，形成第二次破坏性变更。","suggestion":"本次至少实现并测试歧义检测和显式 segments 的通用消费接口，生产周可以暂不填写 segments。若确实推迟完整实现，也必须现在冻结可兼容的返回/错误契约，并加入合成歧义 fixture，确保未来只增加数据而不改调用接口。"},{"severity":"high","category":"contradiction","location":"实施方案 §4 第3步、§5 构建验收","problem":"“第3步不可分割”并非技术必然。引擎完全可以在短期兼容字符串和数组；真正的问题是方案没有区分仓库提交原子性与运行时 schema 原子性。另一方面，如果只迁移部分周，校验器和 HTML 反解析也必须明确识别旧、新格式，避免把字符串 `ai` 错当两个 ID。","suggestion":"优先采用短期双读、单写新格式：增加 `normalizeIdList(value)`，字符串仅按旧单字符 schema 展开，数组按 ID 原样保留；先部署消费者兼容层，再逐周迁移，最后删除字符串分支并加拒绝旧格式测试。代价是多一个临时分支和清理提交，但能显著缩小回滚面。若仍选择一次迁移，应把原因写成“降低临时双 schema 复杂度的工程选择”，而非“不存在渐进路径”。"},{"severity":"high","category":"edge-case","location":"实施方案 §4 第5步","problem":"双字母冒烟放在迁移和基线重取之后，意味着最关键的新能力直到破坏性迁移完成后才首次被验证。第1步虽测试 tokenizer，但不能覆盖 G2/G4/G5、墙、快闪、模板序列化和 HTML 反解析的集成链路。","suggestion":"在第3步之前建立不依赖正式周数据的 W5/ai 集成 fixture，并让消费者兼容层先跑通它；第3步迁移后再次运行同一 fixture。基线重取应放在所有旧周回归和 ai 集成测试通过之后，作为最后确认旧周预期变化的独立提交。"},{"severity":"medium","category":"missing-constraint","location":"实施方案 §3.4、基础假设 4","problem":"改用 `SOUNDS[id].type` 能正确分类，但不足以证明分组固定位不变。当前分组函数是稳定过滤，固定位置还依赖 rack 数组顺序、重复项顺序以及 CSS 布局；同时 G4/G5 自己的 tile class 也仍分别使用 `vowels.includes(c)`，不能只改 `groupedRackHTML`。","suggestion":"统一使用 `soundType(id)` 生成 tile class 和分组；增加测试断言分组前后每个 tile 的原始 rack 索引、重复项数量和组内顺序不变，并覆盖 `ai` 为元音的案例。"},{"severity":"medium","category":"performance","location":"实施方案 §5 体量、基础假设 5","problem":"“不加媒体所以不会触线”证据不足。虽然数组和共享脚本增长通常很小，但单文件构建会把公共 JS 复制进每个产物；若新增解析器、错误文本或兼容分支，week04 的 0.38 MB 余量仍应以实际产物为准。方案自身也承认 data.js 会增大，因此不能将此假设视为成立。","suggestion":"把体量判定改为“低风险但必须测量”，在每个接线提交记录所有产物的字节增量及剩余余量；为 week04 设置明确的最大增量预警阈值，不只依赖最终 sizeBudget 通过。"},{"severity":"medium","category":"other","location":"实施方案 §4 第4步；baseline.py `create`/`check`","problem":"基线重取步骤没有写清 `baseline --create --force` 的干净工作树前置条件及 sourceCommit 语义。该命令会先构建并要求构建不产生 tracked 变更，因此第3步必须已经提交所有受版本控制的派生物；随后生成的 baseline fixture 再单独提交。若没有明确流程，基线步骤可能因工作树不干净直接拒绝。","suggestion":"把第4步细化为：提交第3步源码及允许的派生物，确认工作树干净；完成旧周完整回归和 ai fixture；在该 HEAD 上重取基线；核对差异仅为预期 skeleton/sha 变化且 media 表完全不变；最后单独提交 fixture。"},{"severity":"medium","category":"ambiguity","location":"实施方案 §4 第2步；shared/graphemes.js 引入方式","problem":"公共分词函数同时要给浏览器 include、Node 校验器、assessment_contract 和 load_data 使用，但方案没有定义模块封装方式。直接写浏览器全局函数可能无法 `require`，使用 CommonJS 又可能破坏模板内联；多份拷贝会违背“同一实现”。","suggestion":"预先确定单源双环境导出方式，并用测试证明浏览器构建和 Node require 使用的是同一文件。避免在 load_data 中重新实现一份简化 tokenizer。"},{"severity":"low","category":"readability","location":"实施方案 §2.2、§4","problem":"“13处”把文件级位置、模板数量和多条语义混合计数，容易产生覆盖完整的错觉。例如 #11 本身包含六处，#10 又是四个模板，而 G4/G5 一个位置内包含多种字符语义。","suggestion":"改成按消费场景维护迁移矩阵：输入解析、分词、显示、槽位长度、摆词比较、首字位、最小对立、墙、快闪、音频、校验、序列化/反解析、无障碍文本；每行列出生产代码、校验器和测试 fixture。"}],"recommendations":["基础假设1：证据不足。逐字节输出快照只能证明当前样本的外部输出未变，不能证明内部字位语义等价；需要逐词、逐规则的双实现差分断言。","基础假设2：不成立。可以通过消费者双读和 `normalizeIdList` 实现安全渐进迁移；一次完成是可选的复杂度权衡，不是技术必然。","基础假设3：不成立。保留 rack 重复项是必要条件，但还必须把槽位长度、数组比较、表面串生成、白名单/保留词查询以及 confirms 键语义一并改成字位安全。","基础假设4：证据不足。`SOUNDS[id].type` 是正确方向，但必须同步替换 G4/G5 tile class 的元音判断，并验证稳定过滤、重复项和原 rack 索引顺序。","基础假设5：证据不足。不加媒体意味着风险较低，不等于产物不增长；共享解析代码会进入单文件产物，应实际记录字节增量。","基础假设6：不成立。上游已经规定歧义失败；若不预留候选集和显式 segments 接口，W13 前很可能发生第二次 API 级变更。合成 fixture 足以现在验证。","基础假设7：不成立。至少漏掉 `word.length`、`slots.join('')`、`from.slice(1)`、`head + tail`、ID 到 grapheme 的显示转换、句子/书页着色、ARIA 文本以及墙的新点亮语义。","建议将步骤重排为：冻结字位接口与歧义行为并完成纯函数测试；建立旧逻辑差分测试和 ai 集成 fixture；加入字符串/数组双读兼容层；迁移四周数据并逐周验证；删除旧格式兼容；完成全量回归后再重取基线。","在方案中增加“契约要求—代码入口—正向 fixture—失败 fixture—完成步骤”的追踪表，尤其覆盖 DATA-PATTERN-02、DATA-WALL-01、DATA-ASSESS-01 以及同形多音规则。"],"risks":["G4 对 `rain` 创建四槽会使核心 W5 场景无法完成，即使 rack 已正确迁移。","稳定 ID 被直接渲染为 `oo_short` 或拼接进词字符串，会造成儿童界面泄漏内部标识并破坏查词。","在 ai 集成测试之前重取基线，可能把迁移引入的旧周渲染错误固化为新基线。","Node 与浏览器各自实现分词逻辑会导致校验通过但运行时拆分不同。","方案与上游规范对里程碑范围理解不一致，会导致实现完成后仍无法满足规范激活条件。"],"confidence":"high","notes_for_claude_code":"本审查仅基于所提供的方案、规范及代码片段，没有执行命令、测试或修改文件。更深一轮应继续阅读 `load_data.js`、`assessment_contract.js`、四个 `weekNN.template.html`、各周 data.js、构建 include 机制、墙动画实现、`colorWord` 的全部调用点及现有测试目录；这些文件决定双模式迁移和模块复用的具体可行性。"}