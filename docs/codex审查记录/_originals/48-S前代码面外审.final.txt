{"task_type":"code-review","mode":"advice-only","summary":"当前实现存在一个会直接破坏第 7 步验收的高风险缺陷，以及一个会把歧义词伪装成“未知词”而假绿的覆盖漏洞。四个 commit 可以保留作为阶段性基础，但不建议在修正差分分类与迁移后测试策略之前直接进入 4b 大规模接线。","issues":[{"severity":"high","category":"contradiction","location":"tests/unit/test_migration_diff.js，totalSkippedMultiLetter 汇总断言","problem":"`assert.equal(totalSkippedMultiLetter, 0)` 位于 `if (!anyMultiLetterInData)` 之外，因此第 7 步只要出现能够成功解析的多字母字位词，测试就必红；这与后面“迁移后允许跳过多字母词”的分支及文件头声明直接矛盾。","suggestion":"把该断言移入 `!anyMultiLetterInData` 分支；迁移后改为断言执行数、跳过数和按来源覆盖下限，并增加至少一个真实多字母词确实进入预期分类的断言。"},{"severity":"high","category":"edge-case","location":"tests/unit/test_migration_diff.js，五个 diff 的 segmentWord catch 与 tallySourceCoverage","problem":"所有 `segmentWord` 异常都被当成未知或直接跳过。引入 `ai` 后，最关键的真实词往往会抛 `segment-ambiguous`，却被计入 `skippedUnknown`，而不是 `skippedMultiLetter`；这既污染“零解”统计，也可能让迁移真正影响的词全部假绿消失。","suggestion":"严格按错误码分类：仅 `segment-unknown` 进入 skippedUnknown；`segment-ambiguous` 应读取词条的显式 `segments` 再解析和判断是否为多字母，缺少显式 segments 则直接失败；其他错误必须重新抛出。"},{"severity":"high","category":"missing-constraint","location":"tests/unit/test_migration_diff.js 与 tests/unit/test_grapheme_semantics.js 的迁移后覆盖边界","problem":"迁移后兼容差分主动排除所有受多字母迁移影响的词，而语义测试只使用合成数据。两者合起来仍没有验证真实周数据中的显式 segments、真实消费者输入及真实教学词是否符合新语义。","suggestion":"第 7 步新增真实数据语义套件：枚举所有声明多字母 segments 的真实词，断言分词结果、表面串、首字位、长度及相关消费者语义；兼容差分继续只守旧子集，但不能被描述为迁移后的主证明。"},{"severity":"medium","category":"other","location":"tests/unit/test_grapheme_migration.js，globSoundsSources 与 listTrackedJsFiles","problem":"两处发现逻辑都依赖 `git ls-files`，因此本地新建但尚未加入 Git 的 week05 数据或源码不会被门槛扫描。注释宣称新增周会“自动纳入”，对开发中未跟踪文件并不成立。","suggestion":"数据来源使用文件系统 glob，并显式排除构建及临时目录；源码扫描可将 Git 跟踪文件与目标目录下未跟踪的候选文件合并，或至少增加检查，发现匹配命名但未跟踪的文件时明确失败。"},{"severity":"medium","category":"missing-constraint","location":"tests/unit/test_grapheme_migration.js，EXCLUDED_ENTRIES 与 scanForLResidue","problem":"白名单粒度实际是“整文件+整个模式”，并非具体命中位置。比如适配层获准 `dot-access` 后，该文件未来新增任何生产性 `.L` 读取都会被整体放行；陈旧自检只能证明文件中仍有至少一个合法命中，无法发现额外非法命中。","suggestion":"把白名单绑定到可核对的具体命中，例如文件、模式、行内容签名或预期命中数量；扫描时只消耗已声明的匹配，剩余同模式命中必须报错。"},{"severity":"medium","category":"contradiction","location":"tools/validation/sounds_grapheme_adapter.js 与 tools/validation/check_data.js","problem":"适配层现已成为默认严格校验器，但名称和职责仍是 legacy fallback；同时常规 `check_data.js` 只用真值检查 grapheme，没有统一复用严格 schema 校验。结果是相同 SOUNDS 数据经不同入口可能得到不同严格程度的结论。","suggestion":"将冲突、非法 grapheme、遗留 L 检测抽成正式的 `validateSoundsSchema`；`check_data.js`、生成器和审计共同调用。仅把真正的旧数据派生逻辑留在明确命名的 legacy 审计辅助函数中。"},{"severity":"medium","category":"other","location":"frontend/src/shared/graphemes.js，normalizeIdList；后续 4b 调用点","problem":"三段式签名把数据迁移状态传播到每个消费者：4b 需要逐点决定是否传 `legacy:true`，第 7/8 步又要逐点撤销。任何漏撤都会继续静默接受旧字符串，形成长期兼容后门。","suggestion":"把旧字符串转换集中在数据加载边界，加载后统一产出 ID 数组；消费者始终只接数组。若暂时保留现签名，应建立静态清单并在第 7 步加门槛，禁止生产调用点出现 `legacy:true`。"},{"severity":"medium","category":"other","location":"tools/validation/gen_segments.js，resolveWord 与 writeSuggestions","problem":"现有 `bat` 用例已证明“字位数最少”可能选错，但 `--write` 仍批量写入所有 resolved 候选。机器标记只能阻止交付，不能减少复核者逐条推翻错误建议的成本；材料不足以估计真实错误率，因此无法证明该启发式净收益为正。","suggestion":"在真实 W5 语料上先统计命中率再决定默认行为；在此之前更稳妥的是默认只输出全部解析，不写回预选结果，或要求用户显式选择候选后才写入。可保留最短解作为排序提示，而不是自动写回依据。"},{"severity":"low","category":"readability","location":"tests/unit/test_check_data_unreviewed_marker.py，run_check_data","problem":"命令首项写成 `sys.executable and 'node' or 'node'`，表达式无论如何都等于 `node`，容易让人误以为存在 Python 可执行文件回退逻辑。","suggestion":"直接写 `['node', ...]`，或通过统一的 Node 可执行文件配置获取路径。"}],"recommendations":["优先修复歧义错误分类和无条件 `totalSkippedMultiLetter === 0`，并用真实多字母数据建立会失败的回归用例。","将迁移后证明拆成“旧单字母子集兼容”与“真实多字母词语义”两项独立门槛，分别报告覆盖数量。","把 legacy 转换和 SOUNDS schema 校验集中到加载边界，避免 4b 的几十个消费者携带迁移状态。","收紧源码扫描白名单，使其核对具体预期命中，而不是放行整文件中的某类语法。"],"risks":["当前版本进入第 7 步后可能出现两种相反结果：唯一多字母解析导致测试误红，歧义多字母解析则被当作 unknown 而假绿。","4b 接线规模较大，若继续由调用点决定 legacy 模式，后续撤销兼容逻辑容易遗漏。","生成器候选即使被交付门槛拦截，也可能制造大量低价值源码改动和人工复核负担。","基于固定迁移前总数的覆盖表在合法数据演进时需要人工更新，若评审只机械更新数字，可能掩盖抽取器退化。"],"confidence":"high","notes_for_claude_code":"本审查仅基于所附完整 diff 与上下文文本，未执行测试。结论是：暂不宜把这四个 commit 直接视为 4b 的稳固地基；修复前两项 high，并补真实多字母数据覆盖后，可以作为 4b 的基础。更深入审查还应读取 `segmentWord` 的完整实现、`word_consumers.js` 的来源定义、`load_data.js` 的声明提取逻辑，以及 4b 计划修改的消费者清单。"}