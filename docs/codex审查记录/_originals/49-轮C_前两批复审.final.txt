{"task_type":"code-review","mode":"advice-only","summary":"复审结论：H3、M1、T5 的修改方向基本自洽，但 H2 仍留下高风险静默通道，M6 的生产接线也使新增集合校验接近恒真；B-M1 与 T8①还存在格式和大小写边界。材料足以确认 2 个高风险问题，但因未提供若干依赖文件及课程构建测试，无法确认整条链路已闭合。","issues":[{"severity":"high","category":"edge-case","location":"tests/unit/test_grapheme_semantics.js: classifyOneWordForSemantics、classifyStrictConsumptionWordsFromRealWeeks","problem":"【已验算】所有 `segment-unknown` 都无条件归为 `zero`，随后计入 `skippedUnknown`，未根据 `rec.kind` 判断该来源是否允许包含未教字位。G1 干扰词或认读词出现零解可能是教学设计，但 `book-page`、`words`、`sentences`、`wordforge-family` 等严格拼读入口出现零解通常是数据错误；当前实现会让这些错误满足 `executed + skippedUnknown === total` 并保持绿色。该守恒式只证明每条记录被二选一计数，不能证明 skip 合法。","suggestion":"建立按来源和记录语义控制的零解豁免函数，例如仅允许明确标记的认读词及 G1 听音干扰记录跳过；其余来源遇到 `segment-unknown` 直接失败，并在错误中携带周、来源和词。至少增加 `book-page` 或 `wordforge-family` 零解必须失败、G1 干扰词零解允许跳过的正反测试。"},{"severity":"high","category":"contradiction","location":"tools/validation/check_data.js:182-207，规范 DATA-PATTERN-01","problem":"【已验算】规范写明例外只有“认读词、G1 干扰词”，实现却额外保留 `META.week === 1 && w.toLowerCase() === 'nat'` 的全来源豁免；同时 W1 的 G1 `pos` 桶也整体豁免。尤其 `nat` 的豁免不限制来源，因此即使它未来出现在书页或普通拼读块中，也会绕过校验。这与“例外只有两类”直接冲突，并可能掩盖抽取器接入后的真实问题。","suggestion":"删除 `nat` 特例；如果确有业务理由，应把例外编码为精确的来源与记录位置，而不是按周和词形全局放行，并同步修改规范。重新确认 G1 `pos` 是否属于规范所称的干扰词；若不是，只允许真正的负例记录豁免。"},{"severity":"medium","category":"edge-case","location":"tools/validation/wall_order.js: getExpectedWeeksUpTo、gatherWeekRecordsUpTo；tools/validation/check_data.js:285-290","problem":"【已验算】生产调用把 `gatherWeekRecordsUpTo` 和 `getExpectedWeeksUpTo` 都建立在同一份 `project.json.weeks` 上；前者按该列表逐项生成记录并把历史记录的 `week` 强制写成文件名对应的 `w`，没有校验历史文件中 `box.META.week`。因此传给 `computeTeachingOrder` 的周号集合天然等于 expectedWeeks，新集合比较在正常接线中接近恒真；例如 `week01.data.js` 内误写 `META.week: 99` 仍可能被当作 week 1。","suggestion":"加载历史周后断言 `box.META.week === w`，并为错周号 fixture 增加入口测试。若目标还包括发现 `project.json` 自身漏掉开头周，应另定义独立规则（例如课程必须从 1 开始或与 `courseWeeks` 对照），不能用同一列表同时生成实际值和期望值。单文件纯函数 fixture 继续显式传入自身预期周号数组是合理的。"},{"severity":"medium","category":"ambiguity","location":"tools/validation/load_data.js:16-60","problem":"【已验算】`META_DECLARATION_RE` 只接受行首严格形态 `const META = `：缩进、多个空格、`const META=`、等号后换行、`const /*注释*/ META` 等合法写法都会被误报为“缺 META”。虽然它与 `declaration()` 当前正则一致，但这只是保持两处同样狭窄，并未形成稳健的“是否存在内联 META”判定。另有【怀疑】：正则扫描整份 HTML 而非 script 节点，正文或示例代码中的同形文本可能造成前置探测假阳性，随后仍泄漏 VM/截取异常。","suggestion":"至少统一使用容忍空白的模式，如行首可缩进、`const\\s+META\\s*=\\s*`；更稳妥的是先限定到实际脚本内容，再以解析或可靠的声明提取结果判断。增加缩进、无空格等号、换行等号、正文伪声明四类测试，并明确哪些格式属于支持契约。"},{"severity":"medium","category":"edge-case","location":"tools/build_course.py: render_course payload 构造；frontend/src/course.template.html:45-49","problem":"【已验算】把 JSON 文本中的小写 `</` 替换成 `<\\/` 对原本已含 `<\\/` 的 JS 字符串不会重复处理：`json.dumps` 会先转义反斜杠，使其不再形成相邻的 `</`，JSON.parse 后可还原原值。但替换是大小写敏感的，`</SCRIPT>` 或 `</ScRiPt>` 仍会被 HTML 解析器按不区分大小写的 script 结束标签处理，导致 payload 截断。附件中也没有看到覆盖 raw-HTML payload、特殊结束标签以及逐周实际打开 iframe 的课程级测试。","suggestion":"优先把 JSON 中所有 `<` 转成 `\\u003c`，从根源消除 script 结束标签；或至少对 `</script` 做不区分大小写转义。课程集成测试应构建 payload、由浏览器解析页面，逐个点击所有周并断言 iframe `srcdoc` 成功加载，同时覆盖普通 `</script>`、混合大小写结束标签以及原始 `<\\/` 字面量。"},{"severity":"medium","category":"other","location":"tools/validation/check_data.js: DATA-PATTERN-01 实现","problem":"【怀疑，需读取 `word_consumers.js`、`graphemes.js` 和完整规范确认】该规则名称和文档要求“所有词的字位在累计已教字位内”，实现仍按 `[...word]` 拆 Unicode 字符并与 `TAUGHT` 比较，而不是先按字位分词再比较字位 ID。接入 15 类来源只扩大了字符级检查覆盖面；引入真正的多字母字位后，它可能依赖组成字母此前恰好已教而误绿，也可能对仅教组合字位、未分别教组成字符的合法词误红。","suggestion":"将非豁免词统一通过 `idsForWord` 或 `segmentWord` 得到字位 ID，再逐 ID 与累计教学集合比较；零解和多解缺显式 segments 应结构化失败。增加一个组成字符均已教但目标多字母字位未教的反例，防止继续按字符误绿。"},{"severity":"low","category":"contradiction","location":"tools/size_budget.py: check 文档与分支","problem":"【已验算】新增说明称 `size >= limit`（包括恰好等于预算）走 OVER/硬失败，但代码仍以 `size > limit` 才失败；恰好等于预算时 verdict 为 `ok`，并因余量为零打印 WARN。原测试文件标题也说明 equal passes，文档、输出和既有契约不一致。","suggestion":"按既有契约保留“等于预算通过”时，修改注释为“仅 `size > limit` 硬失败，`size === limit` 通过但预警”；并增加等于预算时的明确输出断言。若产品决定等于预算也失败，则应改代码和原边界测试，而非只改注释。"},{"severity":"low","category":"readability","location":"tests/browser/smoke_w3_browser.py: NEW_SOUNDS 定位循环","problem":"【怀疑】无录音字位使用拼接的 CSS `:text-is(\"{label}\")` 定位，未转义引号、反斜杠等选择器特殊字符；当前单字母数据大概率安全，但未来字形标签扩展后测试可能因选择器语法错误而非产品缺陷失败。","suggestion":"使用 Playwright 的结构化文本过滤或 locator 后在页面侧按 `textContent === label` 筛选；更理想的是让无录音的 `div` 也携带稳定的 `data-grapheme-id`，两种元素统一按 ID 定位。"}],"recommendations":["H2 结论：当前四分类没有修闭合；零解必须按来源及记录语义区分“允许跳过”和“数据失败”，不能全局 skip。","M6 结论：纯函数接口改进有效，单文件 fixture 显式传 `[week]` 或实际合成周集合是正确用法；但生产接线需补历史文件 `META.week` 校验，并避免实际与期望完全同源造成恒真。","B-M1 结论：前置顺序修复了给定合成故障，但严格正则会拒绝多种合法格式，建议扩大契约或改为脚本级解析。","B-M2 结论：15 类来源接线方向正确，认读词及 G1 豁免仍在；但 `nat` 全局特例与“仅两类例外”冲突，且字符级判据尚未真正升级为字位级。","T8① 结论：`<\\/` 的原有字面量不会被本次替换双重处理；仍需处理大小写混合的 script 结束标签，并补逐周 iframe 浏览器集成测试。","恒真式结论：H2 的逐来源守恒目前是分类计数恒等式，M6 的生产周集合比较则由同一 project 列表同时生成两侧；两处都需要独立的失败判据或异源证据。","建议进一步阅读 `tools/validation/word_consumers.js`、`frontend/src/shared/graphemes.js`、`tools/validation/assessment_contract.js`、完整课程构建/浏览器测试及两份被排除 fixture，以确认来源语义、分词契约和端到端加载覆盖。"],"risks":["严格消费入口中的未教字位可能被 H2 的 `skippedUnknown` 静默吞掉，未来 W5 数据错误仍显示测试通过。","历史周文件的 `META.week` 与文件名或项目周号不一致时，M6 可能无法发现，累计教学顺序会建立在错误身份上。","合法格式变化可能被 B-M1 误判成旧 HTML；反之，非脚本正文中的伪声明可能使前置探测失效。","混合大小写 `</script>` 可截断课程 JSON payload，导致课程入口解析失败或后续内容逃逸到外层页面。","额外的 `nat` 和可能过宽的 G1 正例豁免会掩盖普通拼读来源中的未教字位。","体积预算等号边界的文档与实现不一致，可能导致发布流程对“刚好触线”的处理产生误解。"],"confidence":"medium","notes_for_claude_code":"本审查仅依据提供的 diff 与任务说明进行静态复核，未执行命令、测试或行为验证。优先处理 H2 的无条件零解跳过和 DATA-PATTERN-01 的额外豁免；随后为 M6 增加历史 `META.week` 错配用例，并确认课程浏览器测试是否真的逐周打开 iframe。任务明确排除的 `assessment_contract.js` 字符级 CVC 问题未重复列为本轮 finding。"}