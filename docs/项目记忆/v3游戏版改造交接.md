---
状态: active（改造本身已完结；本文作为六个游戏的来历与编码期约束长期有效）
source_of_truth: docs/声音积木v3游戏版实施规格_20260829_v1.3.md（规格是真相源，本文是交接说明）
日期: 2026-08-29（现状信息更新至 2026-09-01）
---

# v3 游戏版改造 · 交接

**v3 改造已全部完成**（2026-08-29 单日三段 + 晚间增补）。

`week01-v3.html` = 周一首课最终课件，**兼分发课件**：发给任何家庭，各自设开课日期
（`state.startDate` 本地隔离），首页温和「今天」角标，日序 ∉[1,7] 时静默不催（归档 11）。
内含 56 条内嵌音频 + 完整播放层、G1 抓抓乐 / G2 积木合体 / G3 两扇门 / G4 点单 /
G5 造词工坊（周检程序锁）/ G6 闪卡纪录、点亮墙 + 战利品墙 + Nat 庆祝、
周检隔离 + 保留词五入口守卫（一审 critical 的终局闭环，DOM 级一致实测）。

## 状态速查（2026-09-01 核对）

- git：`main` 与 `v3-p0` 都在 `632d30c`，**均已推送 GitHub**；改造前基线 `7f3eb85`，其后 33 个 commit
- 审查链：归档 `docs/codex审查记录/01–12`，全部 0-high 收敛
- 任务锚点：[`docs/_archive/任务_v3-P0编码_20260829.md`](../_archive/任务_v3-P0编码_20260829.md)（§11/§11b/§11c 三段收尾报告）
- 规格：[`docs/声音积木v3游戏版实施规格_20260829_v1.3.md`](../声音积木v3游戏版实施规格_20260829_v1.3.md)，v1.0–v1.2 已归档到 `docs/_archive/`

> 三套 P0 期回归冒烟（`smoke_test.py` / `smoke_p1.py` / `smoke_p2.py`）当时放在 `tmp/`，
> 该目录是 `.gitignore` 的，**不在仓库里**。现有可用的自检是
> `tools/week-checks/check_data.js` 和 `tools/week-checks/smoke_w2_browser.py`。

## 使用与运维

- **部署**：仅拷 `week01-v3.html` 单文件到首课电脑；固定一台电脑一个浏览器（进度在 localStorage）；
  更新一律同名覆盖（D9，进度不丢）
- 音频单词不满意：删 `assets/audio_raw` 对应 mp3 → `tools/gen_audio.py` → `tools/build_audio.py`（注入模式）
- 改任何题库 / 数据结构：`tools/build_audio.py` 七项自检 + tripwire 会强制拦截不一致

## 遗留与后续

- **待追认 P6**：G4 摆放定为一步式入槽（规格 §4.4 已注记；要两步式 = `initG4` 加 `selectedTile` 约 20 行）
- ~~**week02 复用**~~ **已完成**（2026-09-01，`28ed4a8`）：换数据层模式跑通，并顺带固化成周课件生产线
  ——见 [周课件生产线](周课件生产线.md)。渲染器数据化的判断是对的，77 项锚点替换一次成功

## 编码期硬约定

- **真相源 = 规格 v1.3**（含编码期修订：§2.2 `stopForPhone` 例外、§7.1 #4 调试旗口径）；
  播放层（v3 约 772–990 行）已 0-HIGH，**改它需重审**
- G2/G4/G5 落地题库 / 白名单时 `tools/build_audio.py` 的 **tripwire 会强制 FAIL**，
  必须同步加提取源（S2 修复 M2 机制）
- G2/G4/G5 确认用 `confirmWord` / `unconfirmWord`（来源级）；战利品 = `derivedTrophies()` 派生
- 每 commit 的流程：预筛 → codex 审 0-HIGH → 归档顺延

## 一处需要注意的历史引证

规格 v1.3 文中有若干 `week01-v2.html:行号` 形式的溯源引证，指 v3 改造前的第一周旧版。
该文件与 `week01.html` 已于 2026-09-01 删除（被 `week01-v3.html` 取代），
需查证时用 `git show 7f3eb85:week01-v2.html`。

相关：[修复也是改动](修复也是改动.md)（修复也是改动 + 取消语义三层设防）
