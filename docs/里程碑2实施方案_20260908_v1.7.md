---
状态: **定稿**（2026-09-08）。九轮 codex 审共 74 条全部落实，0 驳回；九审判定「可以定稿」，0 critical / 0 high。三个决策点由用户拍板：范围走中间路线、迁移用双读兼容层、里程碑 2b 截止 2026-10-31。**2026-09-09 段 2 前置按用户裁定改五处**：截止日 09-27 → **09-13**（与 W5 审定截止撞车）、§3.4 作废「生产周 segments 可暂不填写」口径并加生成器（原 P3）、新增 §3.9 差分拆两套并改第 4b/7 步验收措辞（原 P7）、§7 把 W40 列进 2b 交付物（原 P1）、§7 记入分裂字位不在契约表达能力内（原 P5）
source_of_truth: 契约层以《周课件数据层交接规范 v2.0》§3 为准，本文只管怎么落地
日期: 2026-09-08
截止: **2026-09-13**（2026-09-09 用户拍板改期）。原为 2026-09-27，与课程方案 §5.3 的 **W5 审定截止 09-27 同一天** —— 两件事串行则 W5 没有制作时间（一周课件至少主机 4 小时 + 副机几天配图）。改到 09-13 后 W5 有整两周制作期。**W5 审定截止不变，仍是 09-27**
---

# 里程碑 2 实施方案 v1.7

## 0. 这份文档管什么

**管**：双字母字位改造的范围边界、改动面、实施顺序、数据迁移、基线重取时机、验收方式。

**不管，只指向**：字位 ID 与 `grapheme` 的契约、同形多音稳定 ID、四个字段改数组——这些在《周课件数据层交接规范 v2.0》§3 已写过并审过五轮，本文不重写定义，只列它们落到代码的哪一行。

### 0.1 范围映射（首审 C-2）

v0.1 声称以规范 §3 为契约来源，却把规范已经明确要求的三件事排除或遗漏。首审判定这是「宣称以上游为权威又局部排除其强制要求」。**用户 2026-09-08 拍板走中间路线**，逐条映射如下：

| 规范条目 | 规范怎么说 | 本次 | 依据 |
|---|---|---|---|
| `DATA-PATTERN-02` | 全部拆词逻辑走同一分词函数；同形多音 ID 唯一；四个字段是 ID 数组 | **纳入** | 里程碑 2 的本体 |
| `DATA-WALL-01` | 墙与首教日按 §3「唯一模型」；`wallLetters` 是完整集合，本周新点亮由 `newPatterns` 表达 | **纳入** | 与 `wallLetters` 是同一个字段，类型迁移和语义统一拆不开。后果见 §2.4 |
| 同形多音消歧 | 多解而 `segments` 缺失时校验器必须失败 | **纳入**（检测 + 显式 `segments` 消费接口，用合成 fixture 验证） | 首审 H-6：规范已规定该行为，合成 SOUNDS 现在就能验；不冻结接口，W13 前要改公共 API 和全部调用方，就是第二次破坏性变更 |
| `DATA-RESERVED-02` | 周检 5 词每个**三个字位** | **纳入** | 就是 `check_data.js:127` 那一行，属字位改造 |
| `DATA-PAIR-01` | `G3_PAIRS` 每对只差一个**字位** | **纳入** | 就是 `check_data.js:173`，属字位改造 |
| `DATA-ASSESS-01` | 测评常量按 `assessmentMode` 矩阵，不按周号；W5/W10/W20 三 fixture | **最小路由纳入，完整矩阵拆出** | 见 §0.3。复审 C-1：整体拆出会**挡死 W5 装配** |
| `DATA-STRATEGY-01` | `strategy` 方向与辅音群位置一致 | **拆出** | 规范自己就标「里程碑 2 后，依赖分词」 |

**不能同时宣称以上游为权威又局部排除它。** 所以 `DATA-ASSESS-01` 的拆分必须落到规范文本上，不能只在本方案里声明——见 §0.2。

### 0.2 本方案要求对规范做的修改

实施第 0 步执行，先于一切代码改动：

1. 规范 §4.1 第 17 条 `DATA-ASSESS-01` 的状态从「待实现（里程碑 2 同期）」改为「**部分实现（里程碑 2：最小路由）/ 待实现（里程碑 2b：完整矩阵）**」，按 §0.3 的两段切分写明各自内容，并在 §8 给里程碑 2b 的截止日写为 **2026-10-31**（2026-09-08 用户拍板）。推算依据：课程方案 §5.3 的 W5 自 09-28 起、每周一轮，则 W9 为 10-26 至 11-01、**W10 自 11-02 开始**；2b 的交付物含 W10 fixture，所以必须赶在 W10 开课前就位。
2. 规范 §8（2026-09-09 后为**门槛 B**）生效条件第 2 条拆成两条：**W5 fixture 与 `assessmentMode` 最小路由留在里程碑 2**；**W10 / W20 / W40 三个 fixture**与完整矩阵改挂里程碑 2b（W40 于 2026-09-09 补入，理由见 §7）。
3. 规范 §8（2026-09-09 后为**门槛 B**）生效条件第 1 条补全本次实际范围：除四个字段改数组和 `check_data.js` 六处外，加上墙语义统一、歧义检测与 `segments` 消费接口、`assessmentMode` 最小路由、`L` → `grapheme` 收敛。
4. **规范 §3 的"最长匹配"要限定**（六审 H-1）。规范第 127 行写的是「按已教字位集合**做最长匹配**」——把最长匹配写成了算法本身；第 129 行的"多解"也停在局部候选层面。而本方案 §3.2 要求的是**枚举完整解析集合、多于一解即失败**，最长匹配只能当剪枝。两份文本并存，两个实现者会分别做出贪心最长匹配和全局唯一性检测，**同一个词在浏览器与 Node 两端得到不同结果**。

   所以第 0 步同步修订规范：删除或限定"最长匹配"的表述，改为**以所有完整解析的基数判定 0 解 / 唯一解 / 多解**，并写明最长匹配仅可作搜索优化、不得改变解析集合。把 `abc` 在 `a`/`ab`/`bc`/`c` 下有两解的例子同时写进规范与 fixture。**此项必须在第 2 步实现接口之前定死。**
5. **规范的 `SOUNDS` schema 删除 `L`**（五审 H-1）。规范现在两处同时写着 `L` 与 `grapheme`——§3「`SOUNDS` 单条」的示例是 `ai:{L:'ai', grapheme:'ai', …}`，§3「12 个常量」表里是 `{字位ID:{L,grapheme,ipa,…}}`（第 158 行），而字段说明只解释了 `grapheme` 没解释 `L`。**按 v1.3 的文本直接删 `L`，代码会符合方案却违反被声明为权威的规范。** 所以第 0 步就要把规范的 schema、示例、字段说明统一改成只有 `grapheme`（必需、唯一显示字段），并注明旧数据的迁移要求。规范改完之后，第 4a 步才执行代码与数据的重命名。

规范 §8 的其余条件（`noveltyPolicy` 逐周取值、三方分工 v1.2 dry run、第五周实跑一轮）不动。

### 0.3 `DATA-ASSESS-01` 为什么不能整体拆出

v1.0 把它整体拆到里程碑 2b，复审 C-1 判为**阻断性矛盾**，本地核实成立：

- `assessment_contract.js:5` 是 `if(d.META.week < 4) return []`——**W5 会进入完整校验**，`:7` 要求 `RESERVED`、`RESERVED_RETEST`、`PROBE_A`、`PROBE_B`、`GLOBAL_RESERVED` 五个池齐全，`:10` 要求 `RESERVED` 与 `RESERVED_RETEST` 各 5 词，`:13` 要求 `GLOBAL_RESERVED` 完整保留附录 B 的 40 词。
- 而规范 §4.3 矩阵里 weekly 周**只允许 `RESERVED`**，正是要消除「普通周被迫携带无意义的全局常量」。
- `assessmentMode` 字段**目前全仓 0 处引用**，尚不存在。

所以整体拆出的结果是：W5 要么塞进一堆无意义常量才能过校验，要么根本装配不了。**基础假设「拆出后不阻塞 W5 装配」不成立。**

**改为两段切分**：

| 部分 | 归属 | 内容 |
|---|---|---|
| **最小路由** | **里程碑 2** | `META` 新增 `assessmentMode` 字段；`validateAssessment` 的门槛从 `week < 4` 改为按 `assessmentMode` 路由；weekly 周只要求 `RESERVED`。**这是 W5 能装配的前提** |
| **完整矩阵** | 里程碑 2b | monthly / stage / annual 三种模式的全部常量组合、`RESERVED` 释义来源、**W10 / W20 / W40 三个 fixture**（W40 = annual 列唯一 fixture，2026-09-09 补入，见 §7） |

W5 自己的 fixture 属最小路由部分，随里程碑 2 落地。

**最小路由阶段的临时行为必须逐档定义**（五审 H-2）。只写"weekly 走新逻辑、其余走旧逻辑"会留两个坑：W10 / W20 可能按周号旧逻辑假绿；全部拒绝又会打断 W4 现有的 monthly 回归。所以：

| `assessmentMode` | 里程碑 2 阶段的行为 |
|---|---|
| `weekly` | 执行规范 §4.3 矩阵 weekly 列的完整校验（必需 + 8 项禁止） |
| `monthly` | **仅 `assessmentMode:'monthly' && META.week === 4`** 可进入兼容路径，执行下表逐项枚举的检查集合；**其他 monthly 周一律报「里程碑 2b 前不支持」** |
| `stage` / `annual` | 返回固定的**「里程碑 2b 前不支持」错误**，不放行也不假装通过 |
| 缺失 / 非法值 | **一律失败** |

**禁止用默认分支回落到旧的周号逻辑**——那正是 `DATA-ASSESS-01` 要废掉的东西。

**W4 monthly 兼容集合逐项枚举**（六审 H-2）。v1.4 写的"仅执行 `week === 4` 分支内容"是错的：`validateAssessment` 在走到那个分支之前还有一整批公共检查，只搬分支会让 W4 悄悄失去它们。定义为**「当前校验器对合法 W4 实际执行的全部检查」**，逐项列出：

| # | 检查 | 现位置 |
|---|---|---|
| 1 | 五个池必须都是数组 | `:8` |
| 2 | `RESERVED` 与 `RESERVED_RETEST` 各 5 词 | `:10` |
| 3 | `PROBE_A`/`PROBE_B` 数量与段末周匹配（W4 非段末，须为 0） | `:12` |
| 4 | `GLOBAL_RESERVED` 完整保留附录 B 的 40 词 | `:13` |
| 5 | 词项合法 `^[a-z]+$` | `:17` |
| 6 | **池间互斥**（同一词不得同属两池） | `:18` |
| 7 | 周检/复测词是**已教 CVC** | `:23` |
| 8 | 测评词**不得泄漏进教学内容** | `:39` |
| 9 | 巩固周专项：不得声明新字位首教日、四类块齐全 | `:40-43` |
| 10 | W4 专项：必须是巩固周、点亮墙 19 字位 | `:47-48` |
| 11 | `TAUGHT_SIGHT` 精确等于前三周六词 `a/i/is/see/the/to` | `:51` |
| 12 | `ASSESS_TEXT` 不少于 60 词 | `:52` |
| 13 | 测评短文与测评池词冲突 | `:55` |
| 14 | 测评短文内容词不得进入教学词表 | `:56` |
| 15 | **测评短文不得含未教字位** | `:57` |
| 16 | 测评短文整句不得复用教学句 | `:60` |
| 17 | 测评短文全文不得复用 | `:61` |

**v1.5 只列到第 10 项就宣称"完整"，漏了 11–17 这七条**（七审 H-1）——我读代码时只看到 `week === 4` 分支的头两行，没往下读完分支体。若按十项表重写兼容路径，W4 会实质性退化：测评短文可能不足 60 词、复用教学句子、或含不合规内容词而仍然通过。

**实现约束**（采纳七审建议）：不要照着这张表重新实现。**先把现有 W4 路径原样提取为独立函数，用「重构前后错误完全一致」的特征测试锁定**，再接到 `assessmentMode:'monthly' && week===4` 上。表只作为核对清单，特征测试才是门槛。

**比较语义写死**（八审 M-3）：现行校验器对重复词或多处冲突会产生**重复错误**，用普通集合比较会把重复次数和顺序变化一起吞掉，"等价"就成了假的。所以——**逐项比较有序错误数组**（错误顺序视为外部行为的一部分）；若将来判定顺序不属于契约，**至少比较多重集**，并另行固定错误码与关联字段。**禁止用会去重的普通集合。**

第 7 项的已教 CVC 判定与第 15 项的未教字位判定都是字符级的，改走字位后一并处理——见 §2.2 矩阵「字位数」与「陌生度」行。

## 1. 为什么现在做

课程方案 §5.3：W5（09-28 起）教 `ai` `j`，技能重点是"两个字母一块积木"。§9.2 对里程碑 2 的描述是：

> 拆词按字位表而不是按单字母；`SOUNDS` 键允许双字母；G2、G3、G4、G5、点亮墙、快闪、校验器同步。现引擎按单字符拆词，第 4 组六块积木有五块摆不出来。

W5 审定截止 09-27。**不做这个，W5 出不来。**

**本方案自己的截止是 09-13，不是 09-27**（2026-09-09 改期）：两者同为 09-27 时 W5 将没有制作时间，必须给它留出两周。

「五块摆不出来」的真实机制在 §2.2 的槽位长度一行：G4 按**字符长度**建槽，`rain` 建四个槽却只有三块积木，永远填不满。

## 2. 现状事实（2026-09-08 首次实测；**2026-09-09 15:20 全节重核**）

> **本节已于 2026-09-09 段 2 开工前逐条重核。** 触发原因：副机在 2026-09-08 20:36 前推了 11 个提交（`df1d0fa..41f08b2`，家长面板日期排期 UI），改了 `state.js`、`week01.data.js`、**四个模板**、`baseline_20260907.json`。
>
> **重核结论：核心链路未受影响。** 副机**没有触碰** `tools/validation/*.js`、`frontend/src/shared/games.js`、`frontend/src/shared/render-blocks.js` —— 而 §2.2 迁移矩阵的绝大多数 `file:line` 引用正来自这四处，**行号原封不动**。受影响的只有两类：
>
> 1. **四个模板行号整体下移 16—23 行**（各插入约 39—43 行家长面板代码）。§2.2 的「元音分类」与「墙」两行已按实测改写；**后续每一步定位模板代码前仍须重新 grep，不得照抄本表行号**。
> 2. **`week01.data.js` 的词表内容变了**（移除 `spin` 及其 `steps`/`flash` 条目，`flashCapacity.flash_words` 18→17），但 `META.wallLetters`/`rackG4`/`rackG5` 三个字段**一字未动**，§2.3 的数值表不受影响。
>
> 重核同时修正了**两处 authoring 期就存在的原始笔误**（不是漂移，`git show 226e619` 里同样是错的）：`load_data.js` 的 `:21`→`:20`、`games.js` 的 `:595`→`:594` 与 `:716`→`:715`。
>
> **另外两条要带进后续步骤**：①副机给持久化状态新增了 `pausedOn`／`openThrough`／`startDatePromptDismissed` 三个字段，与字位无关，但 §3.1 第四条的「持久化状态键全集」审计要把它们扫进去，否则清单不完整；②`tests/fixtures/baseline_20260907.json` **已被副机在 09-08 重取过一次**（`sourceCommit` 现指向 `fc6b194`），它既不是 09-07 那份也不是本次改造前的那份 —— 第 9 步重取基线时的对照基准要以**本次改造正式开始前最后一次干净状态**为准，不要拿本节讨论时的那份当"改造前基线"。

### 2.1 六个游戏对应的块

方案说"G2、G3、G4、G5 同步"，但代码里只有 `g1`/`g4`/`g5` 三个挂载点。对照周课件规范 §8：

| 游戏 | 名称 | 实际载体 |
|---|---|---|
| G1 | 声音抓抓乐 | `g1` 块 |
| **G2** | 积木合体 | **`blend` 块**（不是独立挂载点） |
| **G3** | 两扇门 | **`pair` 块** |
| G4 | 点单游戏 | `g4` 块 |
| G5 | 造词工坊 | `g5` 块 |
| **G6** | 计时闪卡 | **`flash` + `timed`** |

**G2 是逐词拼读，直接决定孩子看到 `rain` 被拆成几块；G4 是唯一会因字位改造而彻底玩不了的游戏。**

### 2.2 迁移矩阵

v0.1 按 `split('')` / `charAt` / `[...w]` 三个 grep 模式列了「13 处」。首审 L-13 指出这种计数把文件级位置、模板数量和多条语义混计，**产生覆盖完整的错觉**——事实确实如此：`word.length`、`slots.join('')`、`from.slice(1)` 都不在那三个模式里，因此 v0.1 漏掉了 G4 无法完成这个最重的缺陷。

改按**消费场景**列，每行给出生产代码、校验器、测试三层：

| 场景 | 生产代码 | 校验器 | 测试 / fixture |
|---|---|---|---|
| **输入解析** | — | `load_data.js:24` 按字符切 rack；**兜底 `Object.keys(FIRST_TEACH_DAY).join('')`** 在键含双字母时会拼成 `'aij'` 无法反解析。兜底只在 HTML 缺内联 `META` 时触发（**`:20`** 的 `if (!box.META && html)`；v1.7 前误记为 `:21`，2026-09-09 重核修正），四周现有产物都内联了 `META` 所以目前是死路径——但**不能靠"碰巧不触发"**，见 §3.8 | — |
| **分词** | `games.js:22`（G2 积木态）、`render-blocks.js:2`（`colorWord`） | `check_data.js:133` `[...w.toLowerCase()]` 陌生度（**带 `toLowerCase`**，分词函数须定义大小写规则） | 新增纯函数单测 |
| **槽位长度** | **`games.js:430` `slots = new Array(w.length)`**（G4 按字符建槽，`rain` 建 4 槽填不满）；`games.js:594/661/715/721/729` G5 的 `slotCount`（v1.7 前把 `594`/`715` 误记为 `595`/`716`，2026-09-09 重核修正） | — | ai 集成 fixture |
| **摆词比较** | `games.js:451`、`:710` 逐字母匹配；**`games.js:483/491` G4 `slots.join('')`**；**`games.js:673/691/766` G5 `slots.join('')`** | `check_data.js:139-140` `canSpell` | 差分测试 |
| **首字位** | `games.js:851`、`:915` `charAt(0)`；**`render-blocks.js:169` `charAt(0)` 与 `:164` `from.slice(1)` 成对**（`aid` 这类 `ai` 开头词，`slice(1)` 得 `id` 而非 `d`） | `check_data.js:60` `w[0]` | 差分测试 |
| **字位数** | — | **`check_data.js:127` `RESERVED.every(w => w.length === 3)`**（周检词"三个字母"→"三个字位"，`DATA-RESERVED-02`）；**`assessment_contract.js:23`** 周检/复测词的 `/^[^aeiou][aeiou][^aeiou]$/` CVC 正则——**字符级正则，双字母元音的 `rain` 判不出 CVC** | 差分测试 |
| **陌生度 / 已教范围**（七审补） | — | `check_data.js:133` `[...w.toLowerCase()]`；**`assessment_contract.js:23`** 的 `[...normalize(w)].some(c=>!taught.has(c))`；**`assessment_contract.js:57`** 的 `[...w].some(c=>!taught.has(c))`（测评短文未教字位，v1.5 前从未列入） | 差分测试 |
| **最小对立** | — | `check_data.js:173` `[...a].filter((c,i)=>c!==b[i])`（`DATA-PAIR-01`） | 差分测试 |
| **积木架** | `games.js:342`、`:593` `rack.split('')` | `check_data.js:148` `new Set([...rackG4,...rackG5])`（数组下自动等价） | — |
| **元音分类** | `games.js:583` `groupedRackHTML`；**四个模板各 1 处 `vowels.includes(c)`**（**2026-09-09 实测：week01:1341 / 02:1408 / 03:1417 / 04:1401**；v1.7 记的 1323/1385/1394/1383 已因副机插入家长面板代码而漂移）；G4/G5 的 tile class | — | 分组固定位断言 |
| **墙** | **week01:1339 / 02:1403 / 03:1412 是硬编码字面量** `'satipn'.split('')` / `'ckehrmd'` / `'goulfb'`，**不读 `META.wallLetters`**；week04:1401 读 `META`（**行号为 2026-09-09 实测**；v1.7 记的 1323/1385/1394/1383 已漂移。硬编码这个**事实本身未变**） | `check_data.js:153` 遍历（自动等价）、**`:158` `wallLetters.includes(c)`**（字符串下是子串匹配）；`assessment_contract.js:48` `new Set(...).size !== 19` | — |
| **显示 / ARIA** | `tileHTML`、G1 目标字位、G4/G5 rack 按钮**直接显示 ID**；契约要求显示 `SOUNDS[id].grapheme`，否则将来 `oo_short` 泄漏到孩子界面 | — | — |
| **旧显示字段 `L`**（四审 H-3） | **两条数据流混在同一个函数里**：`render-blocks.js:46` 传**字形**（4a 后是 `tileHTML(SOUNDS[f].grapheme, …)`），而四个模板的 **daycard**（`week01.template.html:1362` 一带）是 `tileHTML(s, …)` 传 **ID**。单字母阶段 `L === ID` 所以一直假绿。改法：**`tileHTML` 一律收 ID，内部用 `graphemeLabel(id)` 取显示文字**，禁止调用方先转成 `L`；`L` 按 §3.1 重命名为 `grapheme` | — | — |<br>⚠️ **2026-09-09 实测更正**：原文写「传 ID 的是四个模板的**首页墙**」——**错**。首页 hero 积木墙**根本不调用 `tileHTML`**，它是四份各自手写的按钮拼接（`week01.template.html:1339-1342` 一带的 `'satipn'.split('').map(c => <button class="tile …">${c}</button>)`，week02/03/04 同构）。真正把 ID 传给 `tileHTML` 的是 **daycard 的 `d.sounds`**。**这个差别对第 4b 步是要害：只改 `tileHTML` 的实现，四处 hero 墙纹丝不动**，而它们正是把裸 ID 直接塞进按钮文字与 `aria-label` 的地方。
| **文案** | **`games.js:390` `听到的词是 ${word.length} 个字母`**——`rain` 会说"4 个字母"却只给 3 块积木 | — | — |
| **句子 / 正文着色** | `colorWord` 共 **42 个调用点**（**2026-09-09 精确复核仍是 42**：`games.js` 8 + `render-blocks.js` 12 + week01 4 + week02/03/04 各 6；`grep -o 'colorWord('` 得 43 处，减去 `render-blocks.js:1` 的函数定义本身），其中 `colorWord(BOOK.title)`、`colorWord(pg.line)`、`colorWord(pageData.line)` 传的是**含空格标点的整句**，不能对整串做严格分词 | — | — |<br>**2026-09-09 逐点分类实测：整句 10 处 / 单词 32 处**。整句 = `BOOK.title` ×4 + `pg.line` ×4 + `pageData.line` ×1（`games.js:1284`）**外加 ⚠️ 第 10 处 `render-blocks.js:139` 的 `colorWord(en)`**——本方案与 §3.3 原先都只点名前三类，**漏了这一处**。它在 `case 'sentences'` 里，形参名 `en` 看着像单词，实测数据是 `['I see a pin.','我看见一枚大头针。']`（`week01.data.js:275`），**是含空格与句号的整句**。第 4b 步若把它误判成单词走严格分词，会直接抛「未知片段」。
| **机器身份 / 外部键** | 确认状态与 `confirms` 键、DOM `data-*`（`data-sayph`、`data-g4-slot`、`data-wf-word`）、CSS tile class、`audioKey` 录音解析——按 §3.1 第四条分别用 ID / 表面串 / audioKey | — | — |
| **传输与编码**（五审 RECOMMENDATIONS） | HTML 属性转义、JSON / 逗号序列化、DOM dataset、持久状态键、**错误信息里展示的 ID** | — | 引号 / 尖括号 / `&` / 非法 ID fixture |
| **序列化 / 反解析** | 四个模板的 `META` 内联 | `load_data.js` 从 HTML 反解析 | — |
| **测试层** | — | — | **`tests/fixtures/week02-data.js:6-8`、`week03-data.js:6-8`** 三字段；**`tests/unit/test_assessment_contract.js:5-6`** 内联 `wallLetters` 字符串并用它 `.split('')` 造 SOUNDS |

其中**槽位长度、文案、句子着色、显示层、测试层**五行是 v0.1 完全没有的，**机器身份 / 外部键**一行是复审 M-10 补的，**旧显示字段 `L`** 一行是四审 H-3 补的——它是附上四个模板后才看出来的第三种漏法；**陌生度 / 已教范围**一行是七审补的第四种——`assessment_contract.js` 的 `:23`、`:57` 两处按字符展开，前六轮一直没进矩阵。

### 2.3 `rack` 是多重集，不是集合

> **2026-09-09 重核：本节表格全部数值仍成立。** 副机虽改了 `week01.data.js` 18 行，但改的是 `W` 词表（移除 `spin`）与 `flash` 条目，`META.wallLetters`/`rackG4`/`rackG5` 三字段未动。**注意**：后续若有步骤要读 `W`/`G5_WHITELIST`/`DAYS` 的**具体条目数**，须重新核对，不能沿用本节写作时的快照。

四周实际值（2026-09-08 实测）：

| 周 | `wallLetters` | `rackG4` | `rackG5` |
|---|---|---|---|
| W1 | `satipn` | **`ssaattiippnn`**（每字母两块，12 块） | 同左 |
| W2 | `ckehrmd` | `atpncehmrd`（10 块） | `satipnckehrmd` |
| W3 | `goulfb` | `aouhmdglfb`（10 块） | `satipnckehrmdgoulfb` |
| W4 | `satipnckehrmdgoulfb` | `satipnckehrmdgoulfb` | 同左 |

**第一周每个字母有两块积木**，因为它要摆 `nn`、`pp` 这类。迁移成数组时**必须保留重复项**——用 `Set` 或去重会让第一周的 G4/G5 直接摆不出词。

首审 H-3 补充：保留重复只是必要条件，还必须把槽位长度、数组比较、表面串生成、白名单查询一并改成字位安全，见 §3.1。

### 2.4 墙语义统一的实际后果

规范 §3 定义 `wallLetters` 是「墙上**完整**的积木集合，有序」，等于 `SOUNDS` 全部键；本周新点亮的由 `newPatterns` 表达。现状按周型跳变。统一后：

| 周 | `SOUNDS` 键数 | `wallLetters` 现值 | 统一后 | 后果 |
|---|---|---|---|---|
| W1 | 6 | 6（`satipn`） | 6 | 不变 |
| W2 | **13**（12 + 追加的 `k`） | **7**（`ckehrmd`） | **13** | **墙 7 块 → 13 块** |
| W3 | **19**（18 + 追加的 `k`） | **6**（`goulfb`） | **19** | **墙 6 块 → 19 块** |
| W4 | 19 | 19 | 19 | 不变 |

**这是已交付课件的呈现改变，不只是类型迁移。** 第二、三周的孩子会看到墙上多出此前学过的积木（未点亮态），这正是规范想要的"墙是完整集合、新的靠动画区分"。

两件事必须同时做，否则改了等于没改：W2/W3 的模板是**硬编码字面量**，不读 `META.wallLetters`——只改 data.js，页面还是显示旧的 7 / 6 块。

`k` 是**追加键**（`week02.data.js:106`、`week03.data.js:150` 的 `SOUNDS.k = Object.assign({}, SOUNDS.c, { L:'k' })`，c/k 同音共享录音但是两块积木）。分词函数取字位表必须在追加之后，否则 `k` 不在表里。反解析侧已经支持——`load_data.js:17` 有 `raw.match(/^SOUNDS\.\w+ = Object\.assign\(.*?\);$/gm)` 专门收这类追加声明。

**墙的顺序需要独立真相源**（三审 H-3）。规范既要求 `wallLetters` 顺序有教学意义，又明确说不依赖对象键枚举顺序——那么校验时的"期望顺序"不能取 `Object.keys(SOUNDS)`（违反后一句），也不能取 `wallLetters` 自己（循环校验）。

**真相源定义为：按 `project.json` 的周序，依次累计各周 `newPatterns`，同周内按 `newPatterns` 自身顺序追加。** `DATA-WALL-01` 拿 `wallLetters` 与这个独立序列比，而不是与 `Object.keys(SOUNDS)` 比。

**取数规则写死**（四审 H-4），否则单文件 fixture、历史缺周、别名跨周会产生不确定结果：

- 计算函数是**纯函数**，输入为**按周排序的规范化周记录数组**，不直接读工作树——否则独立 fixture 会受仓库现状影响而不可复现。
- **当前被校验的文件替换仓库里的同周记录**，不重复计入。
- 周号必须**连续且唯一**，不满足即失败。
- 追加时**重复即错误**，不是静默去重。
- **别名派生键**（`k` 这类）**必须出现在某周的 `newPatterns` 里**，与本体同周按数组顺序表达（六审 M-6 拍板）。不引入额外的插入元数据，也不靠 `Object.assign` 的语法形状去猜。
- 第 7 步启用该校验时，**同时迁入 W1–W4 的 `newPatterns` 数组**——这个字段和 `assessmentMode` 一样是本次新增，不先迁就没有期望序列可算。

### 2.5 基线比对四项

fixture 里记四项：`sha256`、`skeletonSha256`、`declarationOrder`、`media`。但**自动门槛只有后三项**——`baseline.py:210-224` 的 `differences()` 只比较 `skeletonSha256`、`declarationOrder` 与 `media`，**不比较周 `sha256`，也不比较 `course.sha256`**（三审 H-4）。

所以口径写清楚：**`sha256` 是审计元数据，不是自动门槛**。改 `META` 会改变 data.js 内容进而改变产物，`sha256` 与 `skeletonSha256` 都会变，但校验器只会因 `skeletonSha256` 变化而报错。基线仍必须在改造完成后重取，验收按工具真正检查的三项写（§5）。

首审 M-11 补充：`baseline --create` 会先构建并要求构建**不产生 tracked 变更**，所以重取之前必须已提交全部受版本控制的派生物，工作树干净——否则该命令直接拒绝。

⚠️ **2026-09-09 重核补充**：`tests/fixtures/baseline_20260907.json` **已被副机在 2026-09-08 重取过一次**（`takenAt` 09-07→09-08，`sourceCommit` 改指 `fc6b194`，四周的 `sha256`/`skeletonSha256` 与 `course.sha256` 全变，`declarationOrder` 未变）。**文件名里的 `20260907` 已不代表其内容日期。** 本节讨论的机制不受影响（自动门槛仍是后三项），但第 9 步重取时的对照基准要取**本次改造正式开始前最后一次干净状态**，不要把这份当成"改造前基线"。

## 3. 设计决策

### 3.1 三层概念：字位 ID / 字形 / 表面串

首审 C-1 的根因是 v0.1 只想着"把字符串换成数组"，没区分这三层。定义如下，本方案后文一律按此用词：

| 层 | 是什么 | 例 | 用在哪 |
|---|---|---|---|
| **字位 ID** | `SOUNDS` 的键，稳定标识 | `'r'`、`'ai'`、将来的 `'oo_short'` | 槽位、比较、rack、四个字段 |
| **字形 `grapheme`** | `SOUNDS[id].grapheme`，它实际的字符串 | `'r'`、`'ai'`、`'oo'` | 一切显示：tile 文字、ARIA、CSS 类 |
| **表面串** | 词本身，等于各字位字形依次拼接 | `'rain'` | 查词：`W` 的键、G5 白名单、`confirms` 键、音频键 |

**`grapheme` 在实作里已经存在，叫 `L`**（四审 H-3）。`SOUNDS` 每条都有 `L` 字段，`week02.data.js:106` / `week03.data.js:150` 的 `SOUNDS.k = Object.assign({}, SOUNDS.c, { L:'k' })` 改的正是它——c 与 k 共享发音数据、只有显示字母不同。现在全部字位是单字母，所以 **ID === `L`**，两者随便混用都不出错；`oo_short` 一出现就分家。

**本次拍板：`L` 就是 `grapheme`，做重命名**。`SOUNDS[id].grapheme` 是唯一字形字段，`L` 不保留为别名——留着会让"到底该读哪个"永远有歧义，而现在四周数据的 `L` 值与键完全相同，重命名是纯机械替换，代价最低。校验器加一条：`SOUNDS` 条目不得再出现 `L` 字段。

**影响面（2026-09-08 全仓扫描，按属性访问 / 字符串键 / 解构 / 派生赋值四种模式）**：

| 类别 | 位置 | 处置 |
|---|---|---|
| 生产代码 | `render-blocks.js:46` `tileHTML(SOUNDS[f].L,…)`、`:49` `forms.map(f=>SOUNDS[f].L).join(' 和 ')`、`games.js:1154` flash 的 `s.L` | 改走 `graphemeLabel(id)` |
| **校验器** | **`check_data.js:83` `ok(s.L && s.ipa && …, 'SOUNDS.${k} 缺 L/ipa/type')`** | **改判 `grapheme`**。这条是硬门槛，不改则删 `L` 当场全红 |
| 数据 | 四周 `weekNN.data.js` 每个 `SOUNDS` 条目（week04 是 `"L": "s"` JSON 风格 19 条，前三周是 `L:'s'` 紧凑风格）；`week02.data.js:106`、`week03.data.js:150` 的 `Object.assign({}, SOUNDS.c, { L:'k' })` | 随第 4a 步重命名 |
| 测试 fixture | `tests/fixtures/week02-data.js:108`、`week03-data.js:152` 的同一句派生 | 同步 |
| 权威规范 | `周课件数据层交接规范 v2.0` §3 示例与第 158 行的 12 常量表 | **第 0 步先改**，见 §0.2 **第 5 条** |
| 归档文档 | `声音积木周课件设计规范_20260831_v1.0.md:222/237/244`、`第三周数据层交接_20260901_v1.0.md:82` | **不改**：已归档的历史文档，改了反而与它们描述的历史实作不符 |
| 遗留工具 | `tools/legacy/w2data.py:107` | **不改**：`tools/` 与 `project.json` 均无引用，纯归档 |

**验收门槛是数据与代码双保险**（六审 M-4）。四模式扫描是**发现手段**，不能单独当完成证明——它漏得掉计算属性访问、别名后访问、辅助函数间接读取。所以：

1. **运行时 schema 断言**（兜底）：加载全部现役周与 fixture 后，递归断言每个 `SOUNDS` 条目**没有自有键 `L`**、且**有合法 `grapheme`**。
2. **源码扫描**：用 AST，或明确覆盖 `.L`、`['L']`、`["L"]`、对象字面量键、解构、`Object.assign` 派生六种写法；归档目录走**显式排除清单**，不靠路径猜。

两条都过才算第 4a 步完成。

**硬规则**：

- 凡是"有几块""摆对没有"——用 **ID 数组**，逐项比较，不用 `join('')`。
- 凡是给人看的——用 **grapheme**，不要把 ID 直接渲染出去。单字母字位两者相同，所以现在看不出差别；`oo_short` 一旦出现，直接泄漏到孩子界面。
- 凡是查表——用 **表面串**，由 `surfaceOf(ids, SOUNDS)` 生成，不靠 `slots.join('')` 偶然成立。

**第四条：机器身份与外部键**（复审 M-10）。上面三层覆盖的是数据转换，但还有一类消费是"拿什么当标识符"：

- **确认状态、槽位、DOM `data-*`、业务比较** → 用稳定 **ID**。同形多音若用 grapheme 当键，`oo_short` 和 `oo_long` 会共享确认状态。
- **词级持久状态** → 用规范化**表面串**。
- **音频** → 必须经 `audioKey` 解析，不能拿 ID 或 grapheme 直接拼文件名（`k` 与 `c` 共享录音正是靠它）。
- **CSS 类名** → 不得直接拼接未经转义的 ID；`oo_short` 这类带下划线的还好，将来若有特殊字符会产生非法或难选择的选择器。

这四类已列进 §2.2 迁移矩阵。

G4 因此改成：先取目标词的字位序列 `targetIds`，槽数用 `targetIds.length`，判定逐项比较 ID；G5 的 `slots` 存 ID，查词前先过 `surfaceOf`。

**文案同步改**：`games.js:390` 的「听到的词是 N 个字母」改成「N 块积木」。课程方案 §5.3 的说法就是"两个字母一块积木"，说"字母"会和教学口径打架。

### 3.2 `segmentWord` 契约

v0.1 的 `tokenize(word, patterns)` 只有签名没有语义，`patterns` 到底是 ID 数组、字形数组还是映射也没定义（首审 H-3）。改为：

```js
segmentWord(word, sounds, explicitSegments?) -> string[]   // 字位 ID 数组
```

契约逐条写死：

- **匹配依据固定为 `SOUNDS[id].grapheme`**，不是键本身——`oo_short` 这个键不会出现在任何单词里。
- **规范化**：先 `toLowerCase()`（`check_data.js:133` 现在就是这么做的），规范化后的串用于匹配。
- **歧义按「整词有几种完整分词」判，不是按「当前位置有几个同长度候选」**。复审 H-3 指出 v1.0 的「最长匹配优先 + 同长度多解抛错」只能抓局部冲突：若字位表含 `a`、`ab`、`bc`、`c`，则 `abc` 有 `ab`+`c` 与 `a`+`bc` 两种完整解，**首段长度不同**，局部规则一个都抓不到。规范 §3 要的是「出现多解即失败」。
  所以契约是：**求整词的全部可行分词——0 解报未知，恰好 1 解返回，多于 1 解报歧义**。最长匹配只能用作剪枝或在已证明全局唯一后的选择依据，不能用来消灭合法的较短起始路径。
- **复杂度有界**（复审 M-12）：用按 offset 记忆化的动态规划或 trie，状态只保留「0 解 / 唯一解 / 多解」三态，**不枚举全部解**，避免前缀高度重叠时的指数回溯。

  **但三态只能截断"解的条数"，不得截断"最早歧义位置的候选集合"**（九审 L-3）：`candidates` 必须从该位置**可达且能到达终点**的状态边直接算出，不能从留存的有限见证解析里反推——否则三解以上时会漏候选。
- **未知区间 → 抛未知错误**。**空串 → 抛错**。
- **`explicitSegments` 的完整校验**（复审 H-5）：必须是非空字符串数组；每个元素必须是 `sounds` 的**自有键**；`surfaceOf(explicitSegments)` 必须等于规范化后的 word。任一条不满足即抛结构化错误。`displayOnWall:false` 只影响墙展示，**不影响分词资格**。
- **`surfaceOf`、`graphemeLabel`、`soundType` 遇到未知 ID 一律抛同一种结构化错误**，不返回 `undefined` 让它在下游变成难定位的异常。
- **错误是结构化对象**，带 `{word, offset, candidates}`，让校验器能指出是哪个词的哪个位置需要消歧。浏览器构建可只带精简错误码，Node 校验器补详细位置说明。
- **输入 schema 先验**（三审 M-7）。建索引前先校验，不合法立即抛：`word` 必须是字符串；`sounds` 必须是普通映射且**只读自有键**（不走原型链）；每项的 `grapheme` 必须是非空字符串，且规范化后仍非空。**空 grapheme 必须在进 DP 前失败**——否则会产生零长度转移，DP 状态图出现自环而不前进。
- **`offset` 的口径**：以**规范化后**的串计，用 UTF-16 索引，浏览器与 Node 必须一致。
- **歧义错误的定位必须确定，且不能依赖"挑哪两条解析"**（七审 M-4 → 八审 H-1）。v1.6 写的"取两条见证解析的最早分歧点"仍不确定：**三解以上时没规定选哪两条见证**，不同 DP / trie / 遍历顺序会选出不同见证对，偏移量又不一致了——候选排序稳定并不能消除见证选择的不确定性。

  改为**按位置定义、与见证无关**：

  - **`offset`** = 在所有**能够延伸到完整解析**的路径中，**最早出现两个及以上不同「下一字位 ID」的位置**（规范化串的 UTF-16 索引）。
  - **`candidates`** = 该位置上所有**能够延伸到完整解析**的下一字位 ID，按**明确的比较器**排序：**先按 grapheme 规范化后的 UTF-16 长度升序，再按 ID 的 Unicode 码点字典序升序**。ID 已限定为 `^[a-z][a-z0-9_]*$`（纯 ASCII），**禁止用 `localeCompare`**——区域设置与 ICU 版本会让两端漂移（九审 L-2）。

  注意"能够延伸到完整解析"这个限定不可省：一个在当前位置匹配得上、但后续走不通的字位不是真歧义，不该进候选。

  fixture 至少覆盖：**三解及以上**、**不同分叉深度**（分歧点不在词首）、**打乱 `SOUNDS` 插入顺序后结果不变**、**同长度的多个 grapheme**、**同一 grapheme 对应多个 ID**（十审）；并含**同一分歧位置存在三个可完成候选**的案例，防止实现从有限见证路径反推候选集合。浏览器与 Node 同时断言错误码、`offset` 与**完整候选数组**。
- **确定性**：同输入同输出，不依赖 `SOUNDS` 的键枚举顺序。

配套导出：

```js
surfaceOf(ids, sounds)            // 字位 ID 数组 → 表面串
graphemeLabel(id, sounds)         // 字位 ID → 显示字形
soundType(id, sounds)             // 字位 ID → 'c' | 'v'
normalizeIdList(value)            // 迁移期双读，见 §3.5
colorStrictWord(word, ctx)        // 单词：分不出就是数据错误，抛
colorPlainText(text, ctx)         // 纯文本句子：逐段转义，未知词降级
colorRichText(html, ctx)          // 受限富文本：白名单解析后只着色文本节点
```

**所有会拆词的上层入口收的是解析上下文，不是裸的 `sounds`**（四审 C-2）：

```js
ctx = { sounds, segmentsOf(normalizedWord) }
```

**`segmentsOf` 的调用契约同样写死**（五审 H-4），否则 plain / rich 各自采用不同的规范化方式，显式消歧仍会丢：

- 着色器**先用唯一的 `normalizeWord` 得到查表键**再调用，传进去的一定是规范化后的词，不是原始 token。
- 找不到词条返回 **`undefined`**，表示"无显式分段"，走自动分词。
- 返回非 `undefined` 时，**必须再交给 `segmentWord` 走完整校验**（§3.2 的 `explicitSegments` 那套：非空字符串数组、ID 是自有键、表面串一致），不能直接信任。
- 返回的数组**不得被调用方就地修改**。

测试覆盖四类：大写 token、同一个词在一段文本里重复出现、一段 HTML 里有多个不同的消歧词、未知词降级。

v1.2 把三个着色接口写成 `(text, sounds)`，**没有任何通道能拿到 `W[word].segments`**——遇到 `oo_short`/`oo_long` 时，规范要求"用显式 `segments` 消歧"的数据在渲染层重新变成歧义错误，等于 §3.4 说的"冻结消费接口"根本没做完。`colorStrictWord` 必须读显式 segments；`colorPlainText`/`colorRichText` 里的每个 token 也要能查到对应词条。要加同形多音词在 strict / plain / rich 三条路径都正确着色的用例。

底层可以另有一个收预编译索引的纯函数做性能优化，但**对外只暴露上面这组**，不暴露含义不明的 `patterns`。

`sounds` 参数必须传**追加键之后**的 `SOUNDS`（§2.4 的 `k`）。

### 3.3 句子与正文的着色

`colorWord` 有 42 个调用点，其中**十处**传的是含空格标点的整句（2026-09-09 逐点实测更正，原写「三处」）（书名、书页正文）。对整串做严格分词必然抛错。

结果会进 `innerHTML`，所以**按输入是不是富文本拆成两个接口**（复审 H-6）：

| 接口 | 输入 | 规则 |
|---|---|---|
| `colorStrictWord(word, sounds)` | 单个词（词卡、tile、G2 积木态） | 走严格分词，分不出来就是数据错误，**抛结构化错误** |
| `colorPlainText(text, sounds)` | 纯文本句子（书名、书页正文） | 先保留分隔符地切词，逐词尝试分词；能分的着色，不能分的降级为原样；**每一段非着色文本必须 HTML 转义**后再拼回 |
| `colorRichText(html, sounds)` | 允许标记的字段 | 按白名单解析后**只对文本节点着色**，标签结构保留 |

**白名单是完整结构，不只是标签名**（三审 M-8）：`<b>` 无属性、`<br>` 无属性、`<span>` 仅允许 `class` 且值严格等于 `en`。**其余标签与任何其他属性一律拒绝，不是保留**——只限制标签名挡不住 `span` 上的事件属性、`style` 或危险实体。

**校验顺序固定为「先解析 → 再检查解析后的元素与属性 → 最后受控序列化输出」**（四审 M-7）。顺序反了会出事：先扫源码字符串再交给 `DOMParser` 解码，实体可以在解码后改变文本或属性语义。同时**拒绝注释节点、命名空间元素和任何非白名单节点类型**。

测试比较**规范化后的 DOM 语义**，不要求浏览器与 Node 逐字节序列化一致。用例含实体编码的属性、嵌套标签、畸形标签。

**42 个调用点必须逐项登记归类**（三审 H-2）：不能只凭"有三个接口"就声称覆盖。落地时产出一份调用点清单，每项标注走 strict / plain / rich 哪一条，作为第 4b 步的交付物之一。

**另产出「模板内字位展示来源清单」**（四审 M-8）：模板里还有不经过 `colorWord` 的字位展示——首页 hero 文案、品牌周标题、示例墙字面串、声音块、flash、打印小书路径、各处 ARIA。每一项逐条标记为**数据驱动 / 刻意静态文案 / 必须迁移**三者之一。只登记 42 个着色调用点覆盖不到这条。

**禁止用正则直接在整段 HTML 上分词**。单词场景仍走严格路径，分不出来就是数据错误，应该报。

四周现有数据的 `line` / `title` 实测**都不含标签**，但 `assessment_contract.js:2` 的 `tokens()` 里有 `replace(/<[^>]*>/g, ' ')`——系统是**预期文本可能带标签**的，所以边界必须现在定义，不能靠"当前数据碰巧没有"。要加尖括号、`&`、引号和允许标签的 fixture。

不能分的词原样输出（转义后），是"降级要换形态"的应用：句子里遇到不认识的词，换的是渲染方式，不是删掉着色能力。

### 3.4 同形多音：本次冻结接口并实现检测

v0.1 说"现在只在 `SOUNDS` 键上留出空间，不写消歧逻辑"，理由是"没有数据可验"。首审 H-6 判这个理由不成立，**用户已拍板纳入**：

- 规范 §3 已经规定了行为（多解且无 `segments` 即失败），这条**现在就能用合成 `SOUNDS` 验证**，不必等 W13 的生产数据。
- 不冻结返回与错误契约，W13 前要改公共 API、全部调用方和缓存结果——那就是第二次破坏性变更，正是本次想避免的。

所以本次做：歧义**检测**、显式 `segments` 的**消费接口**、合成 fixture（`oo_short` / `oo_long`）。

⚠️ **口径更正（2026-09-09 用户拍板·原 P3）**：v1.7 原文写「生产周的 `W[词].segments` 可以暂不填写」，**这句从 W5 起就不成立**，已作废。

**真实机制**（措辞以规范 §3「字位与积木的对应」为准，本节不复述判断步骤，只说影响与处置）：当一个多字母字位的字形**也能由若干已教的更短字位拼出来**时，所有含该字形的词都会多解，缺 `segments` 即校验失败。`ai` 与 `a`/`i` 并存时 `rain` 有 `r|ai|n` 与 `r|a|i|n` 两解，两个都合法。

⚠️ **这不是「新教那一周」的一次性义务，是从教会那个字位起长期存在的义务**。`ai` 在 W5 教过之后，W6、W7 直到第 40 周，凡含 `ai` 的词照样要写 `segments`。**按「本周新教了什么」走查是错的** —— 那会让 W6 起每一周都漏掉此前各周教过的多字母字位，**而第一次把数据层交给副机的正是 W6**。走查对象必须是**累计已教字位集合**里全部 `grapheme` 长度大于 1 的字位。

**成本**：W5 起每周都有，随已教多字母字位累加，到第二年是每周几十个词的量级，落在阶段 B 的副机身上。

处置**不改算法**（多解就该报歧义，这正是契约要的行为），改为三件：

①**规范 §3 把 `segments` 的触发条件从「同形多音」扩写成两类**（已完成）。⚠️ **判断方法几经反复，最终口径以规范 §3 为准**：2026-09-09 早些时候我在规范里写过一套「找出能被更短字位拼出的多字母字形，再看词里含不含它」的四步扫描法，**当天被外部审查判 critical 并证明会漏判** —— 反例是本方案 §3.2 自己给出的那个：字位集合 `a`/`ab`/`bc`/`c` 时 `abc` 有 `ab|c` 与 `a|bc` 两解必须写 `segments`，但 `ab` 与 `bc` 都拼不出，四步法算出的风险集合为空、判它安全（已实测复现）。**重叠型歧义不需要「谁能拼出谁」，任何基于子串或可拆性的扫描都不完备。** 现行唯一权威判据是**逐词枚举完整解析、按基数分档**（0 解=未知片段、1 解=自动分词、多于 1 解=必须写 `segments`），字形扫描只能当预筛优化。

②**写 `tools/gen_segments.py`**：对每个词跑分词，多解时输出**全部完整解析**并按「字位数最少」给出**候选建议**。⚠️ **这只是编辑启发式，不具契约优先级**（外部审查 medium）——算法无从判断哪个拆法是教学意图，**每条建议都要人工复核**，工具默认 dry-run。

③**第 6 步的合成 fixture 增加一张 `r`/`a`/`i`/`n`/`ai` 共存的表**，堵住"合成表不含单字母、这条路径既不触发也不验证"的盲区。

**接口本次仍只冻结不改**，变的是数据生产的义务说明。

> **不要拿示例词当词表用。** 本节与规范里出现的 `rain` 等词只为说明机制。早期草稿曾列过一份含 `air`/`hair`/`chair` 的 12 词表并写成「W5 的 … 全部落入此类」——**那是错的**：`ch` 到 W15 才教，`chair` 在 W5 拆不出；`air` 是第二年才列的独立映射且读音 /eə/ 与刚教的 /eɪ/ 不同。某一周实际要写 `segments` 的词按规范 §3 的「枚举完整解析、数它有几个」判据逐词求出，**词表本身见课程方案 §5.3**。

### 3.5 双读兼容层（用户拍板）

v0.1 说数据迁移那一步（它的第 3 步，编号与本版无关）"不可分割、没有中间态"。首审 H-7 判这不是技术必然，是没区分**仓库提交原子性**与**运行时 schema 原子性**。**用户拍板采纳双读**：

```js
normalizeIdList(value)   // 字符串 → 按旧单字符 schema 展开；数组 → 按 ID 原样保留
```

关键不在"能逐周迁移"——四周数据同构，逐周迁移发现不了新问题；关键在**让含 `ai` 的集成 fixture 在破坏性迁移之前就跑通全链路**（首审 H-8）。v0.1 把双字母冒烟排在最后一步，意味着最关键的新能力直到破坏性迁移完成后才第一次被验证。

**不靠值内容猜版本**（复审 M-11）。`normalizeIdList` 只按**字段类型与来源上下文**决定怎么解析：声明为旧格式的入口才走"字符串按单字符展开"，展开后还要逐项验证该 ID 在 `sounds` 里存在。否则一个误传的普通单词会被静默解释成一组"合法"的 ID。

**存续期写死**：第 2 步引入，第 8 步**收紧为 `assertIdList`**——只接受数组、逐项验证 ID 存在，字符串分支删除（复审 L-14：删的是分支不是函数，数组校验入口仍被消费者依赖）。同一步给两条入口分别加"拒绝旧格式"失败测试：**HTML 反解析**一条、**数据层 JS**一条。

反解析侧同样要能区分新旧格式，不能把字符串 `"ai"` 错当成两个 ID。

### 3.6 分组固定位与 tile class

`groupedRackHTML`（`games.js:583`）用 `vowels.includes(ch)` 判元音，改字位后 `vowels.includes('ai')` 必然失配。改用 `soundType(id) === 'v'`——`type` 字段 schema 里本来就有，四周实测齐全（W4 = 14 辅音 + 5 元音）。

首审 M-9 指出两件 v0.1 漏的：

- **不止 `groupedRackHTML`**：四个模板各有 1 处 `vowels.includes(c)`，G4/G5 自己的 tile class 也在用，必须一起换成 `soundType`。
- **分类正确不等于固定位不变**：固定位还依赖 rack 数组顺序、重复项顺序和 CSS 布局。要加断言——分组前后每个 tile 的**原始 rack 索引、重复项数量、组内顺序**都不变，并覆盖 `ai` 作为元音的案例。

### 3.7 四个字段的数组形态

```js
wallLetters: ['s','a','t','i','p','n']                       // 有序，等于 SOUNDS 全部键（§2.4）
rackG4: ['s','s','a','a','t','t','i','i','p','p','n','n']    // 保留重复，长度即块数
rackG5: [...]                                                 // 同上
newPatterns: ['ai','j']                                       // 本周新点亮
```

**保留重复是硬要求**（§2.3）。`canSpell` 的逐个消耗语义不变，只是消耗单位从字符变成字位 ID。

### 3.8 `load_data.js` 的 HTML 兜底怎么处置

三审 M-9：第 8 步要求"两条入口拒绝旧字符串"，但 `load_data.js:21-25` 在 HTML 缺内联 `META` 时会**自行合成字符串** rack/wall，等于留了一条旧格式的后门。

已检索全部 `loadData` 调用方（7 处）：`build_course.py:15`、`check_data.js:30`、`export_data.js:5`、`test_assessment_contract.js:42/45`、`test_media.js:6`，另有三处只用 `declaration` 不走 `loadData`。四周现有产物都内联了 `META`，兜底目前不触发。

**处置写死，不留二选一**（四审 M-6）：兜底分支列为第 4b 步与第 8 步的**显式迁移对象**。

- 第 4b 步：给旧入口一个**显式 `schemaVersion` 或适配器函数**，不允许普通消费者自己按值猜类型。
- 第 8 步：**拒绝缺内联 `META` 的旧 HTML**，用固定错误码报错。四周现有产物都内联了 `META`，拒绝不影响任何现役路径。
- 第 8 步验收**不只看那 7 个 `loadData` 调用方**——还要过一遍全仓的字符串构造清单，用字段级负例证明没有新的字符串拼装或字段别名残留。

顺带注意：兜底的 wall 正则 `\$\{'([a-z]+)'\.split\(''\)\.map\(c=>\{` 匹配的正是前三周模板的硬编码字面量。第 7 步把模板改成读 `META.wallLetters` 后，这条正则会失配——这本身不影响正确性（那时 `META` 已内联），但说明兜底与模板写法是耦合的，改一处要想到另一处。

### 3.9 差分测试拆两套（2026-09-09 用户拍板·原 P7）

第 3 步落地的五项差分，把**字符级算法的结果**与**字位级算法的结果**强制断言相等。codex 判 high 并指出：**第 7 步把双字母字位迁进数据之后，两侧会「合法地不同」** —— `word.slice(1)`（去掉第一个字符 vs 去掉第一个字位）、字符长度 vs 字位数、字符级已教集合 vs 字位级已教集合、字符最小对立 vs 字位最小对立，四项都会变，**而且变了才是对的**。于是方案原先在第 4b 步与第 7 步验收里写的「差分仍绿」与判据自相矛盾：要么迁移被大量合法差异误阻断，要么把判据放宽到失去证明力。

**拆成两套，各管一段时间**：

| | **迁移前兼容差分** | **迁移后语义测试** |
|---|---|---|
| 判据 | 字符级结果 == 字位级结果 | 按**明确的预期差异**逐项断言，不再要求两侧相等 |
| 语料 | **只用单字母基线**（当前 W1–W4 的真实数据，全是单字母字位） | 含双字母的合成语料 + 第 7 步后的真实数据 |
| 有效期 | 第 7 步之前必须全绿；第 7 步之后仍对**单字母词**保持全绿 | 第 6 步起建立，第 7 步后成为主判据 |
| 覆盖证明 | **必须按语料来源输出并断言「总词数 / 执行数 / 跳过数」，且断言「执行数 + 跳过数 == 总词数」**（2026-09-09 外部审查 medium：只断言比较次数大于某阈值，挡不住「抽取器少收一类」或「判定写错误跳一大批」——剩下的词照样够阈值，测试照样绿）。**跳过要分两类**，见下方说明。第 7 步后至少断言执行数 > 0，不得跳空 | 合成语料，逐条断言预期差异 |

**跳过必须分成两类，混在一起就失去判据价值**（2026-09-09 实测后补）：

| 跳过类型 | 含义 | 断言 |
|---|---|---|
| `skippedMultiLetter` | 该词含多字母字位，超出「迁移前兼容差分」的语料范围 | **第 7 步之前必须为 0**；非零即前置判定逻辑有问题 |
| `skippedUnknown` | 该词用当周已教字位**根本拼不出来**（零解） | **合法存在，但必须参与守恒**（执行数 + 两类跳过数 == 总词数） |

`skippedUnknown` 不是缺陷，是教学设计的正常产物 —— **实测四周共 22 个**（去重后），全部落在两类：**G1 听音找开头的图片干扰词**（`dog` `fish` `milk` `sun` `book` `tree` 等，W1 只教了 `s a t i p n`，这些词本来就不该按当周字位表解码，孩子是看图选首音）与**认读词**（`see`，整词记忆不解码）。**这正是外部审查那条「零解与多解的优先级」在真实数据上的体现**：若不先按零解分流，这 22 个词会被误判成「需要写 `segments`」。

**关于「两套测试的语料并集等于共享语料全集」**（外部审查提的更强判据）：**本次不按字面实现，改用等价更强的「逐来源自证守恒」**。理由：「迁移后语义测试」用的是**合成语料**（`r`/`a`/`i`/`n`/`ai` 共存表这类），**不消费真实四周数据**，两个文件的语料全集本就不是同一个集合，谈不上取并集。要真做到语料同源，得让语义测试也消费真实周数据，那是结构性变更、且会让它在第 7 步之前无双字母语料可用。**逐来源守恒（每个来源的「执行 + 跳过 == 总数」）已经挡住了「静默削减语料」这个它真正要防的失效形态**。若将来语义测试改为消费真实语料，本条口径要重新定。
| 证明什么 | 重写没有改变旧行为 | 新行为**确实**发生了，且正是预期的那种不同 |

**预期差异断言写成正例**，例：`rain` 在字位级**就该**是 3 个字位而不是 4 个字符。按 §3.2 已冻结的真实签名写（`graphemes.js` 里**没有** `graphemeAt` 这个函数，早期草稿里的写法是错的）：

```js
const ids = segmentWord('rain', SOUNDS, EXPLICIT);   // 返回字位 ID 数组
assert.deepEqual(ids, ['r', 'ai', 'n']);              // 不是 ['r','a','i','n']
assert.equal(graphemeLabel('ai', SOUNDS), 'ai');      // ID → 字形，两者可以不同（oo_short → 'oo'）
```

**注意 ID 与字形是两回事**：`segmentWord` 返回的是**字位 ID**，要显示给人看的字形一律经 `graphemeLabel`。**断言要锁元素类型与配对关系，不锁瞬时状态**（项目已有实证）。

**同时补一个已知缺口**（codex 一并指出）：现在**没有任何合成双字母语料能证明差分抓得到差异** —— 当前四周数据全是单字母，两套算法必然处处相等，差分绿只说明它没坏，说明不了它有分辨力。拆两套时**必须补合成双字母语料**，并按「改坏副本」的办法证明它会红。

## 4. 分步实施

每步一个 commit，逐步送审。

**先说清"全绿"是什么意思。**

`run_checks.py:54` 的 `single-file baseline` 作业**不在** `if not args.quick` 里，所以 `check --quick` **会跑基线**（复审 H-8）。消费者一接线，产物的 `skeletonSha256` 必变，基线作业从接线那一步起到重取之前**必然红**。

v1.1 据此写成"除基线作业外全绿 + 基线差异单独记录"，**这个表述仍然不成立**（三审 C-1）：`run_checks.py:84-86` 里

```python
if name in ('build', 'reproducible build', 'reproducible build: compare', 'single-file baseline'):
    break
```

**基线一失败就 `break`**，它后面的 size budget、baseline contract、size budget contract、doctor、archive inventory、theme palette、directory layout、build boundaries、assessment contract、media coverage **十个作业根本不会执行**。人工"忽略基线失败"证明不了其余作业是绿的，只能证明它们没跑。

**所以第 1 步先改工具**：给 `run_checks.py` 加 `--skip-baseline`，只跳过 `single-file baseline` 这一个作业，**不吞掉任何其他失败**（其余作业失败时仍照原样 break 并以非 0 退出）。

于是每步验收统一表述为：**`check --quick --skip-baseline` 全绿，且基线差异用 `baseline.py --check` 单独跑一次并逐项核对**；第 9 步重取基线后，`python tools/project.py check` 全量全绿，不再需要该开关。

| 步 | 内容 | 验收 |
|---|---|---|
| **0** | 按 §0.2 改规范五处（`DATA-ASSESS-01` 按 §0.3 两段切分、§8 生效条件拆挂、最长匹配限定、`SOUNDS` 删 `L`） | 规范内无「里程碑 2 同期」残留；§8 第 1 条覆盖本次实际范围；规范 §8 里程碑 2b 的截止日写着 **2026-10-31**（已拍板，见 §0.2 第 1 条） |
| **1** | **工具准备**：给 `run_checks.py` 加 `--skip-baseline`。**实现方式写死**（四审）：在 `jobs` 构造完成后，仅过滤掉**名称严格等于 `single-file baseline`** 的那一项；**不修改 `:84` 的 break 名单**，也**不跳过 `baseline contract`**（那是单测作业，与产物基线是两回事） | 单测证明三件事：① 加开关后基线作业被跳过且**后续十个作业实际执行**（断言执行清单，不只看退出码）；② **其他作业失败时仍然 break 且退出码非 0**；③ 不加开关时行为与现在完全一致 |
| **2** | 新增 `shared/graphemes.js`：§3.2 的接口与契约。**不接线** | 纯函数单测：单字母词、双字母词、**全局多解失败**（含 `a`/`ab`/`bc`/`c` 上的 `abc` 这种不同首段长度的两解）、同长度多解失败、未知片段、大小写、空串、重复 grapheme、`explicitSegments` 四类非法输入、`surfaceOf`/`graphemeLabel`/`soundType` 未知 ID 抛统一错误、**输入 schema 四类非法**（`word` 非字符串 / `sounds` 非普通映射 / `grapheme` 空或非字符串 / 原型链键）、`normalizeIdList` 两种入口 |
| **3** | **差分测试 + 迁移前审计门**：除五项差分外，输出真实 W1–W4 的只读审计，**结果必须机器可读并带来源位置**，供第 5、7、9 步直接消费而不是人工抄写（五审 RECOMMENDATIONS）。审计内容见 §5「迁移前审计」行。**该审计结果作为第 5、7、9 步的前置条件** | 差分全绿。**语料来源必须是共享的词消费抽取器**，不能复用 `check_data.js` 的 `usedWords`——它 `:57-71` 只收 blend / 听音找开头 / words / pair / sight / flash / sentences / `G1_ROUNDS` / `G4_WORDS`，**漏掉 `wordforge` 与 `BOOK.pages`**（复审 H-7）。测试要断言各生产入口均被覆盖 |
| **4a** | **`L` → `grapheme` 收敛**：规范已在第 0 步先改，本步做代码与数据——四周 `data.js` 的每个 `SOUNDS` 条目、两处 `Object.assign` 派生、两个测试 fixture、`render-blocks.js:46`/`:49`、`games.js:1154`、**`check_data.js:83` 的校验条件**（§3.1 影响面表） | **双保险门槛**（§3.1）：① 加载全部现役周与 fixture 后递归断言每个 `SOUNDS` 条目无自有键 `L`、有合法 `grapheme`；② AST 或六写法源码扫描通过，归档目录走显式排除清单。`check --quick --skip-baseline` 全绿 |
| **4b** | **消费者兼容层**：§2.2 矩阵的生产代码、校验器、模板改走字位接口，输入经 `normalizeIdList` 双读。数据仍是字符串。**墙只换渲染与读取，不启用 `DATA-WALL-01` 新校验**（复审 H-4）。产出 **42 个 `colorWord` 调用点分类清单**与**模板内字位展示来源清单**。`load_data.js` 兜底列为显式迁移对象（§3.8） | `check --quick --skip-baseline` 全绿；**迁移前兼容差分仍绿**（单字母基线，见 §3.9 —— 此时数据尚未迁移，两侧本就该相等）；基线差异单独记录。**旧墙校验必须消费 `normalizeIdList` 的归一化结果**——单字符字符串归一后是数组，`for...of` 与 `includes` 语义仍等价于旧模型 |
| **5** | **`assessmentMode` 最小路由**（§0.3）：`META` 加字段，`validateAssessment` 按 §0.3 的临时路由表分档；四周现有数据补声明，**并修复由首次启用 weekly 契约暴露的 W1–W3 数据缺口**（四审 H-5） | 见 §5「测评路由」行。**依赖第 4b 步的分词接线与第 3 步的审计结果**。**允许修改的范围 = 第 3 步审计证明为 weekly 契约所必需的常量、块及对应 `W` 条目**（五审 H-3：v1.3 限死"仅 `RESERVED` 相关常量"，但若真实数据含禁止块就修不了）；**超出该清单即停下复审，不擅自扩大**。不得以新增正例 fixture 代替真实四周数据回归 |
| **6** | **`synthetic-ai` 集成 fixture**（六审 M-5：不用 W5 命名，避免与第 5 步的正式 W5 测评 fixture 混淆——正式 W5 教 `ai` 与 `j` 两个字位，本 fixture 只验双字母链路、不证明 W5 数据契约）：造不依赖任何正式周数据的合成测试周，跑通全链路 | G2 拆 3 块；**G4 建 3 槽且能摆完**；G5 表面串查得到词；模板序列化与 HTML 反解析往返一致；文案说"3 块积木"；**墙显示全部可上墙字位（至少 `r`/`ai`/`n` 三块），只有 `ai` 是本周新点亮态**，`newPatterns` 与 `FIRST_TEACH_DAY` 键都只有 `ai`。**rack 构造写死**：`target='rain'`、`segments=['r','ai','n']`、`rackG4=rackG5=['r','ai','n']`、`G4_WORDS` 与 `G5_WHITELIST` 含 `rain`，断言 `ai` 只占一块；**另加一个需要重复字位的合成词 + 重复 rack 项**，验证按索引消耗而非 `Set` 去重（三审 M-6）。**本 fixture 只验第 6 步的消费者链路，不得直接用于 `DATA-WALL-01`**（八审 L-5：它的 `newPatterns` 只有 `ai`，说明不了 `r`、`n` 从哪一周进入累计教学顺序）——墙校验在第 7 步另建**带连续周记录**的 fixture。

**2026-09-09 追加三件**（原 P3 落点② + P7 落点①②④，此前只写在 §3.4 / §3.9 没落到本步）：
①**加一张 `r`/`a`/`i`/`n`/`ai` 共存的合成表**——即多字母字位与能拼出同一字形的更短字位并存，此时 `rain` 必然两解、必须靠 `W.rain.segments` 消歧。堵住「合成表不含单字母、这条多解路径既不触发也不验证」的盲区（原 fixture 只有 `r`/`ai`/`n`，`a`、`i` 不在表里，永远走不到多解分支）。
②**建立「迁移后语义测试」**（§3.9 的 B 套），本步起可用，第 7 步后成为主判据。
④**补一张重叠型歧义的 fixture**（2026-09-09 外部审查 critical 的落点）：字位集合 `a`/`ab`/`bc`/`c`，词 `abc` —— 它有 `ab|c` 与 `a|bc` 两个完整解，**而两个多字母字位谁也拼不出谁**。这张表证明「按可拆性扫描字形」的判据不完备，只有「枚举完整解析数」才对。
⑤**补一张「最少字位不是目标读法」的 fixture**（外部审查 medium 的落点）：某词的正确读法**不是**字位数最少的那个拆法，用来留证 `gen_segments` 的建议只是候选、必须人工复核。
③**补合成双字母语料并证明差分有分辨力**——按「改坏副本」的办法让退化成逐字符的实现跑红，验证过程记录在案 |
| **7** | **数据迁移**：四周 data.js 四个字段改数组 + **墙语义统一**（W2 7→13、W3 6→19）+ **前三周模板改读 `META.wallLetters`** + `tests/fixtures/week02-data.js`、`tests/fixtures/week03-data.js` 与 `tests/unit/test_assessment_contract.js` 同步。**同一提交启用 `DATA-WALL-01` 新校验**及正反 fixture。<br>**开工前检查（2026-09-09 加·codex S3 审提出）**：第 3 步审计里 `status:'unknown'` 的 finding（当前是 `newPatterns` 的 4 条）**必须被视为未完成证据，不能等同于 `pass`**。开工前逐条落定成 `pass` 或 `fail`，落不定的写明为什么落不定、以及本步为它做了什么假设。**理由**：`unknown` 的语义是"没有独立真相源可判"，把它当 pass 就是在没有依据的情况下放行——而第 7 步是破坏性迁移，放行错了要整批回滚。<br>另：模板行号已因副机改动漂移（见 §2 重核框），**定位模板代码前重新 grep，不得照抄 §2.2 表里的行号** | `build` 六个 BUILT 失败 0；**迁移前兼容差分在单字母词上仍绿**、**迁移后语义测试按预期差异断言通过**（两套的分工见 §3.9；**不得再要求字符级与字位级在双字母词上相等**）；ai fixture 绿；墙三条断言通过（§5）；每个产物的字节增量与剩余余量逐一记录 |
| **8** | **收紧兼容层**：`normalizeIdList` 收成 `assertIdList`（只收数组、逐项验 ID），删字符串分支；按 §3.8 处置 `load_data.js` 兜底 | HTML 反解析与数据层 JS 两条入口各有一条"拒绝旧格式"失败测试；**重新记录五个产物的最终大小、相对里程碑前的净增量与剩余余量**（复审 M-13） |
| **9** | **基线重取**：确认工作树干净 → 全量回归含浏览器 → `baseline --create --force` → 核对差异 → **单独提交 fixture** | 差异仅为预期 `skeletonSha256` 变化，`declarationOrder` 与 **`media` 表完全不变**；`project.py check` 全量全绿（此时不再需要 `--skip-baseline`） |

**步间依赖一览**（四审基础假设 7 判"不成立"，因为表里没写全）：

| 步 | 依赖 | 为什么 |
|---|---|---|
| 4a | 0 | 规范 schema 必须先删 `L`，否则代码改完反而违反权威规范 |
| 4b | 2、4a | 消费者要用 `graphemes.js` 的接口；字形字段已收敛为 `grapheme` |
| 5 | 3、4b | 依赖第 3 步审计结果确定 W1–W3 缺口范围；weekly 查"三个字位"要用第 4b 步接线的 `segmentWord` |
| 6 | 4b、5 | ai fixture 走全链路，测评路由要先能放行 weekly |
| 7 | 3、6 | 墙顺序期望序列要迁入 `newPatterns`（第 3 步审计给现状）；破坏性迁移前 ai fixture 必须已通过 |
| 8 | 7 | 数据已是数组才能收紧为只收数组 |
| 9 | 7、8 | 基线重取要求受版本控制的派生物**已提交**且工作树干净，所以必须在第 7、8 步提交之后 |

**第 4b 到 6 步是这次重排的要害**：兼容层先落地、测评路由先修好，让 `ai` 的集成 fixture 在破坏性迁移（第 7 步）**之前**跑通 G2/G4/G5/墙/模板/反解析全链路。v0.1 把它放在最后，等于改完才第一次验新能力。

**墙契约的启用时机单独说**：第 4b 步只换消费方式，第 7 步才与数据迁移**同一提交**启用新校验。这样避免了"契约已生效但数据还是旧的"这段必红的窗口。

第 7 步仍是一个大 commit（四周数据同构，拆开验不出新东西），但它已经不再是"第一次接触双字母"——那件事在第 6 步就做过了。

## 5. 验收

| 项 | 判据 |
|---|---|
| **语义等价** | **不用"输出逐字节不变"作为等价证明**。首审 H-5：不同内部字位序列可能产生完全相同的输出，现有数据也不覆盖重复 rack、最长匹配冲突、未知片段、显示转换和槽位状态；遍历顺序变化还会造成无关字节差异。改用第 3 步的**双实现差分**，语料取自共享词消费抽取器（覆盖 `wordforge` 与 `BOOK.pages`）。输出快照降级为附加回归。**差分自第 7 步起拆成两套**（§3.9）：迁移前兼容差分只在单字母语料上要求两侧相等，迁移后语义测试改用明确的预期差异断言 —— **双字母词上两侧相等反而是错的** |
| **迁移后语义测试**（§3.9·2026-09-09 补） | 第 6 步起建立、第 7 步后成为主判据。四项预期差异各有正例断言：`rain` 是 3 个字位不是 4 个字符；`aid` 的首字位是 `ai`、去首字位后是 `d` 不是 `id`；已教字位集合按字位计不按字符计；最小对立按字位比较。**并须证明这套测试有分辨力**——按「改坏副本」的办法让退化成逐字符的实现跑红，验证过程记录在案。断言锁元素类型与配对关系，不锁瞬时状态 |
| **分词** | 全局多解即失败：0 解报未知、1 解返回、多于 1 解报歧义；含"不同首段长度的两种完整解析"的失败 fixture；复杂度按 offset 记忆化，不枚举全部解 |
| **新能力** | 第 6 步 ai fixture：G2 拆 3 块、G4 建 3 槽能摆完、G5 表面串查词、反解析往返一致、文案说"3 块积木" |
| **歧义** | 合成 `oo_short`/`oo_long` fixture：无 `segments` 时校验器**失败并指出该词**；有 `segments` 且表面串一致时通过；表面串不一致、含未知 ID、非数组、空数组时分别失败 |
| **墙**（`DATA-WALL-01`，复审 M-9 + 三审 H-3） | 三条集合与顺序断言：① `wallLetters` 等于**独立教学顺序序列**过滤 `displayOnWall !== false` 后的结果，**顺序、缺项、额外项、重复项都查**；② `FIRST_TEACH_DAY` 的键集合与 `newPatterns` **完全相等**；③ 每个首教日落到对应 `DAYS[].sounds`。配缺项 / 额外项 / 乱序 / 重复项 / `displayOnWall:false` / 键与 `newPatterns` 不等六个失败 fixture。迁移后 W2 墙 13 块、W3 墙 19 块，前三周模板不再有硬编码字面量 |
| **迁移前审计**（五审 H-3） | 第 3 步逐周输出 W1–W4 的 weekly 契约审计表，**覆盖规范 §4.3 weekly 列的每个必需项与全部 8 个禁止项**（6 常量 + `retest`/`probe` 块），外加：`RESERVED` 词数、每词在 `W` 里是否有释义、每词能否唯一分词成三个字位（含歧义情况）、`RESERVED` 是否泄漏进练习/游戏及泄漏位置、`newPatterns` 现状、墙顺序、旧格式入口、`L` 字段消费点。输出机器可读。**完整 schema 在第 3 步开始前先定死**（八审 M-4 + 九审 M-1）：**顶层** `{ schemaVersion, generatedAt, findings: [...] }`——`schemaVersion` 在顶层不在每条记录里；**每条 finding** 含 `findingId`（同一规则多条发现的唯一键）、`ruleId`（规范的 `DATA-*` 命名空间，weekly 各检查项一一映射）、`week`、`status`（**闭集枚举** `pass`/`fail`/`not-applicable`/`unknown`）、`source`（对象 `{file, line, column}`，**`file` 必填**——只有可空的 `line` 无法在多周多文件审计里唯一定位；`line`/`column` 可空并写明何时为空，如整文件级发现）、`details`（**规则级对象结构**，不是自由文本）；**稳定排序键** `ruleId` → `source.file` → `source.line`（**null 排最后**）→ `findingId`。**`findingId` 的唯一性要作为机器校验条件**（十审）——`ruleId` 已参与排序，只要 `findingId` 在同一规则内唯一，排序键即构成全序。**`generatedAt` 让审计产物每次生成字节不同**，若后续要把它纳入快照或要求可复现，比较时须忽略该字段或固定时间来源（十审 RISK，非阻塞）。配 schema 正反 fixture 与重复记录检测 |
| **测评路由**（三审 H-5 + 四审 C-1） | **精确检查集合直接取自规范 §4.3 矩阵的 weekly 列，不自行重列**：**必需** `RESERVED`；**禁止**（出现即失败）`RESERVED_RETEST`、`ASSESS_TEXT`、`GLOBAL_RESERVED`、`PROBE_A`、`PROBE_B`、`ANNUAL_DECODING`，以及 **`retest` 块**与 **`probe` 块**——共 6 个常量 + 2 个块类型，**每一项各配一条参数化失败用例**，不许只测其中几个。`RESERVED` 自身的共用校验（5 词、每词三个字位、在 `W` 里有释义、不泄漏进练习/游戏）**照常执行**。W5 正例的完整定义照抄规范 §4.3（`teachingMode:'phoneme'`、`consolidation:false`、`bookMode:'new'`、`assessmentMode:'weekly'`，只带 `RESERVED`，无 `retest`/`probe` 块），另配规范点名的"weekly 漏掉 `RESERVED`"失败 fixture。**逐周回归**：W1、W2、W3、W5 各一个 weekly 正例——W1–W3 此前被 `week < 4` 早退**完全跳过**，补 `assessmentMode` 后是首次进入 weekly 路径，历史数据可能不满足新检查，必须逐周验而不是只验 W5；W4 继续走 monthly 旧行为（`assessment_contract.js:46` 有 `week === 4` 专门分支）并加回归 fixture。**失败例**：缺 `RESERVED`、携带任一禁止常量、`RESERVED` 词数不足各一条 |
| **分组固定位** | 分组前后每个 tile 的原始 rack 索引、重复项数量、组内顺序不变；含 `ai` 为元音的案例 |
| **显示层** | 四周产物中不出现裸字位 ID；合成 `oo_short` fixture 渲染出的是 `oo` 不是 `oo_short` |
| **着色安全** | 纯文本路径逐段 HTML 转义；富文本路径按白名单解析后只对文本节点着色；尖括号、`&`、引号与允许标签各有 fixture |
| **机器身份** | 确认状态 / 槽位 / DOM `data-*` 用 ID，词级持久状态用规范化表面串，音频经 `audioKey`，CSS 类不直接拼未转义 ID |
| **构建** | `project.json` 声明的每周与入口都 BUILT，失败 0 项 |
| **分步全绿** | 第 4a 到 8 步：`check --quick --skip-baseline` 全绿（第 1 步已让该开关只跳基线、不吞其他失败），基线差异用 `baseline.py --check` 单独跑并逐项核对；第 9 步后 `project.py check` 全量全绿（含浏览器套件） |
| **基线** | 第 9 步重取后 `check` 的基线作业通过。按工具**真正检查的三项**判：`skeletonSha256`（预期变）、`declarationOrder`（不变）、`media`（**完全不变**）。周 `sha256` 与 `course.sha256` 写进 fixture 但 `differences()` 不比较，属审计元数据，不作自动门槛（三审 H-4） |
| **体量** | 五个产物都在 `sizeBudgetMB` 内；第 7 步记录迁移增量，**第 8 步收紧兼容层后重新记录最终净增量与剩余余量**，基线只基于该最终形态重取。week04 设明确预警阈值（余量 0.385 MB） |
| **模块形态** | 一份实现同时被浏览器构建与 Node `require` 使用，有测试证明是同一文件；`load_data.js` 不得自己再写一份简化 tokenizer |
| **别名教学顺序**（五审 M-5 + 六审 M-6） | **已拍板取更简的一条，不再二选一**：**所有上墙 ID 都必须在某周 `newPatterns` 里出现**，`c`/`k` 同周按数组顺序表达。这样墙顺序真相源只需累计 `newPatterns`，**不引入 `aliasOf`/`wallAfter` 字段**。只有将来真出现"不属于任何首教周却必须上墙"的用例，才另立独立顺序元数据。**在第 2 步之前定死**（六审 M-6：拖到第 7 步会迫使第 2 步的 schema、第 3 步的审计格式返工） |
| **ID 字符集与属性编码**（五审 M-6，**第 2 步开始前冻结**：编码函数与失败 fixture 要先落地，不能等接线完成再定义） | 字位 ID 限定为 `^[a-z][a-z0-9_]*$`；显示字形走 HTML 文本转义、属性值走属性转义，两者分开；数组型 `data-*` 用 **JSON 编码**，不再靠逗号拼接（现在模板里大量 `${c}`、`${head}` 直接插值）。配引号 / 尖括号 / `&` / 非法 ID 四类失败 fixture |
| **Node 侧 DOM 解析**（五审 M-8，**第 4b 步开始前冻结**：解析适配边界与失败 fixture 要先落地） | Node 没有原生 `DOMParser`，而 §5「模块形态」又要求浏览器与 Node 用同一份实现。二选一：同一个可复用解析库同时打进两端；或把平台适配器**限定为只提供 DOM 解析原语**，白名单校验与序列化逻辑保持共享。两端跑同一组 fixture 验语义等价 |
| codex | critical 与 high 为 0 |

## 6. 风险

- **week04 余量 0.38 MB**。首审 M-10 判 v0.1 的"不加媒体所以不会触线"证据不足：单文件构建会把公共 JS 复制进每个产物，新增解析器、错误文本和兼容分支都会进去，方案自己也承认 data.js 会增大。改为**低风险但必须测量**，每个接线提交记录增量。
- **墙语义统一改变已交付课件的呈现**（§2.4）。W2、W3 的家长和孩子会看到墙上块数变多。这是规范要的效果，但属于可见变化，第 7 步后要实际打开页面确认排版没被撑坏。
- **第 7 步仍是大 commit**，回滚范围是整个 commit。做之前先 `git rev-parse HEAD` 记基线。
- **兼容层是临时代码**。第 8 步必须收紧干净，否则字符串分支会变成长期技术债，也让"四个字段是 ID 数组"这条契约失去强制力。
- **审查轮次不可控**。2026-09-08 三份文档分别走了五、六、五轮；代码改动的审通常更细。首审 2 critical + 6 high，复审 2 critical + 6 high（两个 critical 都是我 v1.0 修订时引入的），三审 1 critical + 4 high（critical 又是 v1.1 新引入的）。**连续三轮都有我改稿引入的新矛盾**，后续每步接线都要送审，且送审前必须跑步骤号与跨章节一致性核对。

## 7. 不在本次范围

- `DATA-ASSESS-01` 的**完整矩阵**（monthly / stage / annual 的常量组合、`RESERVED` 释义来源）与 **W10 / W20 / W40 三个 fixture** → **里程碑 2b，截止 2026-10-31**（W10 开课前）。最小路由与 W5 fixture 留在本次，见 §0.3。
  **W40 归属的更正（2026-09-09 用户拍板·原 P1）**：规范 §4.3 矩阵本来就列了四个 fixture 含 W40，而本方案 §0.2/§0.3/§7 原先只点名 W10/W20，属遗漏。W40 是 annual 列的唯一 fixture，annual 完整矩阵本就在 2b 范围内 —— 删掉它等于实质削减规范要求。故按**规范为准**，2b 交付物为三个 fixture。规范 §8 已写「W10 / W20 / W40 三个 fixture」，两边现已一致
- `DATA-STRATEGY-01`（辅音群方向）→ 规范自己标「里程碑 2 后」
- ⚠️ **分裂字位（`a_e` / `i_e` / `o_e`）不在本次冻结的契约表达能力内**（2026-09-09 用户拍板记入·原 P5）。本次把「字位序列拼接后等于词形」定死成**表面串连续拼接**：`SOUNDS[id].grapheme` 依次相连必须等于规范化词形。Magic e 违反这个前提 —— `cake` 的字位序列是 `c` + `a_e` + `k`，而 `'c'+'a_e'+'k' !== 'cake'`，字形 `a_e` 的两半被 `k` 隔开。**课程方案 §9.2 的里程碑 5「Magic e 分离积木」到时必须扩展本契约**（实测日期：W20 开课 **2027-01-11**、审定截止 **2026-12-28**，Magic e 首教在 **W21（2027-01-18）**。原写「2027-03 前后」差约三个月，2026-09-09 更正——这条警示的全部价值就是排期提示，日期错等于失效），可能的方向是给 `SOUNDS` 加不连续字形的表达（如 `{pattern:'a_e', split:true}`）并改写拼接校验。**现在记这一句的理由**：此刻正是冻结契约的时刻，等到 W20 才发现时，`segments`、拼接校验、`surfaceOf`、模板序列化已有大量消费方绑死「连续拼接」这个假设，改动面会大得多。**本次不做任何实现，只留这条警示**
- 生产周 `W[词].segments` 的实际填写（接口本次冻结）。⚠️ **「数据将来补」这个说法已作废**（2026-09-09·原 P3）：W5 起，含新教多字母字位的词**当周就必须写**，否则校验器直接失败，不存在「将来补」的窗口。本次不在范围内的只是 W5 及以后各周的**数据本身**，写它们的**义务与工具**（`tools/gen_segments.py`）属本次范围，见 §3.4
- 数据层交接规范 v2.0 的其余生效条件（`noveltyPolicy` 逐周取值、三方分工 dry run、第五周实跑）
- 第 1 期媒体清单化、第 1b 期目录交付（独立排期）
